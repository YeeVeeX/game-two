# SPARK sesión 30 — the pre-walk pin (open-gate composition test, OWNER-APPROVED) + map recon + brainstorm prep; harvest lane stays armed; v19 waits at the brainstorm

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register); Junior surfaces pt-br. Quality over cost: council 0
(no design fork is open); paid calls = Rule-2 gate/critic runs ONLY if a
gated change ships (default this session: zero — ONE named exception in
J1's defect branch). Evidence-first: claims are not evidence — file:line,
log line, or UNVERIFIED tag. Bot lines are never fun/oracle evidence.

## Program state (2026-08-21 session-29 close — verify live)

- v18 CLOSED · **v19 NOT open** (brainstorm at the owners' word; agenda:
  `drafts/_v19-intake-docket-20260820.md`). s28 shipped T5 (THE WORLD IS
  JOINED, wall 21/21) + the J1 frame-tail render fix (`dd8ff40`). s29 was
  a clean no-delta harvest gate (`57aaeb0` — every baseline unmoved) plus
  one delivery: **a pt-br status message went out to Junior** (clipboard,
  owner-delivered) naming the T5 gate, pull-first, and his re-measure
  two-liner. Consequence: **Junior deltas are LIKELY now** — his
  re-measure bank, docs commits, or a play session may land any time.
- **NO HUMAN HAS WALKED THE GATE YET** (as of s29 close: save md5
  `98fe75ed…` unmoved, logs 40×2). The owners' earned
  `boss_1_defeats: 1` opens low_quay [44,19] → zone_7 (spawn [2,14]);
  the return [1,14] → [43,19] is free. Verified live in s29:
  `data/zones/low_quay.json` / `zone_7.json` declare exactly that, no
  `type` field = default rest-on-tile auto-fire.
- **THE VERIFIED GAP (s29 audit — this is J1's reason to exist):** the
  gated direction at defeats>=1 has NEVER run end-to-end through the
  real zone pair. Coverage today: `test/game/typed_transitions_test.rb:193`
  (`test_requires_defeats_opens_on_the_persisted_fact`) proves the sim
  logic on a SYNTHETIC 2-zone fixture · `test/core/tile_registry_test.rb:87`
  pins JSON shape only · the `multi_floor_descent` replay runs the real
  pair but at defeats=0 (proves the LOCK + refusal, never the opening) ·
  `harness/support.rb:24` `apply_start` has banked/zone/inscribed knobs,
  NO defeats knob — no wall script CAN stage the open state. The
  first-ever composed execution is currently scheduled to be the owners'
  payoff moment. **OWNER APPROVED 2026-08-21 (s29 chat, recorded):**
  ship the composition test (J1) + map recon (J2) + brainstorm prep (J3).
- **J-remeasure (Junior's frame-probe, the decisive seat for `dd8ff40`)
  still PENDING** — his newest bank is `_junior-pilot-walk-20260821.md`
  @ `f60d51e`, the PRE-fix probe. Adjudication classes are PRE-DECLARED
  in J4 below — never move goalposts post-hoc. He must PULL first.
- **Owner-pending (never nag):** ear-checks + audio-v12 batch · T3
  footstep/bed renders (frozen cue-spec mail, audio-seat inbox, md5
  `d556358a…`) · coop S1 invite · the v19 brainstorm.
- **R-A2 measure (silent, NEVER prime):** `sustain bought=0` on every
  banked live-world human log. Harvest `TELEMETRY sustain` from every
  NEW human launcher log; never mention economy/provisions to the owners.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: origin/main tip = the s30 spark commit — `git log
--oneline -3` reads: that commit · `57aaeb0` s29 no-delta docs · `8032b1d`
s29 spark. Save `saves/world.json` md5 `98fe75edb6d72deab18cd48eaa88bdaf`
mtime 08-20 15:51 (banked=7 provisions=0 seals=2 sessions=13
boss_1_defeats=1 home=camp) · launcher logs **40×2** (`$TEMP` + `/tmp`,
pattern `game_two_session_*.log`) · seat mail lives at
`~/.pi/agent/mail/` (VERIFIED s29): `game-two/` inbox EMPTY done/=22,
`game-two-audio/` holds the unanswered T3 cue-spec · tmp/soak newest
`20260820-232208` · untracked `drafts/_refs/` only ·
`tmp/pilot_walk/world.json` = the owners' scratch dev-walk world
(sessions=2 banked=75) — never delete, never play it · coop S1 evidence
dir `drafts/_lag-t2-evidence/` = README only.
EXPECTED deltas (classify, then proceed): **live save moved + sessions
up** = the owners walked (J5 harvest; verify digest chain log↔save;
their FIRST crossing verbatims are fun-lane gold — bank quotes) · new
human launcher logs (harvest silently) · **Junior commits/bank** (the
re-measure is THE expected one — read before rebasing; re-verify cited
hashes after any rebase) · audio receipt / assets re-pin mail (archive,
never reply-ask) · coop S1 artifacts (override lane below).
`git pull --ff-only` FIRST. Single-instance guard before any launch
(separate call, judged by printed output). `--fresh` NEVER.

**Ordering law for this session:** deltas landed (owner play or Junior
bank) → J4/J5 adjudicate+harvest FIRST (docs-only, fast), THEN J1 —
the test ships regardless (regression pin), but if their logs show the
gate MISBEHAVED, J1 becomes the defect lane (its branch below) before
any test-writing. No deltas → J1 → J2 → J3, stop.

## GATE 0 OVERRIDE — any live owner order preempts this whole queue

- **Ear-check verdicts** = LAW: bank verbatim, route per the checkpoint
  questions; stinger-overlap failure → depth-aware-duck grilled in
  game-two-audio, never a data tweak here.
- **T3/T4 renders landing** → sha-pinned fixture conversion (v1.1
  pattern) + data-only rows + suite + noDevice re-walk + one es-CR
  ear-check line. Water family needs a NEW cue-spec mail — the sent one
  is FROZEN.
- **A live coop session = lag segment S1**: support + harvest verbatim
  per `drafts/_lag-t2-evidence/README.md`; BOTH seats at the s30 tip
  first (mixed builds refuse NAMED; exit 2 = auto-rehost/rejoin).
- **The v19 brainstorm opening in chat** = it takes over as THE session:
  facilitate from the intake docket, ONE candidate at a time, dev
  proposes with touchstones (`hub kb query --domain game-research`),
  owners decide, one recorded line each; output
  `drafts/_v19-foundation-<date>.md` + AGENTS rewrite only after BOTH
  ratify. NO CODE in the brainstorm.

## J1 — the pre-walk pin: open-gate composition test (OWNER-APPROVED; the session's one code deliverable)

**Problem it kills:** the owners' payoff walk is currently the first
end-to-end run of the open gate. If a composition seam is wrong, it
fizzles exactly there. Pin it in-suite first.

**Shape (verified pointers, s29):** ONE new focused test file under
`test/game/` (own header naming the law — house pattern; do NOT extend
`typed_transitions_test.rb`, its header pins it to fixture data). Model
the mechanics on `typed_transitions_test.rb:79-100,176-199`:
- `Game::World.new(store, seats: 1, save: <hash>)` accepts a save hash
  directly — build it like `boss_facts` (`:193`) but REAL:
  `Core::DataStore.new(<real data path>)` (see
  `test/game/world_test.rb:9` — the real store, NO FixtureStore overlay),
  `home_zone` a real zone, `counters => {"boss_1_defeats" => 1,
  "sessions" => 0}`. This exercises the SAME restore path the save
  decoder feeds (`src/game/save_state.rb:270` →
  `world.load_counters!`, `src/game/world.rb:249`).
- `w.start_in("low_quay")` (world.rb:158) if the save's home doesn't
  land you there · `w.possessed.walker.teleport(44, 19)` onto the gate
  tile · park allies adjacent (copy the `park_allies_adjacent` helper
  shape, `:93`) · `drive` a few ticks with `Core::ScriptedInput`.
- **Three tests, one file:** (1) defeats=1 → assert `zone_name ==
  "zone_7"` AND possessed at **[2,14]** (the data-declared spawn beside
  the return tile — the no-ping-pong law); (2) defeats=0 on the SAME
  real pair → zone stays low_quay (the mfd refusal, now suite-pinned);
  (3) the free return: zone_7 [1,14] at defeats=0 → low_quay, spawn
  [43,19]. Read `at`/`spawn` expectations from the ratified literals
  (they're pin-protected data; hardcoding them here is the point —
  drift must scream).
- **Watch for:** enemy/challenger interference is a non-issue at 2-3
  driven ticks from a teleported start, but verify the assert messages
  name the law, not the mechanism. Deterministic — no wall clock, no
  sleeps. Suite stays fast (real store load is seconds; `low_quay_test`
  already does it).

**Laws:** no mocks (AGENTS #5 — real files, real World) · minitest ·
read-before-edit every file you touch · ONE one-concern commit
(`test: pin the T5 open-gate composition on the real pair` or similar)
· hooks run the suite. **No Rule-2 gate owed** — mechanics-only test,
zero visual surface moves (same class as the s28 edge-pin test); state
that in the commit body. **Fresh-eyes review is still owed (Rule 6):**
scrubbed read-only pi sub-session over the diff before push; its prompt
forbids touching ANYTHING, including seat mail (s29 memory lesson —
a reviewer once handled mail out of brief). Then push.

**DEFECT BRANCH (the named paid-call exception):** if any of the three
tests FAILS against real data — STOP. The failing test IS the
reproduction; bank output verbatim in the ticket note. Classify in
writing (zone JSON defect / sim seam / decode path), write the blast
radius, and PUT IT IN THE OWNER'S CHAT before fixing — the payoff
moment is at stake and he decides fix-before-play vs defer. A fix that
moves sim/data/live-zone bytes owes its Rule-2 gates (affected wall
scripts, possibly the full 21-script wall — detached, never under a
bash-call timeout) + fresh-eyes. Never hotfix in the same breath as
discovery.

**Ticket note either way:** `drafts/_s30-gate-pin-20260821.md` — the
gap audit (carry the four coverage rows from Program state), what
shipped, suite counts before/after, review receipt path.

## J2 — map recon: does god-view v0 know the joined world? (read-only, free)

`rake map` (= `ruby -Isrc src/map_main.rb`; SAVE defaults to the live
save — READ-ONLY through the strict decoder; run only after the
single-instance guard shows zero ruby). Questions: (a) do the four
pilot zones (zone_7, basement_1/2, dungeon_1) render at all? (b) is the
T5 edge visible/locked-marked? (`src/app/map_artifact.rb:63` already
prints `BOSS 1 DEFEATS n` — see what the artifact carries). Bank the
PNG path + a 5-line finding in the J1 ticket note. **Coverage gaps =
RECORDED v19-class candidates (map-data class), zero code now.** No
probes gate, no critic — nothing ships from this job.

## J3 — brainstorm prep note (docs-only; pointers, never positions)

`drafts/_v19-brainstorm-prep-<date>.md`: for each docket row
(`drafts/_v19-intake-docket-20260820.md` — J-1..J-7, solo findings,
triggered verdict rows 3/4/6/9, parked forks) pre-fetch candidate
touchstones: `hub kb query --domain game-research "<topic>"` (+
`docs/design-corpus/systemic-worlds-research-shelf.md` tiers). Format
per row: touchstone name · one-line relevance · kb doc id. **HARD
BOUNDARY: zero recommendations, zero rankings, zero verdicts** — the
dev proposes AT the brainstorm, not in prep; this doc only loads the
reference wall's magazine. FLAGGED shelf numbers stay flagged. Skip
rows with no research angle rather than padding.

## J4 — IF Junior's re-measure bank landed: adjudicate (classes PRE-DECLARED, verbatim from s29)

Read FIRST: his new `drafts/_junior-*` file (his lane — harvest, never
edit) + the T5 ticket §"Fix SHIPPED" + §"Junior re-measure"
(`drafts/_wb-t5-wirein-20260821.md`). Compare like with like: his 16.8%
over20 was the T4-zones walk on HIS machine; his ~7% was the live
world. A different route weakens the compare — NAME it, never average.

1. **CLOSES** — over20 ≤ ~8% AND draw p95 clearly down (toward/below
   ~8-9 ms): fix stands on the decisive seat; docket frame-tail row
   closes; AGENTS lane-3 one-line close. Motif-strip lever stays
   unbuilt (leftover, named).
2. **PARTIAL** — material drop but over20 > ~10%: next lever =
   motif-strip authoring (DATA-class; low_quay 164 singles,
   district_two 158). RECORD as v19-class with his numbers; build ONLY
   on owner word behind its own Rule-2 gate + wall. Texture/macro
   composition stays DEAD (float-camera law).
3. **REFUTED-ON-SEAT** — no material change: row stays FIRED; reopen
   diagnosis with a fresh A/B on HIS numbers (one more probe walk only
   if he offers; no speculative fix-stacking without a new mechanical
   A/B here first).

Between-band numbers → write the honest reading, owners weigh in.
Adjudicate in writing: ticket + docket row + checkpoint one-liner.

## J5 — IF humans played: harvest (ZERO code by default)

Every NEW human launcher log → ONE note
`drafts/_s30-live-harvest-<date>.md`: log md5 + `TELEMETRY persist`
(digest chain + sessions; chain-anchor against the save) · `TELEMETRY
varekka` (dread-exposure row — the gate guarantees challenger exposure)
· `TELEMETRY sustain` (R-A2, silent) · `AUDIO ambience` keys (zone_7 /
dungeon beds are live now) · `AUDIO drift` (wall-clock oracle) · any
`frame_probe`. Owners' chat verbatims about the gate moment = fun-lane
evidence, QUOTES not summaries. Watch items (route, never patch):
"couldn't find the corner" = wayfinding → brainstorm item · ambush-feel
at return spawn [43,19] (Chebyshev 4 from challenger — the owned
consequence) → ONE-NUMBER data candidate, ships ONLY on explicit owner
order behind full gates (mfd replays through that tile → captures move
→ gate + wall) · wipe near the gate → brainstorm item · toll/economy
wishes → bank silently (R-A2-adjacent, never prime).

## J6 — docs close

Checkpoint entry (s30) · docket rows where verdicts landed (pointers) ·
AGENTS one-liner ONLY if a verdict landed (e.g., frame-tail close) ·
commit docs + push. Honest no-delta lines for whatever stayed pending.

## Laws that bite (short list)

- Deterministic gates decide; failed gate/critique BLOCKS ship — never
  downgraded. Presentation never mutates sim; audio pure sink; replays
  deterministic by tick count.
- Read-before-edit · one-concern commits, explicit paths (never `git
  add -A`) · hooks run the suite (~60 s/commit; pre-push re-runs) ·
  long jobs DETACHED (nohup + poll; NEVER under a bash-call timeout) ·
  JSON edits surgical · multi-line scripts in temp files, never inline
  heredocs.
- Two instances fork a save — guard EVERY launch in a separate call
  judged by printed output; re-guard after Start-Process (~40 s ruby
  lag). Freeze ALL code/data edits while any sweep/replay runs.
- Junior's lane (`drafts/_junior-*`, docs/JUNIOR.md) is his — harvest,
  never edit. Owner overrides are law — one line, never re-litigated.
  No lore; placeholders only; es-CR everyday words in owner lines.
- Cross-repo: read siblings freely, write ONLY via seat mail at
  `~/.pi/agent/mail/<repo>/` (+ RECEIPT lines); never write into a held
  seat. Sub-agent prompts forbid seat-mail handling ("touch NOTHING,
  including seat mail").
- Verbatim means verbatim; partial evidence = bank + name the gap.
  A rebase over a peer's push rewrites local hashes — re-verify after.
- **Sim-class anything** (tile behaviors, spawn logic, AI, balance,
  economy) → RECORDED for v19, refuse in writing. Return-spawn tile =
  the one named data candidate, J5's rule only.

## Budget + stop conditions

One attended session ~2-3 h. Council 0. Paid calls 0 — the ONLY
exception is J1's defect branch (owner-notified, gate-owed fix). Sub-
agents: the fresh-eyes reviewer for J1's commit (required), nothing
else. Context guard 85% → compact-checkpoint skill.
**Stop when:** J1 shipped + reviewed + pushed, J2/J3 banked (or
honestly skipped with a named reason), any landed deltas adjudicated/
harvested, checkpoint + push. **Stop early, honestly, if:** the owners
redirect (their word routes) · J1's defect branch fires (owner chat
decides the rest of the session) · any gate fails non-mechanically
(STOP, classify in writing) · a sim-class ask arrives without the
brainstorm (refuse in writing, RECORD for v19).
