# SPARK sesión 32 — harvest-first support + TileMap tile-shape named refusals (s31 review nit 1, RECORDED); adjudication lanes stay armed; v19 waits at the brainstorm

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register); Junior surfaces pt-br. Quality over cost: council 0
(no design fork is open); paid calls 0 by default — TWO named exceptions
only: J1's defect branch (real-data fix owes its wall gates) and any
override lane that ships a gated change. Evidence-first: claims are not
evidence — file:line, log line, or UNVERIFIED tag. Bot lines are never
fun/oracle evidence.

## Program state (2026-08-21 session-31 close — verify live)

- v18 CLOSED · **v19 NOT open** (brainstorm at the owners' word; agenda:
  `drafts/_v19-intake-docket-20260820.md`; reference-wall magazine
  pre-loaded: `drafts/_v19-brainstorm-prep-20260821.md` — pointers only,
  one named SHELF GAP: no curated note covers companion/ally AI).
- **s31 shipped edge validation** (`3c4b988`, reviewed PASS-WITH-NITS,
  pushed): `Crossing.validated_arrivals` — every zone edge validates at
  world load; unknown-target / malformed-spawn / impassable-spawn refuse
  NAMED with the full source/at/to/spawn tuple. All 19 real edges across
  11 zones pre-scanned LEGAL (bad=0); legal worlds byte-identical. Suite
  999→1004. Ticket: `drafts/_s31-edge-validation-20260821.md`.
- **world.rb sits at EXACTLY 1800/1800 — ZERO headroom.** s31's first
  draft failed `line_caps_test` at 1830; extract-on-touch moved the
  logic into Crossing. ANY net world.rb growth fails the suite — the
  next material world.rb touch owes its own extraction. J1 below
  deliberately touches Core only.
- **NO HUMAN HAS WALKED THE GATE YET** (as of s31 close: save md5
  `98fe75ed…` unmoved, logs 40×2). Their earned `boss_1_defeats: 1`
  opens low_quay [44,19] → zone_7 [2,14] on their next play (s30 pin
  proves the composition; return [1,14] → [43,19] free).
  First-crossing verbatims are fun-lane gold when they land.
- **J-remeasure (Junior's frame-probe, the decisive seat for `dd8ff40`)
  still PENDING** — his newest bank is `_junior-pilot-walk-20260821.md`
  @ `f60d51e`, the PRE-fix probe. Adjudication classes PRE-DECLARED in
  J2 below (carried verbatim s29→s30→s31→s32 — never move goalposts).
  He must PULL first; a pt-br status message reached him at s29.
- **Owner-pending (never nag):** ear-checks + audio-v12 batch · T3
  footstep/bed renders (frozen cue-spec mail, audio-seat inbox, md5
  `d556358a…`) · coop S1 invite · the v19 brainstorm.
- **R-A2 measure (silent, NEVER prime):** `sustain bought=0` on every
  banked live-world human log. Harvest `TELEMETRY sustain` from every
  NEW human launcher log; never mention economy/provisions to the owners.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: origin/main tip = the s32 spark commit — `git log
--oneline -3` reads: that commit · `ffe0005` s31 docs · `3c4b988` s31
feat. Save `saves/world.json` md5 `98fe75edb6d72deab18cd48eaa88bdaf`
mtime 08-20 15:51 (banked=7 provisions=0 seals=2 sessions=13
boss_1_defeats=1 home=camp) · launcher logs **40×2** (`$TEMP` + `/tmp`,
pattern `game_two_session_*.log`) · seat mail at `~/.pi/agent/mail/`:
`game-two/` inbox EMPTY done/=22, `game-two-audio/` holds the unanswered
T3 cue-spec · tmp/soak newest `20260820-232208` · untracked
`drafts/_refs/` only · `tmp/pilot_walk/world.json` = the owners' scratch
dev-walk world (sessions=2 banked=75) — never delete, never play it ·
coop S1 evidence dir `drafts/_lag-t2-evidence/` = README only. Known
ignored leftovers (NOT deltas): `captures/map/world_41a9683d_*.png` ·
`tmp/s30_review_out.txt` · `tmp/s31_diff.txt` ·
`tmp/s31_review_prompt.txt` · `tmp/s31_review_out.txt`.
EXPECTED deltas (classify, then proceed): **live save moved + sessions
up** = the owners walked (J3 harvest; verify digest chain log↔save;
their FIRST crossing verbatims = fun-lane gold, bank QUOTES) · new
human launcher logs (harvest silently) · **Junior commits/bank** (the
re-measure is THE expected one — read before rebasing; re-verify cited
hashes after any rebase) · audio receipt / assets re-pin mail (archive,
never reply-ask) · coop S1 artifacts (override lane below).
`git pull --ff-only` FIRST. Single-instance guard before any launch
(separate call, judged by printed output). `--fresh` NEVER.

**Ordering law for this session:** deltas landed (owner play or Junior
bank) → J2/J3 adjudicate+harvest FIRST (docs-only, fast), THEN J1. If
a harvested log shows a crossing landed WRONG (bad tile, wrong zone,
crash at a gate), J1 pivots to that defect's reproduction lane (classify
+ owner chat) before any hardening. No deltas → J1 → docs close, stop.

## GATE 0 OVERRIDE — any live owner order preempts this whole queue

- **Ear-check verdicts** = LAW: bank verbatim, route per the checkpoint
  questions; stinger-overlap failure → depth-aware-duck grilled in
  game-two-audio, never a data tweak here.
- **T3/T4 renders landing** → sha-pinned fixture conversion (v1.1
  pattern) + data-only rows + suite + noDevice re-walk + one es-CR
  ear-check line. Water family needs a NEW cue-spec mail — the sent one
  is FROZEN.
- **A live coop session = lag segment S1**: support + harvest verbatim
  per `drafts/_lag-t2-evidence/README.md`; BOTH seats at the s32 tip
  first (mixed builds refuse NAMED; exit 2 = auto-rehost/rejoin).
- **The v19 brainstorm opening in chat** = it takes over as THE session:
  facilitate from the intake docket + the prep doc
  (`drafts/_v19-brainstorm-prep-20260821.md` — pointers only; the dev
  proposes LIVE with touchstones, `hub kb query --domain game-research`),
  ONE candidate at a time, owners decide, one recorded line each; output
  `drafts/_v19-foundation-<date>.md` + AGENTS rewrite only after BOTH
  ratify. NO CODE in the brainstorm.

## J1 — TileMap tile-shape law: ill-shaped tiles refuse NAMED at zone load (the session's one code deliverable)

**Provenance:** s31 fresh-eyes review nit 1, RECORDED
(`drafts/_s31-edge-validation-20260821.md` §review — "next spot if the
named-refusal doctrine extends"). Same doctrine class as s31:
engine hardening in the ACTIVE lane 3 (strict-importer / named-refusal
law), NOT v19-class, NOT sim-class — any legal zone keeps byte-identical
behavior; only illegal DATA changes outcome. State that classification
in the ticket.

**Problem it kills (probed live at s31 close — all six modes verified
on this machine):** `TileMap#check_passable!`
(`src/core/tile_map.rb:198`) destructures its tile blind, and
`passable?` feeds `Array#[]`, so ill-shaped tiles split two ways:
- **UNNAMED crash** (NoMethodError `negative?`): `at: nil` ·
  `at: ["3","1"]` (string pair) · a nil `pack_spawn` entry ·
  `gradient_anchor: "middle"`.
- **WORSE — constructs CLEAN, dies silently at runtime:**
  `at: [1.5, 1]` (Array#[] truncates the float — validates against the
  wrong tile; `transition_at`'s `==` then never matches → DEAD
  transition) · `at: [1, 1, 9]` (destructure drops the 3rd element →
  validates; `==` never matches → DEAD transition). The exact
  silent-corruption class s31 killed for spawns.

**Shape (verified pointers):**
- ONE choke point: change `check_passable!(label, (tx, ty))` →
  `check_passable!(label, tile)`; guard shape FIRST —
  `tile.is_a?(Array) && tile.length == 2 && tile.all? { |v|
  v.is_a?(Integer) }` else `raise BadMap, "<label> must be an [x, y]
  tile (got <tile.inspect>)"` — then destructure and run today's
  passability check unchanged. Every caller stays untouched
  (`tile_map.rb:100-104`: pack_spawn, enemy spawns, transitions' at,
  stations' at, gradient_anchor — the one guard covers all five).
- **Class is BadMap** (Core layer house style — World's ArgumentError
  precedent does NOT carry down; tile_map.rb raises BadMap everywhere).
- **OUT of scope (RECORD if tempted, never build):** semantic
  cross-checks (a seal's `opens` naming a real transition tile; `opens`
  isn't routed through check_passable! today) · touching world.rb AT
  ALL (1800/1800) · importer changes (s31 decision: name-only universe,
  recorded follow-up).
- **Tests:** read `test/core/tile_map_test.rb` FIRST — the BadMap
  assertion idiom lives there; extend it or add one focused file (dev
  call after reading; house header naming the law either way).
  Message-asserted (the s31 bar: assert the refusal TEXT, not just the
  class), one case per verified mode above at minimum: nil at ·
  string pair · float pair (the silent-truncation kill) · 3-element ·
  nil pack_spawn entry · a legal map control (existing constructions
  all over that file already serve — name one explicitly anyway).
- **Blast radius — GREEN BY CONSTRUCTION:** the suite (1004 green at
  s31 close) constructs every real zone + every fixture through
  `TileMap.new` → `validate!` today; an ill-shaped tile would already
  crash. Expect zero trips. If a FIXTURE trips anyway: fix the fixture,
  one commit. If REAL `data/zones/*.json` trips → **DEFECT BRANCH**
  below.

**Laws:** no mocks (AGENTS #5) · minitest · read-before-edit every file
touched · ONE one-concern commit (`feat: refuse ill-shaped tiles NAMED
at zone load` or similar) · hooks run the suite. **No Rule-2 gate
owed** — load-time refusal, zero visual surface moves; state that in
the commit body. **Fresh-eyes review owed (Rule 6):** scrubbed
read-only pi sub-session over the diff before push
(`env -u PI_CODING_AGENT -u PI_SESSION_FILE -u PI_SESSION_ID pi -p
--no-session`); its prompt forbids touching ANYTHING, including seat
mail ("touch NOTHING, including seat mail"); audit the mail dir after
it runs (s29 lesson; s31 ran clean under the same wording). Then push.

**DEFECT BRANCH (the named paid-call exception):** if the new guard
refuses any REAL zone — STOP. The refusal line IS the reproduction;
bank it verbatim. Classify in writing (authoring defect / importer gap /
engine seam), write the blast radius, PUT IT IN THE OWNER'S CHAT before
fixing. A fix that moves live-zone bytes owes the affected wall
scripts' Rule-2 gates (possibly the full 21-script wall — DETACHED,
never under a bash-call timeout) + fresh-eyes. Never hotfix in the same
breath as discovery.

**Ticket note either way:** `drafts/_s32-tile-shape-<date>.md` —
provenance (s31 nit 1 + the live probe table), classification
paragraph, what shipped, suite counts before/after, review receipt
(verdict + findings banked in the note; tmp/ is ephemeral), scope
refusals recorded (opens cross-check, importer).

## J2 — IF Junior's re-measure bank landed: adjudicate (classes PRE-DECLARED, carried verbatim s29→s30→s31→s32)

Read FIRST: his new `drafts/_junior-*` file (his lane — harvest, never
edit) + the T5 ticket §"Fix SHIPPED" + §"Junior re-measure"
(`drafts/_wb-t5-wirein-20260821.md`). Compare like with like: his 16.8%
over20 was the T4-zones walk on HIS machine; his ~7% was the live
world. A different route weakens the compare — NAME it, never average.

1. **CLOSES** — over20 ≤ ~8% AND draw p95 clearly down (toward/below
   ~8-9 ms): fix stands on the decisive seat; docket frame-tail row
   closes; AGENTS lane-3 one-line close. Motif-strip lever stays
   unbuilt (leftover, named).
2. **PARTIAL** — material drop but over20 > ~10%: next lever =
   motif-strip authoring (DATA-class; low_quay 164 singles,
   district_two 158). RECORD as v19-class with his numbers; build ONLY
   on owner word behind its own Rule-2 gate + wall. Texture/macro
   composition stays DEAD (float-camera law).
3. **REFUTED-ON-SEAT** — no material change: row stays FIRED; reopen
   diagnosis with a fresh A/B on HIS numbers (one more probe walk only
   if he offers; no speculative fix-stacking without a new mechanical
   A/B here first).

Between-band numbers → write the honest reading, owners weigh in.
Adjudicate in writing: ticket + docket row + checkpoint one-liner.

## J3 — IF humans played: harvest (ZERO code by default)

Every NEW human launcher log → ONE note
`drafts/_s32-live-harvest-<date>.md`: log md5 + `TELEMETRY persist`
(digest chain + sessions; chain-anchor against the save) · `TELEMETRY
varekka` (dread-exposure row — the gate guarantees challenger exposure)
· `TELEMETRY sustain` (R-A2, silent) · `AUDIO ambience` keys (zone_7 /
dungeon beds are live now) · `AUDIO drift` (wall-clock oracle) · any
`frame_probe`. Owners' chat verbatims about the gate moment = fun-lane
evidence, QUOTES not summaries. **If they crossed: also confirm the
crossing landed exactly as the s30 pin predicts (zone_7 arrival [2,14],
return [43,19]) — a mismatch is a live defect, classify + owner chat.**
Watch items (route, never patch): "couldn't find the corner" =
wayfinding → brainstorm item · ambush-feel at return spawn [43,19]
(Chebyshev 4 from challenger — the owned consequence) → ONE-NUMBER data
candidate, ships ONLY on explicit owner order behind full gates ·
wipe near the gate → brainstorm item · toll/economy wishes → bank
silently (R-A2-adjacent, never prime).

## J4 — docs close

Checkpoint entry (s32) · docket rows where verdicts landed (pointers) ·
AGENTS one-liner ONLY if a verdict landed (e.g., frame-tail close) ·
commit docs + push. Honest no-delta lines for whatever stayed pending.
Author the s33 spark at close (house pattern), commit it, clipboard it
for the owner via the verified CF_UNICODETEXT recipe (python ctypes,
restype/argtypes pinned, read-back vs SOURCE string).

## Laws that bite (short list)

- Deterministic gates decide; failed gate/critique BLOCKS ship — never
  downgraded. Presentation never mutates sim; audio pure sink; replays
  deterministic by tick count.
- Read-before-edit · one-concern commits, explicit paths (never `git
  add -A`) · hooks run the suite (~60 s/commit; pre-push re-runs) ·
  long jobs DETACHED (nohup + poll; NEVER under a bash-call timeout) ·
  JSON edits surgical · multi-line scripts in temp files, never inline
  heredocs.
- **world.rb = 1800/1800.** Any net growth fails line_caps_test —
  extract-on-touch into a plain object (Crossing precedent, s31) or
  don't touch it.
- Two instances fork a save — guard EVERY launch in a separate call
  judged by printed output; re-guard after Start-Process (~40 s ruby
  lag). Freeze ALL code/data edits while any sweep/replay runs.
- Junior's lane (`drafts/_junior-*`, docs/JUNIOR.md) is his — harvest,
  never edit. Owner overrides are law — one line, never re-litigated.
  No lore; placeholders only; es-CR everyday words in owner lines.
- Cross-repo: read siblings freely, write ONLY via seat mail at
  `~/.pi/agent/mail/<repo>/` (+ RECEIPT lines); never write into a held
  seat. Sub-agent prompts forbid seat-mail handling; audit the mail dir
  after every sub-agent run.
- Verbatim means verbatim; partial evidence = bank + name the gap.
  A rebase over a peer's push rewrites local hashes — re-verify after.
- **Sim-class anything** (tile behaviors, spawn logic, AI, balance,
  economy) → RECORDED for v19, refuse in writing. Return-spawn tile =
  the one named data candidate, J3's rule only.

## Budget + stop conditions

One attended session ~2-3 h. Council 0. Paid calls 0 — the ONLY
exceptions are J1's defect branch and an override lane shipping a gated
change. Sub-agents: the fresh-eyes reviewer for J1's commit (required),
nothing else. Context guard 85% → compact-checkpoint skill.
**Stop when:** J1 shipped + reviewed + pushed (or its defect branch
banked + parked in the owner's chat), any landed deltas adjudicated/
harvested, checkpoint + push. **Stop early, honestly, if:** the owners
redirect (their word routes) · the brainstorm opens (it takes over) ·
any gate fails non-mechanically (STOP, classify in writing) · a
sim-class ask arrives without the brainstorm (refuse in writing, RECORD
for v19).
