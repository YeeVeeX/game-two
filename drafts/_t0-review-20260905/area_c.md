T0 FINDINGS — area c: LDtk tools, importer, normalizer, AfterSave, authoring — HEAD bc5dc49 — 11 findings (B=0 M=2 m=7 nit=2)
1. [MAJOR] test/game/zone_identity_data_test.rb:21-22 — the six zones this range added to the live graph (dungeon_2/3/4, ember_1/2/3) are missing from the zone-identity ZONES list and FAIL the identity contract when it is applied (tower: wall DARKER than floor, spread −31.1/−28.0/−25.4 luma, motif darker than floor; brasa: spread 28.7/25.5/22.9 < 40). Law/test: MAP_EDITING §7 "a NEW zone = wall script + gate rows + zone-identity row"; test header "every REACHABLE real zone carries an identity block … a palette failure here is an AUTHORING finding — fix in the sidecar + re-import, never relax the check". Evidence: `ZONES = %w[nest district district_two camp slow_door low_quay zone_7 basement_1 basement_2 dungeon_1 zone_8].freeze` (no tower/brasa rows; only grass_fixture is a recorded exemption). Fix shape: add the six rows and re-author `authoring/{dungeon_2..4,ember_1..3}.sidecar.json` palettes through the importer (or record a conscious law amendment for the buried-rock value structure).
2. [MAJOR] tools/gen_premium_art.py:46 (+ tools/premium_art/humanoid.py:228,379,631,635,637) — fiction character names for the three pack kits live in code comments. Law/test: NO LORE standing order 2026-08-16 ("no fiction names anywhere in code, data, or docs"); pre-banked in docs/CHECKPOINT.md:31 with stale line numbers (348/558-564). Evidence: `# --- the pack (Fio / Aro / Pomo): ember orange / deep rust / pale amber`; `# Fio - the blade: lean hooded runner …`. Fix shape: comment-only rename to striker/blocker/lobber.
3. [MINOR] tools/build_tower_floor.py:95 — the builder's iid prefix `d157-00{n:02d}` collides with tools/build_district_two_v3.py:396 (`d157-0002`), leaving 36 duplicate `iid`s in authoring/pilot.ldtk (e.g. `00000000-d157-0002-0000-000000000033` is dungeon_1 Transition [33,25], district_two EnemySpawn [71,12] AND the dungeon_2 Entities layer). Law/test: LDtk schema — iid is the unique instance identifier; harmless to the importer today (iids unread, emissions unaffected, WB-T6 save 2 was byte-identical) but WB-T7's named candidate (`spawn` as EntityRef) depends on uniqueness. Evidence: `return f"00000000-d157-00{n:02d}-0000-{SERIAL[0]:012d}"`. Fix shape: per-builder unique prefix (or derive from the level uid) + a pilot_authoring_test asserting project-wide iid uniqueness.
4. [MINOR] tools/import_ldtk.rb:434 — the CLI writes emissions in text mode, so on Windows Ruby the on-disk bytes are CRLF while `emit` (the declared "CANONICAL zone-JSON byte format") is LF; verified: `tmp/ldtk_out/zone_7.json` begins `{\r\n`, `git show HEAD:data/zones/zone_7.json` begins `{\n` — the pin holds only via core.autocrlf=true normalization, and a seat with autocrlf=false commits CRLF emissions (pilot_authoring_test's `gsub("\r\n","\n")` hides the flip). Law/test: importer header "The emitter below defines the CANONICAL zone-JSON byte format"; digest discipline. Evidence: `File.write(path, bytes)`. Fix shape: `File.binwrite` (same class: `MANIFEST.write_text(... encoding="utf-8")` without `newline="\n"` in gen_premium_art.py:147 / gen_tileset.py:478 / gen_placeholder_art.py:357).
5. [MINOR] docs/MAP_EDITING.md:26-28 — the §1 "one lawful edit path" table is stale: it names four LDtk emissions ("pilot four") and files `district`, `district_two`, `low_quay` under "hand-authored JSON / direct JSON edit", while pilot.ldtk carries 13 levels and pilot_authoring_test.rb:14 pins all 13 (this range added six); `wall_fixture` is absent from the table; §2's dungeon_1 example (`rusher`/`rusher_hater`/`husk`) predates the swap. Law/test: §7 "the table in §1 is the first thing to check, every time"; §4.3 in the same file says "the 13 emitted zones". Evidence: `| \`camp\` (HUB 1), \`district\`, \`district_two\`, \`low_quay\`, \`nest\`, \`gate_fixture\`, \`grass_fixture\`, \`slow_door\` | hand-authored JSON (pre-WB era) | direct JSON edit`. Fix shape: rewrite the §1 rows from `authoring/pilot.ldtk` levels + `ls data/zones`.
6. [MINOR] authoring/pilot.ldtk (Transition entities) / docs/MAP_EDITING.md §4.5(2) — the five GUI-fragile out-of-bounds spawns are exactly {basement_1 [4,3]→zone_7 [26,4] (12x8 level), basement_2 [4,3]→zone_7 [35,4], dungeon_1 [29,7]→zone_8 [62,18] (52x52), ember_3 [1,15]→ember_2 [60,14] (56x32), zone_7 [1,14]→low_quay [24,33] (44x28)} (verified against every emitted level's rect); today they are protected only indirectly (importer `required field "spawn" missing or null` refusal → provenance test errors → hook blocks) and by prose — nothing names the set, so a sixth cross-zone spawn joins the hazard silently and WB-T7 has no "these five" oracle. Law/test: §4.5 "never edit those five transitions in the GUI" (discipline, not mechanical). Evidence: import_ldtk.rb `refuse "#{ctx}: required field #{id.inspect} missing or null" if out[id].nil?`. Fix shape: a lint INFO row (spawn outside SOURCE rect) or a pilot_authoring_test pin of the exact OOB-in-source set.
7. [MINOR] tools/build_tower_floor.py:38, tools/build_brasa.py:34-35, tools/swap_medusa_to_dungeon1.py:36 — the builders import `drafts/_moss-candidates/build_moss_candidates.py:7` and `drafts/_medusa-tower/build_tower_candidates.py:9`, which `from PIL import …` at module top, so a geometry-only re-run needs Pillow installed for whatever `python` resolves to (3.14.x on Junior's seat → Pillow ≥ 11.3/12 wheels); stdlib-only claim holds for normalize/aftersave, not for the builders. Law/test: §4.1 "The builders … pin pilot.ldtk" / focus 6. Evidence: `from PIL import Image, ImageDraw, ImageFont`. Fix shape: lazy-import PIL inside the render functions of the drafts modules.
8. [MINOR] test/tools/normalize_ldtk_test.rb:19 — the suite probes `py -3.12` before `python`, while LDtk's AfterSave runs `python` (3.14.2 here, 3.14.7 on Junior's seat): the tests certify a different interpreter than the hook executes. Law/test: Rule 3 spirit (real service under test); §4.2 "the first token must be an executable name on the Windows PATH of the LDtk process — `python`". Evidence: `CANDIDATES = [%w[py -3.12], %w[python], %w[python3]].freeze`. Fix shape: probe `python` first, `py -3.12` as fallback.
9. [MINOR] tools/build_tower_floor.py:11 — "Re-running is IDEMPOTENT" is false once a lower floor exists: a re-run without `--down` (:140/:220) rebuilds the Entities layer wholesale (:258 `li["entityInstances"] = ents`) and drops the floor's existing `stairs_down` to dungeon_{n+1}; the world-graph lint reports the orphaned return only as INFO, so only tower_floor_test catches it after the deliberate copy. Law/test: §3 return check is report-only by design. Evidence: `if link_down:` … `li["entityInstances"] = ents`. Fix shape: preserve/re-link the existing stairs_down when `level(below)` exists, or refuse a re-run without `--down`.
10. [NIT] tools/normalize_ldtk.py:39 — docstring cites `build_tower_floor.py:85`; the pin expression is at :86 (block 83-88, which §4.1 cites correctly). Evidence: `"""The builders' pin, verbatim (tools/build_tower_floor.py:85)."""`. Fix shape: cite 83-88.
11. [NIT] tools/ldtk_aftersave.py:113-122 — a missing `tools/lint_world_graph.rb` is a soft skip with exit 0 (step 3 silently passes). Evidence: `say("lint: tools/lint_world_graph.rb not present, skipped")`. Fix shape: named refusal, exit 1.
UNCERTAIN: (a) "medusa" / "MUSGO" / "BRASA" codenames (tools/swap_medusa_to_dungeon1.py, tools/build_brasa.py, drafts/_medusa-tower/, tower_floor_test `test_descending_from_the_medusa_…`) — descriptive shape/theme words or fiction names under the 2026-08-16 order? owner word settles. (b) Emission byte-stability across Ruby json default-gem versions — Gemfile.lock pins no `json`; `JSON.pretty_generate` float/empty-container formatting differs between json 2.9.x and 2.10+ in edge cases; settled by running pilot_authoring_test on Junior's Ruby or pinning `gem "json"`. (c) drafts/_medusa-tower/build_tower_candidates.py geometry uses `math.cos/sin` + `round` — a libm ulp difference could flip a `round` on another platform; both seats are Windows x64 so not observed; settled by regenerating on Junior's machine and `--semantic-diff`ing pilot.ldtk. (d) Whether the six-zone identity omission (finding 1) was a conscious exemption — no record found in the test header, MAP_EDITING, or CHECKPOINT.
CLEAN: tools/normalize_ldtk.py (parse→re-dump only, idempotent, exit codes 0/1/2, cp1252-safe; pilot.ldtk has 0 tabs, CRLF throughout); tools/ldtk_aftersave.py (normalize→import into tmp/ldtk_out→lint, cwd/paths correct, writes only the given project + the out dir, no bytecode); tools/lint_world_graph.rb (checks 1–4, allowlist shape, repo-rooted defaults, exit codes) and authoring/world_graph_allowlist.json — recomputed from data/zones: exactly the 14 live floor-delta violators, 6 INTENDED (basement_2.json:144 [10,1]→basement_2 rope_spot; grass_fixture.json:35; wall_fixture.json:31; zone_7.json:195 [1,14]→low_quay; low_quay.json:265 [24,34]→zone_7; zone_8.json:159 [63,19]→dungeon_1) + 8 LEGACY (camp.json:49 [19,5]→district_two 0→−2; district_two.json:321 [0,22]→camp; district_two.json:332 [42,13]→slow_door −2→0; district.json:322 [0,13]→nest −1→0; nest.json:52 [29,8]→district; slow_door.json:51 [7,7]→district_two; slow_door.json:62 [7,1]→low_quay 0→−3; low_quay.json:254 [1,18]→slow_door), all record paths exist; provenance — pilot_authoring_test covers all 13 pilot levels (key-set equality + byte compare) and the Terrain def mirrors data/tiles.json 1–14 with every used value (1–10,12) declared; tmp/ldtk_out emission == HEAD data/zones for all 13 modulo CR; importer `ambience_regions` appended after LDtk regions and loader-validated (dup id / intent / rect bounds); no dict/set-iteration nondeterminism in any emission path (sorted or total-key max everywhere it matters; seeded `random.Random` only); no removed-stdlib imports for Python 3.14; data/zones/{camp,slow_door,zone_8} hand edits consistent with their laws (zone_8 pin moved consciously in-range); tools/dev_save.rb rides the real World/encoder/strict decoder and refuses the live save path by name.
