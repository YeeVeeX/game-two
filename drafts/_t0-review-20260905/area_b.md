T0 FINDINGS — area b: art layer (renderer, tileset, fx, light, hud, minimap, atlases) — HEAD bc5dc49 — 17 findings (B=0 M=6 m=10 nit=1)

1. [MAJOR] harness/gate_checks.json (86 rows) — the PREMIUM passes 3–11 shipped new visual behaviors with NO checklist row judging them: hit sparks / death bursts / footstep dust (fx.rb), floating damage numbers, enemy hp bars, boss bar (renderer.rb:980, 1121), ally callouts + HUD row pulse (fx.rb callout!, hud.rb:78), fire glows / vignette / kill punch / level flash (light.rb), dodge roll tint + anim (renderer.rb:1228, art.rb anim_for), pickup gleam + low-hp pulse (fx.rb:54, light.rb:116), interact prompt (renderer.rb:1678); only exit_signage_reads and minimap_reads got rows. Law/test: Rule 2 ("every visual change has a gate row + a scripted capture"); the vision critic judges rows only, so "Gate X PASS" in those commits never judged the new surface. Evidence: `#   * hit spark: a 4-point star + 3 flying chips where an attack LANDED` (fx.rb:6) vs zero matches for spark/number/callout/vignette/punch/prompt/dodge in gate_checks.json. Fix shape: one row per new surface (accuracy vs presentation axes) on the scripts that exercise them, then re-gate.

2. [MAJOR] src/app/renderer.rb:1098 — the partner-seat cyan ground halo is a new visual path reachable ONLY with art AND a 2-seat world, and no capture anywhere renders it: the wall is single-player by law and harness/scenes/netplay_scene.rb:67 constructs `App::Renderer.new(display:, strings:, bindings:)` with no `art:`/`ambience:`/`tileset:`, so netplay gates render the quad fallback (partner_ring rect), never the halo. Law/test: Rule 2. Evidence: `draw_possession_halo(x, y, world.frame, @display.fetch(:partner_ring_rgb, [80, 200, 220]), chevron: false)`. Fix shape: one `Renderer.build(data, ...)` factory used by window.rb, world_scene, menu_scene AND netplay_scene (the same 3-loader wiring is now copy-pasted in three places), then a netplay gate row for the partner halo.

3. [MAJOR] src/app/renderer.rb:1684 — the INTERACT prompt shows when the possessed is ON or BESIDE any station, but the sim's verb only fires on the station's own tile (`station = map.station_at(*source.tile)`, world.rb:495) and totems have no verb at all ("deliberate no-op", world.rb:503), while the rope_spot transition (interact_rope, world.rb:513) IS an interact and gets no prompt — the bubble promises "H INTERACT" on 4 tiles + totems where H does nothing. Law/test: Rule 2 accuracy axis / human-facing-output (a hint must be true). Evidence: `near = [[0, 0], [1, 0], [-1, 0], [0, 1], [0, -1]].any? { |(dx, dy)| map.station_at(tx + dx, ty + dy) }`. Fix shape: prompt iff `map.station_at(*me.tile)` with a verb type (bank/altar/vat/seal) or `transition_at(*me.tile)[:type] == "rope_spot"`; headless test against World#interact's rule.

4. [MAJOR] src/app/minimap.rb:58 — every transition is baked GOLD into the once-per-map zone image regardless of `Renderer.way_locked?` (unpaid toll, requires_defeats, requires_level), so the radar points the player at doors they cannot take and cannot update when a toll is paid; the gate row minimap_reads says "gold dots for open ways" and the map law says gold means walkable. Law/test: Rule 2 (contradicts the ratified grammar at renderer.rb:448 `# A shut way is NOT gold — gold means walkable.`). Evidence: `if map.transition_at(tx, ty) then gold`. Fix shape: draw ways as live dots (like the actor dots) colored by way_locked?, not into the cached image.

5. [MAJOR] data/display.json — ~40 knobs the pass 3–11 commits advertise as "display.json knobs" exist only as `fetch` defaults in code and are absent from the file: fx_enabled, fx_damage_numbers, fx_ally_callouts, fx_number_font_px/big_px, light_enabled, light_glows, light_fire_rgb/alpha, vignette_alpha, vignette_alpha_safe, kill_punch, level_flash, low_hp_pct/alpha/rgb, minimap, minimap_size, minimap_scale, boss_bar(_w/_h/_y), enemy_hp_bars, exit_pulse(_alpha), exit_arrows, exit_arrow_max/margin, interact_prompt, hud_plate_rect/alpha/rgb/edge_rgb, hud_chips_y, hud_portrait_ox/oy, tileset, tileset_gain, art_dodge_tint_rgb; `hud_plate_rect` is a default literal duplicated in hud.rb:51 and renderer.rb:1652 (drift risk). Law/test: data-driven law (AGENTS non-negotiable 3) + the renderer's own rule (renderer.rb:113 "the fetch defaults only keep a bare Renderer.new drawable"). Evidence: light.rb:16-17 `display.json knobs: light_glows, vignette_alpha / vignette_alpha_safe, kill_punch, level_flash` vs `grep vignette_alpha data/display.json` → only hurt_vignette_*. Fix shape: write every fetched key into display.json (harness reads the same file, so captures stay byte-identical) and add a test that every `@display.fetch(:k` key in src/app exists in the file.

6. [MAJOR] src/app/cli.rb:131 — `--start-zone` is now accepted on HUMAN `--host --save <scratch>` and bare `--join`, but the handshake `Params` (src/net/session.rb:56: session_id, seed, d, digest_every, link_slow, …, save) carries no start_zone, so each seat applies its own flag; two humans typing different (or one missing) zones start in different zones and die at the first digest window as a DESYNC instead of a handshake refusal. Law/test: deterministic lockstep (cli.rb:50 `# Both netplay seats must receive the SAME zone (lockstep identity;` — "by construction" was true only for run_soak.sh bots). Evidence: `return unless mods[:start_zone] && !mods[:bot] && !mods[:save]`. Fix shape: host-authoritative start_zone in Params (joiner inherits, mismatch refused at handshake like the save digest), or refuse `--start-zone` on non-bot `--join`.

7. [MINOR] NO-LORE hits (owner order 2026-08-16; comments count; docs-only rename owed): tools/gen_premium_art.py:46 `# --- the pack (Fio / Aro / Pomo): ember orange / deep rust / pale amber`; tools/premium_art/humanoid.py:228 `# two orange heads (Fio's hood, Aro's rust helm) read as one kind;`; humanoid.py:379 `"""Aro's round shield: rust rim, iron boss, engraved ring. Left arm.`; humanoid.py:631 `# Fio - the blade: …`; humanoid.py:635 `# Aro - the ring: …`; humanoid.py:637 `# Pomo - the sling: …`; src/app/renderer.rb:1479 `# T3 regression fix (s46, varekka gate catch): the tell rides at` (harness/scripts/varekka_duel.json still carries the name too). Fix shape: striker/blocker/lobber and a script rename.

8. [MINOR] src/app/renderer.rb:1187 — presentation reaches into the sim's private state: `k = total - c.instance_variable_get(:@blink_flash) # 0 → total`; Creature exposes only `blink_flash?` (creature.rb:389). Law/test: sim/presentation seam (reader methods, never ivars). Fix shape: `attr_reader :blink_flash` on Creature.

9. [MINOR] src/app/fx.rb:37-62, src/app/light.rb:36-37 — presentation subscribes closures onto the WORLD's bus at first draw; they execute inside `World#tick`'s `@bus.process` (world.rb:347), are never unsubscribed, and an exception in draw-layer code (`v.x` on an unexpected payload) would abort the sim tick. Not digested, tick-keyed, deterministic — but the "sim never runs presentation code" boundary is crossed. Evidence: `world.bus.subscribe(:actor_died) { |_ev| st[:kill_at] = world.frame }`. Fix shape: renderer-owned event tap (world exposes a read-only per-tick event log) or guard the callbacks with rescue + named stderr line.

10. [MINOR] src/app/renderer.rb:145 — Fx callout labels are translated ONCE at construction (`labels: { drink: tr("hud.provisions", "POTION"), roll: tr("overlay.dodge", "dodge").upcase, …`), so the menu's live locale switch (`strings.switch!`, window.rb) leaves callouts in the old language until restart; every other string resolves at draw time. Fix shape: pass the resolver and `tr` in draw_callout.

11. [MINOR] src/app/minimap.rb:75 — `rescue StandardError` → `nil` swallows every zone-image build error, so a real bug (bad char_at/registry shape) makes the radar silently vanish with no named line. Law/test: refusals NAMED (AGENTS fallback law names the absence). Evidence: `rescue StandardError` / `nil`. Fix shape: warn once to stderr with the exception class + zone name.

12. [MINOR] src/app/renderer.rb:1436 — `BOSS_NAMES = { challenger: "BOSS 1", serpent_boss: "BOSS 2", ember_boss: "BOSS 4" }.freeze` bypasses the `tr` i18n seam (renderer.rb:1946 "every player-visible string funnels through here") while challenger.name goes through strings; placeholder-invariant today, but two paths for one surface. Fix shape: `boss.<kit>.name` keys in the three string files.

13. [MINOR] src/app/art.rb:163 — `k = hf.fdiv(8).clamp(0.0, 1.0)` hardcodes the sim's hurt window (creature.rb:322 `@hurt_frames = 8`) in the art layer; the squash silently desyncs from the sim if that number moves. Law/test: prose-number law (2026-09-05). Fix shape: `Game::Creature::HURT_FRAMES` constant read by both.

14. [MINOR] test/app/art_registry_test.rb:47 — `%i[idle walk windup active hurt dead].each do |anim|` never range-checks the new dodge / special_windup / special_active anims (indices 16–21 vs 22 cols — correct today, verified by IHDR + manifest, but unpinned); the drift message at line 40 sends the fixer to `tools/gen_placeholder_art.py`, which would overwrite the premium atlases (the manifest carries no `generator` key to disambiguate). Fix shape: iterate `atlas.anims.keys`; point the message at tools/gen_premium_art.py and emit a generator key.

15. [MINOR] stale docs after the range: src/app/renderer.rb:17-19 header still says `the possessed body is brightened and white-ringed … facing notch` (now gold halo + sprites, notch off); src/app/minimap.rb:15 `display.json: minimap (on/off), minimap_rect, minimap_scale.` but the code reads `minimap_size`; tools/gen_premium_art.py:10 `COLS = 16: idle 4 | walk 6 | windup 2 | active 2 | hurt 1 | dead 1` (core.py:17 `COLS = 22`); src/app/renderer.rb:73 `TAUNT_RUST = Gosu::Color.new(255, 190, 80, 35) # blocker body color — ownership` no longer matches the blocker body (158/52/30, line 26). Fix shape: doc pass.

16. [MINOR] kit identity colors live in three hand-synced places — renderer.rb:24-56 KIT_BODY, controls_overlay.rb:34 `VESSEL_RGB = { striker: [235, 120, 40], blocker: [158, 52, 30],`, tools/gen_premium_art.py:46-52 ramps — commit a18b40b had to edit all three for one color truth; the range added 11 new KIT_BODY entries the same way. Law/test: data-driven (one source). Fix shape: `data/art/kits.json` colors read by renderer/overlay/generator.

17. [NIT] dead code: src/app/hud.rb:244 `def small_font` unused; src/app/renderer.rb:1082 `rp = @art ? 4 : 3` — `rp` unused on the art branch; `RawBlob = Struct.new(:columns, :rows, :to_blob)` duplicated in light.rb:63 and minimap.rb:17; fx.rb's `public`/`private` toggling leaves draw_number/num_font/big_font public.

UNCERTAIN: (a) whether the netplay gates (harness/net/*, CHECKS=harness/net/gate_checks.json) were re-run after the art layer landed — pins.json / tmp/wall logs would settle it; I ran nothing. (b) whether the minimap revealing the full unexplored zone layout is the peers' intent (no fog) — a design call for the hub chat, not a defect. (c) Gosu's update:draw is 1:1 by construction (tick-locked), which is what keeps the Fx hp-delta poll in `draw` deterministic; if any future path draws fewer times than it ticks (headless/minimized), damage numbers would differ — a determinism test that ticks N frames with and without draws and compares Fx state would pin this.

CLEAN: data/art/manifest.json ↔ PNGs (20 atlases, all 704×192 = 22 cols×32 × 4 rows×48, every md5 matches, every anim index < cols); data/art/tiles.json ↔ PNGs (16 materials 512×128 = 16 masks×4 variants, details 256×32 = 8 sprites ≥ pool max 7, all md5 match; corner-bit convention tl1/tr2/bl4/br8 identical in gen_tileset.py CORNERS and tileset.rb build_cells); data/strings en/es/pt-br 88 keys each, full parity, placeholder/functional register only, 7 added keys identical across the three; src/app/window.rb 270/300 with only the 3 renderer-wiring lines changed, no orchestration creep; the 25 knobs that WERE added to display.json are all read; all six subscribed events (attack_hit, actor_died, drop_picked_up, provision_used, dodged, level_up) are in World::EVENTS; no wall clock / rand / Object#hash in any src/app art file — every animated value in fx/light/ambience/renderer keys on world.frame or FNV seeds, minimap is position-only; corpse `kit_name` is excluded from digest_snapshot by the stated law; save_store.rb L9 migration is named, pure, pre-validation; controls_overlay.rb, zone_identity.rb, tile_art.rb, ambience.rb, tileset.rb geometry caches hold no state (drained swaps at draw time).
