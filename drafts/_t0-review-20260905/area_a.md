T0 FINDINGS — area a: sim primitives + boss block + save/progression/netplay — HEAD bc5dc49 — 17 findings (B=1 M=5 m=8 nit=3)

1. [BLOCKER] src/game/creature.rb:664 — Boss skill rotation advances at action START but `action_config` re-reads the merged `kit` at activation/resolution, so every phased-boss attack winds up as skill N and resolves as skill N+1 (serpent_boss phase 2/3: spread telegraph → petrify melee, stone telegraph → spread/beam); for ember_boss phases 2/3 a beam-start followed by a dash-resolve reaches `activate_action` with `@dash_plan` nil'd at line 657 and crashes the session (`nil.duration`) — deterministic on both seats, untested (boss_phases_test only checks arcs ∈ legal set; its `attack_started` handler at test line 74 reads `action_config` post-advance; no wall script reaches phase 2). Law/test: wrong behavior + crash; boss block phase/rotation contract. Evidence: `advance_boss_skill! if kind == :attack   # FASE 5: next skill in the phase's rotation` (begin_action, after `cfg = kit.fetch(kind)`), `@dash_plan = nil unless cfg[:arc] == "dash"` (657), `return unless action_config[:arc] == "dash"` / `@iframes = [@iframes, @dash_plan.duration].max` (710/712). Fix shape: snapshot the begun skill (`@action_cfg = kit.fetch(kind)`) in `begin_action` and make `action_config` return it (also freezes hp-phase drift mid-windup); add a test that started arc == resolved arc across a multi-skill phase and one driving ember_boss into phase 2.

2. [MAJOR] src/game/creature.rb:623 — `revive!` does not clear poison state: a body killed by a melee/aura hit while the DOT was active (tick_poison zeroes `@poison_ticks` only on a poison-caused death) revives at the vat or the judgment with `poisoned?` still true and takes the remaining ticks as "fresh flesh". Law/test: revive-is-the-full-reset comment; no test covers poison across death→revive. Evidence: revive! resets `@hp/@exhaust/@special_exhaust/@iframes/@stagger/@dodge_cooldown/@hurt_frames/@carried`, `clear_taunt!`, `release_seize!` — no `@poison_*` reset. Fix shape: add `clear_poison!` to `revive!` (+ test).

3. [MAJOR] src/game/controllers.rb:211 — Ally-brain "focus fire on the leader's target" is a dead branch: `focus` is only ever assigned to humans (aggro.rb:65, world.rb:1127), so `bodies.compact.filter_map(&:focus)` is always empty and focus_fire degrades to "finish lowest-hp below finish_pct, else nearest" — the A3 audit's ON evidence measured this, not the documented rule. Law/test: claimed sim rule not implemented; no focus-fire test (ally_brain_test header at line 9 promises "focus fire · … · dodge a provoked telegraph" — only ranged-hold and drink are tested; the `human.coward` rule enabled in `world_on` has zero assertions). Evidence: `leader_focus = bodies.compact.filter_map(&:focus).find { |f| pool.include?(f) && !f.dead? }`. Fix shape: expose the possessed's `@last_damaged[seat]` through a view reader and use it; add focus-fire, dodge, and coward tests before any ON flip.

4. [MAJOR] src/game/world.rb:984, src/game/creature.rb:267,398, src/game/controllers.rb:214,257,280,284,364,486 — Balance fallbacks live in code (`spread_count` 3, `beam_length` 6 ×2, `ranged_hold_tiles` 3, `finish_pct` 0.35, `ring_min_adjacent` 2, dash `max_tiles` 4, `coward_pct` 0.25, `flash_frames` 8); all shadowed by data today, so a renamed/missing key would silently play a hidden default instead of refusing NAMED. Law/test: AGENTS non-negotiable 3 (zero balance constants in code). Evidence: `count = cfg.fetch(:spread_count, 3)`, `(1..cfg.fetch(:beam_length, 6))`, `hc.fetch(:coward_pct, 0.25)`. Fix shape: strict `fetch` (KeyError names the kit/key at first use) or a boot-time kit-shape validator.

5. [MAJOR] harness/gate_checks.json (86 rows) — Three new visual sim tells have no gate row: petrify stone telegraph (renderer.rb:1131 `action_config&.dig(:petrify)`), blink departure/arrival flash (`draw_blink_flash`), aura ring (`draw_aura`); tower3_run/tower4_run/brasa*_run scripts exist but no row judges them (rows present: ground_telegraphs_read, boss_phase_pips_read, moss/brasa identity). Law/test: Rule 2 (every visual change = gate row + scripted capture). Evidence: `grep -i "stone|blink|aura" harness/gate_checks.json` → no row ids. Fix shape: three rows keyed to the serpent/ember sentinels, re-gate.

6. [MAJOR] test/game/ally_brain_test.rb:9-10, data/balance/threat.json:27 — The `human.coward` retreat rule (controllers.rb:359-368) ships with no test at all (only `enabled=false` is asserted), and the ally brain's dodge/ring/dash-special branches are untested; both are sim rules gated only by a data flag one edit away. Law/test: missing test for a sim rule. Evidence: `refute w.threat_config.dig(:human, :enabled)` is the only coward assertion; 4 tests total. Fix shape: one deterministic test per branch under `world_on`.

7. [MINOR] src/game/creature.rb:349,376 — `@poison_interval` is sim state read by `tick_poison` (`@poison_countdown = @poison_interval`) but absent from `digest_fields`; harmless while every poison source shares interval 30 and a divergence would surface through `poison_countdown` within one interval, but it is the W1 pattern the digest comment warns about. Fix shape: add `["poison_interval", @poison_interval]` + coverage pin.

8. [MINOR] src/game/world.rb:1024 — Aura cadence is `@frame % period_frames`, and `@frame` keeps advancing through hitstop (tick early-return), so a hitstop straddling the multiple SKIPS a burn rather than pausing it — the possessed's own hit timing modulates ember_b's damage output; deterministic but the "no per-creature clock" shortcut buys a balance quirk. Evidence: `next unless aura && (@frame % aura[:period_frames]).zero?`. Fix shape: a per-bearer countdown ticked in tick_world (digested) or accept + document.

9. [MINOR] src/game/world.rb:986 — `half = count / 2; (-half..half)` emits count+1 pellets for even `spread_count` (4 → 5); the comment documents only 3 and 5. Fix shape: assert odd at boot or compute the ring walk from `count` directly.

10. [MINOR] data/balance/combat.json:233,466,585 (+ `charge: true` at 403,575,597…) — `boss.defeat_counts: true` and `attack.charge: true` are read by no code (grep src/ → none): BOSS 2/BOSS 4 deaths accrue nothing, stamp nothing, and respawn in 3600 frames — only `kit[:seize]` records a defeat (world.rb:1602). Misleading data keys implying behavior. Fix shape: delete the keys or wire `defeat_counts` to a per-boss counter (its own gated piece).

11. [MINOR] src/game/world.rb (1776/1800) — Extraction seams a refactor should cut into plain objects (Crossing/Stations precedent): (a) combat resolution 844-1046 (`resolve_attacks`…`tick_projectiles`, `leveled_damage`, `emit_attack_hit`) → `Game::Combat`; (b) respawn scheduler 247, 1406-1489, 1691-1775 (`respawn_tells`, `telegraph_due_humans`, `respawn_due_humans`, `schedule_human_respawn`, `respawn_target`, `pocket_anchor`, `seed_anchor`, `nearest_spawn`, `scatter_pick` + `@human_respawns`/`@respawn_rng`) → `Game::Respawns`; (c) challenger clock 656-783 (`tick_challengers`, `tick_seizures`, `end_seizure`, `abort_all_chants!`, `nearest_controlled_to`) → `Game::Seizures`; (d) seat pointer law 812-842, 1375-1403, 1614-1667 (`handle_swap`, `handle_seat_death`, `assign_waiting_seats`, `assign_seats_after_judgment`, `camera_anchor`) → `Game::Seats`. Note the range added spread+aura (~50 lines) directly into World at the cap instead of into (a) — the "material touch at the cap owes its extraction" clause.

12. [MINOR] test/game/challenger_test.rb:68,95, test/game/dread_test.rb:58 — Fiction name `varekka`/`face_varekka!` survives in method names inside hunks this range rewrote (staging comments/tiles). Law: NO LORE standing order (2026-08-16: no fiction names anywhere in code). Evidence: `def face_varekka!(dist: 3)`. Fix shape: rename to `boss_1`/`face_boss_1!` in the tests (the `TELEMETRY varekka` line in telemetry.rb:367 is untouched here and frozen under measurement hygiene — leave it to an owner-word ticket).

13. [MINOR] test/game/zone_identity_data_test.rb:21 — `ZONES` omits the seven new zones (dungeon_2/3/4, ember_1/2/3 + the re-authored ones are present), so their palette/decor identity laws are unchecked by this test. Fix shape: derive ZONES from `DATA.keys.grep(%r{\Azones/})` minus fixtures.

14. [MINOR] test/game/boss_phases_test.rb:74 — The rotation test samples `action_config` from a queued `attack_started` handler (bus flushes after `advance_boss_skill!`), so it records the NEXT skill and cannot detect finding 1; `legal[max_phase]` accepts any skill of any reached phase. Fix shape: assert started arc == resolved arc per cast (payload should carry the cfg).

15. [NIT] src/game/creature.rb:83 — `|| @boss_skill_index.nil?` guard in `kit` is dead (initialized to 0, only `+= 1`).

16. [NIT] src/core/tile_map.rb:75-76 — `safe?`/`hub?` duplicate the existing `safe`/`hub` attr_readers (two spellings for one fact).

17. [NIT] tools/a3_leash_trace.rb:5 — Hardcodes `brasa2_run` (a3_stream_diff takes ARGV) and prints Portuguese labels (`leashed por ator`, `mediana`) — working language is English.

UNCERTAIN: Finding 1's crash is a static trace (rules forbade executing the sim; the seat-lease gate also blocked a `ruby -e` repro) — settle by driving an ember_boss staged at ≤50% hp in grass_fixture for ~300 ticks (expect NoMethodError from creature.rb:712) and by asserting started-vs-resolved arc on serpent_boss phase 2. Whether tower4_run's pinned verdict/manifest already encodes the scrambled BOSS 2 rotation (phase 2 reached?) needs the wall log, not the JSON.

CLEAN: src/net/** (no diff; Fingerprint hashes data/** so a local `ally.enabled` flip refuses at HELLO), data/netplay.json, src/core/event_bus.rb + data_store.rb + tile_registry.rb, src/game/save_state.rb (`apply!` order unchanged; `migrate_retired_seals!` is a named-tuple list run before validation in save_store.rb:63, tuple matches the base zone's seal `opens` [18,2]; hub zones and every other seal tuple unchanged base→HEAD; the only live-save writer remains `SaveCoordinator#close` on reason :quit), src/game/camera.rb + field_economy.rb `kit_name` (presentation, not digested), World#digest_snapshot (new sim facts blink_cooldown/boss_skill_index/poison_* digested; `@blink_flash`, corpse kit_name, cameras excluded; classification table + coverage pins updated), ally brain OFF path (traced: `pick_provoked(nil)` ≡ old `nearest(provoked)`, `try_blink` inert without kit[:blink], `in_attack_range?` else-branch ≡ old melee rule; world_loop canary hash unchanged across the range), EVENTS registration (blinked/poisoned/aura_burn), progression.json kill_xp covers every human kit, tools/a3_stream_diff.rb.
