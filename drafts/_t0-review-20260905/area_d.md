T0 FINDINGS — area d: harness, gate rows, gate scope, pins, canary, wall runner, soak — HEAD bc5dc49 — 19 findings (B=0 M=9 m=9 nit=1)

1. [MAJOR] harness/scenes/netplay_scene.rb:67 — The netplay gate scene still builds the Renderer WITHOUT `art:/ambience:/tileset:` while `src/app/window.rb:104-108`, `world_scene.rb:29-36` and `menu_scene.rb:35-38` all wire them, so `rake gate SCRIPT=harness/net/*.json` judges the quad fallback (white square ring, flat tiles) that no coop player sees; the partner-seat cyan HALO (58e6153) and every sprite/tile/Fx/Light surface in coop have no gate reel. Law/test: Rule 2 (every visual change verified by capture) + `harness/net/gate_checks.json` `partner_ring_distinct` ("exactly one carries the white ring") now describes only the fallback path. Evidence: `@renderer = App::Renderer.new(display: @data["display"], strings: Core::Strings.new(@data, locale: "en"), bindings: Core::BindingMap.load(` (no art kwarg) vs renderer.rb:1089-1091 `draw_possession_halo(...)` / `else Gosu.draw_rect(... POSSESSED_RING)`. Fix shape: wire the same three presentation objects in NetplayScene (546375b did it for menu_scene), re-word the two ring rows to halo grammar, re-run the three net gates.

2. [MAJOR] harness/scripts/*.json (all 42) + harness/gate_checks.json:217/221/237/317/337 — No active wall script stages any boss beat: grep shows `challenger_engaged` / `challenger_chant_started` / `vessel_seized` only in `harness/retired/{varekka_duel,burn_duel,floor3_run}.json`; in the banked verdict log `boss_phase_pips_read`, `challenger_tell_reads`, `writ_frame_reads` have NEVER passed affirmatively (every entry "not exercised"), and `seizure_reads` / `floor3_boss_march_reads` (rewritten in range) passed floor3_run on the stamp-only branch. The BOSS BAR added in d3a00c5 has no row at all. Law/test: Rule 2 + Rule 6 (BOSS 1 chant/seizure/writ un-gated since the T7 retirement 2026-08-30; BOSS 2 / BOSS 4 phases never gated). Evidence: verdict 20260905-160425 floor3_run `floor3_boss_march_reads pass=True why=Boss body stays off camera but BOSS 1 SPAWNED stamp is legible after engagement.`; tower4_run 20260905-051040 `boss_phase_pips_read pass=True why=not exercised by this script`. Fix shape: author one boss sentinel per boss (tuner route into the vault / loop end / dais, manifest `challenger_engaged>=1`), split `seizure_reads` into seizure vs stamp rows, add a boss-bar row.

3. [MAJOR] harness/gate_checks.json (86 rows) — Six range passes shipped player-visible surfaces with NO gate row: Fx hit spark / death burst / footstep dust (c6f5fcf, `src/app/fx.rb:6-8`), Light fire glows + vignette (064bd80, `src/app/light.rb:73,106`), floating damage numbers + wounded-hostile hp bars + boss bar (d3a00c5, `fx.rb:83-90`, `renderer.rb:979`), ally callouts + HUD row pulse (d626550, `fx.rb:149`, `hud.rb:84-88`), pickup gleam + low-hp red pulse (f70ff2d, `fx.rb:55-59`, `light.rb:116-121`), interact prompt (a18b40b, `renderer.rb:1678`); only signage (row 85) and minimap (row 86) got rows. Law/test: Rule 2 ("every visual change has a gate row + a scripted capture"); keyword scan of all 86 `check` texts finds zero hits for vignette/callout/prompt/gleam/dust/low-hp/boss bar/damage number. Evidence: commit d3a00c5 "floating numbers ... BOSS BAR top-center ... Gate floor3_run PASS" — gated only for non-regression of existing rows. Fix shape: one row per surface (or one "impact fx" row + one "screen light" row + boss-bar row + interact-prompt row), each with a script that stages it.

4. [MAJOR] harness/pins.json:1 + harness/run_wall.sh:35 + drafts/_wall-premium-v22-20260905.log:1 — The 42-script wall ran in worktree `game-two-wall3 @ 064bd80` (after 1cae748 added the pins line), so pins.rb wrote pins into that worktree's harness/pins.json; the worktree no longer exists (`git worktree list` shows only s59_pre and t0-review), the tracked ledger is still `[]` at HEAD and at main d58ccd9, and the same worktree kept the per-row verdict JSON (drafts/_gate-verdicts.log has NO 2026-09-05 entries for aim_hold/totem_pulse/sustain_run/etc.) and the MANIFEST FAIL detail lines — only the curated GATE/rc lines were pasted into the drafts log. Law/test: AGENTS "Process artifacts are tracked: drafts/ (verdicts...) lives in git"; pins.rb:23-24 admits the leak ("A pin recorded from a worktree ... is committed with that worktree's wall record") but nothing enforces it. Evidence: `harness/pins.json` = `[]`; pins.rb:98 would print `PINS: no pins recorded yet (42 wall scripts; the next harness/run_wall.sh sweep populates ...)` after a sweep already ran. Fix shape: run_wall.sh takes `--pins <path>` defaulting to the MAIN clone's ledger (git-common-dir based) and tees the verdict log there too, or the wall-record step refuses to bank without the worktree's pins.json + verdict entries.

5. [MAJOR] harness/run_wall.sh:35 + Rakefile:137-140 — A `SKIP_CRITIC=1 harness/run_wall.sh` sweep (documented at run_wall.sh:13 as "NOT shippable") exits 0 per script and is recorded as `--gate-rc 0`, so `rake pins` reports PINNED ("verdict current") for scripts that never met a critic; likewise an exported `CHECKS=harness/net/gate_checks.json` leaks into every `rake gate` (Rakefile:120 `ENV.fetch("CHECKS", ...)`) and all world rows go unjudged. Law/test: Rule 6 / pins.rb header ("the last time a wall script went through the blocking gate + manifest check"). Evidence: Rakefile `puts "GATE PASS (determinism only)"` with no nonzero exit; run_wall.sh `ruby harness/pins.rb record --script "$s" --tag "$TAG" --gate-rc "$gate_rc" --manifest-rc "$man_rc"` carries no critic flag. Fix shape: run_wall.sh unsets CHECKS and refuses to record (or records `critic: skipped`) when SKIP_CRITIC=1; report shows such rows as UNPINNED.

6. [MAJOR] drafts/_wall-premium-v22-20260905.log:84,104,129,159,194,209,214,216 — Seven scripts' manifests fail deterministically every sweep (level_gate, loot_loop, nest_advance, threat_pull, vat_economy, zone8_crossing, zone_catchup — the "T7/C2 census", red since 2026-08-29 per drafts/_v20-t3-potions-20260829.md §9), so `harness/run_wall.sh` has exited 1 for three cycles and PASS is decided by a human matching the red set against a census ("honest pass law ... supersedes the spec's stale rc=0"); the range banked another wall on this basis ("manifests = T7 census exactly") and the new pins ledger will mark these FAILED forever. Law/test: Rule 6 (a failed gate never downgraded to advisory); manifest_check.rb header ("a missing staged beat is a semantic desync even when the vision critic passes"). Evidence: `WALL FAIL: basement_pocket level_gate loot_loop menu_tour mercy_floor multi_floor_descent nest_advance threat_pull toll_pocket tower2_run vat_economy zone8_crossing zone_catchup`. Fix shape: re-cut or re-pin the 7 manifests to observed counts (tools/tune_sentinels.rb already does "observed-count manifests") or retire them to harness/retired/, so the runner's exit code means something again.

7. [MAJOR] harness/scripts/zone8_crossing.json:1 + harness/gate_scope.json:28 — The range retired DUNGEON 1's seal (70258b9; `data/zones/dungeon_1.json:265 "stations": []`, pre-range 4d54a01 had a breach_cost station + sealed transition at [18,2] and the zone_8 rope at [29,4], now [29,7]) but zone8_crossing (untouched since dc0d700 2026-08-24) still stages it: `"interact": [[40, 40], ...]`, `"manifest": {"zone_entered": 4, "seal_breached": 1, "body_regrown": 1, "tribute_paid": 1}` — a seal breach that cannot happen, inputs choreographed on the old map, absorbed as "census" (dungeon_fork was retired for exactly this). gate_scope.json still claims `"seal_breach_reads": ["basement_2", "dungeon_1"]` and the audit test passes because the row text contains the family word "station". Law/test: Rule 2 (stale staging), gate_checks_audit_test `test_scoped_check_texts_mention_their_zone_by_display_name_or_id` (family-word escape). Evidence: banked verdict 20260905-014633 zone8_crossing `seal_breach_reads ... Not exercised by this script.` Fix shape: re-author zone8_crossing on the new geometry (drop seal_breached or move the seal beat to basement_2) and remove dungeon_1 from the seal_breach_reads scope.

8. [MAJOR] harness/vision_critic.py:72-77 — The PERSONA (the system prompt of EVERY gate verdict, `"system": [{"text": PERSONA}]` / gateway `"system": PERSONA`) still tells the critic the art is deliberate flat rectangles after the range shipped drawn sprites (03259d0), dual-grid material tiles (7189be7) and sprite-based facing (`facing_readable`), so the gate is framed against frames that no longer exist. Law/test: Rule 2 critic framing; contradicts row `facing_readable` ("from the SPRITE itself"). Evidence: `Flat-rect placeholder art is a DELIBERATE choice (feel first, art later) — do not ask for sprites; critique what the rectangles communicate. 32px tiles. Player is ember orange, enemies (husks) pale bone, telegraph flashes yellow, attacks show as white tiles, gates are gold, town is warm brown, dungeon is cold blue.` Fix shape: rewrite the persona's art paragraph to the v22 presentation (sprites, material tiles, hostile families, HUD panel) and re-run one baseline gate to bank the flip rate.

9. [MAJOR] harness/gate_checks.json:106,158,238,254 — Four rows still anchor on the retired white possession ring: `taunt_convergence_reads` ("may legitimately BE the possessed white-ringed body"), `god_mark_reads` ("distinct from the white possession ring (full-body outline)"), `writ_frame_reads` and `burst_legibility_budget` ("the possessed's white ring") — only three rows were re-worded in 58e6153, and with art loaded the ring is never drawn (renderer.rb:1089 `draw_possession_halo(x, y, world.frame, possess_rgb)`; the white rect is the `else` fallback at :1091). Law/test: Rule 2 row hygiene (a critic hunting a white ring can false-FAIL or discount the possessed body). Evidence: gate_checks.json:158 `clearly distinct from the white possession ring (full-body outline)`. Fix shape: s/white ring/gold ground halo + chevron/ in the four rows.

10. [MINOR] harness/gate_checks.json:110,182 — `corpse_load_reads` ("free drops (small FILLED magenta squares) ... filled square inside a hollow outline") and `deep_drop_band_reads` ("ember/gold square, larger than the magenta drops") describe the pre-41e1d95 square drops; renderer.rb:625-628 now draws every band as a bobbing diamond ("a GEM, not a square"). Law/test: Rule 2 row hygiene. Evidence: `distinct from free drops (small FILLED magenta squares)`. Fix shape: re-word both rows to the diamond grammar (as `drops_read_as_pickups` was).

11. [MINOR] harness/gate_checks.json:226 — `zone_identity_reads` asserts "Value structure stays constant everywhere: floor dark, walls lighter, gold strictly reserved for open ways" — the range broke both by design (gold possession halo/chevron, gold "+N" pickup numbers, gold exit arrowheads, gold HUD frame/minimap dots; DUNGEON 1's near-black VOID wall class darker than floor per `dungeon_1_abyss_reads`), so the clause is either ignored or a false-FAIL source. Law/test: Rule 2 row hygiene. Evidence: `gold strictly reserved for open ways`. Fix shape: drop the invariant clause or restate it ("gold on the ground = an open way").

12. [MINOR] harness/gate_checks.json:346 + src/app/minimap.rb:49-59 — New row `minimap_reads` contradicts itself ("gold dots for open ways/stations, a magenta dot for stations") and claims "open ways" while the code paints EVERY `map.transition_at` gold, shut/level-gated ways included (`if map.transition_at(tx, ty) then gold`), inconsistent with `exit_signage_reads` ("never on a shut, dark-slab way"); "the pattern plausibly matches" is soft language. Law/test: Rule 2 row accuracy. Evidence: `gold dots for open ways/stations, a magenta dot for stations`. Fix shape: fix the row wording; decide whether the minimap should dim shut ways and say so.

13. [MINOR] harness/gate_scope.json:9-43 + test/harness/gate_checks_audit_test.rb:26-32 — Coverage is audited only over `start.zone`, so live zones that are only ENTERED mid-reel escape both the row requirement and the `unread_start_zones` allowlist: slow_door (ZONE 4) and zone_8 (ZONE 8) have no zone-specific row (both transitions changed in range), and ZONE 7's range-added torches/decor are read only via `safe_boundary_reads` (SAFE chip). Law/test: gate_scope `_doc` promise ("a new gap fails") is one-directional. Evidence: scope zones = 16 of 20 data/zones; `def start_zones ... script.fetch("start", {}).fetch("zone", Game::World::HOME_ZONE)`. Fix shape: audit `data/zones/*` minus fixtures against scope+allowlist, and allowlist ZONE 4/8 with a reason or add rows.

14. [MINOR] harness/pins.rb:34,89 + harness/run_wall.sh:35 — `RENDER_PATHS = %w[src/app src/game data]` omits `harness/scripts`, `harness/gate_checks.json` and `src/core`, so a re-cut script or re-worded row leaves its pin "current"; the pins `record` rc is not captured in run_wall.sh (a `PINS REFUSED` lands only in the per-script log, never in the sweep summary); and a hand `rake gate` re-gate (the wall record's REGATE section) never updates the ledger, so FAILED sticks after a green re-gate. Law/test: pins.rb header purpose ("computed, never remembered"). Evidence: `out = git("log", "--format=%h", "#{commit}..HEAD", "--", *RENDER_PATHS)`. Fix shape: add the harness paths to drift, capture the record rc into the summary, and let `rake gate` record a pin with tag `regate`.

15. [MINOR] test/harness/sim_identity_canary_test.rb:15-29 — The header's "ACTIVE bank (2026-08-30, v20 T7 …)" paragraph was not moved to history when FASE 6.1/6.7 replaced the bank (ACTIVE at :129 = world_loop + floor3_run + brasa2_run; old floor3_run preserved as F6_RETIREMENT at :135), and the re-authored floor3_run reuses the retired script's name, so `drafts/_gate-verdicts.log` and pins conflate two choreographies under one key (the last affirmative `challenger_tell_reads` pass "20260830 floor3_run" is the OLD script). Protocol itself was followed in shape (retire + preserve + peer word, record drafts/_junior-mundo-vivo-f6-20260905.md; world_loop hash unchanged e0b1f38f). Law/test: versioned-bank protocol header. Evidence: `# ACTIVE bank (2026-08-30, v20 T7 — floor-3 MEDUSA LOWER retheme,`. Fix shape: retitle the T7 paragraph PREVIOUS, add an ACTIVE (FASE 6.7) paragraph, consider `moss_run` as the new sentinel's name.

16. [MINOR] harness/vision_critic.py:62,88 — The two transports judge with different critics: Bedrock `MODEL = "us.anthropic.claude-fable-5"` vs gateway default `GATEWAY_MODEL = ... "fable-5.1"`, while the docstring (:14-16) promises "Same prompts, same verdict law, same retry shape — only the wire changes"; verdicts become seat-dependent (Junior's gateway sweeps vs Gabriel's Bedrock sweeps). Law/test: cross-machine gate comparability law (world_scene.rb:23-28 spirit). Evidence: `GATEWAY_MODEL = os.environ.get("CRITIC_GATEWAY_MODEL", "fable-5.1")`. Fix shape: pin both defaults to the same model id family or record the critic model in each verdict entry.

17. [MINOR] harness/vision_critic.py:225 + harness/gate_checks.json:326,330,314 + harness/scripts/brasa{1,2,3}_run.json, tower{2,3,4}_run.json — Lore/theme names in harness: `KEY_MOMENTS_PROMPT` still says "gate into the dungeon (Threketh)" (a fiction zone name), rows use "MEDUSA TOWER" / "BRASA" / "MUSGO" and script files are `brasa*_run` / `tower*_run` while the mechanical ids are `ember_1..3` / `dungeon_2..4` / `low_quay` (retired `varekka_duel.json` keeps a fiction name). Law/test: standing owner order 2026-08-16 NO LORE ("no fiction names anywhere in code, data, or docs"; identifiers mechanical) — project-wide, other areas will see the src/data half. Evidence: `spawn in town -> walk east -> gate into the dungeon (Threketh) -> husk fight`. Fix shape: s/Threketh/DUNGEON/; s/MEDUSA TOWER/DUNGEON 2-4 (tower)/, s/BRASA/DUNGEON 5-7 (ember)/ in rows; rename scripts to ember*_run / dungeon*_run (one commit, pins/scope follow).

18. [MINOR] harness/make_tour.sh:44,47 — Owner-facing tour cards promise "BOSS 2 (phased) at the far end of the loop" (tower4_run) and "BOSS 4 (phased) on the dais" (brasa3_run) while the banked gate verdicts for both reels say `boss_phase_pips_read ... not exercised by this script` (no nameplated boss on camera). Law/test: human-facing-output accuracy; "Judge builds, not briefs". Evidence: `"tower4_run|DUNGEON 4 - the bottom|BOSS 2 (phased) at the far end of the loop|0"`. Fix shape: subtitle what the reel shows, or extend the two sentinels to reach the boss (also fixes finding 2).

19. [NIT] harness/vision_critic.py:280-291 — The self-gating guard is one-directional: `pass=false` + "not exercised" voids the verdict, but `pass=true` + "not exercised" on rows WITHOUT a self-gating clause (`zones_distinct`, `actors_distinct`, `hud_three_bars`, `hud_level_strip_reads`) is accepted silently — the banked verdicts show `zones_distinct` passing "not exercised" on every non-nest reel. Evidence: `self_gating = {c["id"] for c in checks if "pass with why=" in c["check"]}`. Fix shape: also void a verdict when a non-self-gating row passes with a not-exercised why.

UNCERTAIN: (a) whether tower4_run / brasa3_run frames ever show BOSS 2 / BOSS 4 — settle by opening `captures/tower4_run_gate_a/frame_1971.png`, `brasa3_run_gate_a/frame_0501.png` or grepping `EVENT challenger_engaged` in their tmp/wall logs; (b) the exact manifest keys failing for the 7 census scripts at 064bd80 (MANIFEST FAIL lines live only in the pruned worktree's tmp/wall) — a fresh `rake manifest` over one gate log per script settles it; (c) whether multi_floor_descent's captures (aimed on the pre-swap dungeon_1 map, last cut 2026-08-21) still land on the intended moments — manifest passed (zone_entered>=4) but the critic's 09-05 verdict said "reel stays in one place"; compare frame_0376/0407 against the old reel; (d) whether `corpses_persist`'s "pale warm patch with a thin near-black rim" still describes the sprite dead pose — settle by one post-kill PNG; (e) whether pins were ever written in game-two-wall3 (the curated log dropped the `PIN recorded:` lines).

CLEAN: soak/** (untouched in range; run_soak.sh exit paths explicit, no `|| true`); harness/run_wall.sh rc capture itself (pipefail + PIPESTATUS[0] on both gate and manifest, `exit 1` on any fail); Rakefile `:gate` (double replay, md5 compare, `sh` raises on critic nonzero, INFRA error = rc 2, no silent path when SKIP_CRITIC/CHECKS are unset); vision_critic.py gateway transport (coverage-mismatch + contradiction guards intact, HTTP 4xx surfaces immediately, no key printed); harness/scenes/world_scene.rb + menu_scene.rb (art/ambience/tileset wired identically to window.rb; locale en + local:false pins intact); harness/replay_runner.rb (captures inside update, video subdir excluded from gate globs); test/harness/pins_test.rb, run_wall_test.rb, wall_pin_test.rb, gate_checks_audit_test.rb (real-process, no mocks, assertions match the code); sim-identity ACTIVE bank: world_loop hash byte-unchanged across the range (e0b1f38f…), new sentinels' retirement preserved; harness/tour_card.py; make_clip.sh; harness/scripts/tower*/brasa* JSON structurally valid (start zones exist, manifests present, out_dirs unique).
