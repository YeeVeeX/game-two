# SPARK -- v20 T3: POTIONS IDENTITY + STRIP ESCALATION -- locale rename + always-on sustain pair + full-wall re-pin -- staged s117, fresh session (2026-08-29)

You are the fresh session for ticket T3 in game-two (cwd
~/workspace/game-two). AGENTS.md + MEMORY auto-inject beside this
brief; live files beat this spark on any drift. Ruby per shell:
export PATH="/c/Ruby34-x64/bin:$PATH". Working language English;
player surfaces stay PLACEHOLDER register (functional dictionary
words only). Quality over cost (owner order): thoroughness on
judgment, never skipped verification, economize context instead.

MISSION: execute spec ticket T3 (docs/superpowers/specs/
2026-08-28-v20-descent-cycle.md section T3) end-to-end: the
provisions system becomes legible AS potions -- (a) locale identity
rename across en/es/pt-br, (b) the R-A2 STRIP ESCALATION (always-on
sustain pair -- see MISREAD GUARD below), (c) afield U/R verb
legibility -- plus two coupled obligations this seat recorded for
you: the boot-banner-on-start-jump harness quirk fix (pre-sweep) and
the sustain_run re-cut (its choreography is provisions-shaped). DoD
= suite green + attended gates green + language critique PASS +
full-wall re-pin DETACHED with an HONEST pass law (below) + record +
push. T3 lands BEFORE T4 (foundation L4 order law).

NOT this session: T4 totem (next spark, unblocked by your review) --
kill_xp/cap/k values (T2 closed them; curve untouched) -- provision
PRICE/effect/cap values (economy.json frozen, R-A2 recorded
non-goal) -- per-reason refusal CUE text (R-A2 non-goal; TELEMETRY
reasons{} already carries it) -- vat/use-side hint (R-A2 non-goal;
the always-on pair IS the use-teaching) -- level-up juice rider
(parked, own lane-B session) -- inventory/equipment (deferred, L14)
-- the 10 remaining C2-QUEUED wall reds + threat_pull + zone_catchup
(C3's debt, stand red BY NAME).

## MISREAD GUARD (the #1 risk in this ticket, read first)

The spec's line "(b) R-A2 escalation -- bank BUY hint strip" is
COMPRESSED and can send you to re-implement the bank hint. Do not.
Ground truth (verify in code before any edit):

- R-A2 Ticket A (bank BUY hint) ALREADY SHIPPED: renderer.rb
  sustain_hint (~line 214, drawn in draw_station_ledger bank branch).
- R-A2 Ticket B (telemetry reasons{...}) ALREADY SHIPPED:
  src/game/telemetry.rb ~line 331.
- What executes NOW = the recorded ESCALATION PATH (R-A2 grill spec
  section "Escalation path"): the eighteenth's verdict showed
  bought=0 WITH the hint live, so the always-on sustain pair fires,
  owner-ratified at the v20 grill (foundation L14). Concretely:
  src/app/controls_overlay.rb decision 7iii gate (~lines 54-57
  sustain row + ~71-77 provisions counter) flips from
  provisions>0-conditional to ALWAYS-ON. Presentation-class change,
  layout untouched; its cost is exactly the priced full-wall re-pin
  (~35 scripts, ~3h detached).

## STEP 0 -- ENTRY + CLAIM (before anything else)

1. git fetch origin + ff-merge. Read from disk, in this order:
   docs/CHECKPOINT.md TOP entry (s117) -- spec section T3 --
   foundation L14 + lane B (drafts/_v20-foundation-20260828.md) --
   drafts/_rA2-grill-spec-20260820.md WHOLE (Q1-Q4 + non-goals +
   escalation path + tickets; it is this ticket's design law) --
   drafts/_v20-t1-wall-20260828.md sections 2 and 9 (standing reds
   BY NAME + the sustain_run watch item + the boot-banner debt) --
   src/app/controls_overlay.rb + test/app/controls_overlay_test.rb
   (~line 105: the 7iii test block you will rewrite) --
   data/strings/en.json + es.json + pt-br.json --
   harness/gate_checks.json rows: provisions_counter_reads (~241),
   sustain_success_cue_reads, sustain_refusal_cue_reads.
2. Collision check (s56/s104 law): new Junior commits since THIS
   SPARK's landing commit + checkpoint CLAIMED grep + seat mail inbox
   (~/.pi/agent/mail/game-two/inbox/). Already handled, do not
   re-process: Junior's 664d4f8 priority ask (D2+BOSS2 post-T4 slot +
   J-T3 pockets) is SURFACED to Gabriel in the hub chat -- it claims
   nothing before post-T4; if his ratification verbatim has landed by
   your session, bank it in the checkpoint, one line. Junior
   ratifications (floor -1 played verdict, L3 pick) may also have
   arrived -- bank verbatims the same way. T3 already claimed by his
   seat -> take T6 (B4 mercy-floor, data-only, lane E) and record the
   swap; NEVER improvise T4.
3. Push a CLAIMED: T3 (seat, date) line at checkpoint top, commit,
   push, THEN start.

## THE SURFACES YOU INHERIT (verified live s117)

- Locale keys (the rename set; grep for consumers before renaming
  VALUES -- key NAMES stay mechanical per placeholder law, only
  display strings change): overlay.sustain ("provision" / es+accent /
  "suprimentos") - hud.provisions ("PROVISION" / es+accent /
  "SUPRIMENTOS") - cue.provision_bought/_used ("PROVISION BOUGHT"
  family) - cue.provision_refused ("REFUSED"/"RECHAZADO"/"RECUSADO"
  -- NOT potion-specific; keep unless the critique argues otherwise).
  (Exact accented es forms: read them from the live es.json -- this
  spark stays ASCII by clipboard law.)
  The bank hint composes from hud.provisions so it inherits the
  rename free. pt-br "SUPRIMENTOS" (supplies) is the identity bug in
  its purest form -- the pt-br rename is the felt fix.
- Word choice: functional dictionary words in each locale's everyday
  register (es-CR owner, pt-br Junior); match the accent conventions
  ALREADY LIVE in es.json/pt-br.json (the accented PROVISION-word
  precedent in es.json). The
  natural family is the potion word in each language; you own the
  final call WITH the blocking language critique (accuracy vs
  presentation, separate axes -- human-facing-output skill).
- GLYPH PROOF EARLY: es accents render live today; pt-br cedilla/
  tilde glyphs are UNPROVEN in captures. Before committing to words,
  render the pt-br candidates in one scripted capture and READ the
  PNG. No lawful glyph -> pick a diacritic-safe dictionary word, note
  it in the record.
- Telemetry: UNTOUCHED. "TELEMETRY sustain ... reasons{...}" is
  machine/oracle-facing vocabulary (soak chain_check + tests consume
  it); the identity fix is a PLAYER-surface concern. The spec's
  "wording may extend" is permission, not an ask.
- AGENTS.md controls line + docs/JUNIOR.md provision mentions: update
  register at close (docs commit; no freeze pending -- verify none
  armed in the checkpoint first).

## PHASES

1. **Boot-banner quirk fix (pre-sweep micro-fix, s116 debt).** Stale
   ZONE 1 banner over start.zone ground poisons zone-identity vision
   rows (dash_strike_rip QUIRK-RED twice). Fix = apply_start banner
   refresh in the harness (read the s116 wall record's diagnosis
   first). Verify: rake gate dash_strike_rip PASS + one more
   start.zone script (low_quay_run or varekka -- varekka is C2-red,
   prefer low_quay_run) BEFORE the strings work. Small, contained,
   own commit.
2. **Strings + escalation + tests, one bundled change-set.**
   (a) rename the display strings in the locale trio; (b) flip
   decision 7iii in controls_overlay.rb (sustain pair + counter
   always-on) + rewrite its test block (~105+) to the affirmative;
   (c) verb legibility rides (a) via overlay.sustain. Rewrite the
   three gate_checks rows affirmative for the new law (always-on pair
   at ALL provision counts, new nouns in cue rows). Residual-
   tolerance lesson (2026-08-26, project memory): after rewording a
   row, gate on a reel staging the class's STRONG member for the
   affirmative read. Suite green via hook at commit.
3. **Language critique (BLOCKING) before any sweep spend.** All
   changed player-visible lines, three locales, accuracy and
   presentation scored separately (human-facing-output checklist).
   Cross-vendor taste on es/pt: one council consult, budget declared
   (this is the session's single council spend). A BLOCK = fix words
   + re-critique BEFORE re-pinning 35 captures against wrong words.
4. **sustain_run re-cut (T3's own Rule 2 obligation).** Its manifest
   choreography (10 buys / 4 uses / 6 refusals) is C2-drifted AND its
   surface is THIS ticket's. Re-cut against today's sim (waypoint-
   tuner pattern, tools/tune_*.rb precedents; write
   tools/tune_sustain_run.rb if none exists), manifest wants
   byte-honest (never weakened), then rake gate sustain_run = the
   strong-member affirmative gate for the potion surfaces (hint +
   always-on pair + buy/use/refusal cues all read). This gate doubles
   as phase 2's strong-member obligation.
5. **Attended spot-gates.** rake gate on: world_loop (everyday-loop
   canary, strip line moved in every frame) + ledger_loop (bank-hub
   surface, green since s116). Both must PASS before the sweep.
6. **Full-wall re-pin DETACHED (harness/run_wall.sh v20-t3).** Launch
   via powershell Start-Process (detach law), unset MSYS_NO_PATHCONV
   inside the script, judge by DONE marker + per-script rc lines,
   NEVER under a bash-call timeout, NEVER edit code/harness while it
   runs (2026-08-20 contamination lesson -- the sweep loads source
   fresh per episode; stage ALL commits before launch, then hands
   off the tree). OPTIONAL pre-launch tooling (recommended, s116
   debt): a single bounded retry-on-timeout around the critic call in
   the gate path, proven on ONE attended gate before the sweep; if it
   misbehaves, revert it and run the old behavior instead -- never
   debug tooling mid-sweep. Crashed critics after the fact re-run
   STANDALONE on the frozen gate_a captures (same-artifact law).
   **HONEST PASS LAW (supersedes the spec's stale "rc=0 all
   scripts"):** the red set must be a SUBSET of the named standing
   reds -- {aoe_specials, basement_pocket, burn_duel, level_gate,
   loot_loop, nest_advance, toll_pocket, varekka_duel, vat_economy,
   zone8_crossing} (C2-QUEUED, 10 after sustain_run re-cuts) +
   threat_pull (RETIRED-PENDING-C3) + zone_catchup -- each red for
   its SAME recorded manifest reason (compare against the s116
   census/wall record). ANY new red, any new failure class on a
   standing red, or any determinism red = a T3 finding: STOP,
   classify, fix or surface -- never rebank over it. Greens must
   include every script that was green in the v20-t1 harvest.
7. **Fresh-eyes review (Rule 6).** Headless scrubbed pi over the
   ticket diff (scrub PI_* env; reviewer reads spec T3 + R-A2 spec +
   record + diff; touches NOTHING including seat mail; last message =
   one JSON verdict). Bank verbatim. BLOCKING items: fix this
   session. Review blocks T4.

## HARD LAWS

- Layout untouched: strip row geometry, fonts, positions -- strings
  and the 7iii visibility condition ONLY. Zero sim changes; zero
  balance numbers move anywhere.
- Manifest wants are never weakened to pass a gate (s116 law).
- Two-instance law: never launch bin/play beside a live owner seat.
- Saves: untouched, unread, no probes owed this ticket.
- src/game/world.rb untouched (extraction rides T4). Line caps green.
- Commits: explicit paths, one concern each (quirk fix / feat strings
  +escalation+tests+gate_checks / tooling retry if taken / record+
  checkpoint docs). Never git add -A. drafts/_gate-verdicts.log rides
  the docs commit.
- Size guard (T1 precedent, s115/s116): everything through phase 5 +
  critique lands attended IN-session; the detached wall may harvest
  in a same-day follow-up commit if the window tightens -- verdicts
  owed before T4 unblocks either way.

## DoD (then STOP)

- drafts/_v20-t3-potions-<date>.md: word table (old->new x3 locales +
  critique verdicts both axes verbatim) + glyph-proof note + quirk
  fix + escalation diff summary + sustain_run re-cut ledger (wants
  before/after, byte-honest) + attended gate rc lines + wall red-set
  vs named-standing-reds comparison table + review verdict JSON.
- Suite green via hooks on every commit; push.
- Checkpoint s118 entry (top): what shipped, wall outcome one-liner
  (red set == subset check), critique one-liner, review one-liner,
  next = T4 (totem pilot -- NINETEENTH delta clock starts there),
  CLAIMED resolved DONE. Commit, push.
- Queue line for the owner (es-CR, everyday words): que las
  provisiones ahora se llaman pociones en los tres idiomas, que el
  boton y el contador ahora se ven SIEMPRE en la barra (ya no hay que
  descubrirlos), que la pista de compra en el banco ya existia y
  ahora dice pocion, y que sigue T4 (el totem de curacion en el piso
  -1).

## BUDGET/STOP (Rule 7)

One attended session + one detached ~3h wall sweep. Council: 1
(language critique, cross-vendor es/pt taste). Bedrock: ~4-5 attended
gates (dash_strike_rip, low_quay_run, sustain_run, world_loop,
ledger_loop) + the 35-script sweep + one timeout retry each
(standalone re-run on frozen gate_a captures). STOP and surface
(never improvise) on: any NEW wall red or new failure class on a
standing red -- determinism red anywhere -- language critique BLOCK
twice -- pt-br glyphs unrenderable with no dictionary-safe fallback
-- the same gate red twice after honest fixes -- Junior collision --
context >= 75% with attended work unlanded (compact-checkpoint skill,
stage the continuation).
