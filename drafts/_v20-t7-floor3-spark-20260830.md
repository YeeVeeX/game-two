# SPARK — v20 T7: FLOOR -3 CONTENT (low_quay retheme to MEDUSA LOWER + new kinds + cap 15) — lane D closes its content arc; CONTENT PRIORITY IS OWNER LAW — cut at T6b close (2026-08-30)

You are a fresh session executing v20 ticket T7 in game-two
(cwd ~/workspace/game-two). AGENTS.md + MEMORY auto-inject beside this
brief; live files beat this spark on any drift. Ruby per shell:
`export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language English;
peer surfaces es-CR / pt-br. Quality over cost: design and review ride
maximum thinking; only mechanical closed-DoD sub-steps ride lower.

OWNER RULING (s120, standing): CONTENT PRIORITY — "I don't see any new
zones, areas, enemies, items, mechanics." T6b answered with floor -2
(s122, SHIPPED). THIS ticket completes the descent: ZONE 5 becomes
floor -3, the jellyfish abyss, with its own fauna and the band's last
cap step. After T7 the whole v20 vision is PLAYABLE ground:
-1 sand/roots -> -2 submerged plain -> -3 abyss.

Law of the ticket: spec §SECOND WAVE
(`docs/superpowers/specs/2026-08-28-v20-descent-cycle.md`) + foundation
L5 (cap never outruns content; 15 rides -3) · L6 (deep pays more;
existing kill_xp rows untouched at first ship) · L13 (Rule 2 costs
priced) (`drafts/_v20-foundation-20260828.md`) — foundation wins on
disagreement. Method precedent: T6b end-to-end
(`drafts/_v20-t6b-floor2-20260829.md` — READ IT FULLY, it is the
pipeline you will reuse: build script -> pilot.ldtk splice -> importer
-> deliberate copy -> gates). Alternatives named for the record: C3
stance re-session and B4 knob stay E-lane (anytime, own sessions); the
NINETEENTH fun-verify declaration is a peer/owner call AFTER lane D
closes — building -3 first completes the delta it will measure.

## STATE YOU INHERIT (verified at T6b close s122 — re-verify from disk)

- **T6b SHIPPED** (record `drafts/_v20-t6b-floor2-20260829.md`, commits
  `edfd6a0..e1029bc`): district_two = floor -2, 88x44 FIEHONJA, lurker
  (xp 40) + warden (xp 90) live, cap 13, review PASS 0-blocking, wall
  sweep 36/38 (mercy_floor = critic flip re-run PASS; burn_duel =
  NAMED pre-existing debt, seizure staging broken since T1 — NOT
  yours). Suite 1344 runs green.
- **low_quay TODAY** (read `data/zones/low_quay.json`): ZONE 5, 46x21,
  hand-maintained v1 JSON, floor UNSET, drop_gradient already deepest
  ([[0,3.0],[15,3.5],[30,4.0]] anchor [2,4]). Transitions — the
  ENDPOINTS CONTRACT: `[1,4] -> slow_door (spawn [7,2])` free both
  ways; `[44,19] -> zone_7 (spawn [2,14])` with **requires_defeats: 1
  outbound; the return is free** (the world-JOIN law, AGENTS.md). NO
  seal station inside low_quay and NO low_quay tuple in the live
  chain's breached list (verified: saves/world.json breached =
  basement_2/district/district_two/dungeon_1/zone_7 only) — the
  T6b-style seal-coordinate pin DOES NOT bind here; geometry is freer.
- **THE BOSS LIVES HERE (the ticket's sharpest edge):** enemy_spawns =
  rusher x22, rusher_hater x7, **challenger x1 = BOSS 1**. The whole
  boss machinery stages in this zone: boss_1_defeats save counter,
  BOSS 1 SPAWNED/DEFEATED lines, seize/inscription-burn, the
  requires_defeats gate on the zone_7 way, and the test families
  low_quay_test / challenger_test / dread_test (+ world_test rows).
  The retheme must keep BOSS 1 spawning with a workable arena and
  re-coordinate those stagings. BOSS-1-dread iteration stays PARKED
  (OPEN-FOR-EXPOSURE, zero code owed) — retheme is presentation+graph
  around him, never dread design.
- **Reference** (banked, Junior's): `drafts/_junior-zone2-map-directions-20260828.md`
  §v3 "MEDUSA LOWER" — 52x52, PNG
  `drafts/_refs/junior-zone2-v3-medusa-lower.png` (git-file md5
  `124155a71359c8b23c37c0dbbe183e3b`, verify at entry), drawn as a
  ZONE 2 candidate, BANKED FOR THE DEEPEST FLOOR by foundation L3;
  his floor-2 doc ratifies the trilogy: "-3 (ZONE 5) será o MEDUSA
  LOWER, o abismo das águas-vivas" + deeper = darker (+bluer). His
  drawing: 5 medusas guarding the core + 20 minions. There is no
  88x44-class dedicated -3 drawing — ADAPTATION is expected: keep his
  grammar (abyss chambers, medusa core), re-shape to this zone's own
  size (52x52 or your defended call), and RECORD the adaptation as
  the recorded lean for async ratification (T6b §1 pattern; Junior
  approved iteration: "iterar = mudar parâmetro").
- **Engine available:** wall_inner `%` (T5) + sand `s` (T6b) tile
  types; TileVariants/map_artifact fully render-ref generic; importer
  mapping = pure data (char_for_int_grid); per-ford/per-feature exit
  law lives in tools/build_district_two_v3.py (copy its skeleton).
  Hostile PROJECTILE arcs are engine-supported but UNUSED by any
  hostile kind (world.rb launch_projectile + retreat_step anticipate
  it — verified T6b session): a ranged abyss kind would be the first
  genuinely NEW behavior class. A10 vocabulary: ambush (lurker) and
  guardian (warden) are taken; hit-and-run/ranged is open.
- **Balance surfaces:** progression.json curve {k:40, level_cap:13} +
  kill_xp {husk 8, rusher 15, rusher_hater 25, challenger 120,
  lurker 40, warden 90}. Pacing: dE(14)=6320, dE(15)=7360 (standing
  script `tools/pacing_table.rb`, CAP=15 to preview). NAMED TENSION
  for your design: the -3 gradient must pay > lurker 40 (L6) while
  the challenger at 120 stays the elite bounty — if -3's guardian
  lands too close to 120, either compress (guardian <= ~105) or
  propose a challenger re-price as an EXPLICIT named decision
  (existing-row touch = never silent; L6's "untouched at first ship"
  means the default is compress).
- **Wall/checks state:** gate_checks.json 77 rows (floor2_* trio +
  wall_class last); `low_quay_reads` row (drowned under-harbor
  wording) CHANGES with this retheme; map_checks zone_grids names
  ZONE 5 "drowned dark green-teal" — both updates ride the retheme
  commit. `low_quay_run.json` starts IN this zone and has been
  retired-pending-reauthor since T1 (gate_rc=1 manifest_rc=1 in the
  T6b sweep, baseline-identical) — THIS ticket reauthors it as
  `floor3_run.json` (the zone's own wall member) and retires the old
  script file; one retired debt closes naturally.
- **Pilot set after T6b:** zone_7, basement_1, basement_2, dungeon_1,
  district, district_two (pilot_authoring_test + tile_map_test pilot
  list). low_quay GRADUATES INTO it this ticket; v1 list becomes
  [camp, slow_door]. Zone count stays 14.
- **Hub items NOT this ticket's scope (surface, never execute):**
  unprocessed gamesmith v2 mail in the inbox (still open since s121) ·
  burn_duel seizure-staging re-author (named debt) · B4 knob (T6) ·
  C3 stance re-session · Junior's async ratifications (v20 staging +
  the -2 v3 pick) — if his seat landed commits/mail overnight, READ
  them at entry before building.

## STEP 0 — ENTRY (before anything)

1. `git fetch origin` + ff-merge. Read: checkpoint TOP entry — this
   spark — T6b record FULLY (your pipeline) — the MEDUSA directions
   §v3 + PNG (read tool, it is an image) — spec §SECOND WAVE —
   foundation L5/L6/L13 — `data/zones/low_quay.json` —
   `data/zones/slow_door.json` + zone_7's transition rows INTO
   low_quay (zone_7.json is a PILOT EMISSION: its spawn-coord edits go
   through authoring/pilot.ldtk + re-import, the T6b district-hole
   precedent) — `tools/build_district_two_v3.py` (your skeleton) —
   `tools/tune_floor2_run.rb` (your reel pattern) —
   test/game/low_quay_test.rb + challenger_test.rb + dread_test.rb
   headers (know the stagings you will re-coordinate).
2. Collision check (s56/s104 law): peer commits touching low_quay,
   authoring/, data/balance/, or a T7 CLAIMED line in checkpoint /
   seat mail. On collision coordinate, never duplicate.
3. Push `CLAIMED: T7 — <seat> <date>` into docs/CHECKPOINT.md top
   BEFORE building (two-seat race law).

## MISSION

1. **Floor -3 = low_quay retheme** adapting Junior's MEDUSA LOWER:
   abyss-dark palette (deepest of the trilogy — but the ratified luma
   laws still bind: wall-floor spread >= 40, floor < motif <
   (floor+wall)/2, test/game/zone_identity_data_test.rb), medusa core
   guarded per his drawing, chamber grammar with wall_inner coexisting
   with bounds, sets `floor: -3`. Size = your defended call (his 52x52
   vs today's 46x21; perf-prove whatever you pick). ENDPOINTS
   preserved: slow_door edge free both ways, zone_7 way keeps
   `requires_defeats: 1` outbound + free return; inbound spawn coords
   into low_quay re-anchored lawfully (slow_door.json = one-line hand
   edit; zone_7's row = pilot.ldtk entity edit + re-import). BOSS 1
   keeps a staged arena (his spawn + seize room to breathe) and every
   boss test re-coordinates to the new geometry.
2. **New creature kind(s)** for the abyss: kit block(s) +
   OWN kill_xp rows, gradient above lurker 40 (L6), challenger
   eliteness resolved per the named tension above. Propose-and-defend
   kind count and stats against the pacing table + A10 archetypes —
   the RANGED/hit-and-run slot is open and engine-ready (prove the
   hostile projectile path with a boot+combat test before relying on
   it; if it proves out, the abyss gets the game's first ranged
   hostile). Wardens may ALSO appear here (the águas-vivas theme owns
   the trilogy's depths) — placement is design, their kit/xp rows are
   NOT re-tuned this ticket.
3. **Cap 13 -> 15 riding the floor** (L5's last band step): the
   level_cap knob + `tools/pacing_table.rb` output banked in the
   record. Check spell_growth thresholds still <= cap (lobber row 8 —
   fine) and the at-cap pin test stays formula-relative (it does).

## MISREAD GUARDS (each from a live T6b trap or verified fact)

1. **Record-first, evidence UNCHECKED**: open the ticket record with
   evidence sections as `- [ ]` placeholders and fill ONLY with real
   artifacts. T6b hit the fabricated-evidence trap mid-session
   (caught); the global memory line exists because of it.
2. **Boss stagings re-coordinate, dread does not iterate**: keep BOSS
   1's spawn + a fightable arena; challenger_test/dread_test/
   low_quay_test teleport to authored tiles — re-coordinate
   coordinates, never weaken assertions. If a staging cannot survive
   the new geometry, reshape geometry before touching the test's
   meaning.
3. **Per-feature exit law** (T6b's F2 dead-end, now project memory):
   every chamber door, crossing, and the boss arena mouth gets its own
   far-side reachability assert in the build script — global BFS is
   NOT enough. Copy build_district_two_v3.py's assert battery
   (walkability, BFS targets, water-unreachable-8adj if you author
   water, calm-entry >= aggro+1 at every arrival, per-exit asserts,
   entity-overlap).
4. **Water is PASSABLE engine-wide**: any abyss void/brine must block
   as wall-class (`#`/`%`) or be coral/rock-ringed water with the BFS
   assert (T6b D-RING pattern). Name the choice per area.
5. **Pilot fixpoint + importer-only door**: fixpoint-prove pilot.ldtk
   bytes BEFORE editing (json.load -> dumps(indent=2)+CRLF+trailing
   newline); low_quay.json + zone_7.json become emissions via
   deliberate copy; NEVER hand-edit an emission (pilot pin trips).
   The build script derives char/int maps from data/tiles.json.
6. **Calm-entry with YOUR kinds' aggro**: arrivals into low_quay
   (slow_door return spawn + zone_7 return spawn) need nearest-hostile
   >= aggro+1 per member (spread included). The BOSS's aggro is 45 —
   he sees the whole zone by design; the calm-entry law binds MINIONS,
   and BOSS 1's challenge machinery (range 9) is the exempted,
   intended exception (assert d>=10 from arrivals to HIS tile so
   arrival never insta-challenges).
7. **Reel survivorship** (T6b burned 5 iterations): author floor3_run
   via a tune_floor2_run.rb-pattern tuner with `start.progression`
   at the arriving band (level 13-14) — sync_max_hp! heals the delta
   (harness seam proven). No dwells inside overlapping aggro bubbles;
   free allies WILL brawl anything provoked (C3 debt is real; reel
   around it, do not fix it here). Respawn numbers on NEW rows: price
   so a clear does not refill into its own fight (lurker 1800 / warden
   2400 precedent); the reel must finish before the first respawn
   re-engages. Sync the tuner's emit block to whatever captures/
   manifest you hand-tighten (T6b review advisory 2).
8. **Canaries + baselines BEFORE any src/data edit**: bank capture
   md5s for world_loop, floor1_run, floor2_run + the low_quay
   traversal candidates you identify by start-zone census (town_gates
   starts zone_7 beside the join; multi_floor_descent runs the
   zone_7 deep chain; verify, trust the directory). rm the plain
   out_dirs first (stale-stray law); compare gate_a frames after,
   never the plain out_dir.
9. **Vision-critic discipline**: a FAIL on byte-identical frames =
   flip, re-run standalone; a Bedrock ReadTimeout = infra flake,
   re-run spaced (~2-3 min) — BOTH hit T6b live. A row failing
   CONSISTENTLY across re-runs = real; check the manifest baseline
   (tmp/manifest_baseline.txt + tmp/wall/*_v20-t1..t6b logs) before
   blaming your change. Mid-sweep source freeze: while ANY
   gate/replay/sweep runs, edit no src/ or data/ file (gate_checks
   edits are critic-only but land them BEFORE the sweep reaches the
   affected script, or after DONE).
10. **Sim-class discipline**: the gated piece = new kinds + kill_xp
    rows + cap 15 (+ the challenger decision if you take it). It joins
    the NINETEENTH's accumulated delta (T3 potions + T4 totem + T6b
    fauna/cap + this). Totem scaling to deep floors: NOT this ticket
    (L4 waits on the pilot read). Respawn/difficulty beyond the new
    rows: NOT this ticket. No freeze arms unless a fun-verify is
    DECLARED.
11. **Census pins move deliberately**: tile_map_test v1 list ->
    [camp, slow_door]; pilot list + pilot_authoring_test gain
    low_quay; floor -3 pinned beside the -1/-2 asserts; zone count
    stays 14; map_checks ZONE 5 clause + gate_checks low_quay_reads
    reworded to the abyss read (and the floor2 rows' cross-references
    to ZONE 5 if wording collides).

## RULE 2 / GATES (priced per L13)

- **floor3_run.json** joins harness/scripts/ as the zone's permanent
  member (reauthored from retired low_quay_run — delete the old file
  + its tune_low_quay_run.rb or repoint them); gate it with 2-3 NEW
  affirmative rows on the zone's strongest surfaces (abyss palette +
  medusa core + BOSS 1 presence if stageable) — T5/T6b lesson: gate
  the strong member, never only tolerances.
- **Re-gate traversal members** that the census convicts (likely
  town_gates for the zone_7-side door; anything showing ZONE 5
  banner) — INTENDED map change, versioned rebank.
- **Canaries byte-identical**: world_loop + floor1_run + floor2_run
  gate_a frames vs your pre-change baselines.
- **God-view**: rake map PROBES=1 (add 2-3 low_quay probes: abyss
  floor family, medusa-core landmark, the requires_defeats way state —
  16 -> ~19; copy artifact to frame_0000.png for the critic's glob) +
  critique with the updated ZONE 5 clause.
- **Soak**: rake soak N=1 ZONES=low_quay before close (the bots will
  meet BOSS 1 — chain_check judges logs; a boss fight in soak is
  content, not a defect, unless the chain breaks).
- **Perf** before the commit that lands the new grid (rake perf).
- **Full wall sweep detached at close** (the natural sweep; ~5
  min/script, judge by per-script rc + baseline deltas; expect the
  same 11 baseline-manifest fails + burn_duel).
- Suite green per commit (hooks); language critique on any new
  player-visible strings (placeholder law — expect N/A: kit names
  never render, ZONE 5 label unchanged).

## PHASES (serial, one bundled change-set)

1. Record-first: open `drafts/_v20-t7-floor3-<date>.md` — adaptation
   defense, kind design vs pacing table + A10, challenger-eliteness
   decision, water/wall choices, evidence UNCHECKED.
2. Pre-change baselines (canaries + traversal candidates + pilot
   emission md5s + old low_quay md5 + pilot fixpoint proof).
3. Commit A `feat(balance)`: new kit blocks + kill_xp rows + KIT_BODY
   colors + data-law tests (inert until spawned). Ranged-path proof
   test rides here if you take the ranged kind.
4. Build script (copy T6b skeleton) -> pilot.ldtk splice + sidecar ->
   import -> deliberate copies (low_quay + zone_7 emissions) +
   slow_door one-liner -> test re-coordinations -> perf ->
   Commit B `feat(v20-t7)`.
5. Commit C `feat(progression)`: cap 15 + pacing output banked.
6. Reel: tuner -> floor3_run + gate rows + map probes/checks updates
   (may fold into B if authored pre-commit; T6b folded them).
7. Gates: floor3 + canaries + traversals + map + soak; evidence
   commit + checkpoint s-entry + push.
8. Fresh-eyes review (Rule 6, scrubbed headless pi, read-only brief =
   full diff excl. emission bodies + record + laws, JSON verdict LAST
   message; brief says "touch NOTHING including seat mail"). Execute
   or disposition every advisory; receipt banked as
   `drafts/_v20-t7-review-receipt-<date>.json`.
9. Wall sweep detached; verdicts banked same-day (T1/T6b precedent);
   queue lines for the peers (es-CR + pt-br).

## BUDGET/STOP (Rule 7)

One attended session. LARGE ticket: if transcription+boss
re-coordination fills the window, land floor-geometry-through-importer
+ green suite as its own commit and STOP, staging a follow-up spark
for kinds+cap (scope-break law; L5 then holds cap 15 back). Gates: 1
floor script + traversal re-gates + 3 canaries + map + 1 soak + 1
sweep (+ re-runs only per the disrupted-gate/flake laws). Council: 1
taste consult max (only if genuinely torn on the adaptation). STOP
and surface on: pilot fixpoint red twice — perf red — any canary red
OUTSIDE low_quay/zone_7's expected rows — a boss staging that cannot
be re-coordinated without weakening a test — context >= 75% with
attended work unlanded (compact-checkpoint skill).

## DoD (then STOP)

- Floor -3 live through the importer door: endpoints + requires_defeats
  preserved, BOSS 1 spawning with re-coordinated green stagings,
  floor: -3 set, provenance-pinned, perf-proven, luma laws green.
- New kind(s) spawning there with own kill_xp rows above 40 (existing
  rows untouched OR a challenger re-price as an explicit recorded
  decision); cap 15 riding it; pacing output banked.
- All gates green: floor3_run affirmative on strong members,
  traversals versioned, canaries byte-identical, map probes+critique
  PASS, soak clean, suite green, sweep banked with zero manifest
  regressions vs baseline.
- Record + checkpoint s-entry + review PASS receipt + push.
- Queue line (es-CR): el descenso está completo — piso -3 el abismo,
  con su propia fauna que paga más, jefe en su arena nueva, y cap 15:
  la banda entera de v20 ya es jugable. (pt-br): a descida está
  completa — piso -3 o abismo das águas-vivas, fauna própria pagando
  mais, BOSS 1 na arena nova, cap 15: dá pra jogar a banda inteira do
  v20 agora.
