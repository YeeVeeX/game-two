# M5 slate (C1/C2/C3/C6) + M6 additions adoption — s77, 2026-08-25

Lane-4 queue rung (owner-ratified ladder, s77 spark): adopt-or-refuse
each of the uiux seat's M5 qualified rows + the two M6 additions.
Take-or-leave law: adoption is THIS seat's intake decision, re-measured
at OUR wall (their font/AA differs — mechanism verbatim where adopted;
their absolute numbers are evidence, never authority). s75 discipline:
ONE delta per gate pass, existing-row clauses over new rows, deviations
recorded, zero new strings (no D9 impact anywhere in this slate).

## Evidence triage (all digests computed live this session, s77)

Mails of record: `from-game-two-uiux-spec-batch.md` (M5) +
`from-game-two-uiux-coverage-audit.md` (M6), both in mail done/;
routing ancestor `drafts/_uiux-first-critique-intake-20260824.md`
(D4→C1, D10→C2, D3/D5 dissolved by C7/C4). Verified against
`game-two-uiux @ 3379b9d` git blobs at open:

- `docs/specs/design-language.md` = `83da06a13aee015d9798139f29bc14b9`
  ✓ matches the M8 mail (M8 = reading entry point, no change request;
  F-M8-1 receipt-kind raw CUE_OK noted below as offer-only).
- `data/ui/n2_nameplate_halo.json` = `3f0813a33cdb47c3a83245a3ae156fb8`
  ✓ matches the M6 mail.
- `data/ui/n4_glyph_outline.json` = `f79a57acdf3ba94104ee14284b035acb`
  ✓ matches the M6 mail.
- Staged deltas with no mailed digest — pinned HERE at adoption:
  `data/ui/c1_down_outline.json` = `7dbb6d12123193d4cab1b0d7834a7011` ·
  `data/ui/c2_strip_label.json` = `d1654e07652cce0187a329ed4ecc8e7b` ·
  `data/ui/c6_breach_line_top.json` = `7a2d1cd6812e482ad5844638c684951e` ·
  (C3 refusal record) `data/ui/c3_numeral_halo2.json` =
  `d5649f9cadb174570e564dd3f3f8ae4d`, `data/ui/c3b_numeral_bright.json`
  = `11c60041771e298a6fc87081727b0c64`.
- Live-truth check at open: `breach_line_top` is ALREADY keyed
  (c1428a5, s76 hygiene — their ":1137 literal UNKEYED" note predates
  it); C6 adoption is therefore data-only. `LABEL_RGB` and the C1/N2/N4
  sites confirmed unkeyed constants at HEAD.

## Decisions (the slate, row by row)

**C1 — down-state hairline: ADOPT.** 1px full-alpha outline
`[140,120,110]` under a dead bar (underdraw grammar = the live
POSSESSED_RING mechanism at draw_hud). Keys `hud_bar_down_outline_px`
+ `hud_bar_down_outline_rgb`. **Deviation:** their
`hud_bar_down_outline_alpha` key is NOT adopted — fixed 255 inline.
Their own D4→C1 correction proves partial alpha arithmetic-fails the
3:1 floor it cites; a keyed alpha invites silently re-breaking it, and
a key that must never move is not a tunable (D2 price_text_rgb
precedent: refuse keys that change nothing).

**C2 — strip label tone: ADOPT.** ONE value: label source
`[160,152,140]` → `[220,210,192]`, keyed as `overlay_label_rgb`
(constant removed — no dead code per their M8 ledger_font finding);
alpha arithmetic untouched (text_a−40 hierarchy carrier stays).
Secondary key glyphs ("/Space") ride the label tone by design — they
brighten in-class, recorded. **Residual recorded, not fixed:** vessel
label (kit-identity RGB) ~3.2:1 worst — kit palette is OUR custody,
untouched this pass (identity beats floor here; their spec agrees).

**C3 — numeral class: ACCEPT THE PIN, no delta (refusal, costs 0).**
Their own measurements reject both escalations (2px halo +0.02–0.04;
brightening = identity call priced, not proposed). D2's 1px halo
stands as shipped s75. Residual class ceiling ~3.2–4.2 vs the declared
DROP_CORE luminance cap RECORDED here — revisit only if a play report
names numeral legibility (then the priced escalations are the menu).

**C6 — breach line anchor: ADOPT, data-only.** `breach_line_top`
88→110 in display.json (key exists; zero code). Their M3 constraint
table: 110 turns M3-1 strike-through into a Δ16 gap and M3-2 into
Δ1 edge-adjacency (TIGHT, named); breach peak bottom 161 stays 2 rows
clear of ledger content top 163. **Refused-for-now:** the behavior-side
alternative (suppress/delay breach scale-in while the slot is
mid-entrance) — closes ALL rows including double-peak, but it is a
motion-behavior code change with a bigger blast radius than a
one-value anchor move; double-peak (needs ≥124 = ledger collision)
stays UNCLEARED and recorded. Revisit trigger: a captured double-peak
or M3-2-class collision in play evidence (M4 already proved the
co-occurrence class real at toll_pocket 0620 but never captured the
collision orderings).

**M6 N2 — nameplate halo: ADOPT.** 1px `[20,14,12]` halo under the
10px BOSS 1 cream nameplate; mechanism = D2's halo_offsets verbatim on
nameplate_font. Keys `nameplate_halo_px` + `nameplate_halo_rgb`.
Cream BANNER identity and 10px size untouched (their size-escalation
alternative priced, not proposed, not taken).

**M6 N4 — floating-glyph outline class: ADOPT.** 1px `[20,14,12]`@255
solid underdraw frame (C1/POSSESSED_RING grammar) beneath the three
light-valued glyph families: retarget cues, god mark (its floor inset
still draws on top), edge pips. **Naming (our call, recorded):** ONE
shared key family `glyph_outline_px` + `glyph_outline_rgb` — the class
is one lever by their own framing; per-surface keys invite drift.
Their `glyph_outline_alpha` refused (fixed 255, same C1 reasoning).
**Residuals recorded, not solved:** hate cue stays dark-on-dark worst
~2.5 vs outline — its color identity is the :66-69 wall-critic
arbitration, ours, untouched; the chant-target tell (dark blue family)
is OUT of the class by the same reasoning (their scope too). Drops
excluded by design (v11 size-is-primary custody).

## Order + gates (ONE delta per pass; critic budget ≤5, refusals 0)

1. **C6** → `rake gate` critic-ON on **toll_pocket** (stages seal
   breach + TOLL PAID at the new anchor). Row clause:
   `seal_breach_reads` gains "the TOLL PAID line sits clear below the
   zone-name slot line (no ink interleave where both are live)".
2. **C2** → gate critic-ON on **world_loop** (strip on camera all
   reel, possession swaps exercise the row's full teeth). Row clause:
   `controls_overlay_reads` gains label-legibility tooth (labels read
   against the band; key>label hierarchy preserved).
3. **C1** → gate critic-ON on **multi_floor_descent** (their own
   evidence frames 0376/1677 = downed vessel over dungeon/basement
   fields). Row clause: `hud_three_bars` gains the downed-bar hairline
   tooth.
4. **N2** → gate critic-ON on **varekka_duel** (the only zone that
   spawns BOSS 1). Row clause: `challenger_tell_reads` gains the
   nameplate-halo tooth. Varekka canary law: renderer-only delta, sim
   event stream untouched by construction (suite canary asserts).
5. **N4** → gate critic-ON on **world_loop** (strongest retarget-cue
   stager on the wall: 17 prior exercised judgments; district frames =
   their worst ground class) + offline **pixel A/B** (px=1 gate_a vs
   px=0 probe, s75 trap law: diff gate_a, never plain out_dir) proving
   all three site families incl. god mark (no wall script presses
   mark; class read gated where staged, sites proven deterministically
   — D2 pixel-proof precedent). Row clauses: `retarget_cue_reads` +
   `god_mark_reads` gain outline teeth; **NEW row `edge_pip_reads`**
   (deviation from clause-over-row discipline, recorded: pips had ZERO
   existing row and the M6 H1 finding — "never invisible" IS invisible
   on light walls — is exactly the judgment a wall must hold; s73/s76
   genuinely-new-judgment language applied to a row).

Wall count stays 35 — no new scripts (existing walled surfaces stage
every delta; the god-mark staging gap is a pixel-proof, not a new wall
member — call recorded here).

## Offers noted, no action owed

- F-M8-1 (receipt-kind cue text raw CUE_OK@230, 2.07–2.92 on four wall
  bands): transient + ring-redundant, uiux proposes NO delta; the D1
  chip pattern is the named precedent if this class ever closes.
- M8 specimen plates (drift baselines) available on ask.

## Receipts (filled at close, s77 — all values from captured logs)

- **C6 SHIPPED `be03270`** — toll_pocket GATE PASS (8/8 determinism ×2;
  seal_breach_reads: "TOLL PAID lands with clear air under BASEMENT 2
  in 0620" — the M3-1 interleave class gone at the walled surface).
- **C2 SHIPPED `e4dd3c4`** — world_loop GATE PASS (10/10 ×2;
  controls_overlay_reads: "keys pop over labels, verbs change with
  possession" — hierarchy survived the lift, their 4.87/keys-1.5×
  prediction held on camera).
- **C1 SHIPPED `670ff88`** — multi_floor_descent GATE PASS (11/11 ×2;
  hud_three_bars: "dead bars keep a thin visible outline").
- **N2 SHIPPED `c658e0e`** — varekka_duel GATE PASS (7/7 ×2;
  challenger_tell_reads: "nameplate legible" at frame_1180). Canary
  law: suite green every commit — sim event stream untouched.
- **N4 SHIPPED `d50ac86`** — TWO gates + pixel A/B (the delta's story):
  - world_loop GATE PASS (10/10 ×2) but all three class rows read
    "not exercised" — the critic MISSED the dark hate cue at
    frame_0805 (the recorded-residual member; my own new residual
    clause plausibly taught it to discount rust). Pixel A/B px=1
    gate_a vs px=0 probe: frame_0805 = 100px delta, bbox
    684–693/306–315, all exactly (20,14,12) — 8×8 cue interior (64)
    + 1px ring (36), outline PRESENT and site-exact.
  - **respawn_telegraph GATE PASS** (9/9 ×2) — the affirmative class
    judgment: retarget_cue_reads "small pale-blue cue block above a
    human head in frame_0795 reads as a turn reason" (the LIGHT
    member) + edge_pip_reads (new row, first outing) "small orange
    kit pip hugs the left viewport edge in frame_1265 toward an
    off-screen ally". A/B: 0795 = 100px cue-exact; 1265 = 44px =
    exactly the 12²−10² ring around the opaque pip. No drop deltas
    anywhere (the critic's "gold outlined drop" wording = the v11
    glow, not a class leak — pixel-proven).
  - **BUDGET DEVIATION RECORDED:** 6 critic calls vs the declared ≤5.
    The world_loop call bought a no-regression pass but no affirmative
    judgment of the delta; Rule 2/6 (quality-over-cost, gates decide)
    outrank the session number — the +1 respawn_telegraph call is the
    minimal Rule-2-whole path, recorded here and in the checkpoint.
  - **God-mark staging gap (recorded, not papered over):** no wall
    script stages a real god mark — the ONE historical wall pass
    (aoe_specials 04:32 frame 1189 "small gold glyph on the possessed
    body") is a verified critic MISREAD: the 10×10 block at
    475–484/265–274 is exact (250,235,170) = PROJECTILE (renderer.rb
    :35), a shot in flight. aoe_specials SKIP_CRITIC determinism 8/8
    ×2 + px A/B all-identical proves no mark ever draws in that reel.
    The god-mark site shares draw_outlined_quad with the two
    pixel-proven quad sizes (8 and 10); its on-camera read stays
    judged by the (clause-strengthened) god_mark_reads row wherever a
    pilot/future reel stages one.
- **D11 RESOLVED (first-critique INVESTIGATE-FIRST row):** the
  "UNIDENTIFIED amber quad in strip (world 0701 x≈304–313 y≈522–533)"
  is the off-screen lobber's EDGE PIP clamped to the bottom edge
  (clamp py max = 540−16 = 524; A/B delta bbox 303–314/523–534
  byte-matches their report). Not a draw-order leak — working as
  designed, now outlined + judged by the new edge_pip_reads row.
  D12–D14 (record-only pending D11) are unblocked for future triage.
- **Suite:** 1263 runs / 22762 assertions green via hooks at every
  commit (no new tests — renderer draw sites + data + gate rows only;
  canaries green all session). world.rb/window.rb untouched
  (1731/266). Wall stays 35 scripts; gate_checks rows 68→69.
- **Critic budget final:** 6 calls (C6 1 · C2 1 · C1 1 · N2 1 · N4 2)
  + 1 SKIP_CRITIC determinism run (aoe_specials, costs 0). C3 refusal
  0. Deviation recorded above.
- **Capture-dir note (s75 trap law):** plain `captures/world_loop`,
  `captures/respawn_telegraph`, `captures/aoe_specials` now hold s77
  px=0 PROBE frames — the live px=1 truth is the `_gate_a` dirs.
