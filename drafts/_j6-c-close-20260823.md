# J6-C close — netplay courtesy notice + menu panels (s56, Junior's seat, 2026-08-23)

Ticket: `drafts/_j6-menu-brief-20260823.md` §J6-C + D14 (md5
`00802b28…`). Commits: C1 `feat(netplay)` (partner-left notice) + C2
`feat(presentation)` (menu panels + frozen-menu fix + reel). Dev of
record: Junior's seat, carrying s55's spark. Records deviations +
evidence per the close shape.

## Shipped

- **D14 gap closed:** `NetplayOverlay#flags` maps quit-ended →
  `:partner_left` UNCONDITIONALLY — in the live game the initiating
  window closes before drawing a post-end frame, so exactly the
  ABANDONED seat renders `PARTNER LEFT — SESSION ENDED` (heavy veil,
  desync/conn-lost delivery). `net.partner_left` ×3 VERBATIM from the
  pinned table (es COMPAÑERO / pt PARCEIRO vocabulary kept).
- **Menu panels (session mode only, EXISTING readers only):**
  `Menu#net_model(session, world)` — pure, nil outside a params-known
  session — LINK panel (seat / ticks / D / stalls + stall_ms_max /
  desyncs / LINK SLOW) + SESSION panel (progression level·XP / run
  span mm:ss / live ledger beat). Drawn flanking the root menu; solo
  and the single-player wall pass session nil → absent by
  construction. Geometry/tone in display.json (`menu_net_*`, Rule 3).
- **A-close banked row RESOLVED (menu frozen OPEN over a partner-end
  screen):** chose FORCE-CLOSE — `Menu#close!` (clears the swallow) +
  one window line at the session-end boundary: the end screen owns
  the frame; no z-war, no ticking-on-ended semantics. Esc → close
  window (end-screen law) unchanged.
- **Reel:** netplay_scene gains the Menu at the window's EXACT seam
  (tick + route on the host input); netplay_session.json: menu open
  f570 / close f640, captures +600/+620 (panels over the live
  lockstep world) and +710 (the notice — the script's first-ever
  post-quit frame; the frozen-silent gap was invisible precisely
  because none existed). Checks same commit: scope updated,
  `netplay_text_legible` menu-veil clause, +`partner_left_screen_reads`,
  +`net_menu_panels_read` (both self-scoping).

## Evidence (the ladder, in order)

- **Suite:** 1156 → 1159 green at every commit (menu_net real-loopback
  lane — the netplay_overlay_test pattern, fake clock, no mocks;
  overlay quit pin updated to the NEW law).
- **Identity pairs** world_loop + low_quay_run: **24/24
  byte-identical** vs the s54 baseline (`tmp/j6b_baseline_md5.txt`,
  transitively valid — B held 24/24 against it and C touches no solo
  draw path; `tmp/j6c_current_md5.txt`).
- **Net gates ×3: determinism PASS** (SKIP_CRITIC): netplay_session
  14 captures ×2 byte-identical; desync + conn_lost re-run green.
- **Dev-eye frame read:** f0600 LINK (SEAT 1 · TICKS 371 · D 12 ·
  STALLS 99 · MAX 1500 MS · DESYNCS 0 · LINK SLOW) + SESSION (LEVEL 1
  · XP 0 · RUN 0:06) legible over the netplay world; f0620 TICKS 391
  — the lockstep advanced 20 ticks UNDER the open menu (idle-frames
  law in pixels); f0710 PARTNER LEFT full-screen, frozen world
  ghosting through.
- **Soak N=1 belt: PASS** — ticks 36120/36123, desyncs 0, both exits
  0, chain intact (bots never emit :menu; the force-close line is
  inert for them — sessions end with menus closed).

## Deviations + owed rows (each named, none silent)

1. **VISION VERDICT OWED (critic-ON ×3 net gates → hub seat).** Same
   seat limitation as J6-B (AWS creds dead). Owed:
   `rake gate SCRIPT=harness/net/<netplay_session|netplay_desync|
   netplay_conn_lost>.json CHECKS=harness/net/gate_checks.json` —
   BEFORE anything ships on top of these surfaces. Language critique
   rides those verdicts.
2. **Panel labels are locale-invariant technical register** (SEAT/
   TICKS/STALLS/MAX/DESYNCS/RUN/LINK/SESSION ×3 files, parity green):
   the brief pinned ONLY `net.partner_left` for C — no panel prose
   existed to copy. Chosen register: diagnostics vocabulary
   (TELEMETRY family), locale-invariant like placeholder names.
   Peers may re-pin; rows are data-only.
3. **NO BODY cue dims under the open menu** in f0600/f0620 (z-order:
   menu veil 50 over overlay cues 10) — scoped honestly in
   `netplay_text_legible` ("judge cue legibility on menu-less
   frames"; f0535/f0550 carry the clean reads). Consistent with the
   A-close overrun-counter note.
4. **window.rb: 266** (re-priced budget ≤280 from B's 265; cap 300).
5. **Volume rows defer CARRIES** (audio RECEIPT still absent at
   session close) — now a J6 rider, no longer tied to a ticket.

## Banked-amendment carrier check

Cue-silence guard / spell_growth duplicate keys / EventLog curation:
C touched none of their trigger files — all stay banked. Two-lists
law (machine-written files): no new machine-written file in C — N/A.
