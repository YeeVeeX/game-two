# s30 — the pre-walk pin: T5 open-gate composition test (J1) + map recon (J2)

Session 30, 2026-08-21. Owner-approved (s29 chat, recorded in the s30
spark): pin the open gate in-suite BEFORE the owners' payoff walk.
Dev of record: hub seat. Council 0 · paid calls 0 · defect branch NOT
fired.

## The gap (s29 audit, carried verbatim — why this test exists)

The gated direction at defeats>=1 had NEVER run end-to-end through the
real zone pair. Coverage before this session:

| Surface | What it proved | What it could NOT prove |
|---|---|---|
| `test/game/typed_transitions_test.rb:193` (`test_requires_defeats_opens_on_the_persisted_fact`) | the sim law, on a SYNTHETIC 2-zone fixture | the real low_quay↔zone_7 edge |
| `test/core/tile_registry_test.rb:87` | JSON shape of the declarations | any execution |
| `multi_floor_descent.json` replay (the wall) | the real pair at defeats=0 — the LOCK + the mechanical refusal | the OPENING (no wall script can stage defeats=1) |
| `harness/support.rb:24` `apply_start` | banked/zone/inscribed knobs | NO defeats knob — the open state is unstageable from the wall |

First-ever composed execution was scheduled to be the owners' payoff
moment. That is the s28 T5 arc's one uncovered seam.

## What shipped — `03f2601` (pushed, hooks green both sides)

`test/game/open_gate_composition_test.rb` — ONE file, three tests, on
the REAL store (`Core::DataStore` over `data/`, no fixture overlay)
through the REAL restore path (`World.new(save:)` → `SaveState.apply!`
→ `load_counters!` → `Crossing`'s live `defeats` lambda →
`check_transition`/`cross_through`/`enter_zone`):

1. **defeats=1 opens the real pair** — low_quay [44,19] → zone_7,
   possessed lands on the declared spawn [2,14] beside the return tile
   (the no-ping-pong law). The facts hash is asserted
   strict-decoder-accepted (`SaveState.refusal_for` == nil) so the pin
   can never drift into a state no real session reaches.
2. **defeats=0 refuses on the SAME slab** — clean differential with
   test 1 (only the counter differs); the mfd refusal is now
   suite-pinned too.
3. **the return is free** — zone_7 [1,14] → low_quay [43,19] at
   defeats=0.

All three PASSED on first run against real data — **no defect, the
composition is sound; the owners' walk will not fizzle on a seam.**
Suite 999 runs / 18747 assertions / 0 failures (996 before — the file
adds 3 runs, 7 assertions; derived, the hook run at commit is the
banked green). No Rule-2 gate owed: mechanics-only test, zero visual
surface moves (s28 edge-pin class; stated in the commit body).

## Fresh-eyes review (Rule 6) — VERDICT: PASS-WITH-NITS

Scrubbed read-only pi sub-session over `03f2601` (prompt forbade all
mutation including seat mail; raw output was at `tmp/s30_review_out.txt`,
session-ephemeral — verdict + findings banked here). Findings:

1. Gap genuinely closed — real store/pair/restore path confirmed
   against world.rb:124,144,249 · crossing.rb:27 · save_state.rb:270 ·
   both zone JSONs; same construction shape as the live loader.
2. Not vacuous — the 1/2 differential attributes the refusal to
   `requires_defeats` specifically (a dead `check_transition` cannot
   pass both).
3. Teleport does not bypass the law under test — "ANY rest on the gate
   tile" (world.rb:1109-1117) is exactly the state manufactured; gate
   `at`-tile passability is load-asserted (tile_map.rb:102).
4. Decoder assert is the real decoder (`envelope_refusal` delegates to
   `refusal_for`).
5. House laws hold — no mocks, deterministic (seed 0, empty
   ScriptedInput), challenger at [43,15] cannot act within 2 driven
   ticks.
6. **NIT (RECORDED candidate, engine-hardening class, zero code now):**
   transition SPAWN passability is validated NOWHERE — map load asserts
   `at` tiles only (tile_map.rb:102), `arrival_tiles` places the
   possessed on `t[:spawn]` unconditionally (crossing.rb:46). If a
   spawn drifted into a wall, suite + live both arrive inside it.
   Pre-existing, out of this test's scope; candidate home = importer/
   tile_map load validation (wb-pipeline hardening or v19-class).
7. **NIT (noted, harmless):** `park_allies_adjacent` is inert
   single-seat theater (consent reads controlled bodies only,
   crossing.rb:37-43) — inherited house pattern; would bite loudly
   only if consent ever expanded to AI allies.
8. Reviewer's own test invocation was seat-lease-blocked (live session
   holds the seat; it did NOT route around) — mitigated by static
   cross-check of executed paths + the hook-green suite at commit.

## J2 — map recon: god-view v0 vs the joined world (read-only, free)

Ran after a zero-ruby single-instance guard. `rake map` against the
live save: `MAP loaded save digest=41a9683d… source=saves/world.json` →
`captures/map/world_41a9683d_1787347805.png` (876×828). Save md5
re-verified unmoved after the run (read-only law held). Findings:

1. **All four pilot zones render** — BASEMENT 1/2, DUNGEON 1, ZONE 7
   panels alongside the six originals (10 panels, alphabetical grid).
2. **The T5 edge is visible AND state-aware:** ZONE 5 (low_quay) stamps
   **OPEN** at [44,19] — the live save's `boss_1_defeats: 1` flows
   through `Renderer.way_locked?` into `seal_stamps`
   (map_artifact.rb:99-107 covers `requires_defeats` gates, one
   shut/open grammar with toll seals).
3. ZONE 7's dungeon hole [33,14] stamps SEALED (unbreached — correct,
   nobody has played there); the free return [1,14] renders as plain
   gate-gold, un-gated ways carry no stamp by design.
4. Header carries the live counters (BANKED 7 · MARKS 0 · PROVISIONS 0
   · BOSS 1 DEFEATS 1); ZONE 2/3 OPEN stamps = the save's two breached
   toll seals — chain-consistent with the facts.
5. **Coverage gap: NONE** — no v19 map-data candidate owed.
   Observation only (not a docket row; no one asked for it): panels are
   alphabetical islands, topology/adjacency is not drawn — adequate for
   v0's audit purpose.

## State after s30

- The owners' first crossing is now a REGRESSION-PINNED path, not a
  first execution. Harvest lane stays armed (their walk verbatims are
  still fun-lane gold; the pin changes nothing about that).
- J-remeasure (Junior, `dd8ff40` decisive seat) still pending —
  pre-declared classes in the s30 spark §J4 stand; adjudicate on his
  bank, never before.
- RECORDED candidates from this session: spawn-passability load
  validation (nit 6, engine-hardening). Nothing added to the docket
  (no verdict landed; the docket adds no dev opinions by its own law).
