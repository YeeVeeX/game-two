# Fresh-eyes review — E1 "harness truth" (03e38b1..d637dc6)

Verified read-only from the detached worktree at d637dc6 (`git worktree list` confirms; only `brief.txt` + the JPEG untracked). Headless probes ran through `Harness.apply_start` + `World#tick` in memory (no captures, no writes). The seat-lease gate blocked the test-runner, so the suite itself was not executed; where I assert a test outcome I show the logic.

## 1. Piece-by-piece closure

**(1) Persona — closed.** Every colour/geometry claim matched source: KITS ramps (`tools/gen_premium_art.py:45-94` — striker (235,120,40), blocker rust (158,52,30), warden pink, stinger cyan, lurker (168,205,140), serpent_b (200,190,175) = the petrifier, `combat.json` confirms only serpent_b + serpent_boss phases petrify), halo (`renderer.rb:978,1056-1085`), boss bar top-center at `boss_bar_y 112`. One over-generalization: "bosses carry a nameplate + phase pips + an hp bar" — BOSS 1 has `phases: []` (`combat.json /kits/challenger`) so it carries **no** pips; this primes the critic to look for pips on boss1_writ (see piece 9).

**(2) Ledger home — closed, but the test encodes a false expectation.** Live from this worktree: `ruby -e 'puts Harness::Pins::DEFAULT_PATH'` → `C:/Users/gabri/workspace/game-two/harness/pins.json`; `vc._main_repo_root()` → `C:\Users\gabri\workspace\game-two`. Correct. However `test/harness/pins_test.rb:12` sets `ROOT = File.expand_path("../..", __dir__)` and lines 185/187/200/203 assert the resolvers equal **ROOT**. In a linked worktree ROOT is the worktree, the resolver returns the main clone → both tests are RED exactly in the scenario d4 is about (Junior's `../game-two-wall` commit pattern, which the old pins.rb header documented; the pre-commit hook runs `rake` there). Fix: compute the expected root in the test with the same `git rev-parse --git-common-dir`, not ROOT.

**(3) Pin refusal — closed for the ledger.** `pins.rb:79-87` refuses SKIP_CRITIC / non-default CHECKS; `run_wall.sh:24` unsets CHECKS. Partial vs the T0 wording "leaks into every `rake gate`": a manual `rake gate SCRIPT=harness/scripts/x.json` with a stray exported CHECKS still judges the netplay checklist and prints `GATE PASS` (Rakefile:117 — the checks path is never printed). Minor; the human-pasted "GATE PASS" is Rule-2 evidence too.

**(4) Census — counts honest, one retirement wrong, one re-cut hollow.** Headless single-run observed vs declared: threat_pull `human_retargeted 16` ✓; vat_economy `attack_hit 27 / actor_died 5 / zone_entered 2` ✓ (exactly equal); zone_catchup `zone_entered 6` ✓; zone8 `3/9/25/23` ✓. But:
- **level_gate**: the retired script, run against TODAY's sim, still stages the `LEVEL N REQUIRED` cue live on 94 frames (f350–492) with captures **383 and 409 inside it**, and it emits the wall's only `human_leashed` (observed 1). Its vision gate was green at 064bd80 (`_wall-premium-v22-20260905.log:84` gate_rc=0) and `level_gate_reads` was affirmative on 2026-08-30 ("LEVEL 2 REQUIRED chip reads on dark backing, HUD shows 1"). Only the manifest's level-up half rotted. A re-cut to `{zone_entered: 2, human_retargeted: 3, human_leashed: 1}` (the vat_economy treatment) keeps the wall's only stager of a Rule-2 surface; retiring it destroyed coverage nothing else provides and turned an affirmatively-verified row into a debt row. The record's "the refused-then-pass door story cannot happen" is half true — the REFUSAL half happens.
- **zone_catchup**: observed events are `{zone_entered: 6}` and nothing else — no hostile ever acquires the pack across six transitions. It is now a 3×round-trip reel whose purpose row (`zone_catchup_reads`) can never fire; `safe_boundary`/`multi_floor_descent` already stage transitions. Honest but vacuous; retire-with-owed-re-author (the loot_loop treatment) is the consistent call.
- Leash: not a sim regression (fires in level_gate headless; `controllers.rb:383-391`, `homecoming.rb:81-85` intact) — "observes 0 in EVERY live script" became true by retiring the one script that emitted it.
- loot_loop / nest_advance retirements: coverage claim holds — corpse_run declares all six nest_advance events, world_loop/sustain_run/ledger_loop declare banked + drop_picked_up.

**(5) zone8_crossing — closed.** Headless: `dungeon_1 → zone_8`, level 12, counts as declared; gate PASS banked (`_gate-verdicts.log` 20260905-231649). `dungeon_1.json:265 "stations": []`, scope now basement_2 only ✓. Nit: `"banked": 60` (line 9) is a remnant of the retired toll story — dead staging that puts 60 coins on the HUD.

**(6) Halo rows — closed.** specials_chain `taunt_convergence_reads` affirmative in the log ✓. Nit: the god-mark is `draw_outlined_quad` = FILLED 8px quad with a 1px dark outline (`renderer.rb:856-863`, `GOD_MARK` at :102); the row (re-anchored here) adds "the mark is a hollow SQUARE glyph" — asserts something the code doesn't do. Dormant (no `mark` press in any wall script — verified).

**(7) Renderer.build — closed, equivalent.** All four sites: world_scene/menu_scene identical to before; window.rb passes live `display`/`strings`/`bindings(local: true)`/seat exactly as before (window.rb:104); netplay_scene gains art (the intended change). New requires (`app/key_table` → gosu, `core/strings`, `core/binding_map`) add no cycle; renderer.rb already depended on Gosu. The JPEG (frame_0160) matches the re-worded rows: left body on a cyan ground ellipse with no chevron, right body on a gold ellipse with the gold chevron. Gap: no test pins the factory (nothing asserts `build` yields non-nil art or that scenes use it) — the T0 d1 regression previously survived the vision gate for months, so a unit assertion is the only mechanical guard.

**(8) Ten rows — three misdescribe the code, one staging claim is false.**
- `pickup_gleam_reads` (gate_checks.json:361) says a WAITING drop shows "a few small bright sparks rise from it and fade (no flash ring and no falling chips)". That sentence is `fx.rb:258`'s comment for the **pickup burst** (`fx.rb:54-58`, fired on `drop_picked_up` at the collector's body). A waiting drop is a bobbing gem with ONE 5px 4-point twinkle visible only when `phase % 24 < 8` (`renderer.rb:675-680`) — off two thirds of the time. `world_loop` frame_0724 shows exactly that single cross. A strict critic told to "judge it on frames where a drop sits on open ground" fails any twinkle-off frame → latent false-FAIL, and the grammar is wrong.
- `blink_flash_reads` (:381): "a brief bright flash GROWS at the destination tile". `draw_blink_flash` (`renderer.rb:1206-1216`): `grow = 10 - 10*k/total` with k rising from 0 → the violet (180,120,255) hollow outline starts wide and SHRINKS onto the body ("snapping shut", per its own comment). Also "A hostile that changes position between two frames with no flash anywhere fails" is satisfiable by every walking hostile across 20 sparse samples — a flake source in a blocking gate.
- `boss_bar_reads` (:389) asserts notches + "phase PIPS in a row beneath it" unconditionally; `draw_boss_bar` draws them only `if n > 1` (:1027) and nameplate pips only `if boss_phase_count > 1` (:1461). BOSS 1 has 0 phases → boss1_writ (this ticket's own reel, boss on camera f7–778) shows NO pips and NO notches → false-FAIL on the queued gate for this row and the pre-existing `boss_phase_pips_read`. gate_scope.json:39 scoping `boss_phase_pips_read` to low_quay is the same factual error, left in a file the ticket edited.
- `petrify_telegraph_reads`: the record (line 183) claims staging "verified" on `tower4_run` frame_0310 "pale stone slabs". I read the PNG: the three pale slabs are the possessed blocker's own arc3 `SLASH` tiles (white, inset 4 — `renderer.rb:66,1338`), no body inside them, the possessed mid-swing; the two serpents are further east and idle. The petrify telegraph is a 40px grey square WITH the winding-up serpent inside it — absent. The critic saw all 12 frames (≤20, no sampling) and said "No grey stone windup appears" (main-clone log 20260906-003350). The row has no stager and is not recorded as debt.
- Verified fine vs code: impact_fx (spark 12f outlives the 8f hurt flash, so "struck body with no spark" cannot false-fire), damage_numbers (`big: -d >= max_hp*0.25`, tints at fx.rb:203-206, wounded bar :982-995), screen_light, low_hp_pulse (light.rb:116-121, 0.3, 40f breath, bands 22%/28%), aura_ring, ally_callout (debt correctly named — `threat.json ally.enabled false`). `interact_prompt_reads` deferring ON-vs-BESIDE to E3: **right call** — `draw_interact_prompt` (:1704) fires on-or-orthogonally-adjacent today; a row asserting either would false-fail now or flip after E3, and the row still judges legibility/placement honestly.

**(9) Boss sentinels — beats real, proof thin, d18 untouched.** Headless: boss1_writ chant starts f0, `vessel_seized` lands f120 (seizes the possessed — by design, `world.rb` decision 11: nearest controlled body), capture 45 is inside the chant ✓; boss2 phases 0→1 f253, 1→2 f279, captures 256/282 on them ✓; boss4 0→1 f597, 1→2 f668, captures 600/671 ✓; no pack/hostile tile overlap at f0. But: (a) no `boss_phase*` event exists in `World::EVENTS` (world.rb:36-44), so boss2/boss4 manifests (`zone_entered/actor_died/attack_hit/telegraph`) prove nothing boss-specific — if the phases drift or the boss dies early, manifest AND vision both still PASS (rows self-gate) — the T0 d2 failure mode returns as a silent vacuous pass; (b) trims are needlessly tight: boss4 ends at boss hp **3/380**, boss2 at 35/320 with death 20 frames later — any damage retune kills the boss before the last captures; (c) `telegraph` counts come from all hostiles, adding fauna-retune fragility without proving the beat; (d) `make_tour.sh:44,47` still promise "BOSS 2 (phased)" / "BOSS 4 (phased)" on tower4_run/brasa3_run and `:38` "rope from DUNGEON 1 at level 8" — d18 is claimed in the record header (line 213) and nothing changed.

`start.at` (`support.rb:66-76`): the refusal is right but incomplete — it filters only `map.passable?`, not occupancy (walker `teleport` bypasses the per-frame body blocking, `grid_walker.rb:26-37`) and not transition/station tiles (a body resting on a way auto-crosses at tick 1). Dead allies from `start.dead` are teleported too, so boss1_writ opens with two pack corpses beside the boss that died nowhere. Teleporting into the boss room is legitimate for a legibility sentinel as long as no record ever cites these reels as fight/difficulty evidence — the record is explicit about that; keep it so.

## 2. Record honesty
Not fabricated, but three claims are false-in-effect: the petrify "verified staging" (a misread of the possessed's own attack tiles, contradicted by the critic on the same reel), the "(d2/d18)" closure with make_tour untouched, and the level_gate rationale that omits the still-live refusal half and the leash emission. Everything else I checked against the log or headless run held (96/96 world_loop at 20260905-235707, three net gates PASS with the quoted whys, zone8 PASS, window.rb 267 lines, em-dash cleanup complete, zero `mark` presses).

**What this means for you:** the mechanical pieces (ledger home, refusal, factory, route, halo grammar) are sound; what fails is oracle text and record claims — the same class T0 d8 graded Rule-2-critical, now introduced fresh. All fixes are harness/record edits, well under an hour, plus un-retiring level_gate with a re-cut manifest.

```json
{"verdict": "BLOCK",
 "blockers": [
  {"what": "boss_bar_reads asserts notches + phase pips unconditionally; BOSS 1 has phases: [] so boss1_writ (this ticket's reel, boss on camera f7-778) renders neither -> false-FAIL on the queued gate; same for pre-existing boss_phase_pips_read", "where": "harness/gate_checks.json:389 (and :337); src/app/renderer.rb:1027,1461; data/balance/combat.json /kits/challenger", "why": "a gate row that asserts a visual the code never draws for one of its three target reels cannot pass honestly - it either false-blocks or trains the critic to reason past the text", "fix": "add '(phased bosses only: a single-phase boss such as BOSS 1 shows neither notches nor pips)' to both rows; drop low_quay from gate_scope boss_phase_pips_read or scope boss_bar_reads to the three boss zones; soften the persona's 'bosses carry pips'"},
  {"what": "pickup_gleam_reads describes the PICKUP burst (8 sparks rising at the collector, fx.rb:54-58/258) as the visual of a WAITING drop; the waiting drop is a bobbing gem with one 5px twinkle visible only when phase%24<8", "where": "harness/gate_checks.json:361; src/app/renderer.rb:647-680; captures/world_loop_gate_a/frame_0724.png", "why": "row derived from the wrong code path: wrong grammar in the Rule-2 oracle and a latent false-FAIL on every twinkle-off frame (two thirds of frames)", "fix": "reword to the gem grammar: 'a small diamond gem that bobs and throws a brief 4-point twinkle at its corner (intermittent); when collected a gold +N numeral and a ring of rising sparks fire at the collecting body'"},
  {"what": "record claims petrify_telegraph_reads staging was verified on tower4_run frame_0310 'pale stone slabs'; the PNG shows the possessed blocker's own white arc3 SLASH tiles (no body inside, possessed mid-swing), and the critic's verdict on that exact 12-frame reel says no grey stone windup appears", "where": "drafts/_v22-e1-record-20260906.md:183; captures/tower4_run_gate_a/frame_0310.png; src/app/renderer.rb:66,1152-1159,1338; main-clone drafts/_gate-verdicts.log 20260906-003350", "why": "a false 'verified against the exact PNG' claim is the failure the sampling-artifact law exists to prevent; the row has no stager and is not recorded as debt", "fix": "correct the record line, mark petrify_telegraph_reads a debt row with an owed serpent_b windup stager (tower2_run 'spread + petrify' is the candidate), and re-read frame_0322/0308 for the record"},
  {"what": "pins_test asserts DEFAULT_PATH and _main_repo_root equal ROOT (the tree the test lives in); from any linked worktree the resolvers return the MAIN clone, so both tests go red exactly in the d4 scenario (worktree commits under the pre-commit hook)", "where": "test/harness/pins_test.rb:12,185,187,200,203", "why": "the ticket's own regression test encodes 'ROOT is the main clone', which is false in the workflow it fixes; a red hook in worktrees invites --no-verify or skipped sweeps", "fix": "compute expected root in the test via the same `git -C ROOT rev-parse --path-format=absolute --git-common-dir` parent, or resolve it once in test_helper; run the suite from a throwaway worktree to prove green"}
 ],
 "majors": [
  {"what": "level_gate retired although it still stages the LEVEL N REQUIRED cue (94 live frames f350-492, captures 383 and 409 inside) and emits the wall's only human_leashed (1); only its level-up manifest half rotted (vision gate_rc=0 at 064bd80, row affirmative 2026-08-30)", "where": "harness/retired/level_gate.json; drafts/_v22-e1-record-20260906.md:83; drafts/_wall-premium-v22-20260905.log:84", "why": "destroyed the only affirmative stager of a Rule-2 surface and a leash emitter when a re-cut manifest kept both; inconsistent with the vat_economy treatment", "fix": "move it back and re-cut to observed {zone_entered: 2, human_retargeted: 3, human_leashed: 1}; keep the level-up half as the named debt"},
  {"what": "zone_catchup re-cut to {zone_entered: 6} but the run observes ONLY zone_entered - no hostile ever acquires the pack, so zone_catchup_reads can never fire and the script duplicates transition coverage", "where": "harness/scripts/zone_catchup.json manifest; headless single run", "why": "honest but vacuous: a PINNED line for a purpose the reel no longer stages is the d6 'advisory' pattern in a new form", "fix": "retire with an owed re-author header (loot_loop treatment) or re-tune the route so at least one human displaces"},
  {"what": "blink_flash_reads says the flash GROWS and is a 'bright flash'; draw_blink_flash draws a violet hollow outline that SHRINKS onto the body; the 'position change with no flash fails' clause is met by every walking hostile across sparse samples", "where": "harness/gate_checks.json:381; src/app/renderer.rb:1206-1216", "why": "row contradicts the code (false-FAIL when a blink is finally staged) and carries a flake clause for every reel", "fix": "reword: 'a violet hollow square outline snaps SHUT onto the arriving body over ~10 frames'; replace the displacement clause with 'a hostile displaced 5+ tiles between two adjacent-frame captures with no outline fails'"},
  {"what": "boss2/boss4 manifests carry no boss-specific event (none exists in World::EVENTS) and reels end at boss hp 3/380 and 35/320 (death 20 frames later)", "where": "harness/scripts/boss2_phases.json, boss4_phases.json; src/game/world.rb:36-44", "why": "if phases drift or the boss dies early, manifest and vision both still PASS (rows self-gate) - the d2 failure returns as a silent vacuous pass; the tight trims make that drift likely on any damage retune", "fix": "trim run_until to phase-2 entry + ~60 frames; next SIM ticket adds a boss_phase_changed event (one emit in advance/boss_phase edge) and declares it; until then add a harness-side frame assertion (expect boss_phase==2 at capture 282/671) in the replay runner"},
  {"what": "d18 claimed closed in the piece-9 header; make_tour.sh cards untouched ('BOSS 2 (phased)' on tower4_run, 'BOSS 4 (phased)' on brasa3_run, 'rope ... at level 8' while zone8 stages level 12)", "where": "harness/make_tour.sh:38,44,47; drafts/_v22-e1-record-20260906.md:213", "why": "a claimed closure with no diff", "fix": "point the two boss cards at boss2_phases/boss4_phases (or reword the subtitles), fix the zone8 card, drop the d18 claim if not done"},
  {"what": "start.at refuses only on map passability: no occupancy check (teleport bypasses body blocking), no exclusion of transition/station tiles, and dead allies are teleported too", "where": "harness/support.rb:66-76; src/game/grid_walker.rb:26-37", "why": "a future sentinel can open with a pack body inside a hostile or on a way tile that auto-crosses at tick 1 - a reel that lies with no refusal", "fix": "reject spots occupied by living actors, transition_at and station_at tiles; refuse NAMED; leave corpses at pack_spawn or document the corpse relocation"}
 ],
 "minors": [
  {"what": "god_mark_reads adds 'hollow SQUARE glyph' but draw_outlined_quad draws a FILLED pale-gold quad with a 1px dark outline", "where": "harness/gate_checks.json:157; src/app/renderer.rb:856-863,102", "fix": "say 'small filled pale-gold square with a thin dark outline'"},
  {"what": "manual `rake gate` still honours a stray exported CHECKS silently and never prints the checklist path", "where": "Rakefile:117,141", "fix": "print 'GATE checks: <path>' and refuse harness/net checks for scripts under harness/scripts/"},
  {"what": "zone8_crossing keeps 'banked': 60 from the retired toll story", "where": "harness/scripts/zone8_crossing.json:9", "fix": "drop it (or record why 60 coins on the HUD is wanted)"},
  {"what": "no test pins Renderer.build (art/ambience/tileset non-nil, locale en, local:false) or that every harness scene uses it", "where": "src/app/renderer.rb:127-135; test/app/", "fix": "one unit test on build(data) + a grep-style scene test (the d1 regression survived the vision gate for months)"},
  {"what": "SKIP_CRITIC=1 sweep still ends 'WALL SWEEP DONE ... fails: none' exit 0 while every pin refused", "where": "harness/run_wall.sh:46-50", "fix": "echo a DETERMINISM-ONLY banner in the summary under SKIP_CRITIC"},
  {"what": "persona sentence 'bosses carry a nameplate + phase pips + an hp bar' over-generalizes; BOSS 1 has no pips", "where": "harness/vision_critic.py:86-87", "fix": "'phased bosses add phase pips'"}
 ],
 "verified_clean": [
  "Pins::DEFAULT_PATH and vision_critic._main_repo_root resolve to the MAIN clone from this linked worktree (live probes); git 2.53 supports --path-format=absolute; GIT_* scrub is correct Ruby/Python env semantics",
  "pins.rb refusal logic: SKIP_CRITIC=1 and non-default CHECKS refuse before any ledger write; explicit default records; run_wall.sh unsets CHECKS",
  "Re-cut manifests are declared <= observed single-run: threat_pull 16, vat_economy 27/5/2, zone_catchup 6, zone8_crossing 3/9/25/23 (headless run, this tree)",
  "loot_loop and nest_advance coverage claim holds: corpse_run declares all six nest_advance events; world_loop/sustain_run/ledger_loop declare banked + drop_picked_up; retired headers name owed re-authors",
  "zone8_crossing runs dungeon_1 -> zone_8 at level 12 headlessly; dungeon_1 has stations: []; seal_breach_reads scope basement_2 is staged by toll_pocket (seal_breached: 1 declared); gate PASS banked 20260905-231649",
  "Renderer.build is call-site equivalent at all four sites (window.rb live strings/bindings/seat preserved; capture scenes canonical en/local:false); no require cycle; window.rb 267 lines",
  "frame_0160 JPEG matches the re-worded netplay rows: cyan ground ellipse without chevron (left), gold ellipse with gold chevron (right); netplay_session/desync/conn_lost PASS verdicts present in the log with the quoted whys",
  "Boss sentinel beats exist headlessly: boss1_writ chant f0 -> vessel_seized f120 with capture 45 inside the chant; boss2 phase entries f253/f279 with captures 256/282; boss4 f597/f668 with captures 600/671; no pack/hostile tile overlap at f0; seizing the possessed is decision-11 behaviour",
  "impact_fx_reads, damage_numbers_read, screen_light_reads, low_hp_pulse_reads, aura_ring_reads, ally_callout_reads wording matches fx.rb/light.rb/renderer.rb (spark 12f > hurt 8f; big numeral at 25% max hp; low_hp_pct 0.3 with 40-frame breath; aura hollow square at radius; ally brain disabled in threat.json)",
  "interact_prompt_reads deferring ON-vs-BESIDE to E3 is correct: the prompt fires on-or-orthogonally-adjacent today (renderer.rb:1704) and the row still judges legibility/placement honestly",
  "world_loop 96/96 verdict banked (20260905-235707) with the five affirmative whys quoted in the record; tower4_run 96/96 and brasa3_run 97/97 PASS in the main-clone log; zero double-encoded em-dashes remain; zero `mark` presses across harness/scripts (god_mark debt claim true)",
  "Leash is not a sim regression: controllers.rb leash_home + homecoming.leash_emission intact and it fires in the retired level_gate script"
 ]}
```
