# SPARK: v18 TDD session 4 — increment 8: docs + cycle close (the SEVENTEENTH opens)

You are the dev of record in game-two (cwd `~/workspace/game-two`).
Read `AGENTS.md` FIRST — the v18 scope contract is ground truth, the
spec is FINAL (owner ratified, veto CLOSED), and the standing lore ban
holds: placeholders + functional dictionary words only. Work in
English; owner-facing text es-CR ustedeo, flat mechanics register;
Junior-facing text PT-BR using HIS ratified vocabulary.

Increments 0–7 are ALL green + pushed (`5994eaf` `c94fffb` `c6d8828`
`e7e20d2` `e5befc4` `88fd36d` `e3c4286`): persistence v1 end-to-end,
coop feel, the sustain verb (sim + presentation, Rule-2 gate PASSED
with the real critic, wall now 18 scripts), god-view v0 (`rake map`,
probes 5/5 + critique 6/6). Junior's seat ratified the pt-br sustain
strings WITH AMENDMENT (PROVISÃO → SUPRIMENTOS family, `a51b06e`) and
adjudicated the specials_chain FAIL as critic variance (17/17
effective). **This session ships increment 8 — docs + close. It is a
DOCS-ONLY increment: no code, no data, no harness changes.** Then the
cycle waits on the SEVENTEENTH (owner-paced, two days).

## Read before acting (in order)

1. `AGENTS.md` — whole file (Ruby PATH per shell:
   `export PATH="/c/Ruby34-x64/bin:$PATH"`).
2. `docs/CHECKPOINT.md` top THREE entries (sessions 3/3b/3c — one
   working session, three increments; the 3c entry carries the exact
   increment-8 content list and the SUPRIMENTOS flag).
3. The spec `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md`:
   the "TDD increments" item 8 block, the WHOLE "Fun-verify protocol
   (SEVENTEENTH — pre-registered)" section (Half A arbiter, Half B
   questions VERBATIM, the routing table — all closed, none may be
   rewritten), decision 13 (rake map usage, for the Commands block),
   and Fork verdicts F2/F4 (custody wording must match them exactly).
4. `drafts/_junior-specials-chain-retry-20260818.md` — the ratified
   SUPRIMENTOS values (use them in every pt-br sustain mention) AND
   Junior's discoverability side-signal ("não entendeu o que a
   provisão era") — that signal is PRE-REGISTERED ROUTING CONTEXT for
   the SEVENTEENTH's Half B, NOT a work item; the strip-exposure lever
   already shipped in increment 6.
5. `docs/JUNIOR.md` — whole file. Note the structure: 🇧🇷 half first,
   🇬🇧 mirror second, friendly-direct voice, numbered how-to blocks.
   The new section must match that structure and voice.
6. `PARKING_LOT.md` — the always-online fork entry (owner delegated
   timing 2026-08-17; stage 1/2/3 candidate artifacts already name
   custody-adjacent triggers). The spec's "custody-handoff entry" may
   ALREADY be covered by the stage-2 wording — add only the missing
   delta, or record "already covered" as the smallest faithful
   deviation. Never duplicate an entry.
7. `git pull --ff-only` FIRST and before every push — Junior lands
   commits mid-session (THREE live precedents, latest 2026-08-18).

## Job 0 — verify state (trust git + the suite, not prose)

- History holds `e3c4286` (increment 7) + `2d69ca5` (evidence) +
  `a51b06e` (Junior's ratification); tree clean; branch synced.
- `bundle exec rake` green (was 761 runs / 13889 assertions).
- `tmp/canary_baseline/` holds 18 dirs (sustain_run joined) — no
  re-bank owed; docs increments do not touch the wall.
- `grep SUPRIMENTOS data/strings/pt-br.json` returns the ratified
  family — if it does not, STOP and reconcile with Junior's seat.
- Any check fails → reconcile first; never build on an unverified base.

## Job 1 — increment 8 (docs + close; NOT sim-touching)

**A. `docs/JUNIOR.md` — the persistence/custody section** (PT-BR first
+ EN mirror, inserted after the v17 co-op section, matching the file's
voice). Content, in player terms (panel Kimi-Q6 fold — the custody
CONTRACT, not the architecture):
- O mundo compartilhado mora na máquina do HOST (Gabriel). Junior
  jogando solo = o SEU próprio mundo, no SEU computador. Só jogando
  JUNTO (join) o mundo compartilhado avança. (F2/F4 verdicts, exact.)
- O aviso do `--fresh`: se o host recomeça do zero, a cadeia MOSTRA —
  o contador de sessões reseta, o save antigo vira `.bak-<ts>`, e o
  joiner vê `source=fresh` na tela. Nada se perde em silêncio.
- Pull agora é CRÍTICO de schema: um seat desatualizado recusa NOMEADO
  no HELLO ("protocol version" + a dica de git pull) — pull before
  play, sempre.
- Onde vive o save (`saves/world.json`, gitignored), nunca editar à
  mão, e as linhas `TELEMETRY persist` que provam a continuidade.
- Sustain em pt-br = o vocabulário DELE (SUPRIMENTOS / SUPRIMENTO
  COMPRADO / USADO / RECUSADO), tecla U/R.
Register duties: run the human-facing-output checklist — accuracy and
presentation scored as SEPARATE axes; the language critique is
blocking at ship (global Rules 2/6). PT-BR composed by the dev and
FLAGGED for Junior's ratification in the checkpoint (the strings-flow
precedent — his lane, never blocked on him). No fiction register.

**B. `AGENTS.md` Commands block only** (surgical — the scope contract
prose is the owner's, do not touch it): add `rake map [SAVE=] [OUT=]
[PROBES=1]` (offline artifact + probe mode), the `--fresh` composition
law (solo and `--host`; `--join --fresh` refuses), `saves/world.json`
+ backup law one-liner, and the coop block pointer
(`data/balance/coop.json` seat scalars). Keep each line in the
existing Commands style.

**C. `PARKING_LOT.md`** — per read-item 6: the custody-handoff delta
under the always-online trigger family, or the recorded
"already covered" note. One entry, never two.

**D. The SEVENTEENTH run sheet** —
`drafts/_v18-seventeenth-runsheet-20260818.md`, ONE page, two halves:
- Owner half in es-CR ustedeo: the ritual (two real sessions, DIFFERENT
  days, ≥10 sim-min / ≥36000 ticks each, both seats exit by Esc), the
  harvest checklist (all four `TELEMETRY netplay` lines + every
  `TELEMETRY persist` line captured BEFORE any question; %TEMP% session
  logs as backup — the SIXTEENTH's recovery precedent), solo play
  between sessions allowed and its persist lines join the chain.
- The questions VERBATIM from the spec's Fun-verify section (es for the
  owner, pt-br for Junior) — de-primed wording is panel-ratified
  (Kimi-Q5); do NOT rewrite, reorder, or annotate the questions
  themselves.
- The mechanical Half A arbiter restated: digest chain (session 2 loads
  == latest prior saved; joiner handshake digest == host's, both
  sessions), desyncs=0 + reason=quit on all four lines, one named
  strictly-positive carried fact.
- A routing-context footnote: Junior's discoverability side-signal is
  recorded evidence for the "sustain unused → discoverability first"
  row — the adjudicator reads it AFTER the verdicts, never before the
  questions.

**E. `docs/CHECKPOINT.md` new top entry:** v18 BUILD PHASE CLOSED
(increments 0–8), cycle state = WAITING on the SEVENTEENTH; resume
point = harvest per the run sheet → adjudicate per the spec's routing
table → verdict skeleton in drafts/ (the `_v17-fun-verify-skeleton`
naming precedent). Name the pt-br JUNIOR.md section as awaiting his
ratification.

**F. Close:** commit drafts/ + docs with EXPLICIT paths (shared cwd;
hooks run the suite) → pull → push. The assets-seat pin list
(renderer.rb, creature.rb, display.json, nest.json, district.json)
must NOT move in a docs increment — if a pinned file changed, you have
broken scope: stop and re-read.

## Laws that bite

- **Docs-only means docs-only.** A defect discovered while writing
  docs gets RECORDED (checkpoint + owner queue if it blocks the
  SEVENTEENTH), never fixed inline — code changes here are a genuine
  scope break: name the blast radius and ask (Rule 5).
- The SEVENTEENTH's questions, arbiter, and routing table are CLOSED
  (spec-ratified). The run sheet TRANSCRIBES; it never redesigns.
- Placeholder law + lore ban (standing owner order 2026-08-16).
  SUPRIMENTOS is ratified player vocabulary, not lore.
- Explicit-path commits; pull before push; hooks run
  `bundle exec rake` — fix, never `--no-verify`.
- Budget: this is a small increment. One optional council call
  (≤2K tokens out) ONLY if the PT-BR register genuinely worries you —
  Junior's ratification is the real gate either way.

## Stop conditions

- Stop after increment 8 committed+pushed + the new checkpoint entry.
  **Nothing new starts until the SEVENTEENTH adjudicates** (scope
  contract — the cycle is now owner-paced).
- Owner queue at close (it fires this time, es-CR, ~three lines): ask
  for the two sessions on different days per the run sheet, name where
  the run sheet lives, and note Junior should pull before playing.
- If the suite is red at Job 0 or a seat/mail conflict blocks the
  push: reconcile or hand off via drafts/ + `swarmforge handoff
  validate` — never route around.
