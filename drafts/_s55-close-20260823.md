# s55 close — J6-B debt clearance (hub, 2026-08-23)

Spark: critic-ON menu_tour gate + fresh-eyes Rule 6 review of J6-B +
prefs boot probe; J6-C only on green headroom. Brief law:
`drafts/_j6-menu-brief-20260823.md` (md5 `00802b28…` verified);
Junior's close: `drafts/_j6-b-close-20260823.md` (md5 `cfaf14e6…`
verified). Commits this session: `ed64cea` (D9 boot fix), `cbc887b`
(fingerprint exclusion), `bfa7731` (test pins), + the docs commit.

## 1. Prefs boot-brick probe — D9 VIOLATED live, fixed

Probe (headless, no launch): garbage bytes at `data/prefs.local.json`
→ `Core::DataStore.new('data')` raised `JSON::ParserError` at
data_store.rb:18 — boot bricked BEFORE `App::Prefs.load`'s lenient
decode (window.rb:44 vs :49). D9's own ratified law ("a crash-corrupt
file must not brick boot") was violated in live boot exactly as the
s54 dead-ended live-read suspected.

**Fix `ed64cea`** (dev-of-record call; Junior ratifies async — his B1
code, tests, .gitignore, window wiring all UNTOUCHED): DataStore
skips the exact key `prefs.local` at the ONE eager-load choke point
(`MACHINE_WRITTEN` allowlist, law comment at the site). Shape argued
in the commit message: not a lenient-everything rescue (hand-edited
configs keep bindings' loud-abort law), not a caller opt-in (eight
construction sites — window, main, map_main, three harness scenes,
soak seed, Rakefile — any one forgetting it re-bricks that surface;
a corrupt prefs file would have killed replay/capture/map/soak too),
not a file move (relitigates the brief's ratified D9 placement for
zero extra safety). Layering cost (core naming one app-owned file)
accepted and documented. Re-probe post-fix: BOOT OK, key absent from
the store; nobody consumed `store["prefs.local"]` (grep-verified).

## 2. Fresh-eyes Rule 6 review of J6-B (`git diff 61dbf06..ec8e895`)

Every pin verified against live code, not the close's claims:

- **D9** lenient-NAMED per-key decode real (prefs.rb `valid()` named
  lines, never a raise); round-trip/degradation/self-heal tests real
  (tmpdir, real bytes, no mocks). The eager-glob hole was the ONE
  breach — fixed above.
- **D10** precedence traced: `bin/play es` → `export GAME_LOCALE` →
  window passes `ENV || @prefs.locale` as Strings' explicit ctor param
  (param > env > display in strings.rb) — launcher arg king, pref
  beats repo default ✓.
- **Strings#switch!** mutates the ONE shared instance (window
  constructs once; Renderer/NetplayOverlay/Menu hold it); sim never
  reads strings (grep src/game src/net clean) ✓.
- **D11** reel nav-only: menu_tour settings visit = open/down×2/
  attack/menu/menu — no left/right/attack on a settings row; captures
  show EN/AUTO/OFF; double-protected (harness menu has no prefs and
  nil callbacks) ✓.
- **D12** apply = `Scale.factor` pure math + runtime setters
  (`self.width=/height=/fullscreen=`) only; no capture claims about
  physical dims; observation note owed (carried) ✓.
- **D13** volume rows ABSENT (settings_rows = exactly 3); no library
  patch; defer NAMED in his close ✓ (inbox empty at s55 open+close —
  RECEIPT still outbound).
- **RESUME swallow guard**: one-frame scope verified in code (route
  clears on the first closed route; open! clears stale) + unit case
  (`refute_same` then `assert_same`) ✓. Esc-close still routes real
  input (ratified, :menu world-inert) ✓.
- **window.rb 265** ≤ 300, law comments intact at every seam ✓.
- **Protected surfaces**: Protocol.ACTIONS / TELEMETRY / EventBus
  registry / save schema / soak regexes — 0-line diff ✓. Client prefs
  never in the world save ✓ (SaveStore untouched; prefs write their
  own file). New `left/right` in Menu::TRACKED is autopilot-safe
  (`Autopilot#down?` = include?) ✓.
- **Strings ×3 byte-VERBATIM** vs the brief's pinned table (diffed):
  SETTINGS/AJUSTES/OPÇÕES · LANGUAGE/IDIOMA/IDIOMA · WINDOW SCALE/
  ESCALA DE VENTANA/ESCALA DA JANELA · FULLSCREEN/PANTALLA COMPLETA/
  TELA CHEIA · AUTO/ON/OFF invariant ✓. `.gitignore` row ✓.

**Blocking findings: 1** — the D9 boot-brick (fixed, §1).
**NITs banked:** (a) his close says `menu_settings_reads` is the
"66th check" — actual count 64→**65** (docs miscount, no code effect;
corrected HERE, his draft stays untouched). (b) settings/controls
hint reads ESC: CLOSE while Esc backs out to root — A-shipped
convention, consistent register, not a B regression; noted only.

## 3. The OWED critic-ON menu_tour gate — PASS (debt cleared)

One paid run over the FINAL tree (all fixes in), detached, voice-dev:

- **Determinism**: 7 captures byte-identical across two runs ✓.
- **Vision**: 65/65 PASS (verdict JSON appended to
  `drafts/_gate-verdicts.log` by the critic).
- **Sampling-artifact law**: ALL 7 PNGs read by this dev BEFORE
  accepting — f0330 world+HUD baseline · f0360 root open (4 rows, >
  RESUME, hint, world behind veil) · f0540 menu STILL open ~180f
  later with kit bars 1+3 visibly damaged + level bar gained fill
  (the still-ticking proof — menu_world_ticks claim TRUE) · f0600
  controls sheet incl. MOVE + MENU/Escape rows · f0660 closed, damage
  persists, faded husk remnant (corpses_persist/kill_pop claims TRUE)
  · f0770 SETTINGS panel `> LANGUAGE: EN / WINDOW SCALE: AUTO /
  FULLSCREEN: OFF` + hint, world readable (menu_settings_reads claim
  TRUE) · f0820 closed, state persists. 7/7 frames = 100% of the reel
  read; every critic claim frame-verified, zero refuted.
- **Manifest**: floors re-derived from the SINGLE-run log
  (`tmp/s55_gate/menu_single.log`, grep -c): zone_entered=2,
  attack_hit=5, actor_died=1 — Junior's authored {2,5,1} are TRUE
  single-run counts ✓; teed-gate-log manifest check rc=0.
- **Language critique (rides the gate; axes scored separately)**:
  ACCURACY — byte-verbatim vs the pinned table (diff §2), captures
  render the EN family correctly; PASS. PRESENTATION — LABEL: VALUE
  format and uppercase register consistent with the A-shipped menu,
  placeholder law held (locale-invariant AUTO/ON/OFF, no fiction
  voice); es-CR/pt-br rows everyday gamer words; PASS.

## 4. Sub-agent Rule 6 review of s55's OWN code → SECOND blocking find

Scrubbed read-only reviewer (touch NOTHING incl. seat mail) over
`ed64cea`; verdict `tmp/s55_review_verdict.md`. It confirmed the fix
mechanism, then found the REAL gap (verified live at this seat before
acting): **`Net::Fingerprint::EXCLUDED` carried only bindings.local —
the `data/**/*` glob folds machine-written, gitignored
`prefs.local.json` into the seat-identity hash.** First settings
change on either seat → every later coop handshake REFUSED ("sim
fingerprint" mismatch) with a hint (git pull) that cannot equalize a
gitignored file. Coop S1 is owner-pending — this would have bricked
it at the first post-settings invite. Gap predates s55 (landed with
J6-B's prefs write; the brief pinned only the STATE digest, never the
build fingerprint — unrecorded gap, not a deferral; no fault row on
Junior).

**Fix `cbc887b`**: prefs.local joins the exclusion (the bindings.local
twin), test pins it, cross-reference comments bind
`Fingerprint::EXCLUDED` ↔ `DataStore::MACHINE_WRITTEN` as two halves
of one law — **the next machine-written file (e.g. E3a's capture
contract) lands in BOTH or repeats this gap.** No protocol bump owed
(tree_md5 moves with every commit by construction; mixed-version
seats already refuse with the correct hint). Reviewer NITs 1+2 →
`bfa7731` (test-only: exemption unconditional + exact-key).

## 5. Belt + skips (each named)

- Suite green via hooks at every commit: 1151 → 1153 → 1154 → 1156.
- **Identity pairs** world_loop+low_quay_run vs a PRE-fix `183fd8b`
  worktree baseline captured this session: **24/24 byte-identical at
  `ed64cea` AND at the final tree `bfa7731`** (md5 lists diffed;
  tmp/s55_baseline vs tmp/s55_post, tmp/s55_post2).
- **Soak SKIPPED, justified**: no fix touched window.rb or the
  input-routing/session-pump seams (loader + fingerprint list only);
  bots never open menus; netplay integration tests run REAL handshakes
  over loopback and passed at every commit; s53/s54 soaks stand.
- **Net gates NOT re-run, justified**: cbc887b changes which FILES
  enter the hash, not any rendered surface or refusal text; in-process
  gate seats hash the same tree → identical both runs by construction;
  the exclusion is pinned by its unit twin instead. (C's ship re-runs
  all three anyway — it moves their pixels.)
- Live save md5 `98fe75ed…` verified open/mid/close; launcher logs
  40/newest 08-21 01:39 unchanged; probe file removed; worktree
  removed; inbox EMPTY at open+close.

## 6. Fork disposition

Core green but headroom below half after two unplanned fix commits +
a 7-frame dev-eye read — **J6-C handed to s56** per the spark's own
stop condition (tight green core > sprawling half-fork). J6-C remains
UNCLAIMED by either seat; s56 executes per brief §J6-C + D14 and
CARRIES: the banked frozen-menu-over-partner-end row, the volume-rows
defer (audio RECEIPT still outbound), window.rb re-pricing from 265,
and the new two-lists law from §4.

## Junior async-ratification asks (his B stands untouched; one line each)

1. D9 exemption shape (`ed64cea`) — prefs stay at
   `data/prefs.local.json`; DataStore skips the exact key.
2. Fingerprint exclusion (`cbc887b`) — his settings writes can never
   refuse a coop handshake.
3. His close's "66th check" row reads 65th (count re-derived 64→65) —
   correction recorded here only.
