# s56-hub close — the J6-C concurrent-ship reconcile (2026-08-23)

Companion to Junior's `drafts/_j6-c-close-20260823.md` (his ship = the
base). This close records the HUB session: the race, the owner
disposition, the two reconcile folds, and the merged-tree ladder that
cleared his close's row-1 debt (critic-ON ×3). Ticket law:
`drafts/_j6-menu-brief-20260823.md` §J6-C + D14 (md5 `00802b28…`
verified at open).

## 1. Timeline of the race (both sides acted lawfully until the push)

1. Hub opened on `da8a0ff` (his D12 docs note): J6-C UNCLAIMED — the
   spark's fork check passed. Hub executed the full ticket:
   notice + panels (provider-lambda design) + force-close + reel +
   checks; ladder complete ON THAT TREE (critic-ON ×3 rc=0 first run,
   pairs 24/24, soak PASS, scrubbed review PASS-WITH-NITS, language
   both axes).
2. Junior claimed the same spark as his s56 mid-hub-session and
   pushed first (`157ea51`+`b717274`+`0fe15dc`): same ticket,
   convergent design, determinism-only gates (seat creds — critic-ON
   owed to hub, his honesty row 1).
3. Hub's push refused → **STOP per the no-race law**; both trees
   surfaced to the owner in chat with a comparison table.
4. Owner words, in order: "approved, proceed" (adopt-his + hub pays
   the gate debt) → **"merge, reconcile the best outcome"** (final —
   the law of this close).

Hub's parallel implementation is preserved UNMERGED on branch
`s56-hub-j6c` (commits `a87e3fd`/`7aec517`/`01b08d2` + a docs commit;
its gate verdicts are void for shipping — they gated a tree that never
shipped). Never merge that branch without a fresh ladder.

## 2. Reconciliation (per-surface, best-of-both, argued)

**Kept from Junior's base (winning on the merits, not just
first-push):** the notice mapping + draw (functionally identical to
the hub's) · panels-flank-root structure + `net_model`/draw-args
contract · the 8 `menu.net.*` label rows — byte-identical ×3 locales,
locale-invariant placeholder register (his close row 2 names the
zero-prose deviation and invites re-pin; the merge ratifies the rows
as data-only) · his `net_menu_panels_read` check demanding CROSS-FRAME
TICKS advance (stronger than the hub's single-frame check — it proves
lockstep flows under the open menu in pixels) · his 600/620 two-frame
panel beat · `menu_net_*` display keys · window.rb force-close line.

**Folded in from the hub tree (where his was weaker):**

- `0402719` — XP row gains its threshold (`XP x/Δe`, `MAX` at cap;
  a bare `XP 0` reads as noise without scale; delta_e/level_cap are
  the same HUD readers, D14-legal) · ledger beat renders `KIND ±n`
  (kind alone said nothing; the signed net IS the ledger) ·
  real-loopback beat-present test (registered `:banked` event, real
  bus/handler/flush).
- `0637aa4` — scene seam fidelity: his scene ticked the menu
  UNCONDITIONALLY (handshake frames included) and never force-closed
  on ended; his reel masked it by closing the menu at f640, so the
  force-close law was never photographed. Scene now world-gates the
  tick + `close!` on ended (the window's exact seam); reel reopens
  the menu at f690 INTO quit_at=700 → **f710 = the banked-row proof
  in pixels** (menu GONE, notice unobstructed); capture +695;
  partner_left check gains the no-menu-overlap clause. MIRROR LIMIT
  comments name both remaining live-window divergences (drain-window
  ticking; discarded QUIT-row return — `5db081d`).

**Dropped from the hub tree (his equivalent stands):** provider-lambda
panels contract + `session_panels` formatter + `menu_side_*` keys +
`menu_panels_test`/menu_test additions (his `menu_net_test` covers the
contract; the hub's close!-pins existed in his file already) · the
hub's telemetry `key=value` register (his spaced-uppercase diagnostics
register + invariant rows achieve the same law with a friendlier
read).

## 3. The merged-tree ladder (every verdict re-earned)

- **Suite:** 1159 → **1160** green via hooks at every commit
  (`0402719`, `0637aa4`, `5db081d`).
- **Identity pairs** vs the common-ancestor `da8a0ff` baseline
  (captured at this seat BEFORE any s56 edit): **24/24
  byte-identical** (`tmp/s56_baseline_md5.txt` vs
  `tmp/s56m_post_md5.txt`).
- **All three net gates critic-ON, detached, rc=0:** netplay_session
  **15 captures ×2 byte-identical + 15/15 checks PASS** —
  `net_menu_panels_read`: "TICKS advancing 371 to 466" ·
  `partner_left_screen_reads`: "no menu overlap" — plus desync 4/4 ×2
  PASS and conn_lost 4/4 ×2 PASS (new checks self-scope "not
  exercised"). Verdicts in `drafts/_gate-verdicts.log` (banked with
  this commit; the log also carries the hub-tree run — void for
  shipping, honest as history).
- **Sampling-artifact law:** f0600 + f0710 dev-eye read at this seat
  before commit (f0600: LINK SEAT 1/TICKS 371/D 12/STALLS 99 · MAX
  1500 MS/DESYNCS 0/LINK SLOW + SESSION LEVEL 1 · XP 0/80 + RUN 0:06
  over the veiled world; f0710: notice unobstructed, no menu). Zero
  critic claims refuted. Junior's f0620 read stands from his close.
- **Soak N=1: PASS** (chain intact `fresh → dec77789`, sessions 1→1,
  quarantine held; `tmp/s56m_soak.log`).
- **Fresh-eyes merge review** (scrubbed, read-only, touch-nothing) of
  `da8a0ff..0637aa4`: **PASS-WITH-NITS, ZERO blocking**
  (`tmp/s56m_review_verdict.md`, receipt s56-merge-review). Verified:
  protected surfaces 0-diff (world.rb/balance/ACTIONS/registry/
  DIGEST_VERSION/telemetry/chain_check/save schema) · strings
  od-verified byte-verbatim ×3 · menu.net.* parity ×3 · all D14
  readers pre-exist · D7 held (no session/world ivar/ctor param) ·
  force-close ordering sound incl. pump-discovered ends · no-desync
  argument (maskless :menu, NullInput mask 0, beats in empty-input
  region) corroborated by ×2 determinism · two-lists law N/A.
- **Language critique (axes separate):** ACCURACY PASS — pinned rows
  byte-verbatim (em-dash/Ñ/Ó/Ã od-dumped). PRESENTATION PASS with one
  named low-severity defect: es/pt panels mix invariant labels with
  two reused TRANSLATED keys (`hud.level` → NIVEL/NÍVEL,
  `net.link_slow` → CONEXIÓN/CONEXÃO LENTA). Data-only; captures stay
  EN (D11); re-pin invited — peers' call, one strings edit either way.

## 4. Review nits — disposition

1. C1 shipped its visual one commit before its check (his C1→C2) —
   historical, healed within the commit family; recorded, no action.
2. Scene QUIT-row divergence unnamed → named (`5db081d`).
3. Sequencing letter-bend (folds landed before the owed critic-ON
   ran; his row 1 said "BEFORE anything ships on top") — the FINAL
   tree is what got gated and pushed; spirit satisfied, deviation
   recorded here.
4. Folded beat row (`KIND ±n`) is unit-pinned but has no check
   clause — **BANKED: next gate_checks.json toucher adds a beat-row
   clause to `net_menu_panels_read`.**

## 5. Process gap → proposal (awaiting Junior's ratification)

The fork law checks claims at SESSION OPEN only; both seats lawfully
executed the same unclaimed ticket for hours. Proposal: a ticket
claim is ONE pushed line — `CLAIMED: <ticket> <seat> <date>` in the
checkpoint top entry (or a seat-mail ping when push is inconvenient)
at ticket START; a claim older than one day without commits lapses.
Cost: one commit. It would have converted this race into a 30-second
re-triage.

## Junior async-ratification asks (one line each)

1. The two folds on his C2 (`0402719` XP/Δe + beat ±net; `0637aa4`
   scene seam + f690/f695/f710 force-close photograph).
2. The register mix (nit: hud.level/net.link_slow translated inside
   invariant panels) — re-pin or bless, his surface.
3. The `CLAIMED:` protocol (§5).
4. Standing: his J6-C base + honesty rows all stand as shipped; the
   critic-ON ×3 debt from his close row 1 is CLEARED by this session.
