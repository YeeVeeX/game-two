# SPARK sesión 26 — WB T3 safe-tile-behaviors (owner-ratified lane) + owner-paced harvest lanes

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register); Junior surfaces pt-br. Quality over cost: council 0
(Lane 3 already consumed its consult budget, receipts banked); the only
paid calls are Rule-2 gate critics. Evidence-first: claims are not
evidence — file:line, log line, or UNVERIFIED tag.

## Program state (2026-08-20 session-25 close — verify live)

- v18 CLOSED · **v19 NOT open** (brainstorm at the owners' word; agenda
  ready: `drafts/_v19-intake-docket-20260820.md` — pointers only, adds no
  opinions). Session 25 shipped: **gamesmith R7B intake** (`f22d951`,
  receipt fired, nothing further owed) · **lag T4 vsync release**
  (`ce78bfc` env-gated `GAME_VSYNC_OFF`, canary gate PASS flag-absent,
  fresh-eyes PASS; ticket `drafts/_lag-t4-vsync-20260820.md`) · **v19
  docket** · flake killed at root (`86ca3fa` subprocess laziness pin).
- **Junior is READY for coop S1 and awaits GABRIEL'S INVITE** (his
  `ad517e2`, verbatim "estou pronto e esperando o convite dele") — the
  invite is the owner's to send, NEVER nag. Both seats must pull
  `ce78bfc`+ first (protocol v3 refuses mixed builds NAMED — his ready-doc
  pre-flight was at `746ee8b`, pre-T4).
- **Owner-pending (never nag):** ear-checks (¿se acabó la duplicación? ·
  ¿la música respira? · stinger overlap → si falla, the depth-aware-duck
  library increment, grill §6) + audio-v12 batch · Junior's T4 re-run
  (pt-br two-liner staged in ticket §4; outcome classes pre-declared:
  released / REFUSED / timer-granularity judder = valid negative,
  no-ship-by-default) · the v19 brainstorm.
- **R-A2 measure (silent, NEVER prime):** `sustain bought=0` on every
  banked HUMAN log. Harvest `TELEMETRY sustain` + frame_probe/handshake
  lines from every NEW human launcher log; bot lines are never evidence.
- **World-builder lane (owner-ratified, spec CLOSED):** T1 GO (s19) · T2
  SHIPPED (s20: importer `tools/import_ldtk.rb` = D2 sole door, schema v2,
  `data/tiles.json` registry v0, six zones byte-stable) · **T3 is the
  next ticket** · T4 next after · T5's gate (SEVENTEENTH adjudication)
  is now SATISFIED but T4 precedes it and lane order stays
  owner-steerable — note it, do not promote it.
- **Audio seat is WARM for T3** (their truth-sync mail, banked done/):
  their PARKING_LOT already carries the footstep-material cue family +
  region-ambience beds with named triggers keyed to OUR builder-era
  cue-spec mail (their `2171462`/`2471b5d`). Their gates: 8/8, pins
  unchanged.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: origin/main `20cbad1` + EXPECTED docs-only delta
(this spark's own commit) · save `saves/world.json` md5
`98fe75edb6d72deab18cd48eaa88bdaf` mtime 08-20 15:51 (sessions=13
banked=7 provisions=0 seals=2) · launcher logs 38×2 (pattern
`game_two_session_*.log`; wider globs catch 3 old non-session logs —
count the session pattern) · seat mail inbox EMPTY, done/=18 · tmp/soak
newest `20260820-020422` · untracked `drafts/_refs/` only · tmp/t4/ =
session-25 A/B artifacts (expected). EXPECTED deltas: Junior docs-only
commits (his lane — read before rebasing; he may also pull+re-run T4) ·
new human launcher logs if the owners played (harvest silently) · coop S1
artifacts if the invite went out. `git pull --ff-only` FIRST.
Single-instance guard before any launch (separate call, judged by printed
output). `--fresh` NEVER. Background `bin/play` from pi's bash DIES
silently — the working launcher is `powershell Start-Process bin\play.cmd`;
bot A/B seats launch fine as `nohup bundle exec ruby -Isrc src/main.rb
--bot <seed> --bot-ticks <n> --save tmp/<dir>/world.json &`.

## GATE 0 OVERRIDE — any live owner order preempts this whole queue

- **Ear-check verdicts** are LAW: bank verbatim, route per the three
  checkpoint questions; stinger-overlap failure → the depth-aware-duck
  increment gets grilled in game-two-audio (library), never as a game-two
  data tweak.
- **Junior's T4 re-run lines arriving** (chat or his docs push): bank
  verbatim into the T4 ticket doc §4, adjudicate against the
  pre-declared outcome classes (success shape: period p50 off the
  16.8–16.9 wall, draw/update p50 unchanged; REFUSED and judder are valid
  negatives), write the verdict block, es-CR line to the owner. This is
  a ~30-min lane, do it BEFORE J1 if the evidence is waiting at gate 0.
- **A live coop session = lag segment S1**: support, harvest verbatim per
  `drafts/_lag-t2-evidence/README.md`, bank md5-stamped.

## J1 — WB T3: safe tile behaviors (THE session job; spec is law)

Read IN ORDER before any edit: (1) AGENTS.md whole · (2) checkpoint top 2
entries · (3) `docs/superpowers/specs/2026-08-19-world-builder-pipeline.md`
WHOLE (D1–D12; T3's own ticket text governs) · (4) T2's shipped surfaces:
`tools/import_ldtk.rb`, `data/tiles.json`, schema-v2 zone JSON, their
tests · (5) `src/app/audio_bridge.rb` header + `data/audio/` tables (the
pure-sink seam T3 extends).

**T3 ticket (spec §Tickets, verbatim scope):** footstep materials +
region ambience wired as PURE SINKS (cue-spec mail to audio seat FIRST —
owner records renders at his pace; noDevice fallback fine) + flora visual
variants. Pure-sink test extension; Rule 2 gate per visual surface.
Verify: digest-invariance test + gate PASS. Done: walking district vs
nest vs a grass fixture zone SOUNDS different (or noDevice-logs the
material keys), zero sim delta.

Sequencing the dev of record proposes (defend or improve at execution,
against the spec):

1. **Cue-spec mail FIRST** (~30 min): write
   `~/.pi/agent/mail/game-two-audio/from-game-two-t3-cue-spec.md` —
   footstep-material families (which materials, how many takes, loudness
   lane vs existing −16 LUFS cue law, event cadence = step rate) +
   region-ambience beds (which regions, bed length, crossfade law vs the
   existing zone-change render). Their PARKING entries name OUR mail as
   the trigger — cite their `2471b5d`. Owner records at HIS pace: the
   mail is the ask, T3 ships against noDevice/logged keys regardless.
2. **Data + sim-blind plumbing (TDD red-first):** material key per tile
   type in `data/tiles.json` (registry v0 already carries footstep field
   per T2 — verify live) · region → ambience-bed key in zone JSON/
   `data/audio/` tables · emit `:footstep` facts with material payload
   from the EXISTING movement events at the bridge layer (presentation
   listens, sim never changes — the M5a pure-sink law; if a new
   EventBus symbol is needed it registers at first use, rule 4).
   Digest-invariance test: same replay, audio on/off/noDevice → byte-
   identical StateDigest stream (extend the existing pure-sink tests).
3. **Flora visual variants:** decorative tile-type variants through the
   T2 registry render path — data-driven, deterministic per tile coord
   (seeded from zone+coord, never Random.rand in draw). EVERY touched
   visual surface = Rule 2: wall scripts covering the zones whose look
   moves + `rake gate` per surface; if a NEW fixture zone (grass) enters
   the wall, its script joins `harness/scripts/` (the wall directory is
   the truth, not a list).
4. **Gates before ship:** suite via hooks · digest-invariance green ·
   `rake gate` PASS on every touched visual surface + `world_loop` +
   `low_quay_run` spot-gates (T2's regression bar carries) · noDevice
   walkthrough evidence: bot seat on scratch save through district/nest/
   grass logging material keys (bank the log lines in the ticket doc).
5. **One-concern commits** (data · plumbing · flora · harness · docs) +
   ticket doc `drafts/_wb-t3-safe-behaviors-20260820.md` (evidence,
   decisions, the audio-mail digest stamp, what waits on owner renders).
6. **Fresh-eyes review BLOCKING** (scrubbed read-only pi: diff + ticket
   doc + spec T3 text; receipt in drafts/). A FAIL adjudicates before
   ship — never downgraded.

Hard limits: zero sim-class tile behaviors (lava/water/spawn-gates are
POST-VERDICT increments, one gated piece at a time — and even though the
verdict landed, they are NOT T3; refuse scope creep in writing) · the
live six-zone world's LOOK stays byte-stable except surfaces T3
deliberately moves (flora variants) — name each moved surface in the
ticket doc · `src/app/window.rb` ≤ ~300 lines stands.

## J2 — docket hygiene (only if J1 leaves room; ~15 min, docs-only)

If T3 shipped: one-line status updates in the v19 docket rows T3 touches
(none expected — verify) + checkpoint entry + push. If the session ran
long: checkpoint entry + push, docket untouched.

## Laws that bite (short list)

- Deterministic gates decide; a failed gate/critique BLOCKS ship — never
  downgraded. Presentation never mutates sim; audio stays a pure sink;
  replays deterministic by tick count.
- Read-before-edit · one-concern commits, explicit paths (never `git add
  -A`) · hooks run the suite (~57 s/commit — normal; the pre-push runs it
  again) · long jobs DETACHED (nohup + poll; never under a bash-call
  timeout — a killed gate is judged by a standalone re-run).
- Two instances fork the save — guard every launch in a separate call
  judged by printed output; bot seats need `--save` scratch; phantom
  sessions get NAMED if anything launches the game.
- JSON edits are SURGICAL text ops (a re-dump reflowed 650 lines once);
  multi-line scripts go in temp files, never inline heredocs.
- Junior's lane (`drafts/_junior-*`, his machine docs) is his — harvest,
  never edit. Owner overrides are law the moment they land — one line,
  never re-litigated. No lore anywhere (standing order; placeholder
  names only, register law on every player-visible line).
- Cross-repo: read siblings freely, write ONLY via seat mail (+ RECEIPT
  lines); never write into a held seat. If the audio seat is LIVE, the
  cue-spec mail still just lands in their inbox — no session launch owed.
- Verbatim means verbatim: owner/Junior quotes and telemetry lines bank
  byte-exact; partial evidence = bank what exists + name what is missing.
- A rebase over a peer's push REWRITES your local hashes — re-verify every
  commit hash you cited in docs after any rebase (hit live, session 25).

## Budget + stop conditions

One attended session ~2.5–3 h, context guard 85% → compact-checkpoint
skill. Council 0; paid calls = Rule-2 gate critics only; sub-agents =
J1's fresh-eyes reviewer only. **Stop when:** T3 shipped behind its gates
+ review PASS (or honestly STOPPED with the ticket doc naming the block) ·
cue-spec mail sent · checkpoint + owner queue es-CR (~5 líneas) + push.
**Stop early, honestly, if:** the spec's T3 text conflicts with this
spark (spec wins; re-plan in writing) · a gate fails non-mechanically ·
T2's byte-stable regression bar breaks (STOP, name it, never patch
around) · the owners redirect (their word is the router).
