GOAL: EXECUTE the D1b vat-economy plan end-to-end - owner greenlit 2026-08-12 ("proceed, greenlit and approved"). Subagent-driven per task (A2 pattern; inline executing-plans fallback). NO scope changes: spec is owner-approved + adversarially reviewed (12 findings, 12 refuted).

READ FIRST (Rule 8), in order: project CLAUDE.md (v10 contract); docs/CHECKPOINT.md top entry; docs/superpowers/plans/2026-08-12-d1b-vat-economy.md (14 TDD tasks, branch d1b-vat); docs/superpowers/specs/2026-08-12-d1b-vat-economy-design.md (REVIEWED); drafts/_d1b-exploration-brief.md (code facts incl husk correction); drafts/_d1b-spec-review.md (12 refuted findings - never re-raise).

STATE (measured 2026-08-12): main 193 commits, HEAD fe39ce7, tree clean. Session chain: 867be8d v10 scope -> fc10bef fork ledger -> d65f9b9 spec -> 79d7d14 spec REVIEWED -> fe39ce7 plan+checkpoint. Tests 250/1056 (pre-commit hook runs bundle exec rake, ~13s). Checks 34. Wall 8/8+8/8 at A2 merge e3759c3. Perf p95 0.232ms.

THE INCREMENT (v10, all forks closed - consume, never reopen): inscription (spend banked at altar -> god-mark; marked survive the wipe, mark burns = consumed by the judgment) + priced flesh (vat tribute: ONE all-or-nothing heal+regrow verb) + one-vessel floor (possessed-at-wipe returns free when nothing marked) + Q6 rider (lowhp 0.25, margin 4, why-they-turned cue) + bug bundle (dodge edge-trigger: the one-word pressed? fix, mechanism already at controllers.rb:56-61; deepest_band converts at drop time). LAWS: banked never taxed (spends are station verbs only); judgment clears only UNLOADED pack corpse records (container_id records are D1 pile markers - NEVER delete); interact guards + pickup-first preserved verbatim, bank byte-identical; new check ids avoid the mark_glyph_readable collision (god_mark_reads etc.); checks 34->39 ADD-ONLY.

SEQUENCE: branch d1b-vat -> tasks 1-11 TDD (failing test -> impl -> rake green -> commit, each) -> task 12 price re-anchor (EVENT lines are STDOUT of rake capture, world_scene.rb:25) + vat_economy.json pilot (5 acts incl two wipes) + 5 checks -> task 13 re-pilot ALL 8 existing wall scripts (dodge fix invalidates every stream; re-stage ALL mandatory beats: projectile/telegraph/swap/nest/specials/taunt/pip/ledger/ring/walkback/depth; memory gate-critic-mandatory-beat-checks) + 9/9+9/9 wall + rake perf -> task 14 adversarial impl review (Workflow; declare budget per memory workflow-review-token-calibration: ~110K/finder + ~55K/refuter) -> merge --no-ff, NO push -> checkpoint -> hand owner the SEVENTH fun-verify per spec section Fun-verify (play-first law: owner PLAYS before any question batch - the sixth verify logged a protocol violation for this; telemetry banked first; Q1 meaning is headline; routing pre-registered in the spec, apply it verbatim).

STANDING RULES: export PATH="/c/Ruby34-x64/bin:$PATH" per shell. NO push ever (private remote YeeVeeX/game-two exists; pushing is the owner's action). Vision checks ADD-ONLY, never weaken. Read-before-edit. Zero balance constants in Ruby (CORPSE_FADE_FRAMES-class render constants are existing convention). Judge builds, not briefs. rm -rf deny-listed. Red suite = fix the test, never --no-verify. corpse_run_test rewrite is pre-authorized ONLY for the three-alive-post-wipe assertion (stage marks instead) - everything else untouched-green.

OWNER QUEUE (unchanged): council MCP deepseek-r1 id needs the us. inference-profile prefix; council-via-mmh-gateway upgrade (seats grok-4.3) + when-to-council skill update = own session. Optional banked: tracked bin/install-hooks (fresh clones start ungated).

DONE WHEN: 14/14 plan tasks executed, 9/9 determinism + 9/9 critic wall green, perf green, impl review findings folded-or-refuted (ledger drafts/_d1b-impl-review.md), merged --no-ff to main, CHECKPOINT.md updated with measured numbers, seventh fun-verify handed to the owner. Execution only - no new scope, no economy retunes beyond task 12's measured re-anchor.
