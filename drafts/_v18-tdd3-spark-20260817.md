# SPARK: v18 TDD session 3 — the sustain verb end-to-end (increment 5, then 6, then 7 if room)

You are the dev of record in game-two (cwd `~/workspace/game-two`).
Read `AGENTS.md` FIRST — the v18 scope contract is ground truth and the
spec is FINAL (owner ratified twice 2026-08-17, veto window CLOSED
"aprobado"): build it AS WRITTEN; design forks do NOT re-open. Work in
English; answer the owner in his language (es-CR, ustedeo, flat
mechanics register — no pitch talk). The lore repo stays PARKED;
game-two is placeholders + functional dictionary words only.
Increments 0-4 shipped (`5994eaf` `c94fffb` `c6d8828` `e7e20d2`):
SaveState + round-trip lane, solo persistence, protocol v2 + SESSION
save transfer + the two-session lane (live loopback E2E verified), coop
feel. This session ships the owner-law sustain verb: **spend banked
value, portable, never a free cooldown** (owner law 2026-08-11).

## Read before acting (in order)

1. `AGENTS.md` — whole file (Ruby PATH per shell:
   `export PATH="/c/Ruby34-x64/bin:$PATH"`).
2. `docs/CHECKPOINT.md` top entry ("v18 TDD session 2") — what landed,
   micro-decisions, and the increment-5 resume map at the bottom.
3. The spec `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md`
   — this session executes decisions 9/10/15 + 7iii (test lane 5),
   presentation spec (lane: sustain_run.json), and decision 13 (lane 6)
   if increment 7 opens. Re-read those decisions whole; also re-read
   "Watched risks" W1/W3 and the Fun-verify sustain routing rows (the
   tuning-lever order discoverability → cost → heal is PRE-REGISTERED —
   never re-design the verb on a hunch).
4. `drafts/_v18-spec-review.md` — do-not-relitigate list (Codex #16 =
   edge-trigger/swap-rearm/first-success-law; panel Kimi-Q3 = cost 5 +
   lever order; auto-consume was REJECTED — the invocation stays a
   deliberate press).
5. Source you will extend — read BEFORE designing:
   - `src/game/pack.rb` — `@provisions` + digest field exist since
     increment 1 (`load_provisions!` is the save-apply seam); the
     guarded verbs land beside `bank!`/`spend!`.
   - `src/game/world.rb` — `interact` dispatch (~line 566:
     `map.station_at(*source.tile)` → `when "bank"`): the sustain buy
     path resolves the bank THE SAME WAY (one station lookup, never a
     second); `interact_bank` + `spend!` (never-taxed law); the
     seat-ordered controller loop (~line 636) — the first-success-
     per-tick latch lives in World's sustain path.
   - `src/game/controllers.rb` — `PossessedController::ACTIONS` +
     `EDGE_TRIGGERED` (line ~12): `:sustain` joins BOTH; study
     `pressed?` edge bookkeeping + `rearm!` (swap-rearm law) + the
     seizure branch (which verbs a seized body may still fire is a
     NAMED micro-decision — record whatever you pick, with reason).
   - `src/game/telemetry.rb` — `#summary` emits the TELEMETRY block;
     the sustain line joins it, counters fed by bus events.
   - `src/core/event_bus` usage in world.rb: `EVENTS` whitelist —
     register `:provision_bought` / `:provision_used` / the refusal cue
     event (Rule 4: unknown symbols raise).
   - `data/balance/economy.json` (numbers land HERE — Rule 3),
     `data/strings/{en,es,pt-br}.json` + `src/core/strings.rb`
     (render-time resolver, harness pins "en"), `data/bindings.json` +
     `src/app/key_table.rb` (A-Z covered — "U"/"R" resolve),
     `src/app/renderer.rb` (controls strip + station-cue channel) for
     increment 6.
6. `git pull --ff-only` FIRST (Junior's seat lands CI commits
   mid-session — two precedents; validate any drafts/ handoff:
   `swarmforge handoff validate`).

## Job 0 — verify state (trust git + the suite, not prose)

- History holds `c6d8828` + `e7e20d2`; tree clean; branch synced.
- `bundle exec rake` green (was 737 runs / 13781 assertions).
- `tmp/canary_baseline/` holds 17 dirs + manifests (MACHINE-LOCAL). If
  tmp/ was cleaned: re-bank from the CURRENT sim-identical line BEFORE
  any sim change (`tmp/bank_canary_v18_resume.sh` protocol, ~21 min
  unattended, resumable).
- Any check fails → reconcile first; never build on an unverified base.

## Job 1 — increment 5: sustain sim (decisions 9/15; lane 5, headless)

TDD: failing test first, then code. All numbers in
`data/balance/economy.json`: `provision_cost: 5`, `provision_cap: 3`,
`provision_heal: 30` (strawmen by design — tuning = JSON edit).

- **Pack state (decision 15):** guarded `buy_provision!` /
  `use_provision!` beside `bank!` — pure state, no bus, no world
  knowledge; cap/broke/none guards return a refusal symbol the World
  path maps to cues. `provisions` already rides `digest_snapshot` and
  the save (increment 1) — no digest work needed; the
  classification-exhaustiveness table must stay green untouched.
- **The verb (decision 9), edge-triggered:** `:sustain` joins
  `PossessedController::ACTIONS` + `EDGE_TRIGGERED` (+ the swap-rearm
  law: a key held across Tab is masked until released once — Codex
  #16). Press resolves in World's sustain path: standing ON the bank
  station (`map.station_at` — the interact lookup, reused) → BUY
  (spends `provision_cost` banked via the existing `spend!`,
  never-taxed law holds); anywhere else → USE (consumes 1 charge,
  heals every LIVING member `provision_heal` clamped at max_hp — dead
  untouched, the vat keeps its regrowth monopoly).
- **REFUSALS (cue + spend NOTHING, never a silent eat):** buy at cap ·
  buy broke (banked < cost) · use with zero charges · use
  ZERO-EFFECTIVE (every living member at full hp — a charge can never
  burn for nothing).
- **Same-tick seat race (decision 9):** the FIRST successful sustain
  action per tick wins; the second seat's press refuses that tick —
  implement as a per-tick latch in the World sustain path (the
  seat-ordered controller loop already guarantees seat 1 resolves
  first on both machines; the latch makes the race DETERMINISTIC and
  testable, not incidental).
- **Events + telemetry:** register `:provision_bought` /
  `:provision_used` + refusal cue events (Rule 4); `TELEMETRY sustain
  bought=N used=N refused=N` joins `Telemetry#summary`.
- **Lane 5 tests (headless, real Worlds, seats 1 AND 2):**
  edge-trigger (held key = exactly one buy/use) · swap-rearm (held
  across Tab = masked) · every refusal guard incl. zero-effective ·
  heal clamp (wounded to ≤max, dead untouched) · buy-then-use
  round-trip drains banked by cost×n · same-tick two-seat race
  (both press: exactly ONE action, seat 1 wins, seat 2's press
  refuses; assert on a two-seat world with scripted inputs) · the
  digest mutation sweep in save_state_test may now stage provisions
  through the REAL buy verb instead of `load_provisions!` (recorded
  increment-1 micro-decision 2 — upgrade it if the staging stays
  simple, else leave and note).
- **Seizure × sustain:** decide whether a seized body may fire sustain
  (the seizure branch currently lets verbs through but suppresses
  feet); pick the smallest faithful reading, TEST it, record it in the
  checkpoint as a micro-decision.
- **Gates before commit (SIM-TOUCHING):** full suite green → full
  canary sweep 17/17 byte-identical (`harness/run_canary.sh` — no
  existing script presses sustain, so captures must not move a byte) →
  `rake perf` (p95 < 16.6ms) → checkpoint prose → commit EXPLICIT
  paths → pull → push.

## Job 2 — increment 6: sustain presentation + THE Rule-2 gate (7iii + decision 10 + presentation spec) — ONLY if Job 1 is green+pushed with room to finish green

- **Bindings (decision 10):** `data/bindings.json` gains
  `"sustain": ["U", "R"]` (pair grammar: right hand near JKL / left
  near WASD). Bindings stay a file; no rebind UI. The controls-strip
  label derives from the SAME binding map (v15 law).
- **Render pins (decision 7iii — THE wall-safety pin):** the provisions
  counter renders ONLY when provisions > 0; the strip's sustain row
  renders under the SAME condition; provisions=0 draws NOTHING —
  structural test + the existing 17 canaries stay byte-identical
  because no walled script ever buys (the sweep is the proof).
- **Strings:** `data/strings/en.json` + `es.json` (dev-composed
  functional dictionary words ONLY — placeholder register, standing
  order 2026-08-16; es is functional-label es, NOT lore register) +
  `pt-br.json` (land the functional label, e.g. PROVISÃO + cue verbs,
  and FLAG it for Junior's ratification in the checkpoint — his lane,
  never blocked on him). Buy/use/refuse cues reuse the existing
  station-cue channel (no new channel). Run the human-facing-output
  checklist on the new strings: accuracy and presentation scored as
  SEPARATE axes; the language critique is blocking at ship (global
  Rules 2/6; skill: `.claude/skills`-equivalent is auto-available —
  read it before composing).
- **The new wall exerciser** `harness/scripts/sustain_run.json`: buy 2
  at the bank → walk out → take damage → use both → attempt over-cap
  buy + broke buy + full-hp use (refusal cues) — captures at each
  beat. Checks ADD-ONLY in `harness/gate_checks.json` (existing checks
  untouched). This script JOINS the wall (the runners glob the
  directory — no list edits).
- **FULL Rule-2 gate, blocking:** `rake gate
  SCRIPT=harness/scripts/sustain_run.json` with the REAL critic
  (SKIP_CRITIC is an iteration aid, never a shippable pass). After the
  gate PASSES: bank the new script's canary baseline into
  `tmp/canary_baseline/sustain_run/` (the sweep now covers 18) and
  re-run the full sweep — the other 17 byte-identical.
- Perf + checkpoint + explicit-path commit + pull + push.

## Job 3 — increment 7: god-view v0 (decision 13; lane 6) — ONLY if Jobs 1+2 are BOTH pushed and you can finish the critique inside this session

- `rake map [SAVE=] [OUT=]`: opens the capture window (GL law — never
  headless), renders EVERY zone's full tile grid from the SAME
  palette/identity data the renderer reads (a structural test pins map
  colors to the renderer's palette table — no second color source),
  composites ONE PNG: zones in a labeled grid (ZONE 1..5, HUB 1),
  station glyphs, seal stations stamped SEALED/OPEN from the save,
  home marker at home_zone, header strip `BANKED N · MARKS K ·
  PROVISIONS P · BOSS 1 DEFEATS D`. Filename carries provenance:
  `world_<digest8>_<ts>.png`.
- Deterministic landmark asserts in the lane (pixel probes: a breached
  seal cell differs from a sealed one; the home marker sits in
  home_zone's cell; header present); checks in
  `harness/map_checks.json`; the gate for THIS surface = capture +
  critique only (no replay half — there is no sim). Rule 2's artifact
  is the PNG + a real vision critique — never a text-only review.
- Not sim-touching (a rake task + renderer-side composite): suite +
  the map lane + critique; canary sweep only if anything under
  src/game or a harness-loaded file moved (when in doubt, run it).

## Laws that bite

- Owner sustain law (2026-08-11, binding): priced, portable, banked-
  funded — any drift toward free/regenerating/drop-fed provisions is a
  design regression, not a tweak.
- Spec-vs-code contradictions → smallest faithful deviation, RECORDED
  in the checkpoint (eight precedents listed there).
- Hooks run `bundle exec rake` at commit and push — fix the test,
  never `--no-verify`. Pull before push; explicit-path commits (shared
  cwd; Junior's CI commits land mid-session — two live precedents).
- No mocks: real Worlds, real files, real bank stations (stage with
  `walker.teleport` + real verbs — the save_state_test staging
  pattern). No balance constants in code (Rule 3); no new event symbol
  without registration (Rule 4).
- The wall stays byte-identical: 7iii is the pin, the canary sweep is
  the proof, after EVERY sim-touching increment. New surfaces are
  ADD-ONLY (new script, new checks, new baseline — existing 17
  untouched).
- Placeholder text only — generic-videogame functional words; the
  fiction register is banned in this repo (standing owner order
  2026-08-16); any es dev-to-owner text is ustedeo es-CR.
- Perf after any sim-touching increment (p95 < 16.6ms; ~0.2ms
  current).
- Bounded runs everywhere: scripted tick counts, never wall-clock
  waits; judge by TELEMETRY/digest asserts.

## Stop conditions

- Stop after increment 7 green+committed+pushed (or 6, or 5 — never
  start an increment you cannot finish green) + a new CHECKPOINT.md
  top entry naming the exact resume point (remaining: 8 = docs + close:
  JUNIOR.md persistence/custody section PT-BR-first incl. the --fresh
  notice, AGENTS.md Commands (rake map / --fresh composition / saves/ /
  coop block), PARKING_LOT custody-handoff entry, SEVENTEENTH protocol
  confirm — then the cycle waits on the two-day fun-verify).
- If the Rule-2 critic FAILS the sustain surface twice honestly: STOP,
  bank captures + verdicts in drafts/, checkpoint truthfully — a
  failed gate blocks the ship, it is never downgraded to advisory.
- If the canary sweep breaks (any of the 17 moves a byte): that is W3
  firing — STOP, bank the diff artifact, find the leak (7iii render
  guard or a sim-order change), fix or hand off; never re-bank over a
  broken wall.
- Owner queue at close: one line, only if something needs his hands
  (playtest or verdict). No design homework for him.
