# v22 T1 record — save schema 3: per-PLAYER character records (s136, 2026-09-06, Gabriel seat)

STATUS: IN PROGRESS at write time — evidence boxes are CHECKED only where the output is
pasted below (record-first law: never a pre-filled hash, verdict or proof). Spec:
`docs/superpowers/specs/2026-09-05-v22-one-body-cycle.md` §3 T1 (+ `[council s133]`
amendments). Law: `drafts/_v22-foundation-20260905.md` L9, L20, Rule 1c. Reviewer brief
(staged s133): `docs/sparkups/t1-review-brief-20260905.md`.

Claimed `a41ca0c` (pushed FIRST, s56 law). Built on branch `t1-schema3` in the linked
worktree `tmp/t1-wt` because the E1 re-pin sweep was judging the MAIN tree all session
(one GL replay at a time; a mid-sweep source edit contaminates baselines — project
MEMORY 2026-08-20). Five commits, each hook-green in the worktree:

| piece | commit | what |
|---|---|---|
| 1 | `6c554a7` | `data/player.local.json` + `App::PlayerFile` (lenient-NAMED, `.corrupt-<ts>` backup, twin law) |
| 2 | `b13a392` | `Game::Character` + `Game::Party` (plain objects, validator, D-T1 seed, interim live/mirror rule); `death.json insurance.max_stacks`, `progression.json new_character.level`, `Progression#max_hp_at` |
| 3 (+6) | `7156a84` | `SCHEMA = 3`, frozen v2 rules + `migrate_v2`, strict decoder, `SaveStore` schema-2 lane (`.bak-schema2-<ts>` at first write), `World#players` + Party on both paths, `apply!` bridge, CLASSIFICATION rows, every caller + fixture |
| 4 | `d5753d8` | HELLO `player_id` (Protocol v4), `Session#players`, collision refusal, `DIGEST_VERSION` 4 |
| 5 | `164f9cb` | World-level + across-the-wire pins of the interim rule, W4 two-character worst case, soak chain-length bound |

Piece 6 (CLASSIFICATION rows) landed INSIDE piece 3 because the tripwire test
(`test_every_digest_field_is_classified…`) cannot be green with new digest groups
unclassified — one commit, said here.

## 1. Live numbers at ticket start (prose-number law: computed, not trusted)

`ls harness/scripts | wc -l` = 43 · `world.rb` **1782** (main a41ca0c) → **1782** after T1
(net 0; the cap is 1800; Junior's branch sits at 1798) · `window.rb` 267 → 267 ·
`SaveState::SCHEMA` 2 · `FACT_KEYS` = banked breached counters home_zone members
progression provisions · `MACHINE_WRITTEN = ["prefs.local"]` · `EXCLUDED = [bindings.local,
prefs.local]` · HELLO = `version ruby platform fingerprint digest_version` ·
`data/balance/death.json` had NO `insurance` key · `data/balance/progression.json` had NO
`new_character` key · hubs = camp, nest, zone_7 · `initial_possessed` = **blocker** (not
striker — the spark's own memory was wrong; the pack's seat-1 body is the blocker).

## 2. Decisions taken as dev of record (ONE line each; peers may reverse with one word)

- **D-T1 (owner-pending, "never nag")** — implemented the bolded rec: **legacy seed ONCE,
  keyed by player id**. `migration.legacy_seed_claimed_by` starts UNCLAIMED; the first seated
  player without a record inherits `legacy_level` and the block records that player's id;
  every later newcomer starts at `progression.json new_character.level` (1). Seat order
  breaks a tie when two newcomers arrive together (seat-order law). Surfaced for both
  peers in the checkpoint; nothing gates on it.
- **`legacy_seed_claimed_by` is `false` while unclaimed, not the spec's `null`.** `null` is
  outside the pinned canonical vocabulary (`test_canonical_bytes_raises_on_non_canonical_leaves`
  line "nil is not in the vocabulary") — extending the canonicalizer is a digest-law
  amendment I did not want to smuggle into a persistence ticket. Validator: `false | player id`.
- **Insurance bound = DATA.** Added `"insurance": { "max_stacks": 3 }` to
  `data/balance/death.json` (foundation L8's N=3, still a candidate; T5 owns the tuning).
  Zero behavior: only the validator reads it. Same for `progression.json
  "new_character": { "level": 1 }` (the spec names the key; it did not exist).
- **Harness default ids are seat CONSTANTS (`bot-1`, `bot-2`), not seed-derived.** The spark
  said "bots and harness scenes derive `bot-<seed>`"; a `--bot <seed>` PROCESS does
  (`App::PlayerFile.bot_id`). A World built without `players:` seats `bot-<seat>` because
  facts are seed-independent by law (v18 decision 16, `test_same_facts_different_seeds…`):
  a seed-keyed host id would make facts projected at seed 11 unloadable at seed 999.
- **`form` is RESTORED at apply** (seat 1 resumes the saved body; a seat construction handed
  that body is released; every other seat claims in roster order as before). The spark said
  "seat pointers stay as today", but a persisted fact the load path ignores cannot satisfy
  the round-trip law `facts(apply(facts)) == facts` (THE ROUND-TRIP LANE) — so `form` is
  honored. Player-visible consequence: you resume in the body you quit in (today you always
  came back as the blocker if it lived). The 2→3 migration sets `form` to exactly the body
  the v2 pointer law resumed (blocker if alive, else the first living kit) — a migrated save
  resumes in the same body it did yesterday. Named for the reviewers.
- **T1 interim rule (council s133 Q5/Q6), written in `Character`/`Party` headers and
  test-pinned:** host character = the shared progression (live); a SEATED guest's
  `level/xp` MIRROR the host's in live reads AND in the clean-quit projection ("in this field
  every seated character IS the party level"); a guest's other keys are frozen at their
  stored values; UNSEATED records round-trip verbatim. Named window: between T1 and T2b a
  fresh third player joining a level-13 host is written at 13 — the alternative (freeze the
  guest's record) would make Junior lose every level the two of them earn together until
  T2b; with two peers the mirror is the right default. T2b retires the rule.
- **Bag persistence hook for Junior's S2 = one line**, proven by the mutation-sweep leaf
  `character.bag` (`w.party.host.bag << item` round-trips). Optional keys `bag [] equipment
  {} attributes {} bank_items []` INSIDE the record; the validator accepts absent = default;
  the projector always WRITES every key; every nested leaf must be Integer / ASCII String /
  Boolean (named refusal with its path, never an EncodeError at quit).
- **`players:` is required on `Net::Session` (no default).** A forgotten id would silently
  seat a stranger; main.rb is the only production caller.
- **`Game::Party` exists now** (T2a's "carve Party" becomes "grow Party" — it will own the
  body/forms in the field). It is a plain object with callables for the host's live facts
  (the Crossing/Homecoming pattern), no World dependency inside `character.rb`.

## 3. Evidence

### 3.1 Suite (hooks, every commit; worktree)
- [x] piece 1: `1489 runs, 81173 assertions, 0 failures, 0 errors, 10 skips` (the 10 skips =
  AudioBridgeTest: the audio library resolves relative to the repo root and is absent from
  a worktree — pre-existing worktree behavior, 0 skips in the main tree)
- [x] piece 2: hook green (1490-ish; not pasted)
- [x] piece 3: `1513 runs, 81531 assertions, 0 failures, 0 errors, 10 skips`
- [x] piece 4: `1518 runs, 81563 assertions, 0 failures, 0 errors, 10 skips`
- [x] piece 5: `1524 runs, 81607 assertions, 0 failures, 0 errors, 10 skips`
- [x] canary UNCHANGED: `test/harness/sim_identity_canary_test.rb` → `3 runs, 3 assertions, 0
  failures` on `164f9cb` (the canary hashes the EVENT stream; the digest gained character
  rows by design and the event stream is byte-identical — no rebank).
- [ ] main-tree suite after the rebase onto main (post-sweep)

### 3.2 Migration proved on a COPY of the owner's chain (`tmp/t1_proof.rb`, live save READ ONLY)

Junior's chain: **ABSENT** — no seat mail carried a copy (`~/.pi/agent/mail/game-two/inbox/`
empty at 06:18). Gap recorded by name; proved on the owner's copy + synthetic v2 files
(`test_migrate_v2_of_a_lived_in_chain_shape`, `save_store_test` schema-2 lane).

```
LIVE C:/Users/gabri/workspace/game-two/saves/world.json md5=2bd9c92fc65caf8da34c139a2ce53e36 bytes=448 (read once, never written)
COPY C:/Users/gabri/workspace/game-two/tmp/t1_proof/world.json md5=2bd9c92fc65caf8da34c139a2ce53e36

== 1. load the v2 copy through the real SaveStore (schema-2 lane) ==
result: App::SaveStore::Loaded
notice: save migration: retired seal dungeon_1 [18, 2] dropped (zone geometry changed; no refund)
notice: save schema 2 migrated to 3 (host character 0f7e2c1a-4b3d-4c2e-9a1b-1234567890ab; original backs up beside the save at first write)
migrated facts (canonical):
{"banked":208,"breached":[["basement_2",[6,3]],["district",[42,13]],["district_two",[42,13]],["zone_7",[33,14]]],"characters":{"0f7e2c1a-4b3d-4c2e-9a1b-1234567890ab":{"attributes":{},"bag":[],"bank_items":[],"equipment":{},"form":"blocker","forms":{"blocker":{"hp":275,"inscribed":false},"lobber":{"hp":103,"inscribed":false},"striker":{"hp":137,"inscribed":false}},"home_zone":"zone_7","insurance":0,"level":13,"xp":1740,"xp_debt":0}},"counters":{"boss_1_defeats":6,"sessions":19},"migration":{"from_schema":2,"legacy_level":13,"legacy_seed_claimed_by":false},"provisions":0}
digest=180d714b21e66560ae71663761fa722c
refusal_for(migrated) = nil
file after load untouched: md5=2bd9c92fc65caf8da34c139a2ce53e36 (== live true)
backups after load: []

== 2. construct the World from the migrated facts (host = 0f7e2c1a-4b3d-4c2e-9a1b-1234567890ab) ==
level=13 xp=1740 home=zone_7 zone=zone_7 seat1=blocker hp=striker:137/137,blocker:275/275,lobber:103/103 banked=208 defeats=6 sessions=19 breached=4
record: level=13 xp=1740 xp_debt=0 insurance=0 form=blocker home=zone_7
round trip: digest(facts(world)) == loaded digest -> true
migration block: {"from_schema" => 2, "legacy_level" => 13, "legacy_seed_claimed_by" => false}

== 3. first v3 write (the coordinator's clean-quit path) -> .bak-schema2 FIRST ==
TELEMETRY persist saved digest=cc50605b9bd62e31af9ccec40c7c1cee schema=3 banked=208 provisions=0 seals=4 marks=0 sessions=20
backups: ["world.json.bak-schema2-20260906061917"]
backup md5 == ORIGINAL live bytes: [true]
copy now schema=3 bytes=625 keys=["banked", "breached", "characters", "counters", "migration", "provisions"]

== 4. reload the written schema-3 copy: plain load, no notices, chain holds ==
result: App::SaveStore::Loaded notices=[] digest=cc50605b9bd62e31af9ccec40c7c1cee sessions=20
saved digest == reloaded digest: true
resumed: level=13 xp=1740 home=zone_7 seat1=blocker banked=208
backups now: 1 (expect 1)

== 5. --fresh backup law on a second copy: .bak-<ts> appears BEFORE any write ==
backup_fresh! -> world.json.bak-20260906061917 exists=true md5==live=true
save path present after backup: false (expect false) -> load = App::SaveStore::Fresh
fresh world: characters=["0f7e2c1a-4b3d-4c2e-9a1b-1234567890ab"] level=1 migration_block=false

LIVE untouched at exit: md5=2bd9c92fc65caf8da34c139a2ce53e36 (== start true)
```

The uuid above is SYNTHETIC (the owner's real `data/player.local.json` is created at his
first `bin/play` on main); the live save's own migration happens on that first launch and
prints the same two notices.

### 3.3 Refusal table (verbatim texts, from the same run)

| file | refusal |
|---|---|
| v1 file (the live save minus `progression`) | `save schema: 1 unsupported (expected 3)` |
| schema 4 | `save schema: 4 unsupported (expected 3)` |
| schema 2 envelope carrying v3 keys | `save keys: expected banked,breached,counters,home_zone,members,progression,provisions, got banked,breached,characters,counters,provisions` |
| insurance 9 | `save characters[0f7e2c1a-…].insurance: must be an Integer in 0..3 (death.json insurance.max_stacks)` |
| record keyed `seat-1` | `save characters: key "seat-1" is not a player id (uuid v4 or bot-<seed>)` |
| home not a hub | `save characters[0f7e2c1a-…].home_zone: "district" is not a hub (no hub-capable start there)` |
| retired v2 key `members` under schema 3 | `save keys: unknown members (expected banked,breached,characters,counters,provisions[,migration])` |
| v2 facts under schema 3 (test) | `save keys: missing characters; unknown home_zone,members,progression (expected …)` |
| `bag: [{"qty": 1.5}]` in a hand-written file | `save characters[0f7e2c1a-…].bag[0].qty: non-canonical leaf Float 1.5 (Integer/String/Boolean only)` |
| two seats, one player file (HELLO) | `REFUSED — player id collision: both seats share one player file` + hint naming `data/player.local.json` (exit 1, both seats) |
| v3 build's HELLO at a v4 seat | our seat: protocol fault `hello missing [:player_id]`; the v3 seat names `protocol version: ours 3 / theirs 4` + git-pull hint |

The exhaustive per-key table (43 rows) is `test_refusals_are_named_never_raised` +
`character_test#test_every_violation_is_named_with_its_path` (33 rows) +
`test_v2_facts_validate_under_the_frozen_v2_rules_only` (11 rows).

### 3.4 Tools
- [x] `ruby -Isrc tools/dev_save.rb tmp/dev/world.json` (worktree): `DEV_SAVE tmp/dev/world.json
  digest=c1816f70543e613495e7a2a8ee07b961 schema=3 player=ca29b270-e138-4ba8-96be-f7de14672397
  level=21/21 home=zone_7 seals_open=4 (strict decode verified)` — first run also printed
  `player file: created …/tmp/t1-wt/data/player.local.json (…keep it)` (the worktree's
  scratch identity; gitignored; dies with the worktree).
- [x] `ruby -Isrc soak/seed_save.rb tmp/x.json nest 60 3 bot-12347`: `SEEDED … schema=3
  player=bot-12347 home=nest banked=60 provisions=3 (strict decode verified)`.

### 3.5 Wire budget (W4), measured
```
seals=4 budget=3072 max_line=4096
worst 1 char: save=691 SESSION line=944
worst 2 chars: save=1040 SESSION line=1339 per-uuid-record=349
headroom after 2 maxed uuid characters: 1733 bytes (= 4 more maxed records)
owner's live save (COPY, in memory) migrated: save=576 SESSION line=827
```
Named risks: (a) Junior's S2 bag (20 slots × ~30 B ≈ 600 B per character) is the budget's
next tenant — two full bags eat most of the 1733 B; S2 must re-measure and may need
`wire_budget_bytes` raised toward `MAX_LINE_BYTES` 4096 (a protocol constant — its own
bump). (b) Soak chains accrete two bot records per episode → `N <= 4` per run before the
host's preflight refuses NAMED (recorded in `run_soak.sh`'s header). (c) v23's server does
not ship saves over HELLO — this budget is a v22-only wall.

### 3.6 Gates still owed (GL — after the sweep; the record is updated when they run)
- [ ] `rake soak SEED_SAVE=1 N=1` green (two real processes, scratch save, chain_check)
- [ ] `rake gate SCRIPT=harness/net/netplay_session.json CHECKS=harness/net/gate_checks.json` PASS
- [ ] main-tree suite green after rebase
- [ ] reviews (§4) landed; push

## 4. Reviews (Rule 6)
- [x] **headless scrubbed `pi -p` (Rule 6 fresh eyes) — DONE 06:27→06:46 on `1a45307` in the detached
  worktree `tmp/t1-review/wt`** (brief = the STAGED brief + an s136 addendum naming the commits,
  the diff commands and the recorded deviations; `--thinking max -t read,bash`; verdict verbatim
  `tmp/t1-review/verdict.txt`, 12 KB). **VERDICT: BLOCK** — rows 1–8 + 11 MET, row 9 "NOT MET as
  written" (the form deviation, recorded), row 10 NOT MET (the GL gates unrun — flips on their
  paste). Three BLOCK reasons, landed as follows (commit `<fix commit below>`):
  1. **GL gates unrun** — expected at review time; §3.6 below carries them when they run.
  2. **REAL DEFECT, AGREED — a quit INSIDE the wipe veil broke the round-trip law on `form`.**
     During `:nest_respawn` seat 1 sits on the DEAD wipe vessel (`forced_swap!`) while a marked
     ally revives; `project_forms` resolved the judgment but `form` projected the raw pointer
     kit → a legal save whose `form` names a body with hp 0 → reload seats the roster-first
     body → `facts(apply(facts)) != facts`. Fix: the host's projected `form` = the kit seat 1
     will hold AFTER judgment (held body if it lives in the projected forms, else the stored
     form if it lives, else the first living kit in roster order — the exact rule apply!
     resumes with); the validator now REFUSES a `form` whose body is dead (`…form: "blocker" is
     dead in forms (hp 0) — a form must name a living body`); `project_forms` never raises
     (the reviewer also caught that the digest now ran a save-time assertion every window —
     the one-vessel floor is asserted in `facts` only). Tests: mid-veil `form` + digest row +
     byte-exact round trip every 40 veil ticks; a coop WAITING seat 1 (stored form dead)
     projects the living body and round-trips. Both new tests FAIL pre-fix, PASS post-fix
     (stash-proven).
  3. **AGREED — the v3/v4 mixed-build case lied on the v4 seat.** `player_id` was codec-REQUIRED,
     so a v3 HELLO died in `decode` (a protocol fault nobody prints: CONNECTION LOST / "session
     ended during handshake (protocol)") before `Fingerprint.mismatch` could name the version.
     Fix: the codec requires the FIVE build fields only (`Protocol::HELLO_IDENTITY = :player_id`
     rides beside them); Session judges identity AFTER the build fields — a v3 HELLO is now named
     `protocol version: ours 4 / theirs 3` on BOTH seats (exit 1); a same-version HELLO with no
     usable id refuses NAMED (`peer HELLO carries no player id`); `Session#players` RAISES
     before the exchange (`players_known?` for the check) so a nil map can never reach
     `World.new`'s harness default. Deviation from the spec's letter ("MESSAGES[:hello] gains
     player_id") recorded: the HELLO carries it; the codec does not require it.
  Biggest unnamed risk (AGREED, named in §5): **unbounded record accretion** — every id that ever
  touched a host save keeps a record; a regenerated `player.local.json` orphans the old one; ~6
  uuid records exhaust the 3072 B wire budget and hosting refuses. Landed now: the host's wire
  refusal names the record count + the hand recovery (main.rb). The prune verb is a follow-up.
  Observations landed: `rake map` no longer CREATES the identity file (uses it when present,
  else `bot-map`). Observations recorded, not changed: `clamp_record!` on guest/unseated
  records has only the host path tested; the D-T1 seed is shadowed by the mirror for seated
  guests in T1 (only a host newcomer PLAYS the legacy level) — both true, both named here.
  The reviewer could not run `ruby`/`rake` in its worktree (the seat-lease gate's mutator
  heuristic fires on `ruby`; it did not route around) — every row is code-read + the pasted
  proofs; the suite line was trusted from this record.
- [x] `council` on the migration/refusal table + D-T1 seed rule — DONE 06:27, DeepSeek V3.2 +
  Kimi K2.5, brief `tmp/council_t1/brief.md` md5 `df60e6b004be1c2b88ba938a9afbc64e` (23.6 KB:
  decisions + proof + refusal table + the exact code excerpts), **$0.021 of the ≤ $1 budget**
  (DeepSeek $0.0024 / 12.6 s, Kimi $0.0189 / 24.6 s); raw `tmp/council_t1/out_*.json`, verbatim
  `resp_*.md`. **DeepSeek: PASS** (1 major, 1 minor). **Kimi: BLOCK** (1 blocker, 3 majors, 2
  minors). Reconciled finding by finding — agree/disagree stated, never downgraded:
  - **Kimi BLOCKER — "split-brain claim when two machines independently migrate the SAME v2
    save" — DISAGREE, premise absent by law.** No v2 file is shared between machines: each
    peer's chain is its own WORLD (v18 custody: host-authoritative, the joiner NEVER keeps the
    save; SESSION carries the host's facts into the joiner's memory only). Two chains migrate
    into two worlds, each with its own host record and its own unclaimed seed — exactly the
    intended shape: whoever JOINS a world claims THAT world's seed once. There is no later
    "sync" step and nothing to reconcile. Kimi's fixes (a pre-generated host id in schema 2;
    both seats present to claim) would break solo migration for no protected case. The one
    real scenario inside the premise — the SAME human copying his own v2 save to a second
    checkout — yields a second host record at the same level (identity is per checkout,
    decision recorded in §2); harmless for two peers.
  - **Both — `false` for "unclaimed" is a type union a future reader may misread.** AGREE it
    is a union; DISAGREE with Kimi's fix (a sentinel STRING like `"unclaimed"`): a naive truthy
    check `if claimed_by` would then read UNCLAIMED as claimed, while with `false` the naive
    check is RIGHT. `Party::UNCLAIMED` is the single reader; the validator refuses `true` and
    `nil` explicitly (tests "claimed by true" / "claimed by null"). Kept.
  - **Both — the interim mirror writes the host's level into a seated guest's record.** AGREE it
    is a documented departure from "own story" for the T1→T2b window and that the file has no
    "earned" shadow. DISAGREE that a shadow key helps: in T1's field a guest earns NOTHING
    individually (kills feed the one shared progression), so there is no earned value to
    shadow. DeepSeek's "solo boost" (a guest loading the save alone later) cannot happen — the
    guest never has the host's file. Named for T2b in §5: **the record's level at T2b landing
    is ground truth** (its spec must say so).
  - **Both — duplicate JSON keys inside `forms` are resolved by the PARSER (last wins) before
    the decoder sees them.** AGREE, classed MINOR: only a hand-edited file can carry them (the
    canonicalizer never writes duplicates), the surviving value is still a legal save, and
    `json 2.9.1` (this Ruby) has no duplicate-key refusal option (`allow_duplicate_key:` is
    silently ignored — probed). Not a T1 gate; recorded.
  - **Kimi minor — `legacy_level` above the cap is accepted then clamped.** By design: P3's churn
    law (a cap retune must never brick a save) applies to every level fact; the clamp WARNS
    (`save: clamped level … [new character]`). Recorded, not changed.
  - **Kimi minor — form restore should be owner-visible.** AGREE — it is §2 of this record and
    a line in both peers' checkpoint notes.
  - **DeepSeek confirmed:** migrate_v2 output always valid under the v3 decoder; seat order is
    iteration order, never a fact; sorted id rows are not a desync risk (both seats learn both
    ids from HELLO before construction); form restore satisfies the round-trip law.
  - **Kimi refuted its own "seat identity leaks into a fact" mid-analysis** ("seat number not
    persisted") — quoted, agreed.

## 5. Follow-ups named here (not this ticket)
- **Record accretion / prune verb** (fresh-eyes s136, biggest unnamed risk): a host save keeps
  one record per id that ever joined; a regenerated `player.local.json` (corrupt file,
  reinstall, second checkout) orphans the old record and adds a new one; ~6 uuid records exhaust
  `wire_budget_bytes` 3072 and coop hosting refuses (solo still loads). Today: the refusal names
  the count + the hand recovery. Owed: a `bin/play --forget <player id>` (or menu) prune verb
  with the backup law, or v23's server account model retiring the wire budget.
- **T2b**: the record's `level/xp` at T2b landing is GROUND TRUTH for every character (the T1
  mirror wrote the party level into every seated record; there is no "earned" shadow to
  reconcile) — T2b's spec must say so in one line.
- **T2a**: Party grows into the seat→body owner; `world.rb` extraction (1782 today; Junior's
  branch 1798).
- **S2 (Junior)**: `bag` persistence = `host.bag` (one line); re-measure the wire budget.
- **T5**: `death.json insurance.max_stacks` tuning (3 is L8's candidate).
- **Identity is per CHECKOUT**, like prefs (`data/player.local.json`): a second clone or a
  worktree gets its own id. One live checkout per machine is the norm; recorded.
- **E1c** (from the sweep, not T1): see the s135 close.
