# v20 T4 — TOTEM PILOT on floor -1 (lane C; first SIM delta) — s120, 2026-08-29

CLAIMED: Gabriel's seat, 2026-08-29 (checkpoint line pushed `f817686`
before build). Law: spec §T4 + foundation L4/L10 (foundation wins).
Dev of record this session: Gabriel's seat agent.

## 0. NINETEENTH delta clock — STARTS HERE

Per the ritual-cadence reform (owner order 2026-08-28): fun-verifies
are DELTA-TRIGGERED. **This ticket lands v20's first unverified
player-facing SIM change since the eighteenth's verdict — the
NINETEENTH fun-verify's delta clock STARTS at this ticket's ship.**
No measurement freeze arms now (freeze arms at DECLARATION only);
nothing here declares a ritual.

Named delta at clock start (accumulating, per cadence reform):

1. Heal totem station live at district's contested center (this
   ticket — SIM: new heal source, new territory logic).
2. (Beside it, presentation+graph class, named at T1: the v2b
   district retheme — map rethemes ride the same clock as named
   NON-sim deltas.)

## 1. L4 PRICED-FLIP PRE-REGISTRATION (written BEFORE code — council amendment 4)

**Telemetry rows shipping with the pilot** (oracle/machine wording,
never player surfaces):

- Existing: `TELEMETRY sustain bought=B used=U refused=R reasons{...}`
  (format-pinned, untouched).
- New: `TELEMETRY totem heals=N pulses=P` — its OWN line, printed
  immediately after the sustain line (add-only; `totem heals=` is the
  spec-named grep key). `heals` counts BODY-HEAL events (a pack body
  actually restored by a pulse, +1 per body per pulse); `pulses`
  counts pulse firings (fires on cadence regardless of who is in
  range — the uptime denominator).

**The pre-registered priced-flip condition (foundation L4, measured
at the NINETEENTH ritual, not guessed):**

> If, across the NINETEENTH fun-verify's human sessions that touch
> the descent (district ground), the potions row stays **bought=0
> (B=0)** while the totem carries the sustain load (**totem heals=N
> > 0** in the same sessions' logs), the totem flips to its
> PRICED-ACTIVATION variant (activation costs banked — free-standing
> pulse becomes a paid faucet). Evidence source: the ritual harvest's
> human launcher logs ONLY (bot logs are never fun-evidence). The
> flip itself is a lane-C re-session with its own gate — never an
> inline tweak.

Anti-condition (recorded so the flip can't be argued into firing
early): if potions show ANY buys (B>0) in those sessions, or the
totem shows heals=0 (nobody used it), the pilot reads as
"coexistence" or "undiscovered" respectively — neither fires the
flip; both route to the NINETEENTH's verdict discussion.

## 2. Design (problem → example → solution)

**Problem:** descent floors have no sustain anchor — healing is
retreat-time (walk back to the bank, dead air). Junior's totem
converts healing into the floor's focal FIGHT (king-of-the-hill
sustain; pays in position/risk, not coin).

**Placement — the authored contested center: district `[26,55]`,
mid-bridge-3.** The map's geometric center [26,44] is inside the
wall-ringed chasm (impassable by design). The two center-most
crossings are bridge 2 (rows 34-35) and bridge 3 (rows 54-55), both
9.5 tiles from center row; Junior's own v2b authoring GUARDS bridge 3
(rusher_hater at [26,54] — T1 record: "bridge-1 + bridge-3
guardians"). The totem sits at x=26 (exact horizontal center of the
52-wide floor), one tile south of its guardian: **fighting the
guardian IS fighting for the totem.** One-tile-one-entity holds
([26,54] hater spawn, [26,55] station; wood = passable, station
check_passable! green).

**Numbers** (`data/balance/sustain.json`, NEW file — Rule 3, zero
constants in code):

```json
{ "totem": { "cadence_ticks": 900, "radius": 2, "heal_amount": 10 } }
```

- `cadence_ticks` 900 = ~15 s at the tick-locked 60/s — Junior's own
  "~15s starting suggestion, number explicitly deferred to balance"
  (his proposal, banked 2026-08-27).
- `radius` 2 = Chebyshev (tile_distance law) — covers both bridge
  rows and two columns each side; the pack's 3 bodies fit, the
  guardian's tile is inside (irrelevant: pack-only heal).
- `heal_amount` 10 vs potion 30-to-all-anywhere-for-5-banked: free
  but SLOW and positional (~40 hp/min/body if you HOLD the bridge).
  Pack max_hp for scale: striker 80 / blocker 160 / lobber 60.
- Presentation scalar rides display.json (`totem_pulse_frames` 40) —
  ring animation length, not balance.

**Heal target = pack bodies** (all living members in radius,
hp < max_hp, clamped; dead untouched — the vat keeps its regrowth
monopoly). The spark's "damaged HUMAN bodies" wording collides with
this sim's `@humans` (the HOSTILES) — foundation L4 + Junior's
proposal + the totem-vs-potions telemetry (potions heal the pack)
all fix the target as the PLAYER side; healing hostiles would invert
the design. Foundation wins over spark wording (recorded).

**Pulse fires on cadence regardless of range occupancy** (healed may
be 0): the visible idle pulse is the totem's own discoverability —
T3's always-on lesson (POTION 0 = the buy motivation) applied to
territory.

## 3. L10 seam ledger (extraction owed INTO this ticket)

world.rb enters at **1769/1800**. New object:
`src/game/stations.rb` (`Game::Stations`, plain object — FieldEconomy
/Volleys precedent: explicit deps, callables reaching back).

**Moves OUT of world.rb into Stations:**

| what | world.rb lines (pre) |
|---|---|
| `sustain` verb body + doc comment | ~504-540 |
| `@sustain_done` latch (→ `Stations#reset!`, called where the old reset sat in tick_world) | 593 |
| `interact_bank` | ~1208-1214 |
| `interact_altar` | ~1216-1223 |
| `interact_vat` + comment | ~1225-1247 |
| `spend_banked` | ~1303-1307 |
| `sustain_refuse!` + comment | ~1321-1330 |
| NEW: totem cadence timers + pulse (never enters world.rb) | — |

**World KEEPS** (the cue/event-write law + save-law caution):

- `interact` dispatch + verb guards (input-validity stays World's).
- `interact_seal` — breach registry is SAVE-LAW-COUPLED (strict
  decoder refuses on breached-tuple drift; owners' live chain carries
  `["district",[42,13]]`) and presentation-heavy (@breach_line, feel
  kick, mark_seal!). Not worth the callable fan-out this ticket;
  noted as the next extraction candidate if world.rb re-approaches
  the cap.
- `regrow_binding` / `regrow_tile_beside` (zone-binding law) — vat
  reaches back via callable.
- `station_cue!` / `station_refuse!` (presentation writes) — Stations
  cues via callables.
- Transients writes (wire_events subscribes `:totem_pulse` →
  `@transients.totem_pulse!`, the taunt-pulse grammar).

**Stations deps (constructed after PriceSheet — live Pack law):**
`bus:, pack:, economy:, sustain_cfg:, price_sheet:, zone:` (callable)
`, cue:, refuse:, regrow_binding:, consume_mercy:, assign_seats:,
map:` (callable — totem station lookup).

**Digest:** totem timers are gameplay-affecting countdowns →
`Stations#digest_groups` (FieldEconomy precedent), concatenated in
`digest_snapshot`. Timer semantics: lazily armed at a zone's first
ticked frame, decrements only while its zone is CURRENT and state is
`:world` (hitstop + respawn veil pause it with tick_world — same
pause law as combat). NOT a save fact (per-session transient — L9
untouched).

**EventBus:** `:totem_pulse` registered in `World::EVENTS` at first
use; added to the harness `EventLog::EVENTS` curated list (manifest
countable). Extra EVENT lines in existing district scripts' logs are
lawful (manifest = min counts; no pinned event-md5 anywhere in
test/).

## 4. Known blast radius (priced up front, s118 review risk adopted)

- **sustain_run** (seed-7 zero-slack, LIVES in district/nest): totem
  pulses add EVENT lines (harmless) — but a pulse coinciding with its
  bridge-3 east crossing while bodies are wounded would HEAL them →
  fight-outcome drift → manifest break. Headless manifest proof runs
  BEFORE assuming; re-cut path budgeted
  (`tools/tune_sustain_run.rb`, wants BYTE-UNCHANGED, then gate +
  standalone hint checklist on frozen gate_a).
- **floor1_run / district_hunt**: same headless-diff-first law
  (center-adjacent cameras may catch fixture/ring → capture drift is
  a gate matter, not a manifest one, unless heals land).
- **Wall growth:** new script `harness/scripts/totem_pulse.json`
  joins the wall (new regression surface, wall-directory law) + ONE
  shared gate row worded affirmatively with an honest not-exercised
  exemption; gated THIS ticket on the strong member (totem_pulse
  reel) per the 2026-08-26 residual-tolerance lesson.
- **Not touched:** cap/creature numbers (one gated SIM piece = the
  totem), standing C2 reds, saves/, manifest wants of existing
  scripts.

## 5. Evidence (filled at close — every line lands ONLY with its artifact)

- [x] Extraction commit `73c3985`: pure move, world.rb 1769→1694; suite 1324 runs 0F (hook); 5 headless EVENT-md5s byte-identical vs PRE (§6)
- [x] Totem sim commit `5b66acc`: +4 integration tests (real files), suite 1328 runs 0F (§7)
- [x] Authoring re-import: totem entity via python fixpoint-proven edit → `tools/import_ldtk.rb`; district.json +12 lines, other four emissions md5-identical; pilot_authoring_test green (§7)
- [x] Rule 2 gate PASS: totem_pulse rc=0 — vision 73/73 rows incl. affirmative `totem_pulse_reads` ("Frames 3626 and 4526 show a vivid green square ring expanding around the green fixture beside the dwelling pack"), determinism 10 captures byte-identical, MANIFEST PASS totem_pulse=10 fight_resolved=10 attack_hit=42 (§8)
- [x] sustain_run headless proof: stripped-of-totem-lines EVENT md5 == PRE md5 `4b9b99e1…`, healed_sum=0, frames/zone identical — NO drift, NO re-cut owed, wants untouched (§8)
- [x] Local wall: floor1_run 0/PASS · district_hunt 0/PASS (first run = Bedrock read-timeout in the critic, det+manifest PASS both; standalone re-run clean per the disrupted-gate law) · world_loop 0/PASS · ledger_loop 0/PASS · town_gates 0/PASS (non-district locality probe) (§8)
- [x] Soak: N=1 default PASS (36120/36123 ticks, desyncs=0, chain fresh→dec77789) · N=1 ZONES=district PASS (36120/36116, desyncs=0, chain fresh→2b4b9ae8, EP1 zone=district) — both seats closed `TELEMETRY totem heals=0 pulses=9` IDENTICAL (lockstep held over totem state; bot logs = mechanical evidence only) (§9)
- [x] `totem heals=N` in a scripted replay log: `TELEMETRY totem heals=2 pulses=5` in BOTH replay halves of the totem_pulse gate log (§9)
- [x] world.rb net vs 1769: **1710** (-59 with the totem IN); line-caps row green every commit
- [x] Fresh-eyes review (Rule 6): **PASS, 0 blocking, 2 advisories — both executed** (§10)

## 6. Extraction ledger (commit `73c3985` — pure move, proven)

world.rb 1769 → 1694 (-75) BEFORE the totem; moved verbatim into
`src/game/stations.rb`: sustain verb body + seat-race latch (now
`Stations#reset!` at the old tick_world reset site beside
@aggro.reset!), sustain_refuse!, interact_bank/altar/vat bodies,
spend_banked (public on purpose — the seal spends through the one
audited seam). World kept: interact dispatch + verb guards,
interact_seal (save-law-coupled breach registry), regrow_binding /
regrow_tile_beside (zone-binding law), station_cue!/station_refuse!
(presentation writes). Six callables reach back: cue, refuse,
regrow_binding, consume_mercy (@vat_mercy_armed), assign_seats, zone.
**Behavior identity proven**: full suite 1324/0F + all five blast
scripts' headless EVENT md5s byte-identical to the pre-change
baseline (tmp/t4_blast/pre.txt == post_extract.txt).

## 7. Totem sim + authoring (commit `5b66acc`)

Sim exactly as §2/§3 designed: per-station timers lazy-arm at the
zone's first ticked frame, tick only while the zone is current and
state is :world (hitstop/respawn-veil pause = the combat pause law);
pulse emits ALWAYS on fire (healed may be 0); heal = living pack
bodies, Chebyshev radius, clamped; dead untouched. `:totem_pulse` in
World::EVENTS + the curated EventLog. Telemetry line
`TELEMETRY totem heals=N pulses=P` after the sustain line (guarded
subscribe). Transients ride tick_combat!; renderer: TOTEM_HEAL green
[120,235,160] expanding square ring via draw_pulse_ring (shared with
the taunt ring — color separates families), fixture from sidecar
palette `station_totem` [96,200,120]. display.json
`totem_pulse_frames` 40.

Authoring (lawful pipeline): byte-fixpoint proven on the unmodified
pilot.ldtk FIRST (json.load→dumps(indent=2)+CRLF md5-equal), then
the totem Station appended (px [832,1760] = tile [26,55], type
"totem", null price/opens/line in the bank-entity null grammar, iid
…-0099); sidecar gains station_totem; re-import emitted district.json
(+12 lines) with the other four zones md5-identical (zones_pre.txt ==
zones_post.txt minus district). Provenance pin green.

Tests (+4 runs, real files, no mocks — test/game/totem_test.rb):
authored station present · pulse at exactly cadence_ticks with
in-radius deep wound +heal_amount, shallow wound CLAMPED, in-radius
dead body untouched (healed=2 in the event) · idle pulse fires with
healed=0 and out-of-range wounded body untouched · timer digest group
present, counts down, re-arms at cadence + telemetry line reads
`totem heals=1 pulses=1` after one healing pulse. Two live traps
recorded in the test comments: staging kills kick hitstop (which
pauses the cadence — settle! drains it) and a split pack walks the
map to regroup, dragging rusher aggro onto the bridge.

Suite tripwires that fired (each answered as designed): telemetry
summary pin extended add-only · state_digest mutation sweep now
covers totem.N (flipping the timer flips the canonical form) ·
save_state W1 classification: totem.* = SESSION-ONLY group (L9: the
countdown is never a save fact; totems re-arm each session).

## 8. Rule 2 gate + blast radius (commit `4dc14a0` + runs)

**totem_pulse.json** (new wall member, cut by
`tools/tune_totem_pulse.rb`): seed 7, start district level 5
(sustain_run's staged-progression precedent), 4582 ticks — west
trail (trail-rusher fights), bridge-3 guardian kill (fight_resolved
beside the totem), wounded dwell at [25,55] across the two
post-arrival pulses (f3602/f4502; the first three fire pre-arrival —
empirical frames, hitstop drift included). 10 captures. Manifest
wants totem_pulse=4 fight_resolved=10 attack_hit=20 (double-replay
minimums from the cut's own counts).

Gate table (attended, this ticket — NO full wall):

| script | gate | manifest | note |
|---|---|---|---|
| totem_pulse | 0 | PASS 10/10/42 | affirmative totem_pulse_reads read (strong member, 2026-08-26 law) |
| floor1_run | 0 | PASS 4/12 | district canary |
| district_hunt | 0 | PASS 22 | re-run; first run Bedrock read-timeout (det 7/7 identical + manifest PASS both runs) |
| world_loop | 0 | PASS | canary |
| ledger_loop | 0 | PASS | canary |
| town_gates | 0 | PASS | non-district locality probe |

**Blast radius, measured not assumed** (headless EVENT-md5, PRE at
`f817686` vs POST-totem; tmp/t4_blast/):

| script | verdict |
|---|---|
| sustain_run | stripped md5 == PRE (`4b9b99e1…`), 18 pulses healed_sum=0 — its east crossing never coincides wounded-in-radius with a pulse; NO re-cut |
| district_hunt | stripped == PRE, 3 pulses healed 0 |
| floor1_run | stripped == PRE, 1 pulse healed 0 |
| world_loop | md5 == PRE byte-identical (0 pulses — never in district) |
| ledger_loop | stripped == PRE, 4 pulses healed 0 |

Every event stream is byte-identical modulo the added totem_pulse
lines themselves: ZERO sim drift anywhere; manifest wants untouched
repo-wide. The new shared gate row self-exempted honestly on
district_hunt ("Not exercised — no green mid-bridge totem dwell is
captured") — no false-affirmative.

## 9. Soak + telemetry evidence

- `rake soak N=1`: EP1 PASS — host 36120 / joiner 36123 ticks,
  desyncs=0, clean quits, CHAIN intact fresh→dec77789.
- `rake soak N=1 ZONES=district`: EP1 PASS — 36120/36116, desyncs=0,
  EP1 zone=district, CHAIN intact fresh→2b4b9ae8. Both seats closed
  **`TELEMETRY totem heals=0 pulses=9` byte-identical** — nine pulses
  fired on totem ground under two REAL processes in lockstep, the
  totem digest group holding across the wire (heals=0: bots never
  dwelt wounded in the field — expected, they path randomly; bot
  logs = mechanical telemetry evidence ONLY, never fun-evidence).
- Scripted replay evidence (the spec-named grep key): the totem_pulse
  gate log carries `TELEMETRY totem heals=2 pulses=5` in BOTH replay
  halves + 10 `EVENT totem_pulse frame=… healed=… range=2` lines.

(Sections 10+ at close: fresh-eyes review verdict, checkpoint.)

## 10. Fresh-eyes review (Rule 6) — PASS, 0 blocking, 2 advisories

Scrubbed headless session (PI_* env cleared), read-only brief + the
f817686..4dc14a0 diff + this record attached; JSON verdict demanded
as the last message (tmp/t4_review_out.txt). **Verdict: PASS,
blocking: [].** Advisories, both EXECUTED same session:

1. **Manifest want lax** — totem_pulse want was hardcoded 4 while the
   deterministic double-replay count is 10 (a regression halving the
   pulse rate would have passed). Tightened: script want → 10, tuner
   now derives it like the other two wants; MANIFEST PASS re-proven
   against the banked gate log (10/10/42 ≥ 10/10/20). Tightening a
   want is lawful (never-weaken law binds the other direction).
2. **Silent interact no-op on the totem tile** — the dispatch's
   `else false` covers it by design (the totem has no interact verb);
   one comment now names it and points the L4 PRICED-activation flip
   at this seam. world.rb 1710 → 1712, still net-down vs 1769.

- Measurement freeze NOT armed (nothing declared); delta clock
  running from this ship.
- Standing C2 reds untouched; no full wall sweep (locality proven
  §8); wall grows by one member (totem_pulse).
- Peer queue line (delivered via checkpoint): es-CR + pt-br below.

**Para Gabriel (es-CR):** el tótem de curación ya vive en el piso -1
— está en el puente del medio, se pelea por el centro contra su
guardián, cura por pulsos lentos (cada ~15 s) y gratis. Arranca el
reloj del próximo fun-verify.

**Para Junior (pt-br):** o totem de cura já vive no andar -1 — o teu
desenho: no meio da ponte 3, do lado do guardião que tu mesmo
colocaste, pulso lento (~15 s) e de graça, território que se disputa.
O relógio do próximo fun-verify começa aqui. Os números (cadência
900, raio 2, cura 10) são chute inicial em data/ — teu para mexer.
