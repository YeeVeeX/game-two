# T0 — fresh-eyes review of `restore/pre-mundo-vivo-20260904..HEAD` (2026-09-05, s133)

Range: `4d54a01` (restore/pre-mundo-vivo-20260904) .. `bc5dc49` (HEAD at launch; 221 files,
+95,881 / −9,962): MUNDO VIVO (FASE 1–7), PREMIUM v22 passes 1–11, WB-T6, hygiene, Junior's
A3 audit tooling. Four READ-ONLY headless reviewers (Claude Fable 5.1, `--thinking max`,
tools read+bash, detached worktree `tmp/t0-review/wt`, prompts banked beside this file),
one pass each, ~$5 each declared; all four exited rc=0 with the findings block as the last
message. Raw findings verbatim: `drafts/_t0-review-20260905/area_{a,b,c,d}.md` (md5 `511a6c23`
· `6cb6cb90` · `17241bdf` · `e2f631ef`). Prompt: `prompt_common.md` + `prompt_a.md` (the area
blocks differ only in paths/focus).

Totals: **64 findings — 1 BLOCKER · 22 MAJOR · 34 MINOR · 7 NIT.** Plus one hub-confirmed
finding from the uiux spoke (F-A3-1). T0 is NOT a revert vehicle: BLOCKERs/MAJORs become
tickets (spec §5 E-tickets, amended s133); MINOR/NIT bank here. Every claim below was either
re-verified by the hub against the file (marked ✓) or carries the reviewer's quoted evidence
(unmarked — verify at the ticket, sampling-artifact law).

## 1. BLOCKER (1) → ticket E0

| # | Area | Finding | Evidence | Ticket |
|---|---|---|---|---|
| a1 | sim | **Phased-boss skill rotation is off by one and can CRASH the session.** `Creature#begin_action` reads `cfg = kit.fetch(kind)` (skill N), then `advance_boss_skill!` bumps the index; `action_config` re-reads the MERGED `kit` → resolution uses skill N+1. `ember_boss` phase 1 = `['dash','beam']`: a beam start nils `@dash_plan` (line 657), the advanced index resolves as dash → `activate_action` → `@dash_plan.duration` on nil (line 712). Deterministic on both seats; `boss_phases_test` samples `action_config` post-advance (line 74) so it cannot see it; no wall script reaches phase 2. ✓ hub re-read `creature.rb` 81–88, 649–665, 709–713 and `combat.json` phases (serpent_boss p1 `[spread, arc3]`, p2 `[beam, spread, arc3]`; ember_boss p1 `[dash, beam]`, p2 `[beam, dash, beam]`). | `advance_boss_skill! if kind == :attack` after `cfg = kit.fetch(kind)`; `@dash_plan = nil unless cfg[:arc] == "dash"`; `@iframes = [@iframes, @dash_plan.duration].max` | **E0** |

## 2. MAJOR (22) → tickets E1–E6

**Gate integrity (area d + b) → E1 "harness truth" (FIRST after T0 — it frames every later gate):**
- d8 `harness/vision_critic.py:72-77` — the critic PERSONA (system prompt of every verdict) still says "Flat-rect placeholder art is a DELIBERATE choice … do not ask for sprites … Player is ember orange, enemies pale bone" after sprites + material tiles shipped → every PREMIUM gate was framed against frames that no longer exist. **Hub grades this Rule-2-critical.**
- d4 `harness/pins.json` = `[]` although a 42-script wall ran at `064bd80`: the sweep ran in worktree `game-two-wall3` (since pruned) and pins + per-row verdict JSON were written THERE; only curated GATE/rc lines were pasted into `drafts/_wall-premium-v22-20260905.log`. Explains `rake pins` "no pins recorded yet". ✓ (pins.json empty; worktree list has no wall3)
- d5 `run_wall.sh` + `Rakefile:137-140` — a `SKIP_CRITIC=1` sweep records `--gate-rc 0` → `rake pins` would say PINNED for scripts no critic judged; an exported `CHECKS=harness/net/gate_checks.json` leaks into every `rake gate`.
- d6 `_wall-premium-v22-20260905.log` — 7 manifests (`level_gate loot_loop nest_advance threat_pull vat_economy zone8_crossing zone_catchup`) fail deterministically every sweep since 2026-08-29 ("T7/C2 census"); the runner exits 1 and PASS is a human matching the red set = a failed gate downgraded to advisory for three cycles (Rule 6).
- d7 `harness/scripts/zone8_crossing.json` + `gate_scope.json:28` — stages a DUNGEON 1 seal breach the range retired (`dungeon_1.json "stations": []`); inputs choreographed on the old map; scope still lists dungeon_1 for `seal_breach_reads`.
- d9 `gate_checks.json:106,158,238,254` — four rows anchor on the retired WHITE possession ring (`taunt_convergence_reads`, `god_mark_reads`, `writ_frame_reads`, `burst_legibility_budget`); with art loaded the ring is the `else` fallback, never drawn.
- d2 no active wall script stages any boss beat (`challenger_engaged` only in `harness/retired/*`); `boss_phase_pips_read`, `challenger_tell_reads`, `writ_frame_reads` have never passed affirmatively; the BOSS BAR (`d3a00c5`) has no row. Related: d18 `make_tour.sh` cards promise "BOSS 2 (phased)" / "BOSS 4 (phased)" reels that never show a boss.
- d1 = b2 `harness/scenes/netplay_scene.rb:67` — the netplay gate scene builds `Renderer.new` without `art:/ambience:/tileset:` → the three net gates judge the quad fallback; the partner-seat cyan HALO has never been captured anywhere. (Renderer wiring is copy-pasted in window.rb, world_scene, menu_scene, netplay_scene.)
- d3 = b1 six PREMIUM passes shipped surfaces with NO gate row: Fx spark/burst/dust, Light glows/vignette/kill punch/level flash, damage numbers + wounded hp bars + boss bar, ally callouts + row pulse, pickup gleam + low-hp pulse, interact prompt (only signage 85 and minimap 86 got rows). a5 adds: petrify stone telegraph, blink flash, aura ring have no row.

**Sim correctness (area a) → E2:**
- a2 `creature.rb:623` — `revive!` does not clear poison; a body killed mid-DOT by another source revives poisoned.
- a3 `controllers.rb:211` — ally-brain "focus fire on the leader's target" is a DEAD branch (`focus` only ever set on humans) → the A3 audit measured "finish lowest-hp / nearest", not the documented rule; a6 the `human.coward` rule and the brain's dodge/ring/dash branches have no tests. **Feeds T2d: no flip before these tests exist.**
- a4 balance fallbacks in code (`spread_count 3`, `beam_length 6`, `ranged_hold_tiles 3`, `finish_pct 0.35`, `ring_min_adjacent 2`, dash `max_tiles 4`, `coward_pct 0.25`, `flash_frames 8`) — shadowed by data today; a renamed key would silently play a hidden default (AGENTS non-negotiable 3).
- b6 `src/app/cli.rb:131` — `--start-zone` accepted on human `--host --save <scratch>` and bare `--join`, but `Params` carries no start_zone → two seats with different flags start in different zones and die as a DESYNC instead of a handshake refusal.

**Presentation truth (area b) → E3:**
- b3 `renderer.rb:1684` — the INTERACT prompt shows ON or BESIDE any station (4 tiles + totems, where H does nothing) while the verb fires only on the station tile; rope_spot (an interact) gets no prompt.
- b4 `minimap.rb:58` — every transition baked GOLD regardless of `way_locked?` (gold = walkable law; contradicts `exit_signage_reads`); d12 the `minimap_reads` row contradicts itself.
- b5 ~40 "display.json knobs" exist only as `fetch` defaults in code (fx_*, light_*, vignette_*, minimap_*, boss_bar*, hud_plate_rect duplicated in hud.rb:51 + renderer.rb:1652 …) — data-driven law; fix = write every key + a test that every `@display.fetch(:k` key exists in the file.
- **F-A3-1 (uiux spoke, hub-CONFIRMED ✓ `drafts/_t0-review-20260905/f-a3-1_tour_12s_hud.png`):** the green SAFE chip (`safe_chip_y 98`) overdraws the `0 COINS` chip in every safe zone; proposed `safe_chip_y 138`.

**Authoring (area c) → E4:**
- c1 `test/game/zone_identity_data_test.rb:21-22` — the six range-added zones (`dungeon_2/3/4`, `ember_1/2/3`) are missing from ZONES and FAIL the identity contract when applied (tower: wall darker than floor; brasa: spread < 40) — an authoring finding by the test's own header (fix in the sidecar + re-import or record a conscious law amendment). Also a13.

**No-lore (areas a/b/c/d) → E5 (docs-only renames; Junior's files asked in the pt-br line, never rewritten by this seat):**
- c2 = b7: `tools/gen_premium_art.py:46` ("Fio / Aro / Pomo"), `tools/premium_art/humanoid.py:228,379,631,635,637` (Junior's files) · a12 `test/game/challenger_test.rb:68,95`, `dread_test.rb:58` `face_varekka!` · b7 `renderer.rb:1479` comment "varekka gate catch" · d17 `vision_critic.py:225` "gate into the dungeon (Threketh)"; rows "MEDUSA TOWER / BRASA / MUSGO"; script names `brasa*_run` / `tower*_run`; `harness/retired/varekka_duel.json`. Junior's SYSTEMS proposal §1.2 ("Fio's blade never fits Aro's hand") — his file. **Owner's call (one line):** `TELEMETRY varekka` oracle wording (frozen pre-order) and whether MEDUSA/MUSGO/BRASA are theme words or fiction (c-UNCERTAIN a).

## 3. MINOR (34) and NIT (7) — banked, no ticket unless a lane touches the file

- **a7** `@poison_interval` sim state not in `digest_fields` · **a8** aura cadence on `@frame % period` skips a burn across hitstop · **a9** even `spread_count` emits count+1 pellets · **a10** `combat.json` `boss.defeat_counts` / `attack.charge` read by no code · **a11** `world.rb` extraction seams (Combat 844–1046 · Respawns 247/1406–1489/1691–1775 · Seizures 656–783 · Seats 812–842/1375–1403/1614–1667) → **input to T2a** · **a13** = c1 · **a14** `boss_phases_test` cannot detect a1 · a15–a17 nits (dead guard, duplicate readers, PT labels in `a3_leash_trace.rb`).
- **b8** renderer reads `instance_variable_get(:@blink_flash)` · **b9** Fx/Light subscribe closures on the WORLD bus at first draw (presentation code runs inside `World#tick`) · **b10** callout labels translated once at construction · **b11** minimap `rescue StandardError → nil` swallows build errors · **b12** `BOSS_NAMES` bypasses `tr` · **b13** `art.rb:163` hardcodes the 8-frame hurt window · **b14** `art_registry_test` never range-checks dodge/special anims; drift message points at the PLACEHOLDER generator (would overwrite premium atlases) · **b15** stale headers (renderer, minimap, gen_premium_art COLS) · **b16** kit colours in three hand-synced places → `data/art/kits.json` (**input to the art lane AB/AA**) · b17 nits.
- **c3** 36 duplicate LDtk `iid`s (`build_tower_floor.py` prefix collides with `build_district_two_v3.py`) → **WB-T7 precondition** (EntityRef needs uniqueness) · **c4** `import_ldtk.rb:434` writes emissions in text mode → CRLF on disk, LF canonical; a non-autocrlf seat commits CRLF (`File.binwrite`; same class in three Python generators) · **c5** MAP_EDITING §1 table stale (13 levels, not "pilot four") · **c6** the five OOB spawns named exactly ({basement_1 [4,3]→zone_7 [26,4]}, {basement_2 [4,3]→zone_7 [35,4]}, {dungeon_1 [29,7]→zone_8 [62,18]}, {ember_3 [1,15]→ember_2 [60,14]}, {zone_7 [1,14]→low_quay [24,33]}) — nothing pins the set → **WB-T7 oracle** · **c7** builders import PIL at module top · **c8** `normalize_ldtk_test` probes `py -3.12` before `python` (the hook runs `python`) · **c9** `build_tower_floor.py` "idempotent" is false once a lower floor exists (drops `stairs_down` on re-run) · c10–c11 nits (docstring line, soft skip on a missing lint).
- **d10** drop rows describe squares (now gems) · **d11** `zone_identity_reads` "gold strictly reserved for open ways" broken by design · **d13** coverage audited over `start.zone` only (slow_door, zone_8 escape) · **d14** `pins.rb RENDER_PATHS` omits `harness/**` + `src/core`; `rake gate` never records a pin · **d15** canary header "ACTIVE bank" paragraph stale; re-authored `floor3_run` reuses the retired name (verdict log conflates two choreographies) · **d16** Bedrock critic `claude-fable-5` vs gateway `fable-5.1` → seat-dependent verdicts · d19 nit (self-gating guard one-directional).

## 4. UNCERTAIN (reviewers could not settle read-only; settle at the ticket)

a1's crash by a live repro (ember_boss ≤ 50 % in `grass_fixture`, ~300 ticks) · whether `tower4_run`/`brasa3_run` ever show BOSS 2/4 (open `captures/tower4_run_gate_a/frame_1971.png`) · the exact manifest keys failing for the 7 census scripts (a fresh `rake manifest` per script) · `multi_floor_descent` captures vs the swapped map · emission byte-stability across Ruby `json` versions (pin `gem "json"` or run `pilot_authoring_test` on Junior's Ruby) · whether the minimap revealing unexplored layout is intended (peers' call) · MEDUSA/MUSGO/BRASA as theme words vs fiction (owner's call).

## 5. CLEAN (read fully, nothing found) — from the four reports

`src/net/**` · `data/netplay.json` · event bus / data_store / tile_registry · `save_state.rb` `apply!` order + refusals + the seal-retire migration · `digest_snapshot` classification (new sim facts digested; presentation excluded) · the ally-brain OFF path (byte-inert, traced; `world_loop` canary hash unchanged across the range) · EVENTS registration · manifest ↔ 20 atlases (704×192, md5s, anim ranges) · tiles.json ↔ PNGs · strings en/es/pt-br parity (88 keys) · `window.rb` 270/300 no creep · no wall clock / rand in `src/app` · normalize/aftersave/lint tools + the 14-row allowlist recomputed exactly (6 INTENDED + 8 LEGACY, paths listed in area_c) · provenance over all 13 pilot levels · `soak/**` · `run_wall.sh` rc capture · Rakefile `:gate` · replay runner · harness tests.

## 6. Disposition (tickets cut into the spec §5 s133; owners)

| Ticket | Scope | Owner |
|---|---|---|
| **E0** boss rotation snapshot fix (a1) + started-arc == resolved-arc test + ember_boss phase-2 test; canary rebank INTENDED (a fix that changes streams) | either seat (Junior authored the boss block — told in the pt-br line) | 
| **E1** harness truth: persona rewrite (d8) · pins path + verdict log to the MAIN ledger (d4) · SKIP_CRITIC/CHECKS refusal (d5) · the 7 census manifests re-cut or retired so rc means something (d6) · zone8_crossing re-author + scope (d7) · four ring rows → halo (d9) · netplay scene art wiring + `Renderer.build` factory (d1/b2) · gate rows for passes 3–11 + petrify/blink/aura (d3/b1/a5) · one boss sentinel per boss + boss-bar row (d2/d18) | either seat; **before T1's first gate** |
| **E2** sim correctness: revive clears poison (a2) · strict `fetch` for balance keys (a4) · `--start-zone` host-authoritative in Params or refused on human join (b6) · ally-brain focus-fire dead branch + coward/dodge/ring/dash tests (a3/a6 — precondition of T2d) | either seat |
| **E3** presentation truth: interact prompt iff a verb exists on the tile (b3) · minimap ways live + `way_locked?` (b4/d12) · display.json knobs written + existence test (b5) · SAFE chip y (F-A3-1) | either seat (Junior's surfaces — his line first) |
| **E4** zone identity for the six new zones (c1/a13): sidecar palettes re-authored through the importer or a recorded law amendment | Junior's maps → his call, either seat executes |
| **E5** no-lore renames, docs-only (c2/b7/a12/d17) — Junior's files asked, not rewritten; owner line on `varekka` oracle wording + MEDUSA/MUSGO/BRASA | both peers |
| inputs | a11 → T2a seams · c3/c6 → WB-T7 · b16 → art lane AB/AA · d14/d15/d16 → E1 follow-ups | — |
