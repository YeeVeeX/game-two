# v20 T5 — SECOND WALL CLASS (engine, small) — 2026-08-29, hub seat (s121)

Law: spec §T5 (`docs/superpowers/specs/2026-08-28-v20-descent-cycle.md`) +
foundation L11. Owner content-priority ruling (s120): lane D cuts at T5
close — the lane-D spark stages in §8. CLAIMED `c1fc4dd`.

## 1. Design (record-first, pre-build)

**Problem** (s113 review, CONFIRMED TRUE): one `passability: wall` type
exists; renderer draws every wall through the literal `:wall` palette
key. Junior's v3 grammar (red inner walls + near-black bounds — now
REINFORCED by his floor-2 v3 FIEHONJA: orange reef + dark rim) cannot
render.

**Name: `wall_inner`** — char `%`, int_grid 7, render `wall_inner`,
footstep stone, passability wall. Defense (one line): it names the
CLASS mechanically — the interior wall vs `wall` the boundary — no
fiction, mirrors the palette key. `%` verified unused in every zone and
test-fixture grid (grep, this session).

**Blocking seam — `TileMap::WALL_CHARS = ["#", "%"]`.** The v0 wall
law ("# and passability wall imply each other", one char) generalizes
to a frozen char SET; `passable?` checks set membership. TileMap stays
registry-independent (bare fixture maps keep working). TileRegistry's
`validate_wall_law!` becomes: `passability "wall"` ⇔ char ∈
`TileMap::WALL_CHARS` — a third wall type with an unlisted char still
refuses NAMED (the law keeps its teeth: the grid char alone decides
blocking, so every wall char must be in the set TileMap reads).

**SIM-INERT claim + its proof plan:** no live grid carries `%`, so
every `passable?`/`wall?` truth table is unchanged zone-by-zone;
blocking semantics for `%` are IDENTICAL to `#` (same class). Proof =
pre/post capture md5 A/B on two canaries + gate determinism halves +
`rake perf` (passable? is hot: one string compare becomes a 2-element
set probe). Delta-clock line: **T5 accumulates NOTHING** —
presentation + data plumbing; zero sim numbers move.

**Render resolution — shared, one source:** `App::TileVariants` gains
`specs(map, registry)` (the char→type-spec merge `rects` already
builds) + `wall_ref(specs, map, tx, ty)` → the wall tile's render ref
as a palette symbol, `:wall` fallback. Consumers:
- `Renderer#static_runs` wall pass: `[x, y, w, h, wall_ref(...)]`
  instead of the `:wall` literal. Existing zones: every wall char is
  `#` → type `wall` → `:wall` — merged runs byte-identical by
  construction (same refs merge the same).
- `MapArtifact#cell_rgb` wall branch (line ~82): same resolution,
  memoized per map. **Scope addition beyond the spec's file list,
  defended in one line:** god-view panels ALL zones (world.zone_maps =
  every data/zones JSON — verified this session), so the fixture WILL
  appear there; map_artifact is the SAME hardcode class L11 exists to
  remove, and leaving it ships a known-wrong surface. Cost: ~6 lines +
  the map surface's own gate (probes + critique, decision 13).
- Station fill fallback chain (renderer ~597): UNTOUCHED (brief guard).

**Fallback ruling (guard-2 ask):** a zone using `%` without
`wall_inner` in its palette **refuses NAMED at load** — the EXISTING
`TileRegistry#validate_map!` cross-check already enforces render-ref ∈
palette for every used char (verified: it does not skip walls). The
renderer's `:wall` fallback exists ONLY for registry-less bare fixture
maps (unit tests) that never reach World. Zero new refusal code.

**Importer:** zero code — `decode_int_grid` resolves through
`registry.char_for_int_grid` (verified line 193), so the tiles.json
entry IS the IntGrid mapping. Proof owed: (a) all five pilot emissions
byte-identical post-change (md5 table §4), (b) synthetic-doc tests:
int_grid 7 decodes to `%`, import→emit→import fixpoint holds, emitted
zone loads through the game loader with a wall_inner palette.

**Fixture zone `data/zones/wall_fixture.json`** — hand-authored
(lawful: outside the pilot five), display_name TEST 2 (gate_fixture =
TEST 1 precedent), inbound-INERT (grass_fixture precedent: one
outbound transition → district [1,13] — district declares
gradient_anchor, so the added arrival cannot re-anchor its band map,
v12 trap checked). Palette: `wall` near-black, `wall_inner` red
(Junior's MEDUSA/v3 grammar as the strong member). Grid stages `%`
segments ADJACENT to `#` bounds so both classes share frames.

**Wall script `harness/scripts/wall_fixture_walk.json`** (permanent
regression surface — the wall directory is truth): start.zone
wall_fixture, deterministic walk past inner-wall segments, captures
showing both classes adjacent.

**Gate row (74th, ONE row): `wall_class_reads`** — affirmative on the
strong member, honest not-exercised exemption elsewhere (totem_pulse
precedent). Wording lands in §5.

## 2. Files touched (planned)

- `data/tiles.json` — wall_inner entry (int_grid 7)
- `src/core/tile_map.rb` — WALL_CHARS set (passable? seam)
- `src/core/tile_registry.rb` — wall law over the set
- `src/app/tile_variants.rb` — specs + wall_ref (shared resolution)
- `src/app/renderer.rb` — static_runs wall pass by render-ref
- `src/app/map_artifact.rb` — cell_rgb wall branch by render-ref
- `data/zones/wall_fixture.json` — fixture zone (NEW)
- `harness/scripts/wall_fixture_walk.json` — wall member (NEW)
- `harness/gate_checks.json` — one row
- tests: tile_registry / tile_map / tile_variants / import_ldtk /
  map_artifact (+ pilot pin stays green untouched)

## 3. Pre-change baselines (banked BEFORE any src/data edit)

- [x] world_loop capture md5s: 14 frames → /tmp/t5_world_loop_pre.md5 (rc=0, tmp/t5_pre_captures.log)
- [x] floor1_run capture md5s: 10 frames → /tmp/t5_floor1_run_pre.md5 (rc=0)
- [x] pilot emissions md5s (pre): district b954e2a6ec1368082aba8a04555a8c6b · zone_7 a12dc178943aa86f02f43b827f93fc63 · basement_1 f0bc8a44f4b4a61cce316313a984b45a · basement_2 7c73741070e74b26352bf7ab0f635b47 · dungeon_1 e9beb354c6228581e5255d57ac78427f

## 4. Importer add-only proof

- [x] five pilot emissions byte-identical pre/post (md5 table): full importer re-run
      `authoring/pilot.ldtk → tmp/t5_pilot_emit/` POST-change — all five md5s
      IDENTICAL to the live files and to the pre-change table above (district
      b954e2a6…, zone_7 a12dc178…, basement_1 f0bc8a44…, basement_2 7c737410…,
      dungeon_1 e9beb354…). Zero importer code touched (mapping =
      `char_for_int_grid`, pure data).
- [x] pilot_authoring_test green: 2 runs, 8 assertions, 0 failures (run early, standalone)
- [x] synthetic int_grid-7 tests green: 4 new tests in import_ldtk_test.rb — decode
      ('%' at [2,2]), fixpoint byte-stable, loads+BLOCKS through the game loader,
      and palette-less emission refuses NAMED **at the importer door**
      (validate_emitted! composes the palette law — stronger than the planned
      load-time refusal; test renamed accordingly). Pre-existing
      unknown-int-grid refusal test moved 7→8 (7 is now a deliberate mapped value).
      File total: 56 runs, 153 assertions, 0 failures.

### Suite/perf (post-engine-commit)

- Suite green via hooks on BOTH commits (`1d00d12` engine, script/gate-row commit
  after): 1340 runs, 29431 assertions, 0 failures (was 1328 — +12 T5 tests).
  Two zone-census pins updated 13→14 (tile_map live-zones, map_artifact panel
  labels + TEST 2) — legitimate growth, not weakening.
- rake perf: PERF PASS — ticks=6990 p50=0.295ms p95=0.428ms max=5.634ms
  zone=district (passable? set-membership change measured, budget 16.6ms).

## 5. Rule 2 evidence

- [x] gate row wording (final): id `wall_class_reads`, 74th row, appended with
      byte-preserving CRLF edit (4-line diff): "Where frames show the compact
      two-chamber fixture zone TEST 2 … outer boundary walls read as one very
      dark near-black solid color while interior chamber wall segments read
      clearly RED - two visibly DIFFERENT wall colors coexist in the SAME
      frame … If this replay never shows the TEST 2 fixture zone, pass with
      why='not exercised by this script'."
- [x] fixture gate rc=0 (tmp/t5_gates.log): vision 74/74, determinism 5 captures
      byte-identical across two runs. THE ROW READ AFFIRMATIVELY on the strong
      member: "[PASS] wall_class_reads: TEST 2 shows near-black outer boundary
      walls and clearly red interior chamber blocks in the same frame."
- [x] canary world_loop gate rc=0; honest exemption on the new row ("TEST 2
      fixture zone never appears; not exercised"); pre/post A/B: 10/10
      script-declared frames byte-identical (gate_a vs pre-change baseline; the
      4 remaining pre-md5 entries were STALE strays in the plain out_dir from an
      older script version — ticks 397/805/983/1248 are not in world_loop.json's
      capture list; plain out_dirs accumulate, gate_a dirs are fresh).
- [x] canary floor1_run gate rc=0; 10/10 frames byte-identical pre/post.
- [x] map surface: PROBES=1 asserts 13/13 PASS + critique PASS on all rows incl.
      "All fourteen labeled panels appear" (map_checks panel census row updated
      Thirteen→Fourteen (+TEST 2) — the Eleven→Twelve precedent; artifact copied
      to frame_0000.png for the critic's glob per the recorded invocation note).
- [x] rake perf p95: 0.428ms (PASS, see §4)

## 6. Suite

- [x] bundle exec rake: 1340 runs, 29431 assertions, 0 failures, 0 errors, 0 skips
      (rode both commit hooks; census pins 13→14 updated)

## 7. Review (Rule 6)

- [x] fresh-eyes review receipt: headless scrubbed pi (read-only brief
      tmp/t5_review_brief.txt = full diff c1fc4dd..5d6e575 + laws + asks; JSON
      verdict as last message). **VERDICT: PASS, 0 blocking, 5 advisories** —
      verbatim JSON banked at tmp/t5_review_out.txt, disposition:
      1. tile_types wall-remap validator gap — **EXECUTED**: validate_map! now
         refuses a WALL_CHARS char mapped to a walkable type and a
         passability-wall type on a non-blocking char (both directions,
         NAMED); 2 new tests, tile_registry_test 28 runs green.
      2. build_district_v2b.py hardcoded char/int maps (no %:7) — **DEFERRED
         with teeth**: rider added to the T6b spark (new build scripts derive
         maps from data/tiles.json).
      3. full wall sweep unowed now (2 canaries + by-construction argument
         suffice; reviewer concurs) — **NOTED**: next natural sweep = T6b's
         retheme re-gates.
      4. station palette guidance for wall_inner zones — **EXECUTED**: line in
         the T6b spark's authoring rows.
      5. arrivals list now carries district [1,13] 3x (no dedupe) — inert
         today (gradient_anchor pins the field; Aggro probe idempotent) —
         **NOTED** for any future count/weight consumer of validated_arrivals.

## 8. Lane-D spark

- [x] staged at drafts/_v20-t6b-floor2-spark-20260829.md (+ advisory riders) and
      pushed with this close

## 9. Notes

- Locale surface: `zone.wall_fixture.display_name` = "TEST 2" in en/es/pt-br
  (locale-invariant placeholder, TEST 1 family precedent — no register drift;
  functional-label law holds).
- The fixture smoke frame (captures/wall_fixture_walk/frame_0180.png, read
  this session) already shows both classes adjacent: red chamber pair at the
  NE corner touching the near-black bounds.

- game-two-assets pins src/app/renderer.rb (inbox note 20260829, v33
  re-pin): this ticket moves renderer content again — their standing
  approve-by-default protocol handles it; nothing owed from this side.
- Unprocessed HUB mail exists (gamesmith depth/grid-blending v2) — NOT
  this ticket's scope (bounded ticket session; 2026-08-21 sub-session
  mail law). Surfaced in the checkpoint entry for the hub.
