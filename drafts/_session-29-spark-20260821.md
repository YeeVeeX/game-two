# SPARK sesión 29 — post-T5 harvest: the world's first walked gate + Junior's re-measure verdict (support session; v19 waits at the brainstorm)

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register); Junior surfaces pt-br. Quality over cost: council 0
(no design fork is open); paid calls = Rule-2 gate/critic runs ONLY if a
gated change ships (default this session: zero). Evidence-first: claims
are not evidence — file:line, log line, or UNVERIFIED tag. Bot lines are
never fun/oracle evidence.

## Program state (2026-08-21 session-28 close — verify live)

- v18 CLOSED · **v19 NOT open** (brainstorm at the owners' word; agenda:
  `drafts/_v19-intake-docket-20260820.md`). Session 28 shipped and pushed
  (`c0dc412..0e72e32`):
  **WB T5 wire-in — D12 COMPLETE, THE WORLD IS JOINED.** The ratified
  edge pair low_quay [44,19] ↔ zone_7 [1,14] is live (`requires_defeats:
  1`; the owners' EARNED `boss_1_defeats: 1` opens it; return free).
  zone_7 side through the LDtk door (provenance pin re-emitted); INERT
  law amended to name the completion; `multi_floor_descent.json` joined
  the wall (the LOCKED slab on camera + mechanical refusal proven);
  **FULL WALL PASS 21/21**; fresh-eyes review **PASS 10/10** (3 notes,
  defect-1 wording fixed). Ticket: `drafts/_wb-t5-wirein-20260821.md` ·
  receipt: `drafts/_wb-t5-review-20260821.md`.
  **J1 frame-tail fix shipped** (`dd8ff40`, trigger FIRED by Junior's
  banked numbers): merged static rect runs — zone_7 draw p50 2.2→1.2 ms,
  district 1.0→0.6 on the hub machine, captures byte-identical 57/57 to
  pre-change baselines. Two dead ends banked with artifacts (Gosu macros
  illegal under `Gosu.render` on 1.4.6; rasterized textures byte-fail
  under the float camera's fractional translate — texture/macro
  composition is DEAD, the ticket carries the law).
- **NO HUMAN HAS WALKED THE GATE YET.** The owners' next live session is
  the arc's payoff moment — their earned defeat opens the slab in ZONE 5
  (SE corner, past BOSS 1). Their play may legitimately move
  `saves/world.json` — that delta is THEIRS to make and YOURS to
  harvest (chain-anchor law: the save moves only via logged human play;
  verify the digest chain against the new logs).
- **Junior's re-measure = the decisive seat for the J1 trigger.** The
  pt-br two-liner is staged in the T5 ticket (§Junior re-measure): pull
  + `GAME_FRAME_PROBE=1` walk, expect draw p95 well below 15.3 ms and
  over20 falling toward his ~7% live-world baseline. His numbers rule;
  adjudication classes are PRE-DECLARED in J1 below — never move
  goalposts post-hoc. He must PULL first (T5 moved live-zone bytes;
  protocol v3 refuses mixed builds NAMED).
- **BOSS-1 dread = OPEN-FOR-EXPOSURE, zero code owed** (PARKING_LOT).
  The gate now guarantees organic challenger exposure for every
  ZONE-7-bound player — harvest `TELEMETRY varekka` from every new
  human log (engaged/chants/slain).
- **Audio seat:** T3 cue-spec mail STILL unanswered (md5 `d556358a…`) —
  nothing owed before the owner's renders; a receipt/reply is an
  expected mail delta, not work. **Assets seat:** re-pinned to `1b0d3dd`
  mid-s28 (approve-by-default record, archived done/=22); further
  re-pin notes are expected deltas — archive, never reply-ask.
- **R-A2 measure (silent, NEVER prime):** `sustain bought=0` on every
  banked LIVE-WORLD human log through today. Scratch walks don't count.
  Harvest `TELEMETRY sustain` from every NEW human launcher log.
- **Owner-pending (never nag):** ear-checks + audio-v12 batch · T3
  footstep/bed renders (es-CR list in the frozen cue-spec mail) · coop
  S1 invite (Junior READY at `ad517e2`) · the v19 brainstorm.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: origin/main tip = the s29 spark commit — `git log
--oneline -3` reads: that commit · `a8cc729` s28 gate
ledger · `0e72e32` review bank. Save `saves/world.json` md5
`98fe75edb6d72deab18cd48eaa88bdaf` mtime 08-20 15:51 (banked=7
provisions=0 seals=2 sessions=13 boss_1_defeats=1 home=camp) · launcher
logs **40×2** (`$TEMP` + `/tmp`, pattern `game_two_session_*.log`) ·
mail inbox EMPTY, done/=22 · tmp/soak newest `20260820-232208` ·
untracked `drafts/_refs/` only · tmp/pilot_walk/world.json = the owners'
scratch dev-walk world (sessions=2 banked=75) — never delete, never play
it yourself · tmp/s28_* + tmp/wall/*_t5.log + tmp/pilot/mfd* = s28
evidence, cited by the ticket — keep.
EXPECTED deltas (classify, then proceed): **the live save moved +
sessions counter up** = the owners played (J2 harvest; verify digest
chain log↔save) · new human launcher logs (harvest silently) · Junior
docs-only commits (read before rebasing — his re-measure bank is THE
expected one; re-verify any cited hashes after a rebase) · audio receipt
/ assets re-pin mail (archive) · coop S1 artifacts if the invite went
out (override lane below).
`git pull --ff-only` FIRST. Single-instance guard before any launch
(separate call, judged by printed output). `--fresh` NEVER. Background
`bin/play` from pi's bash DIES silently — the working human launcher is
`powershell Start-Process bin\play.cmd -ArgumentList ...`; it can LAG
~40 s before ruby appears — guard AGAIN after launching, kill any double
BEFORE either instance quits (quit writes the save). Bot seats: `nohup …
src/main.rb --bot <seed> --bot-ticks <n> --save tmp/<dir>/world.json
[--start-zone <z>] &` — always scratch `--save`.

## GATE 0 OVERRIDE — any live owner order preempts this whole queue

- **Ear-check verdicts** are LAW: bank verbatim, route per the three
  checkpoint questions; stinger-overlap failure → depth-aware-duck gets
  grilled in game-two-audio (library increment), never a data tweak here.
- **T3/T4 renders landing** (owner Reaper exports via game-two-audio
  handoff): sha-pinned fixture conversion (v1.1 pattern) + data-only
  `cues.json`/`fixtures.json` rows + suite + a noDevice re-walk banking
  the now-mapped keys + one es-CR line for the ear-check queue. If he
  asks about the water family: it needs a NEW cue-spec mail first — the
  sent mail is FROZEN, never edit it.
- **A live coop session = lag segment S1**: support, harvest verbatim
  per `drafts/_lag-t2-evidence/README.md`, bank md5-stamped. BOTH seats
  at the s29 tip first — T5 moved live-zone bytes; the handshake refusal
  prints the differing field, the fix is pull + relaunch (exit 2 =
  auto-rehost/rejoin via the coop launchers).
- **The v19 brainstorm opening in chat** = it takes over as THE session
  (J4).

## J1 — Junior's re-measure: adjudicate the J1 trigger (only if his bank landed; else leave pending, never nag)

Read FIRST: his new `drafts/_junior-*` file (his lane — harvest, never
edit) + the T5 ticket §"Fix SHIPPED" + §"Junior re-measure" (the staged
expectation). Compare like with like: his 16.8% over20 was the T4-zones
walk on his machine; his ~7% was the live world. A different route
weakens the compare — NAME it, never average it.

**Pre-declared classes (adjudicate in writing against these — ticket +
docket row + checkpoint one-liner):**

1. **CLOSES** — over20 ≤ ~8% AND draw p95 clearly down (toward/below
   ~8-9 ms): the fix stands on the decisive seat; docket frame-tail row
   closes; AGENTS lane-3 gets the one-line close. The motif-strip lever
   stays unbuilt (leftover, named).
2. **PARTIAL** — material drop but over20 > ~10%: the named next lever
   is motif-strip authoring (DATA-class: motif rects don't merge —
   low_quay 164 singles, district_two 158). RECORD it as a v19-class
   candidate with his numbers attached; build ONLY on owner word, behind
   its own Rule-2 gate + wall (motif is a live-world visual). Texture/
   macro composition stays DEAD (float-camera law).
3. **REFUTED-ON-SEAT** — no material change: the row stays FIRED;
   reopen diagnosis with a fresh A/B on HIS numbers (ask him for one
   more probe walk ONLY if he offers; never stack speculative fixes
   without a new mechanical A/B on this machine first).

Class boundaries are judgment bands, not laws — if his numbers land
between bands, write the honest reading and let the owners weigh in.

## J2 — first live-world walk(s) through the gate: harvest + watch items (ZERO code by default)

For every NEW human launcher log: bank md5 + key lines in ONE harvest
note `drafts/_s29-live-harvest-<date>.md` — `TELEMETRY persist` (digest
chain + sessions increment; chain-anchor discipline against the save) ·
`TELEMETRY varekka` (the dread-exposure harvest — BOSS-1 row) ·
`TELEMETRY sustain` (R-A2, silent — never prime) · `AUDIO ambience`
keys (zone_7/dungeon beds live in the real world now) · `AUDIO drift`
lines (per-session wall-clock oracle) · any `frame_probe` if a seat ran
one. Owners' chat verbatims about the gate moment = fun-lane evidence:
bank VERBATIM (quotes, not summaries).

**Watch items — route findings, never patch reflexively:**
- **Finding the corner:** a "no la encontré" report is wayfinding/
  exposure, not slab rendering (the grammar is spec'd + gated) — bank
  it as a recorded debate item for the brainstorm.
- **The owned consequence** (T5 ticket, named): return spawn [43,19]
  sits Chebyshev 4 from the challenger spawn — an ambush-feel-bad
  report makes the spawn tile a ONE-NUMBER data candidate, but it is a
  REAL change-set: `multi_floor_descent` replays through that tile, so
  captures move → its gate + a wall re-run. It ships ONLY on explicit
  owner order behind the full gates — never hotfixed inside a harvest
  session.
- **Wipe near the gate:** death respawns at home (hub law) — a
  lost-bearings report banks for the brainstorm.
- **First toll/well interactions from the LIVE save** (their banked=7
  can't afford the 40 toll — if they report wanting it, that's economy
  reading for the brainstorm, R-A2-adjacent; never prime).

## J3 — docket + checkpoint hygiene (docs-only)

Whatever J1/J2 produce: docket rows updated (pointers, never opinions) ·
checkpoint addendum · AGENTS one-liners where a verdict landed · commit
+ push docs. If NOTHING landed (no logs, no re-measure, no mail): write
the honest no-delta addendum and STOP EARLY — an empty harvest session
is a valid short session.

## J4 — IF the owners open the v19 brainstorm in this session's chat

The brainstorm takes over as THE session. Facilitate from
`drafts/_v19-intake-docket-20260820.md` (the agenda: J-1..J-7 + solo
findings + triggered verdict rows 3/4/6/9 + parked forks): ONE candidate
at a time; the dev of record PROPOSES with touchstones (for
systemic-design topics query the verified shelf FIRST: `hub kb query
--domain game-research "<topic>"` — the reference wall) and DEFENDS;
the owners decide; every ratification = ONE recorded line. Forks close
at the brainstorm (v13 precedent: dev recommendation + owner veto).
Output: `drafts/_v19-foundation-<date>.md` + the AGENTS "Current cycle"
rewrite ONLY after BOTH owners ratify in chat (scope contract: one
cycle lives in AGENTS.md; previous-cycle narrative goes to drafts).
NO CODE in the brainstorm session — v19 tickets are future sessions'
work. Budget the brainstorm explicitly: it ends when the docket is
walked or the owners stop it, whichever first.

## Laws that bite (short list)

- Deterministic gates decide; a failed gate/critique BLOCKS ship —
  never downgraded. Presentation never mutates sim; audio stays a pure
  sink; replays deterministic by tick count.
- Read-before-edit · one-concern commits, explicit paths (never `git
  add -A`) · hooks run the suite (~60 s/commit; pre-push re-runs) ·
  long jobs DETACHED (nohup + poll by rc lines; NEVER under a bash-call
  timeout — a killed gate is judged by a standalone re-run) · JSON
  edits are SURGICAL text ops · multi-line scripts in temp files, never
  inline heredocs.
- Two instances fork a save — guard EVERY launch in a separate call
  judged by printed output; re-guard after Start-Process. The wall is
  21 scripts now (~105 min detached); freeze ALL code/data edits while
  any sweep/replay runs (R-A2 contamination law).
- Junior's lane (`drafts/_junior-*`, docs/JUNIOR.md) is his — harvest,
  never edit. Owner overrides are law the moment they land — one line,
  never re-litigated. No lore anywhere; placeholder names only
  (es-CR everyday words in every owner line).
- Cross-repo: read siblings freely, write ONLY via seat mail
  (+ RECEIPT lines); never write into a held seat. Sub-agent prompts
  forbid seat-mail handling explicitly ("touch NOTHING, including seat
  mail").
- Verbatim means verbatim; partial evidence = bank what exists + name
  the gap. A rebase over a peer's push rewrites local hashes —
  re-verify cited hashes after any rebase.
- **Sim-class anything** (tile behaviors, spawn logic, AI, balance,
  economy) → RECORDED for v19, refuse in writing. The one named data
  candidate (return-spawn tile) ships only per J2's rule.

## Budget + stop conditions

One attended session ~2-3 h (SHORT if no deltas landed — stopping early
on a no-delta gate is success, not failure). Council 0. Paid calls =
Rule-2 critics ONLY behind an owner-ordered gated change (default 0).
Sub-agents = the fresh-eyes reviewer ONLY if code/data ships. Context
guard 85% → compact-checkpoint skill.
**Stop when:** deltas harvested + verdicts adjudicated (or honestly
left pending) + checkpoint + push. **Stop early, honestly, if:** nothing
landed (no-delta addendum) · the owners redirect (their word is the
router) · any gate fails non-mechanically (STOP, classify in writing) ·
a sim-class ask arrives without the brainstorm (refuse in writing,
RECORD for v19).
