# SPARK: v18 execution opens — the persistent world cycle, etapa 1 (brainstorm → forks → spec)

You are the dev of record in game-two. Read AGENTS.md FIRST — the v18
scope contract is ground truth (owner-ratified 2026-08-17: persistence
v1 + coop feel + god-view v0 on the lockstep substrate; the SIXTEENTH
closed CUMPLIDO the same day). This session opens the cycle's
execution. Work in English; answer the owner in his language.

## Read before acting (in order)

1. `AGENTS.md` — whole file: the v18 contract, the seven named forks,
   the STANDING ORDERS (no lore; placeholders only; Rule 2 on every
   visual surface including the new map artifact).
2. `docs/CHECKPOINT.md` — top entry: state at close + what this session
   owes.
3. `drafts/_v18-foundation-20260817.md` — THE foundation: three lanes,
   forks F1–F7 with dev starting positions, SEVENTEENTH oracle shape,
   laws that bite, evidence pointers. Consume, don't re-derive.
4. `drafts/_v17-fun-verify-skeleton-20260816.md` — the SIXTEENTH
   verdict + both seats' telemetry (the baseline evidence for coop
   pacing numbers).
5. `PARKING_LOT.md` — §"Owner vision drop 2026-08-17" (what is parked
   with which trigger — the owner's full vision, routed) + the
   2026-08-11 sustain pre-decision + the D0/inscription economy
   history the persistence fork re-opens.

## The session's job, in order

1. **Brainstorm** persistence v1 against the banked facts: the sim is
   deterministic and tick-locked; the handshake already pins
   fingerprint/seed/D (`src/net/session.rb` — save transfer + digest
   extends it); `StateDigest` exists; saves must be schema-versioned
   with NAMED refusals. Query the research shelf first for
   persistence/economy touchstones
   (`hub kb query --domain game-research "..."`).
2. **Close the seven forks** (dev recommendation + owner veto, v13
   precedent). Batch the owner-level ones (F1 what persists · F3
   banked persists · F4 solo advances the shared world) in ONE
   AskUserQuestion round; F2/F5/F6/F7 are dev calls — record and
   defend them. If the owner goes absent, record recommendations and
   proceed.
3. **Spec** at
   `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md`
   + dual review (Codex leg first, fold findings; then the panel with
   the Rule-7 token envelope declared up front).
4. Stop after the spec commit — TDD starts next session,
   **save/load round-trip lane FIRST** (loaded state must continue the
   digest chain exactly; it is the safety net everything else stands
   on — the v17 digest-lane-first precedent).

## Laws that bite in this cycle

- Persistence touches session boundaries ONLY — the 16.6ms p95 tick
  budget is untouchable; no mid-tick IO.
- Save/load divergence IS a desync: the loaded world feeds the same
  digest machinery and fails LOUDLY at the first boundary.
- No mocks in persistence tests: real files, real save/load, real
  two-session sequences in the netplay lane; the single-player wall
  stays untouched.
- All tunables in `data/**/*.json` (seat-count pacing scalars, sustain
  price, respawn timing); zero balance constants in code.
- Sustain is PRICED in banked value (owner law 2026-08-11) — a free
  cooldown is a design regression, not a shortcut.
- The map PNG is a Rule-2 surface: capture + critique before ship;
  placeholder text only (ZONE N / HUB 1 / BOSS 1).
- Junior's seat is ACTIVE: pull before push, always; handoffs via
  drafts/ + `swarmforge handoff validate`.
- Oracle at cycle end (the SEVENTEENTH ask): (A) two real sessions on
  different days, session 2 provably RESUMES the same world (save
  digest chain + zero desyncs + a carried fact in telemetry); (B) both
  players asked separately — did the world feel CONTINUED, did the
  respawn/sustain frictions disappear. Everything in the spec serves
  those two halves.
