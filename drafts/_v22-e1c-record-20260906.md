# v22 E1c — THE TEN RED PINS (s137, Gabriel seat) — record

Ticket: the ten `FAILED` pins the v22-e1 sweep left (`drafts/_v22-e1-record-20260906.md`
§Sweep). Shape = E1 piece 4: observe headlessly FIRST, then re-cut to observed
counts, retire with an owed re-author header, or convict-and-route — never relax a
row to make red go green. Laws applied: sampling-artifact law (verify every critic
claim against code + the exact PNGs), the s135 lesson (a verdict's `why` proves the
SURFACE, never WHICH frame — settle staging with a headless probe of the renderer's
own draw condition), read-before-edit, one bundled change-set.

## 0. What changed the plan before any edit (orient)

`origin/junior/premium-build` = main + **65 commits** (`git log --oneline
origin/main..origin/junior/premium-build | wc -l` → 65; `origin/junior/premium-build..origin/main`
→ 0, so main is its ancestor and can fast-forward). It merged T1 (`1e3e38e`), extracted
`Game::Interact` (world.rb 1800 → 1726) and — the part the spark-up did not know —
**already carries harness work on four of the ten**: `basement_pocket` re-authored
(`9981ddb`), `toll_pocket` re-authored + corrected (`8e0c942`, `7dcfa50`), the
`impact_fx_reads` row rewritten with the star-lifetime clause (`223c5c8`, `38948f8`),
`floor3_run` captures 1499/1599 (`eab486e`). His lanes BOARD reads `SIM LANE: NONE`
(e3-presentation + signage delivered to `done/`), so no live lane holds `fx.rb` /
`controls_overlay.rb`. His branch cannot land yet (S2+S3 ride it; sequenced after the
TWENTIETH), so main's wall has to be made honest on its own — but a parallel re-author
of the same scripts would hand his merge a semantic conflict.

**Rule for this session:** where his branch has the fix, take his FILE VERSION
verbatim onto main (`git checkout origin/junior/premium-build -- <path>` / his exact
row line), verify it against MAIN's sim/renderer before the gate, credit his commit
(identical bytes ⇒ his merge stays clean). Where it doesn't, do the spark's piece.
His PREMIUM surfaces (`fx.rb`, `light.rb`, `controls_overlay.rb`, `tileset.rb`,
colour truth) stay convict-and-route.

Live numbers at open: `rake pins` = 0 PINNED / 33 STALE / 10 FAILED (the STALE flip is
T1 moving sim paths — T2c owns the full re-pin); `ls harness/scripts | wc -l` = 43;
`wc -l src/game/world.rb` = 1782. CLAIMED pushed first (`f609c31`).

## 1. The ten rows — verdict table (the spine)

| # | script | red row(s) | headless finding (main) | VERDICT | evidence |
|---|--------|-----------|--------------------------|---------|----------|
| 1 | aoe_specials | challenge_reads | cast f1157 with nearest living human at Chebyshev **14**, off camera; `taunted victims=0`. Re-cut: cast f940 → pulse f952 `victims=1` (rusher_hater24 cheb 4, on camera), captures 958+964 (pulse pair) / 985 / 1020 | **REPINNED, AFFIRMATIVE** (v22-e1c-c) | §2.3 |
| 2 | taunt_anchor | challenge_reads | cast f868, nearest human cheb **16**, `victims=0` (the three rushers died f545/615/650). Re-cut: cast f470 → pulse f482 `victims=3` (cheb 1/2/7, all on camera); swap 980→600 so a taunted human stands beside the now-AI blocker (f604; it kills it f608) | **REPINNED, AFFIRMATIVE** (v22-e1c-b) — first affirmative pass ever for taunt_pulse/underline/convergence too | §2.3 |
| 3 | basement_pocket | MANIFEST `drop_picked_up 0` | main reproduces Junior's diagnosis exactly (deaths f767/872/888, drops [9,4]/[10,5]/[9,5], interacts 620/720/760 fire before any drop). His re-author on main's sim: `drop_picked_up frame=922` → MANIFEST PASS. Vision now RED on **one other row** (low_hp_pulse_reads) = a REAL strip-tint defect he already fixed on his branch | **MANIFEST REPAID (his file verbatim) + vision CONVICTED-AND-ROUTED** — pin FAILED honestly (gate_rc=1 manifest_rc=0 @ v22-e1c-a) | §2.1 |
| 4 | toll_pocket | MANIFEST 4/5 beats 0 | main: `seal_breached/actor_died/drop_spawned/drop_picked_up` all **0** (wholesale dead, the loot_loop class). His re-author on main's sim: husk0 dies f148, drop [2,6]; manifest {zone_entered 2, actor_died 1, drop_spawned 1} → GATE PASS + MANIFEST PASS | **REPINNED (his file verbatim, v22-e1c-a)**; seal breach + farm + pickup = owed re-author (named) | §2.2 |
| 5 | corpse_run | impact_fx_reads + damage_numbers_read | no capture inside any spark window; frame_3062's numeral is a hit **26 f old** on the **lobber** (pack) — the critic's "struck human"; rusher5 on camera at 50/50 → no wounded bar owed | **SAMPLING + MISREAD; row recalibrated (Junior's clause, verbatim)**; inherits floor1_run's affirmative read; stays FAILED in the ledger until T2c | §2.4 |
| 6 | floor1_run | impact_fx_reads + damage_numbers_read | frame_0700: three numerals **25 f old**, all on PACK bodies (one tint = correct); rusher12 on camera at 50/50 → no bar owed. Dust present at 5× zoom (2–3 px specks ≤110 α) = sub-perceptual. Captures 1253 + 1287 added inside a real window | **REPINNED, AFFIRMATIVE** (v22-e1c-c) | §2.4 |
| 7 | zone8_crossing | no_render_garbage + impact_fx_reads | (a) the "thin full-width blue lines" are **authored decor**: `data/zones/dungeon_1.json` `edge` ×2 at [10,24]/[10,26] w14 rgb(70,110,190) α70 (+ blue `stain`s) — leftovers of the pre-swap MEDUSA LOWER water layout, now across walkable floor; (b) frame_0413's star IS drawn (pixel-proven cross at (272,330)) on the victim's pale square → low contrast | **(a) FIXED through the importer door (sidecar → re-emission, `3914f1e`; review disagreement adopted) → REPINNED (v22-e1c-e, seams pixel-gone); (b) CONVICTED-AND-ROUTED** (Junior's fx legibility) | §2.5 |
| 8 | floor2_run | new_ground_reads + floor2_channel_reads | the coral class EXISTS and is ON camera (`%` = wall_inner → coral_wall.png, 22–70 tiles in view from f1290 on); the two wall palettes are one hue family (wall 118,66,38 vs wall_inner 150,74,28 → rendered means 74,44,33 vs 136,71,38, per-tile spread ≥ the between-class gap) | **CONVICTED-AND-ROUTED (E4 item: zone palette, sidecar/importer lane)**. Rows keep the contract; pin stays FAILED | §2.6 |
| 9 | grass_fixture_walk | controls_overlay_reads | the "leftmost pair" is the vessel label (`player 2`) in `VESSEL_RGB[:blocker]` (158,52,30) = Junior's ratified colour truth for player 2; contrast vs the band **1.04:1** (glyphs 1.72:1) — identical in floor1_run/basement/zone8 (1.08–1.14), so every blocker reel has it and 32 passes were lenient reads | **CONVICTED-AND-ROUTED** (his colour surface: the strip needs a lifted text variant, not a new identity colour). Pin stays FAILED | §2.7 |
| 10 | tower4_run | lobber_reach_reads | the reel holds `attack` f120→6000 and **never presses special**: zero `special_started`, zero volley impacts — the critic read the blocker's filled attack tile as "one framed tile plus one square". Row recalibrated to what `draw_impacts` draws (brackets come ONLY from `world.impacts`) and — after review B1 — to the volley's LEVEL TIERS from `progression.json` (3 at 2..4 / 4 at 2..5 / 5 at 2..6) | **RECALIBRATED + affirmative gate on the strong member** (lobber_reach: "four brackets … matching the 2..5 tier"); tower4_run "not exercised" | §2.8 |

INFRA (not a row): `taunt_anchor` critic stalls — **INFRA-DIAGNOSED, not payload-shaped** (§2.9).

## 2. Pieces (one commit each, in the confidence order the spark set)

### 2.1 + 2.2 basement_pocket + toll_pocket — Junior's re-authors, verified on main — commit `1aee2bc`

Probe `tmp/e1c/probe_manifest.rb` (real World, the script's inputs, EventLog counts,
manifest law 2 × single ≥ declared):

- [x] main's basement_pocket: `drop_picked_up single=0 RED`; deaths f767/872/888, drops
  [9,4]/[10,5]/[9,5], possessed at [10,6] at f850 (his diagnosis reproduced).
- [x] HIS basement_pocket on MAIN's sim: `EVENT drop_picked_up frame=922 actor=blocker
  amount=1 carried=1` → `drop_picked_up declared>=1 single=1 doubled=2 OK`.
- [x] main's toll_pocket: `seal_breached 0 · actor_died 0 · drop_spawned 0 ·
  drop_picked_up 0` (only 2 zone_entered + 2 possession_changed in 1400 f).
- [x] HIS toll_pocket on MAIN's sim: `actor_died frame=148 actor=husk0 killer=blocker`,
  `drop_spawned frame=148 tile=[2, 6]`; manifest {2,1,1} all OK.
- [x] Gate v22-e1c-a (detached, serialized): `toll_pocket gate_rc=0 manifest_rc=0` —
  `GATE vision: PASS · GATE determinism: 8 captures byte-identical · MANIFEST PASS:
  zone_entered=4 actor_died=2 drop_spawned=2` → `PIN recorded: toll_pocket tag=v22-e1c-a
  commit=f609c31`.
- [x] `basement_pocket gate_rc=1 manifest_rc=0` — `MANIFEST PASS: zone_entered=4
  actor_died=6 drop_spawned=6 drop_picked_up=2`; the one red row:
  `[FAIL] low_hp_pulse_reads: At 26/188 the red wash floods the entire frame including
  room center and HUD, not an edge vignette.` Pixel check (frame_0945 vs 0850, static
  regions): edge bands dG −16 / dB −5.6 (the wine pulse), outside floor dG −2 (nothing),
  HUD plate ≈ 0 (z 19, above the light), **bottom strip dR +11 / dG −7 (tinted)**. So
  two of the critic's three claims are wrong on the pixels, but the row fails
  legitimately on the STRIP: `ControlsOverlay` draws at z 0 under the light (z 17) —
  exactly Junior's `65f52e5` ("strip → z 19 like the HUD plate"). Red pin recorded
  honestly; fix lands with his merge.
- Owed (named): toll_pocket's purpose beats — seal breach + farm + pickup. His `_doc`
  names the sim question for the owner: a drop under a corpse is unreachable.
  `seal_breach_reads`: its SEALED half IS staged and passed affirmatively on toll_pocket
  ("dark slab with thin gold seam, station block, legible -40 price"); its BREACH half
  (TOLL PAID, slab → gold) has zero coverage wall-wide — no wall manifest declares
  `seal_breached` and no v22-e1 sweep log carries the event (review m4) → named ticket:
  a basement_2 breach stager (re-author toll_pocket's tail or a new reel).
  Review m3 (Junior's files, verbatim — routed to him, not edited here): toll_pocket's
  `_doc` ¶1 still narrates `down f200 + left f205 + interact f220` that ¶2 removed;
  basement_pocket's `_doc` says 923/945 show the pickup sparks, but 945 is burst age 23 >
  `BURST_FRAMES` 22 (gleam pruned; only the `+1` numeral survives) — main's critic read
  `pickup_gleam_reads` as not exercised, so that affirmative stager has not landed yet.

### 2.3 challenge_reads ×2 — commit `6289981` (scripts), pins in `22c3308`

Probe `tmp/e1c/probe_challenge.rb` (every `special_started`/`taunted` frame: caster,
every living hostile's tile, Chebyshev distance vs `challenge.range_tiles` 9, on-camera
by ±15×±8 tiles):

- [x] aoe_specials main: `special_started frame=1157 attacker=blocker` … `rusher4
  tile=[9, 28] cheb=14 out off-cam` (nearest) … `taunted frame=1169 victims=0`.
- [x] aoe re-cut (special 940): `taunted frame=952 actor=blocker victims=1`;
  `rusher_hater24 tile=[21, 14] cheb=4 IN-RANGE ON-CAM taunted="blocker"`; alive to
  f1030; at 964/985/1020 still taunted and adjacent. Side effect: `special_started
  frame=1275 attacker=striker` now fires.
- [x] taunt_anchor main: `special_started frame=868` … nearest `rusher4 cheb=16` /
  `rusher_hater24 cheb=16` … `taunted frame=880 victims=0`.
- [x] taunt_anchor re-cut (special 470, swap 600): `taunted frame=482 victims=3`
  (rusher2 cheb 1, rusher0 cheb 2, rusher16 cheb 7, all ON-CAM); f604 `poss=lobber[9,13]
  … blocker[14,12] … rusher16[13,13] hp=5 T=blocker`; `actor_died frame=608
  actor=rusher16 killer=blocker`; second fight still resolves (`fight_resolved f1362`).
- [x] Gate taunt_anchor v22-e1c-b: PASS, 9 captures byte-identical, MANIFEST PASS
  (`special_started=2 taunted=2`); `[PASS] challenge_reads: Humans inside the rust pulse
  carry taunt underlines and turn toward the blocker in 0496 and 0500.` +
  `taunt_underline_reads: Humans in frames 0496 and 0500 carry thin rust underline
  strips…` + `taunt_pulse_reads: One continuous hollow rust square outline expands
  around the pack…` + `taunt_convergence_reads: Underlined humans converge on the rust
  blocker anchoring the pack…`. Ran 4 min 39 s, zero transport retries.
- [x] Gate aoe_specials v22-e1c-b (single pulse capture 964): PASS but every taunt row
  `Not exercised by this script` while `kits_distinct` cited frame 0964 — a HOLLOW pass,
  not accepted as the affirmative read. Re-cut to a pulse PAIR 958+964 →
- [x] Gate aoe_specials v22-e1c-c: PASS, 11 captures byte-identical, MANIFEST PASS
  (`special_started=4 taunted=2`); `[PASS] challenge_reads: The expanding rust pulse is
  answered by a rust-underlined human within its window.` + `taunt_pulse_reads: One
  hollow rust square visibly expands around the pack across frames 0958 and 0964.` +
  `taunt_underline_reads: The bridge human carries a clear rust underline strip…`.
- **L16 `aoe_specials challenge_reads` re-author debt: CLOSED** (CYCLE.md updated).
- [x] Re-runs on byte-identical frames (batch f, after the decor commit moved `data/`):
  aoe_specials `[PASS] challenge_reads: During the pulse the bridge human carries the rust
  underline and humans converge on the blocker.` (affirmative again — 2/2 with the pair);
  taunt_anchor `[PASS] challenge_reads: not exercised by this script.` — the SAME 496/500
  frames that read affirmative in batch b: a late, faint square (70%/90%, alpha 128/101,
  400–518 px) is a coin flip for the judge. Re-cut to an EARLY pair 488/492 (+500):
  probe f488 `blocker[8,12] … rusher0[11,12] T=blocker … rusher2[11,9] T=blocker` (ring
  reach 86 px passing through the two knocked-back rushers at Chebyshev 3), f492 reach
  144 px enclosing them (`e7a5bf4`, commit-then-gate).
- [x] taunt_anchor v22-e1c-g (early pair, commit-then-gate @ `e7a5bf4`): PASS, 10 captures
  byte-identical, MANIFEST PASS — `[PASS] challenge_reads: Humans inside the rust pulse gain
  underlines and turn toward the blocker by frame_0530.` + `taunt_pulse_reads: One hollow
  rust square expands around the possessed blocker across frame_0492 and frame_0500.` +
  `taunt_underline_reads` + `taunt_convergence_reads` affirmative. Pin clean.

### 2.4 The impact-fx family (corpse_run, floor1_run, zone8_crossing) — commit `22c3308`

Draw conditions read from `src/app/fx.rb` (READ-ONLY): spark on `attack_hit landed=true`
at (v.x+14, v.y+10), `SPARK_FRAMES 12`, z 7 (above bodies at z 0); burst on
`actor_died`, `BURST_FRAMES 22`; dust on a tile change, `DUST_FRAMES 14`, three specks
3/2/2 px at ≤110 α; numerals from the per-frame hp poll, `NUM_FRAMES 40`, `:taken`
crimson (240,70,60) for pack, pale/gold for hostiles. Wounded bar: `renderer.rb`
`c.faction == :human && !boss && !seize && c.hp < c.max_hp` (persistent, not a
transient). The replay window is a real `Gosu::Window`, so `Fx#update` runs every frame.

`tmp/e1c/fx_windows.py` (sweep log events × captured frames):
- [x] corpse_run: `landed hits: 53 deaths: 13`; every capture `spark-live hits=0`;
  `frame_3062: numeral-live hits=1 [(3036, 'lobber')]`. Position probe f3062: `lobber
  hp=2/74` (possessed) and `rusher5 hp=50/50` the only human on camera.
- [x] floor1_run: every capture `spark-live hits=0`; `frame_0700: numeral-live hits=3
  [(675, 'striker'), (675, 'blocker'), (675, 'lobber')]`; f700 `rusher12 hp=50/50` the
  only human on camera. Dust probe: frame_0090 three bodies at step age 0 — specks
  visible at 5× (tmp/e1c/f1_0090_crop.png), not at 1×.
- [x] zone8_crossing: `frame_0413: spark-live hits=1 [(411, 'lobber', 'stinger2')]`
  (age 2), `frame_0416: 2 sparks + burst (414)`, `frame_0570: spark + burst (568)`.
  Pixel probe frame_0413 around stinger2: `warm-arm-like pixels: 141`, vertical arm
  cols 271–273 (17–19 px), horizontal arm rows 329–331 (20 px each) = the age-2 star
  (len 10, thick 3) centred (272,330); the pale square (hurt/base) spans x 264–279,
  y 322–337 — the star sits on it, arm tips 2–4 px past its edge.
- [x] Row: main line 350 of `harness/gate_checks.json` replaced by Junior's line
  byte-for-byte (`diff … && echo IDENTICAL` → IDENTICAL; 97 rows parse).
- [x] floor1_run + captures 1253/1287 (probe f1252: `rusher15 hp=25/50 rel=(0,-32)`
  struck f1249; dies f1284). Gate v22-e1c-c: PASS, 12 byte-identical, MANIFEST PASS
  (`fight_resolved=4 actor_died=12`); `[PASS] impact_fx_reads: Frame_1253 shows a
  bright four-point star with chips at the struck human beside the 25 numeral.` /
  `[PASS] damage_numbers_read: Red pack damage, hostile hit numerals and green gains
  read tinted apart, with a wounded-hostile mini bar.`
- Routed to Junior (fx owner), no edit: (i) the star centres on the victim and its
  warm arms sit on pale bodies/flash squares → low contrast at ages 0–3 (his branch
  already re-tuned the spark colours, `3631ccc`); (ii) footstep dust is
  sub-perceptual to the judge — either the row stops demanding it or the puff grows;
  his call.

### 2.5 zone8_crossing no_render_garbage — CONVICTED (data) → FIXED through the importer door (review disagreement, agreed)

- [x] Seam rows found by pixel scan (rows bluer than ±3 px neighbours over >200 px):
  frame_0570 rows 411–412 and 475–476; frame_1067 rows 222–223 and 286–287; none in
  the other eight frames. Camera probe: f570 `cam=(0.0,356.97)` → world y **768** and
  **832**; f1067 `cam=(0.0,546.0)` → world y 768 and 832 again (camera-independent,
  tile-row aligned). Run extent at f1067 row 222: x 320…767 continuous, colour
  (28,47,73)–(38,51,68) over floor (34,35,29).
- [x] Source: `data/zones/dungeon_1.json` `decor`: `{"kind":"edge","at":[10,24],"w":14,
  "rgb":[70,110,190],"alpha":70}` + the same at `[10,26]`; `App::ZoneIdentity.decor_rects`
  draws `edge` as `[x, y, w, 2, rgb, alpha]` → a 2-px blue lip along the top of rows 24
  and 26 from tile 10 to 24 = exactly the observed lines. Rows 24/26 are floor/sand
  today (row 25 is the dirt causeway the E1 d7 route walks): the lips outlined a water
  channel of the pre-swap MEDUSA LOWER layout. The "stray translucent blue rectangle" =
  the blue `stain`s at [31,13] / [40,33] (rgb 40,90,160 α80), same leftovers.
- Fix lane: dungeon_1 is an LDtk importer emission (MAP_EDITING §1 — never hand-edit;
  `pilot_authoring_test` byte-compares). I first routed this to a later WB session; the
  fresh-eyes reviewer disagreed — decor is render-only ("never blocking"), the decor lives
  in `authoring/dungeon_1.sidecar.json` (not in pilot.ldtk), and the importer path is
  mechanical — so it is a SAFE-class data fix inside the flywheel law. Agreed and done:
- [x] `authoring/dungeon_1.sidecar.json`: the two blue `edge` rows + the two blue
  `stain`s removed (the grey ledge `edge` at [27,16] and the three dark grime stains
  kept); re-emitted with the `pilot_authoring_test` recipe (`tmp/e1c/reemit_dungeon_1.rb`):
  the other 12 importer zones re-emit IDENTICAL, `data/zones/dungeon_1.json` = 58 deletions
  and nothing else. `pilot_authoring_test` 3/3 · `zone_identity_data_test` 7/7 ·
  `sim_identity_canary_test` 3/3 byte-identical. Commit `3914f1e`.
- [x] zone8_crossing re-gate (commit-then-gate, v22-e1c-e @ `3914f1e`): `GATE vision: PASS ·
  GATE determinism: 10 captures byte-identical · MANIFEST PASS: zone_entered=6
  actor_died=18 attack_hit=50 projectile_fired=46` — `[PASS] no_render_garbage: All frames
  show intact tilemaps, HUD, and bodies inside walkable ground with no corruption.` →
  `PIN recorded: zone8_crossing tag=v22-e1c-e commit=3914f1e` (no DIRTY-JUDGED). Pixel
  re-scan of the new gate_a frames 0570/1067: seam rows **none** (were 411–412/475–476 and
  222–223/286–287); px(600,411) now (26,27,23) floor. `impact_fx_reads` passed on this reel
  through the lifetime clause ("the 37 numeral outlives its star window") — the star at
  frame_0413 is still pixel-present and still unread by the judge: the contrast note to
  Junior stands.

### 2.6 floor2_run coral — CONVICTED (E4 palette), routed

- [x] `district_two` (`start.zone`): chars `# . , % g ~ w s`; `%` = wall_inner ×249,
  `~` water ×183; no regions/decor for coral. `data/art/tiles.json`: `wall_inner →
  coral_wall.png`, `wall → rock_wall.png`, both TINTED by the zone palette
  (`Tileset#tint` = palette[ref] × gain 1.18). district_two palette: wall (118,66,38),
  wall_inner (150,74,28).
- [x] View probe: `%` on camera in frames 1290 (22), 1520 (36), 2020 (64), 2100 (58),
  2680 (70), 3249 (67) beside water — the coral class is staged, not off-camera.
- [x] Pixel means of interior tiles: frame_2020 `#` (74,44,33) vs `%` (136,71,38);
  frame_3249 `#` (114,68,49) vs `%` (93,48,27); frame_2680 `#` (98,58,43) — the
  per-tile spread (texture speckle + variants) is as large as the between-class gap,
  and both sit in one rust/orange hue family. The E4 contract the rows describe
  ("rust-brown boundary rock AND orange coral, two visibly different wall colours")
  is not delivered by these tints.
- Fix lane: the zone palette (district_two is an importer emission → sidecar) and/or
  the coral texture's own hue — E4 (Junior's zone-identity record
  `drafts/_v22-e4-record-20260906.md` on his branch is the home). Rows unchanged (they
  ARE the contract); pin stays FAILED.

### 2.7 grass_fixture_walk controls overlay — CONVICTED (legibility), routed

- [x] Strip crop (tmp/e1c/gfw_strip.png): leftmost item = the vessel label `player 2`
  in dark rust; pairs `J/Space attack` … in bone.
- [x] `src/app/controls_overlay.rb`: `VESSEL_RGB = { striker: [235,120,40], blocker:
  [158,52,30], lobber: [225,170,90] }`, `GLYPH_RGB [225,218,205]`, `BACKING_RGB
  [10,8,12]`; the vessel label draws at `text_a` (200), the `-40` applies to the action
  labels (review m5). (158,52,30) = `#9e341e` = Junior's ratified
  player-2 colour truth (`a18b40b`; the assets seat followed it, s134).
- [x] Measured WCAG-style contrast, text vs band: grass_fixture_walk vessel **1.04**
  (reviewer's recomputation with a band median: 1.08 — methodology delta, same verdict),
  glyphs 1.72; floor1_run 1.14 / 1.84; basement_pocket 1.08 / 1.75; zone8 1.11 /
  1.75. The vessel label melts into the band in every blocker reel; the critic's
  read is correct here and lenient elsewhere.
- Routed (his colour surface): keep the identity colour, draw the STRIP label with a
  lifted-value variant (or bone text + a small rust swatch). Not a flywheel fix I may
  take: the colour is a ratified truth, and the file is his e3 lane's. Pin stays FAILED.

### 2.8 tower4_run lobber_reach_reads — RECALIBRATED + affirmative gate (batch D)

- [x] `harness/scripts/tower4_run.json` holds `attack [[120, 6000]]` and has NO
  `special` press; sweep log: zero `special_started`, zero volley impacts (only
  serpent/lobber `projectile_fired`). `draw_impacts` draws brackets ONLY from
  `world.impacts` (launched volleys) → no volley telegraph existed in any frame; the
  gold FILLED square beside the possessed in frame_0040 is the blocker's attack tile
  (the s135 frame_0310 misread class). Orange-pixel scan: the only "volley-orange" in
  the reel is the dungeon_4 wall palette.
- [x] Row recalibrated (what the code draws): "A volley telegraph is ONLY the lobber
  special's delayed-impact marker: a CHAIN of orange-FRAMED tiles … A single FILLED
  pale, gold or amber square on a tile beside a pack body is that body's ordinary
  attack windup/strike tile, not a volley … do not count brackets that are not
  there." (harness/gate_checks.json, `lobber_reach_reads`.)
- [x] Affirmative gate on the strong member `lobber_reach` — v22-e1c-d: `GATE vision:
  PASS · GATE determinism: 7 captures byte-identical · MANIFEST PASS: special_started=6
  attack_hit=96 actor_died=34` — `[PASS] lobber_reach_reads: At LEVEL 5 the frame 0350
  chain spans four contiguous brackets ahead of the amber lobber.` → `PIN recorded:
  lobber_reach tag=v22-e1c-d commit=22c3308`.
- [x] tower4_run re-gate — v22-e1c-d: `GATE vision: PASS · 12 captures byte-identical ·
  MANIFEST PASS: zone_entered=4 actor_died=18 attack_hit=88 projectile_fired=30` —
  `[PASS] lobber_reach_reads: No orange bracket chain volley telegraph appears; not
  exercised by this script.` (the misread class is gone) → `PIN recorded: tower4_run
  tag=v22-e1c-d commit=22c3308`.
- [x] **Review B1 re-word (tiers from `progression.json`) + affirmative re-gate,
  commit-then-gate, v22-e1c-e @ `3914f1e`:** `GATE vision: PASS · 7 captures byte-identical
  · MANIFEST PASS: special_started=6 attack_hit=96 actor_died=34` — `[PASS]
  lobber_reach_reads: At LEVEL 5 the chain shows four brackets starting two tiles ahead,
  matching the 2..5 tier.` → `PIN recorded: lobber_reach tag=v22-e1c-e commit=3914f1e`
  (clean). tower4_run's read is unaffected by the re-word (no volley → not exercised);
  re-pinned in batch f only because the decor commit moved `data/`.

### 2.9 taunt_anchor critic stalls — INFRA-DIAGNOSED (no fix owed)

- [x] Payload is mid-pack, not an outlier: taunt2_r1_replay 10 PNG / 3316 KB (sweep) →
  9 PNG / 2964 KB (today) vs aoe 8/2528 → 11/3764, corpse 9/2640, floor2 10/4160,
  tower4 12/4632; all 960×540. The heavier reels never stalled.
- [x] Today's re-gate: `15:48:37 → 15:53:16` (4 min 39 s), `critic transport retry`
  count 0 (the sweep's rerun log had 1). The three stalls were transport-side
  (Bedrock socket), not payload-shaped → no config change; `CRITIC_READ_TIMEOUT`
  stays; `CRITIC_MAX_TOKENS=24000` remains the lever for the long reels.

## 3. Gates + ledger

- Suite green via hooks on every commit (1527 runs / 0 failures at each; 1528 from the
  review-fix commit on).
- Re-gates this session (all detached, serialized, `tmp/e1c/regate.sh`, one GL window):
  a = basement_pocket (red on low_hp_pulse) + toll_pocket · b = aoe_specials (hollow) +
  taunt_anchor · c = aoe_specials + floor1_run · d = lobber_reach + tower4_run ·
  e = lobber_reach (B1 row) + zone8_crossing (decor fix) — e ran COMMIT-THEN-GATE ·
  f = toll_pocket, aoe_specials, taunt_anchor, floor1_run, tower4_run re-pinned at
  `3914f1e` (the decor commit touched `data/`, so the ledger's path rule had flipped
  the five to STALE although none of their reels draws dungeon_1 — all five PASS again,
  frames byte-identical ×2; lesson in MEMORY.md: land data fixes BEFORE the gates they
  do not touch) · g = taunt_anchor with the early pulse pair.
- **Pin provenance (review B2, documentary half):** batches a–d ran gate-then-commit, so
  each pin's `commit` is the PARENT of the edit it judged. Content commits: toll_pocket +
  basement_pocket (pinned @ `f609c31`) → judged content in **`1aee2bc`**; aoe_specials +
  taunt_anchor (@ `1aee2bc`) → **`6289981`**; floor1_run + the impact_fx_reads row
  (@ `1aee2bc`) → **`22c3308`**; lobber_reach + tower4_run (@ `22c3308`) → **`a18079c`**.
  Mechanical half: `Pins.record` now stamps `dirty: [paths]` when any judged path is
  uncommitted and `rake pins` prints DIRTY-JUDGED (commit `802d039`, real-git test);
  batch e's pins are clean by construction.
- No sim code moved. No renderer code moved (every renderer conviction is routed).
  `pilot.ldtk`, `fx.rb`, `light.rb`, `controls_overlay.rb`, `tileset.rb` untouched. ONE
  data change, through the lawful door: `authoring/dungeon_1.sidecar.json` → re-emitted
  `data/zones/dungeon_1.json` (render-only decor; canary 3/3 byte-identical).
- `rake pins` at close (`tmp/e1c/pins_final.txt`): **`PINS: 43 scripts — pinned=7 stale=32
  failed=4 unpinned=0 unknown=0 retired=0`**, zero DIRTY-JUDGED — PINNED (all `current`):
  aoe_specials, floor1_run, lobber_reach, toll_pocket, tower4_run, zone8_crossing @
  `3914f1e`, taunt_anchor @ `e7a5bf4`; FAILED (honest, each with a routed conviction
  above): basement_pocket (@ v22-e1c-a, gate_rc=1 manifest_rc=0), corpse_run, floor2_run,
  grass_fixture_walk (@ v22-e1); the 32 STALE are T2c's full re-pin (T1 moved sim paths
  right after the sweep — expected).

## 4. Review (Rule 6)

Headless scrubbed `pi -p` (PI_* unset, `--thinking max`, tools read+bash), DETACHED
worktree at `a18079c`, brief = ask + this record + the harness diff (47 KB, no images).
Ran 16:30 → 16:53, one pass. Verbatim: `drafts/_v22-e1c-review-20260906.md`.

**Verdict: BLOCK** — 2 blockers, 0 majors, 7 minors, 20 verified-clean, 1 disagreement
with my routing. Both blockers were real and are landed; the disagreement was right and
is landed too.

| # | Finding | Verified how | Resolution |
|---|---|---|---|
| B1 | my `lobber_reach_reads` clause said "2-4 tiles out" and the older text "FOUR contiguous" — `progression.json spell_growth.lobber` is `{5: [2,3,4,5], 8: [2,3,4,5,6]}` over base `[2,3,4]`, so L5-7 = four brackets at 2..5 and L8+ = FIVE at 2..6 (tower4_run is L14) | read `progression.json` + `progression.rb:81-87` myself | row states the three tiers and cites the source (`802d039`); affirmative re-gate on lobber_reach (batch e) |
| B2 | all 8 v22-e1c pins cite the parent commit of the edits they judged (gate-then-commit); `rake pins` said `toll_pocket f609c31 current` while f609c31's toll_pocket.json fails its manifest | `git log --format='%h %cI'` vs pin dates, confirmed | provenance map above; `Pins.record` stamps DIRTY-JUDGED paths + test (`802d039`); batch e ran commit-then-gate |
| m1 | no negative control for the recalibrated rows | text analysis agreed failable | not run this session (one GL window, headroom) — named follow-up: one detached `fx_enabled:false` gate expecting `impact_fx_reads` FAIL, never pinned; "chips still flying" as a circular age cue is in Junior's clause → routed to him |
| m2 | CYCLE/CHECKPOINT edits were uncommitted at review time | true | they land in the close commit |
| m3 | toll_pocket `_doc` ¶1 narrates inputs ¶2 removed; basement_pocket `_doc` claims 945 shows the gleam but 945 is burst age 23 > 22 and main's critic read `pickup_gleam_reads` not exercised | read `fx.rb:117` + regate_a.out | Junior's files verbatim → both corrections routed to him (pt-br line); recorded in §2.1 |
| m4 | "seal_breach_reads has NO live stager" overstated — the SEALED half passed affirmatively; the BREACH beat has zero coverage wall-wide | regate_a.out + `grep seal_breached harness/scripts/*.json` → none | §2.1 reworded; breach-beat stager = named ticket in CYCLE debts |
| m5 | vessel label draws at `text_a`, not `text_a - 40`; contrast 1.08 by band median | `controls_overlay.rb:112-116` | §2.7 corrected |
| m6 | aoe_specials' hollow b pin is superseded by c | ledger reads latest by date | none (disclosed) |
| m7 | MAP_EDITING §1 listed district/district_two/low_quay as hand-authored while `pilot_authoring_test` byte-compares them as importer emissions | read both | §1 table corrected from the test's ZONES list (this close commit) |
| D1 | zone8's blue lines should have been FIXED here: sidecar decor, render-only, mechanical importer path, reversible | agreed — the spark's `data/zones/**` fence was a scope guard, not a law | done through the door (`3914f1e`), re-gated in batch e |

- [x] Verdict pasted above; every blocker resolved with a commit; disagreement adopted.

## 5. Rule 7 declared spend

Critic calls: 16 single re-gates (a2 + b2 + c2 + d2 + e2 + f5 + g1) at Bedrock vision
pricing (AWS-internal, pre-cleared; ~5 min GL each, serialized) + one fresh-eyes review
(Fable 5.1, 23 min, ≈ $1). Stop condition met: every row carries a VERDICT with pasted
evidence, the review's blockers are landed, push follows this commit.

## 6. Named follow-ups (recorded, not nagged)

- E4: district_two wall vs wall_inner tints are one hue family — the coral contract
  needs a palette/texture decision (floor2_run rows).
- Junior (fx/strip): spark-on-pale-body contrast; dust sub-perceptual; "chips still
  flying" is a circular age cue in his impact_fx clause; vessel label contrast ~1.04–1.08:1
  on the strip; low-hp strip tint = his `65f52e5` (lands with his merge); the two `_doc`
  corrections (m3).
- Breach-beat coverage hole: no wall script stages `seal_breached` — a basement_2 breach
  stager (toll_pocket tail re-author or a new reel); drop-under-corpse sim question for
  the owner.
- Negative control for the recalibrated rows (review m1): one detached `fx_enabled:false`
  gate expecting `impact_fx_reads` FAIL, never pinned.
- corpse_run inherits floor1_run's affirmative impact read; its ledger row stays FAILED
  until T2c's full re-pin.
- T2a waits for Junior's merge (or his `junior/e-tickets` cut).
