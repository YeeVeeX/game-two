# v20 T1 follow-up — wall harvest, red triage, review receipt (2026-08-28, s116)

**Session:** T1's recorded same-day debts (checkpoint s115). T1 record
(history, linked not rewritten): `drafts/_v20-t1-floor1-20260828.md` —
two dated CORRECTION markers added this session (review findings 1-2).
**Sweep:** `harness/run_wall.sh v20-t1` detached, 36 scripts, DONE
19:16:14 rc=1, driver `tmp/wall/v20-t1-driver.log`, per-script
`tmp/wall/<script>_v20-t1.log`. **Re-gate sweep:** `v20-t1r` (11
scripts: six re-cuts + reworded-row verify + three critic-timeout
re-runs + one hallucination re-run), results below.

## 1. Full rc table (main sweep, 36 scripts)

| script | gate | manifest | class / disposition |
|---|---|---|---|
| aim_hold | 0 | 0 | green |
| aoe_specials | 1 | 0 | VISION: `challenge_reads` — real staging drift (C2 empty-room cast) → **C2-QUEUED** |
| basement_pocket | 0 | 1 | C2-drift (drop_picked_up 0) → **C2-QUEUED** |
| burn_duel | 1 | 1 | C2-drift (seizure family 0; vision rows judged a wiped run) → **C2-QUEUED** |
| corpse_run | 0 | 1 | T1-delta → **RE-CUT** this session |
| critic_reel | 0 | 0 | green |
| dash_strike_rip | 1 | 0 | critic CRASH (Bedrock read-timeout, no verdict) → re-gated |
| district_hunt | 1 | 1 | T1-delta → **RE-CUT**; its vision FAIL exposed the two_halves row miscalibration (§3) |
| dungeon_fork | 0 | 0 | green |
| floor1_run | 1 | 0 | VISION: `edge_pip_reads` hallucination (probe: all 3 bodies on-screen at f0) → row reworded + re-gated |
| grass_fixture_walk | 0 | 0 | green |
| ledger_loop | 0 | 1 | T1-delta → **RE-CUT** |
| level_gate | 0 | 1 | C2-drift (fight never happens) → **C2-QUEUED** |
| level_up_beat | 0 | 0 | green |
| lobber_reach | 0 | 1 | T1-delta → **RE-CUT** |
| lobber_volley | 0 | 1 | T1-delta → **RE-CUT** |
| loot_loop | 0 | 1 | C2-drift → **C2-QUEUED** |
| low_quay_run | 0 | 1 | T1-delta → **RE-CUT** |
| menu_tour | 0 | 0 | green |
| mercy_floor | 0 | 0 | green |
| moving_square | 0 | 0 | green |
| multi_floor_descent | 0 | 0 | green |
| nest_advance | 1 | 1 | manifest C2-drift → **C2-QUEUED**; gate = critic CRASH (2 timeouts) → re-gated |
| respawn_telegraph | 0 | 0 | green |
| safe_boundary | 0 | 0 | green |
| specials_chain | 0 | 0 | green |
| sustain_run | 0 | 1 | C2-drift (provision beats miss) → **C2-QUEUED**; spot-read clean |
| taunt_anchor | 1 | 0 | critic CRASH (2 timeouts) → re-gated |
| threat_pull | 0 | 1 | T1-delta → **RETIRED-PENDING-C3** (§4) |
| toll_pocket | 0 | 1 | C2-drift → **C2-QUEUED** |
| town_gates | 1 | 0 | VISION: `edge_pip_reads` hallucination (probe: all 3 bodies on-screen at f575) → re-gated |
| varekka_duel | 0 | 1 | C2-drift (boss seizure never fires) → **C2-QUEUED**; spot-read clean |
| vat_economy | 0 | 1 | C2-drift → **C2-QUEUED** |
| world_loop | 0 | 0 | green (T1's own re-cut passes the wall) |
| zone8_crossing | 0 | 1 | C2-drift (regrow/tribute beats) → **C2-QUEUED**; spot-read clean |
| zone_catchup | 0 | 1 | C2-drift (leash family — same law as threat_pull) → **C2-QUEUED** |

Greens: 14 of 36. No DETERMINISM red anywhere — every script's double
replay was byte-identical (the class-1 defect never appeared).

## 2. The C2-drift class (root cause, evidence, disposition)

**Root cause:** `04b218b` (2026-08-26, v19 C2 — "ally defensive-default
engage rule + flee co-tune"). Free pack bodies now acquire PROVOKED
humans only. Every wall script whose choreography leaned on free-ally
aggression re-sequenced: first contact delays (~frame 6 → ~frame 90 in
burn_duel), staged beats (seizure, tribute, provisions, corpse-runs)
miss their blind-tap windows, packs that used to win now wipe (or vice
versa). C2 rebanked the sim-identity canary lawfully (S73_HISTORY) but
the WALL was never re-pinned — the last full-green sweep was
`s72_shake` (08-25, pre-C2). The T1 baseline census
(`tmp/manifest_baseline.txt`, pre-T1 same-day) shows every one of these
reds already failing on the OLD map — **zero of them are T1 damage**
(T1's own canary: varekka/burn byte-unchanged).

**Evidence per script:** every wall manifest FAIL row matches the
baseline census count exactly (0-count beats stay 0; single-replay
counts double). Cross-check table lives in the driver log +
`tmp/manifest_baseline.txt`.

**Disposition — C2-QUEUED (named debt, not silence):** these eleven
scripts (aoe_specials, basement_pocket, burn_duel, level_gate,
loot_loop, nest_advance, sustain_run, toll_pocket, varekka_duel,
vat_economy, zone8_crossing, zone_catchup) stay RED in full sweeps
until the **C3 session** (v20 lane E, verdict debt 2 — the flee/engage
co-tune, already its own owed session). Re-cutting them against
today's ally AI would be re-cut twice: C3 re-tunes the same law they
choreograph against. The C3 session inherits: re-cut or re-stage each
after the co-tune lands, using the waypoint-tuner pattern
(`tools/tune_*.rb`, seven working precedents now). Scripts + manifests
stay INTACT — no want was weakened anywhere this session.

**Watch item for T3 (potions):** sustain_run is in this class AND T3
rebuilds the provisions identity — T3 should re-cut sustain_run as part
of its own Rule 2 obligation, ahead of C3.

## 3. Vision reds — sampling-artifact law applied

Every vision FAIL was read against the actual PNGs + a headless
position probe before accepting any verdict:

- **`floor1_two_halves_read` (district_hunt red): the check row was
  miscalibrated, not the map.** Junior's v2b PNG itself (majority-vote
  at the builder's own 8×8 lens, offset 26) distributes moss: north
  band 20 west / 0 east, mid bands 25/71 and 25/91 (east belt), south
  band 34/7. The shipped grid matches the PNG **exactly in every band**
  (transcription conviction ruled out by per-band count equality). The
  old row hard-coded "west dry / east mossy" for ANY frame; a
  north-band reel honestly fails that. Row reworded (affirmative, no
  tolerance clause): halves must read as two biomes, never one flat
  fill; the east moss belt is asserted where it actually lives (bridge-
  2/3 latitudes). The T1 record's "west dry/dark, east mossy/alive"
  summary overstated a mid-map gradient — named here, record untouched
  (history).
- **`edge_pip_reads` (town_gates + floor1_run reds): critic
  hallucination, probe-convicted twice.** Headless camera probes:
  town_gates f575 — striker/blocker/lobber all on-screen (sx 546-610,
  sy 224-256); floor1_run f0 — all three bodies on-screen at the mouth
  (sy 478-510, partially under the controls strip). The critic inferred
  "off-screen allies" by counting HUD bars against bodies it could
  spot. Row reworded: pips are judged only when the reel stages a
  GENUINE separation; bar-counting is named as non-evidence. The pip
  RENDERER is affirmatively healthy: zone8_crossing f1922 shows working
  kit-colored pips clamped into the strip band for genuinely off-screen
  allies (spot-read §5).
- **`challenge_reads` (aoe_specials red): REAL.** "Rust pulse fires
  into an empty room" — the staged challenge special now casts after
  C2 re-sequencing emptied the room. Not a render defect; the script's
  staging drifted → C2-QUEUED with the class.
- **burn_duel `ledger_beat_reads` + `challenger_tell_reads`: judged on
  a drifted run.** The reel now wipes at f507 (pre-C2 it survived and
  the boss seized). Frozen +0 tally and missing chant tell are
  artifacts of choreography drift, not new render bugs — the same
  surfaces pass in scripts that still stage them (bank_tally,
  writ_frame rows pass elsewhere in the same sweep). C2-QUEUED.

## 4. threat_pull — RETIRED-PENDING-C3 (named standing red)

The only T1-delta script NOT re-cut. Its leash surface is C2-COUPLED:
under the provocation law a door pull always becomes a kill (allies
auto-defend, chasers die before leashing — probe: 3 chasers dead, 0
leashes, leash counter only ticks in-zone). A touchless aggro-edge pull
(striker bait, beachhead-pocket release) was built and probed: **zero
leashes in cycle 1** — the mechanism C2 leaves for leashing is too
marginal to stage honestly today. `tools/tune_threat_pull.rb` carries
the working phases (P1 possession, P3-P4 corpse beat) and the probe
record; the C3 session re-cuts it beside the flee/engage co-tune,
where the leash math it pins will actually be settled. Script +
manifest INTACT; it stays red in full sweeps by name. zone_catchup
(same leash family) rides the same queue.

## 5. Six re-cuts (T1-delta) — headless manifest proofs

All six re-authored via waypoint-tuner generation (per-script
`tools/tune_<name>.rb`, the tune_floor1_run.rb precedent): headless
probe → waypoint-corrected taps (seed-deterministic drift correction)
→ manifest-verified emit. **Every manifest want byte-unchanged.**
Committed `9b2483a` (suite 1318/0F via hook).

| script | headless proof (per single replay → per double vs want) |
|---|---|
| corpse_run | corpse_loaded 1→2/2 · pack_wiped 1→2/2 · corpse_looted 1→2/2 · banked 1→2/2 · drop_picked_up 3→6/6 · vessel_kept 1→2/2 |
| ledger_loop | banked 1→2/1 · corpse_loaded 1→2/1 · pack_wiped 1→2/1 |
| district_hunt | actor_died 11→22/20 |
| low_quay_run | inscribed 3→6/6 · seal_breached 2→4/4 · banked 2→4/2 · tribute_paid 1→2/2 · drop_picked_up 5→10/10 · zone_entered 8→16/12 |
| lobber_reach | special_started 3→6/2 · attack_hit 48→96/63 · actor_died 17→34/23 |
| lobber_volley | special_started 2→4/4 · attack_hit 55→110/106 · actor_died 18→36/34 · drop_picked_up 1→2/2 |

Choreography notes (for future tuner work): the east-north pocket
[38-45, 8-13] is the map's wipe/farm engine (respawn anchors live
there); the door beachhead ([1,13]±4) strips unwaived chasers' focus;
district_two marches ride y=12 (the y=13 west way tile eats knockback
bounces); the y=55-56 notch passes only at x=10-12.

## 6. Fresh-eyes review (Rule 6) — verdict + actions

Receipt: `tmp/v20t1_review/receipt.md` (headless, DONE marker present).
Verdict JSON verbatim:

```json
{"verdict":"PASS","blocking":[],"advisory":["Record inaccuracy (canary section): 'Both streams close the loop: fight resolved → drop picked → nest → banked' is false for the OLD stream — tmp/canary/old_stream.txt (LF-md5 982bfd66… = the genuine S80 bank, 37 lines) ends at frame 988 with respawns and contains NO drop_picked_up/nest-return/banked lines; only the NEW stream (LF-md5 e0b1f38f… verified) closes the loop. The error flatters the baseline, not the change (the re-cut RESTORED loop closure in the headless lens), so it conceals nothing — but the sentence should be corrected in the already-owed same-day follow-up commit.","Deviation 4 wording is loose: 'every door keeps the old map's nearest-hostile-at-11 property' verifies only for the two ARRIVAL doors ([1,13]=11, [11,87]=11, both confirmed by independent probe); exit-side [42,13] has a hostile at Chebyshev 3 and [40,0] at 10. No regression (the old map's camp-side arrival [40,13] had a hostile at Chebyshev 2, and no arrivals land at [42,13]/[40,0] in the new graph), but the record overstates.","Comment typo in two tests: seal_breach_test.rb and world_test.rb comments cite the mouth landing as [11,85]; the shipped anchor and all asserts correctly use [11,87].","The quoted PERF line (ticks=6990 p50=0.526ms p95=0.764ms) is banked nowhere in tmp/ — non-gating machine-local smoke, but next time tee it to a file so the record's numbers are checkable.","Open by design and correctly owned in the record: wall re-pin sweep verdicts (running detached, *_v20-t1.log landing during this review) plus 7 retired-pending-reauthor scripts (lobber_reach, lobber_volley, corpse_run, ledger_loop, district_hunt, low_quay_run, threat_pull) ride the same-day follow-up commit; T1's wall debt is not closed until it lands, and world_loop/dash_strike_rip have not yet individually re-passed the full Rule 2 gate on the new map — only floor1_run has."],"evidence_checked":["tmp/v20t1_review/spec_t1.md + record.md (record md5 c3cf2a7c… == committed drafts/_v20-t1-floor1-20260828.md)","tmp/v20t1_review/diff.patch — every non-LDtk file diff read (zones, gate_checks, 3 scripts, 17 test files, fixture, sidecar, tools)","data/zones/district.json working tree: seal station [41,13] opens [42,13] byte-identical to pre-change (verified vs 005eab3^), [42,13] transition sealed:true + stairs_up, NEW [40,0]→district_two hole, nest row byte-identical, floor -1, ZONE 2 display, gradient [[0,1.0],[35,1.5],[70,2.0]] anchor [11,87] (multipliers unchanged, breakpoints scaled)","data/zones/camp.json: mouth [0,5] re-typed stairs_down spawn [11,87]; nest.json + grass_fixture.json inbound spawn [1,13] untouched and walkable","independent BFS on shipped 52×88 grid: 2368 reachable tiles, 0 of 498 water tiles reachable (wall-ring proven), all 4 endpoints reachable, exactly 4 bridges (wood rows 14-15/34-35/54-55/74-75), blocking planks disconnects east door, 27 spawns (24 rusher + 3 rusher_hater) all on walkable tiles, arrival doors nearest-hostile Chebyshev 11/11 (old map nest door was 11, camp door 2)","test/tools/pilot_authoring_test.rb: district added to ZONES provenance pin; test/tools/import_ldtk_test.rb decoupled to district_expected.json which is semantically identical to the pre-change live emission (verified)","test/harness/sim_identity_canary_test.rb: S80 bank preserved immutable as S80_HISTORY, ACTIVE rebanked, varekka/burn md5 unchanged; tmp/canary streams LF-md5s match both banked constants exactly; prefix identical lines 1-5, line 6 diverges on roster renumbering as claimed","tmp/t1_gate_floor1.log: GATE PASS, gate_rc=0, determinism 10 captures byte-identical ×2, all four new/rewritten district rows affirmatively PASS (two_halves/chasm/bridge/zones_distinct) — strong members staged, not tolerance-clause passes","captures/floor1_run frames 0250/0990/1350 read directly: ZONE 2 banner, dry-dark west + trail fight, mid-bridge crossing with navy void both sides + drop, east moss + LEVEL 2 gold rule-pair stamp; placeholder register throughout","tmp/soak/20260828-153042 + 20260828-154137 report.txt: standard 36k-tick PASS (chain fresh→dec77789) + ZONES=district TICKS=6000 PASS with EP1 zone=district","tmp/manifest_baseline.txt (headless census artifact backing the retirement delta argument); tmp/wall/*_v20-t1.log accumulating live (aim_hold→burn_duel at review time) + v20-t1-driver.log — sweep genuinely detached","git log: CLAIMED 37be092 → landing 005eab3 (suite hook-enforced at commit) → record/checkpoint a584fca = HEAD; no src/, data/balance/, or save-schema changes anywhere in the landing diff (L9, line caps, zero-balance-constants all untouched)"]}
```

**PASS, zero blocking → T4 is UNBLOCKED once this addendum lands.**
Actions per advisory:

1. Canary-sentence inaccuracy → **ADOPTED**: dated CORRECTION marker in
   the T1 record's canary section (this commit).
2. Deviation-4 overstatement → **ADOPTED**: dated CORRECTION marker
   beside it (this commit).
3. [11,85] comment typos → **FIXED** in seal_breach_test.rb +
   world_test.rb (comments only; suite green).
4. PERF line not banked → **ADOPTED AS PRACTICE**: future perf smokes
   tee to tmp/ so record numbers stay checkable. Not retro-bankable.
5. Open items → this session IS the execution; see §1-§5 and §7.

## 7. Re-gate sweep (v20-t1r) results

Driver `tmp/wall/v20-t1r-driver.log`, DONE marker present. Bedrock had
an unstable evening: 4 more critic read-timeouts during re-gates;
crashed vision halves were re-run STANDALONE on the frozen gate_a
captures (same artifact, judge re-convened — logs
`tmp/wall/<script>_v20-t1r-critic2.log`). Verdicts:

| script | det | manifest | vision | net |
|---|---|---|---|---|
| corpse_run | PASS (9 caps ×2) | **PASS** | timeout → standalone PASS (0 FAIL) | **GREEN** |
| ledger_loop | PASS | **PASS** | PASS | **GREEN** |
| district_hunt | PASS | **PASS** | timeout → standalone PASS (reworded two_halves row reads the north band affirmatively: "dark moss-flecked west / open tan east = two biomes") | **GREEN** |
| low_quay_run | PASS | **PASS** | PASS | **GREEN** |
| lobber_reach | PASS | **PASS** | PASS | **GREEN** |
| lobber_volley | PASS | **PASS** | PASS | **GREEN** |
| floor1_run | PASS | PASS | `level_up_beat_reads` FAIL → standalone PASS on the SAME frozen frames | **GREEN**, flap named below |
| town_gates | PASS | PASS | PASS (reworded pip row) | **GREEN** — hallucination cleared |
| taunt_anchor | PASS | PASS | PASS | **GREEN** — timeout cleared |
| nest_advance | PASS | C2-QUEUED (§2) | PASS | vision clean; manifest stays named-red |
| dash_strike_rip | PASS | n/a (det-only) | `zones_distinct` + `zone_identity_reads` FAIL ×2 | **QUIRK-RED** (below) |

**All six re-cuts are fully green** (headless proof + attended gate:
determinism + manifest + vision).

**floor1_run flap:** `level_up_beat_reads` passed T1's morning gate,
failed the evening re-gate demanding the transient gold-shard burst,
passed the standalone re-run — all three readings on byte-identical
frames (2-of-3 PASS). The row's own strong member (`level_up_beat`
script) passed the full sweep 0/0, so the surface is intact; the row
fixates on a transient when sampled unluckily. Left as-is tonight —
if it flaps again, reword toward the durable cues (stamp + HUD
numeral + bar reset) with shards asserted only when a straddle
catches them.

**dash_strike_rip QUIRK-RED (twice-red, surfaced per protocol):** the
critic is READING TRUTHFULLY — its frames show a ZONE 1 banner
dwelling over district pixels. Root: zone-start scripts
(`start.zone`) boot the world in nest (home banner enqueued), then
jump; the stale ZONE 1 banner displays over ZONE 2 ground for the
first ~200 frames (floor1_run f0 wears it too). The zones ARE
distinct when genuinely compared — ledger_loop and low_quay_run
passed both rows with real multi-zone reels in this same sweep. Fix
path (named debt, next session): `Harness.apply_start` clears or
re-enqueues the banner on a start-zone jump; then re-gate the
zone-start scripts whose banner frames shift. dash_strike_rip stays
red by name until that lands.

## 8. Spot-reads (vacuous-pass greens, one line each)

- varekka_recut f0930 (ZONE 4 leg): tiny-zone camera clamp renders
  black beyond map bounds by design; bodies/drops/hit numbers clean.
- zone8_crossing f1922: frontier zone healthy at LEVEL 8; **edge pips
  affirmatively working** (kit-colored, dark-outlined, clamped into the
  strip band for genuinely off-screen allies). Seam named: a
  bottom-clamped pip can cover the strip's "player N" label — minor
  legibility debt, not touched this session.
- sustain_run f3363 (west arena fight): clean render, no garbage; the
  script's provision beats are what's missing (C2-QUEUED), not pixels.

## 9. Named debts leaving this session

1. **C2-QUEUED wall reds (11 scripts + threat_pull + zone_catchup)** —
   owed to the C3 flee/engage co-tune session (v20 lane E, second
   wave). sustain_run may resolve earlier inside T3 (provisions).
2. **Boot-banner-on-start-jump quirk** — stale ZONE 1 banner over
   start.zone ground poisons zone-identity rows (dash_strike_rip
   QUIRK-RED). Small harness fix + re-gate of zone-start scripts;
   next session or riding T2.
3. **Mouth-spawn strip occlusion** — at the district mouth the two
   allies spawn on tiles partially under the controls strip
   (floor1_run f0); cosmetic, candidate one-row-north pack_spawn nudge
   in a lane-D ticket.
4. **Critic-timeout flakiness** — seven Bedrock read-timeouts across
   both sweeps tonight; the wall runner wants a single
   retry-on-timeout around the critic call (tooling debt, not a gate
   law change).
5. **level_up_beat_reads flap watch** — reword toward durable cues if
   it flaps a second time (§7).
