# Staging council pass — reconciliation (s82, 2026-08-26)

Two declared calls (Rule 7: ≤2, spent 2 — Kimi K2.5 on wording/register,
DeepSeek V3.2 on measurement design). Raw briefs + verbatim outputs in
this dir. Every REFUTED verdict re-verified against the primary text
before acting (the false-BLOCK law). One pass, no re-loops.

## Adopted (applied to the spec before freeze)

1. **Growth question was leading** (Kimi Q1, confirmed on re-read:
   "¿sentiste que cambió?" presupposes noticeable change; the follow-up
   primes affirmative). Fix: forced-alternative form — es "¿la fuerza
   del grupo se sintió igual o diferente? ¿Cómo?" / pt "a força do
   grupo pareceu a mesma ou diferente? Como foi?" — matching the house
   style of the proven v18 instruments ("¿retomaban … o partida
   nueva?").
2. **es geography question mixed tú/ustedes and carried two probes**
   (Kimi Q3+Q5). Fix: plural throughout + SPLIT into two administered
   questions (legibility, then behavior). Owner set is now 5 —
   symmetric with Junior's 5, which also dissolves Kimi's
   asymmetric-load finding.
3. **A5 "level above start" baseline** (DeepSeek Q1+Q2): my brief
   framed the state check as ritual-window leveling; DeepSeek
   correctly attacked that framing (dice roll on curve position;
   cap-grind incentive). Re-verified against the foundation: the
   thesis pairs "stronger than at v19 OPEN" with "kills mattered
   beyond the moment" — the ratified baseline is the CYCLE's start
   (level 1 injection), not the ritual's s1-open. A5 rewritten:
   state = level > 1 at both ends + facts CARRIED across the boundary
   (+ regression = K1-class); flow = kills_xp > 0 both sessions;
   ritual-window level crossings + gate crossings named beside the
   reading, never a check. Both objections dissolve under the correct
   baseline (exposure play cannot corrupt a cycle-baseline check).
4. **K3 kill bar** (DeepSeek Q3): "both players, independently: growth
   was FELT" defines success as both-positive → the kill fires on ANY
   seat negative/null. My both-negative bar was too lenient against
   the ratified wording. Adopted; R-G1 re-scoped to hedged/qualified
   answers.
5. **Session date pin** (DeepSeek Q4): date = host-clock calendar date
   at session LAUNCH (midnight-straddle keeps its launch date).
   Adopted into §4.4 + A4.
6. **Serial dependence named** (DeepSeek Q6): the two sessions are
   serially dependent BY DESIGN (persistence is the object);
   pre-registered in §13 as a reading law — Half B judges the cycle
   as a felt whole, never two independent samples (per-feature
   independence was already council-rejected at shape time).

## Refuted (recorded so adjudication never re-litigates)

1. **"terceiro corpo = dev jargon, replace with 'a IA'" (Kimi,
   'critical')** — REFUTED: pt-4 is v18's byte-identical administered
   instrument and produced a fluent verbatim answer; changing the
   wording destroys the A/B baseline. The parenthetical "(a IA)" is
   the accommodation and it is proven.
2. **"difficulty ARC topic missing — add a boss-reach probe" (Kimi,
   'critical')** — REFUTED: boss-reach emerged UNPROMPTED from this
   exact instrument in v18 ("segue muito dificil chegar no boss");
   naming the boss in the question would hand Junior his old frame
   (leading). The arc reads from pt-3's answer + zones-reached
   telemetry. Recorded in spec §7.
3. **"segundo día premise breaks if sessions land same-day" (Kimi
   Q6)** — already covered: spec §9 premise note (pre-registered v18
   variant) + §4.4 compression law. The reviewer did not have the
   full spec.
4. **DeepSeek's counter-proposal on A5 rung 3** ("state FAIL but
   session valid; the failure to level is the named datum") — moot
   under the corrected baseline; noted that its diagnosis was right
   and its prescription contradicted itself (a failed state criterion
   cannot be the strictly-positive named fact).

## Spend

2 council calls (~3k in / ~3.4k out tokens total) · zero drafting by
reviewers beyond quoted minimal fixes · questions stayed sealed from
the peers throughout (reviewers are not the measured population).

## Review gate round 1 (headless fresh-eyes, Rule 6): FAIL — all findings applied

Raw: `s82_review1_brief.txt` + `s82_review1_out_FAIL.txt` (this dir).
Every finding re-verified against primaries before acting; ALL
accepted (unlike the council round, nothing refuted — the reviewer
quoted bytes):

- **B1 (blocker):** growth question was RITUAL-WINDOW scoped while
  the thesis + fixed A5 are CYCLE-scoped — at/near cap "igual" was
  the honest window answer and K3 fired falsely into an unkillable
  re-ritual loop. Fixed: es-2/pt-2 re-horizoned to "desde que
  empezaron en este mundo"; §7 lineage row records both fixes.
- **B2 (blocker):** A5(b) pinned s2-open == s1-close, contradicting
  §4.2's legal between-session play. Fixed: latest-prior-in-chain
  form (A1's law).
- **M1:** digest arbiter `save_state.rb` + `data/zones/` +
  `autopilot.rb` missing from freeze/watch lists. Fixed in §12.3 +
  skeleton watch command.
- **M2:** CUMPLIDO five clauses vs two kills unregistered — fixed:
  §1 clause arithmetic (blocking vs routed-debt clauses, "CUMPLIDO
  with named debts", C3-language defense).
- **M3:** A6 demanded hosting consoles nothing produces
  (`host-coop.cmd` doesn't tee — verified). Fixed: launcher logs
  primary, consoles corroboration-if-produced.
- **m1** exposure-floor mismatch (aligned: the staging commit) ·
  **m2** launch-date byte (original-file ctime recorded at banking) ·
  **m3** es-4 dangling anaphor (now self-contained) · **m4** runsheet
  telegraphed the growth stake (flattened) · **m5** missing
  s1-open decode row (added).
- **Biggest unhandled risk (stall/lag poisoning Half B):** adopted as
  §11 link-quality shortfall — thresholds calibrated on the only two
  points we own (v18 s1 13.2%/1113ms played-through vs s2
  14.9%/3341ms quit-on): ≥14% stalls or ≥2500ms max; peer re-run
  option closes at the first administered question; after that the
  profile is a named confound, never a waiver.

## Review gate round 2 (fresh headless session): PASS_WITH_FIXES — 0 blockers; all fixes applied

Raw: `s82_review2_out.txt` (this dir). Findings → dispositions:

- **M1** §1 cross-ref pointed topics at §8 (economy row) instead of
  §7 — fixed.
- **M2** §5 harvest preamble still demanded "both hosting consoles"
  unconditionally (round-1 M3 residue) — aligned with A6.
- **m1** ΔE notation inconsistency — rewritten as "reaching level N
  costs ΔE(N)" (code stays the arbiter).
- **m2** final byte-forms recorded below.
- **m3** `death.json` added to §12.4.
- **m4** third-body measured-behavior CODE seams
  (creature.rb provocation · aggro.rb acquisition) added to the
  freeze — the reviewer recorded it as faithful-to-foundation either
  way; we chose the tighter reading.
- **m5** solo-exposure evidence named as the weaker form in §3
  (HEAD recorded at exposure time, dev-attested).
- **m6a** JUNIOR.md "A hoja de ruta" → "O roteiro" · **m6b** K3 null
  gloss pre-registered (counter-question/incomprehension = NULL, v18
  P3 precedent).
- **Biggest risk (debrief window unbounded, contamination had no
  reading rule)** → §6 gains the schedule (administration triggers AT
  session-2 close, 10/10 same-day target) and the contamination law
  (independence discount on the novelty axis; K3 unaffected by
  contact; a post-contact second vote cannot COMPLETE K4 — downgrades
  to R-C1; against-interest content keeps full weight).

## Final frozen byte-forms of the council/review-touched questions (m2)

- es-2: "Comparado con cuando empezaron en este mundo, ¿la fuerza del
  grupo se siente igual o diferente? ¿Por qué?"
- pt-2: "Comparado com quando vocês começaram nesse mundo, a força do
  grupo parece a mesma ou diferente? Por quê?"
- es-3: "Moviéndose por el mundo, ¿qué tanto se nota dónde es seguro
  y dónde es peligroso?"
- es-4: "¿El peligro de las zonas les cambia cómo juegan?"

Drift trail: council proposed "¿…se sintió igual o diferente?
¿Cómo?" (window horizon) → review-1 B1 re-horizoned to the cycle
("desde que empezaron en este mundo") → the elaboration clause became
branch-neutral "¿Por qué?/Por quê?" and es-4 became self-contained
(review-1 m3). §9 of the spec is the single frozen authority; this
section exists so the record quotes it byte-exactly.
