# gamesmith Round-7B intake triage — deduplicating dispositions (2026-08-20)

Banking, not adoption. Four-thread corpus-completion evidence banked into the
ignored addenda pattern; this doc + one PARKING pointer are the only tracked
citations. Executed per `drafts/_gamesmith-round7-intake-spark-20260820.md`
(banked verbatim from the gamesmith seat at session-24 close).

## Header facts

- **Authorization:** owner, 2026-08-20 — approved proceeding to the next
  bounded session; Round 7B itself was approved, completed, committed, and
  pushed gamesmith-side.
- **Source identity:** gamesmith `docs/round7-synthesis-threads.md` @ commit
  `c4e49f3` (md5 `8f0ac085bf77f5a00b9def7f40454049`); copy verified
  byte-identical 2026-08-20 at
  `docs/design-corpus/gamesmith/addenda/round7-synthesis-threads-20260820.md`
  (ignored by design; provenance block appended to addenda `_PROVENANCE.md`).
  Source HTML stays gamesmith-side — the addenda precedent banks Markdown only.
- **Citation gate (source's own):** 96 obs + 4 say, zero dangling/malformed/
  alias leaks — recount on the banked copy with the gate's regexes reproduced
  obs=96 say=4 exactly.
- **game-two state at intake (live AGENTS.md + checkpoint, read after pull):**
  v18 CLOSED (SEVENTEENTH cumplido, verdict 2026-08-20) · v19 NOT open
  (owners' brainstorm at their word) · session-24 audio polish shipped
  (`aa02624` + `3d79787`), ear-checks owner-pending · lag T3 verdict banked,
  T4 named. This intake is docs-only evidence banking: it does not reopen
  v18, alter the live queue, promote a mechanic, change telemetry, or adopt
  any gamesmith FR.
- **Queue note:** the spark's design-time queue (cue duplication → ducking →
  Junior coop) SHIPPED in session 24; the live queue at execution is the
  session-25 spark's (R7B intake → lag ticket T4 vsync spike → v19 docket;
  the lag "T4" is a different namespace than gamesmith thread T4). "Active
  queue unchanged" means THAT queue — this intake displaces nothing.
- **No gamesmith FR binds game-two; no tuning number transfers.** FR ids in
  the source are gamesmith provenance handles (current dense FR-001–047 vs
  archived sparse numbering; number reuse is real — archived FR-030 ≠ current
  FR-030). Every event cap, rate, fee, and fraction stays refused per the
  source's own §10.7 rule: replay-instrumented playtests own our numbers.
- **Provenance caveats carried:** source bundle title split recorded, never
  resolved here (`spec.md` = Lanternvale, `direction.md` = MARROWFEN);
  NW-Aeternum material is a reference study OUTSIDE the 13-game corpus;
  footage/rules evidence is era-tagged, never current-game truth, never
  redistributed. The source explicitly found no game-two demand — preserved.

## Dispositions

| Thread | Corpus answer | Existing game-two overlap | Disposition | Named trigger |
|---|---|---|---|---|
| **T4 front events without multiplayer** (§11.4) | Staging grammar only (clock, threshold, stake, state change all observed in NW footage) — the payoff was NEVER observed; substitute for human unpredictability = readable uncertainty, decision-without-baseline. Current gamesmith bundle carries no front-event system. | None. No game-two position names front events (round-6 pull-test negative); the world-builder lane's SIM-class behaviors are a different, post-verdict topic. | **BANK / NO ACTION** — no code, no v19 idea created. | An owner-opened world/front-event design debate explicitly asks for this evidence. |
| **T6 combat screen-event cap** (§11.6) | Six specimens, one failure shape: simultaneous channel occupancy collapses legibility at peak; corpus CANNOT supply a numeric cap (false precision). Deterministic replay-sweep method defined (channel · priority · lifetime · collision region · degradation route · replay result). | Qualitative finding already consumed via Itexo triage 2.9 (actor silhouettes + ground state readable — `drafts/_itexo-intake-triage-20260818.md`); our replay harness + gate checklists are the native surface for any future sweep. | **DEDUPE / BANK METHOD** — method only; no cap invented, no duplicate wall task. | A named target-renderer surface needs a numeric event budget; its own replay captures must produce the number. |
| **T7 instrumentation corrosion** (§11.7) | Register / diagnose / steer taxonomy; the analyser becomes the game on target substitution, action-space substitution, or state divergence (only the third directly observed). A negative P&L readout is not corrosive by definition. | v18 instrumentation is CLOSED and pre-registered (TELEMETRY oracle wording frozen through the verdict; overlay = overrun counter only). This section evaluates none of it. | **BANK / ZERO TELEMETRY CHANGE** — evidence for a future debate only. | The owners explicitly open an instrumentation/overlay decision. |
| **T8 DaD extraction-wager map** (§11.8) | Minimal pair closed (same wager → zero-loss and quantified-payout specimens); four auditable boundaries: consent before exposure, symmetric registration, persistence floor, conversion after survival. Observed-vs-[SYNTHESIS] map against current FR lineage; loss itemization, AP nullification, exit act = named GAPs. | PARKING `Session-ledger / carried-fact shape` entry (from the corpus→v18 brief) is the closest active overlap — already carries the stash-boundary shape candidate. | **DEDUPE / BANK MAP** — the existing pointer stands; bank the full map; adopt no current gamesmith FR, create no economy/death task. | An owner-opened death/economy/session-loop debate asks for the full map. |

## No action owed now

- No code, data, harness, audio, or telemetry changes.
- No v19 promotion — v19 opens at the owners' brainstorm, not via intake.
- No re-handoff of the synthesis bundle; no pipeline spend (source round was
  $0; this intake spends only its ≤$0.20 text-gate budget).
- No lore (standing order) — mechanics-only evidence throughout.
- No change to the live queue inherited at preflight (session-25 spark's:
  R7B intake → lag ticket T4 vsync spike → v19 docket); the spark's
  design-time queue had already shipped in session 24.
- Rounds 5/6 verified existing (round-5 addendum md5
  `a044f986fa3fbebdd1bdc8481939a41c` ✓, PARKING anchors present), not
  replayed.
