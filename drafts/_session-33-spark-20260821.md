# SPARK sesión 33 — harvest-first support + seal `opens` law (the twice-RECORDED refusal, promoted); shared-save first crossing still loaded; v19 waits at the brainstorm

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register); Junior surfaces pt-br. Quality over cost: council 0
(no design fork is open); paid calls 0 by default — TWO named exceptions
only: J1's defect branch (real-data fix owes its wall gates) and any
override lane that ships a gated change. Evidence-first: claims are not
evidence — file:line, log line, or UNVERIFIED tag. Bot lines are never
fun/oracle evidence.

## Program state (2026-08-21 session-32 close — verify live)

- v18 CLOSED · **v19 NOT open** (brainstorm at the owners' word; agenda:
  `drafts/_v19-intake-docket-20260820.md` — now carries the TOWN 1 v0
  row + the banked dread exposure; prep doc
  `drafts/_v19-brainstorm-prep-20260821.md`, one named SHELF GAP:
  companion/ally AI).
- **s32 shipped the tile-shape law** (`38ae352`, reviewed
  PASS-WITH-NITS, pushed): `check_passable!` guards shape FIRST — nil /
  string / Float / 3-element tiles refuse NAMED (BadMap) instead of
  UNNAMED crashes or SILENT wrong-tile validation (DEAD transitions).
  One choke point, five callers, suite 1004→1011, zero data trips.
  Review's integral-float nit banked BY DESIGN ([2.0, 1] now illegal;
  importer refuses non-Integer at import — `tools/import_ldtk.rb:288`).
  Ticket: `drafts/_s32-tile-shape-20260821.md`.
- **Frame-tail lag row: ADJUDICATED CLOSES** (s32, pre-declared class 1
  on the decisive seat): Junior's re-measure — draw p95 15.3→8.5 ms,
  over20 16.8%→5.15%, feel "muito mais leve"
  (`drafts/_junior-remeasure-20260821.md`). Docket row CLOSED. Leftover
  named, UNBUILT: motif-strip authoring (DATA-class v19 candidate, owner
  word + own Rule-2 gate + wall); texture/macro composition DEAD
  (float-camera law). Caveat on record: over100 3→9 isolated spikes
  (zone-load/window-focus suspected) — NOT the continuous tail; watch,
  don't chase.
- **THE FIRST HUMAN GATE CROSSING LANDED** (Junior, HIS solo live save,
  `8478cde`): crossed low_quay→zone_7, walked TOWN 1, descended the T4
  cluster, returned, clean quit — matches the s30 pin (evidence class
  named in `drafts/_s32-live-harvest-20260821.md`). Verbatim banked:
  "me senti em uma nova fase e com meu objetivo concluido". His
  T4-subgraph question CLASSIFIED conformant (INERT law = no DIRECT
  live-six edges; the cluster hangs off the town BY DESIGN, D12).
  **The SHARED save on THIS machine is UNMOVED** (md5 `98fe75ed…`) —
  Gabriel's/coop's own crossing is still ahead; its verbatims are still
  fun-lane gold when they land.
- **world.rb sits at EXACTLY 1800/1800 — ZERO headroom.** ANY net
  growth fails `line_caps_test`; the next material world.rb touch owes
  its own extraction (Crossing precedent, s31). J1 below deliberately
  touches Core only.
- **Owner-pending (never nag):** ear-checks + audio-v12 batch · T3
  footstep/bed renders (frozen cue-spec mail, audio-seat inbox) · coop
  S1 invite (BOTH seats re-declared READY; Junior's seat has the gate
  crossed) · the v19 brainstorm.
- **R-A2 measure (silent, NEVER prime):** still `bought=0`/absent on
  every banked live-world HUMAN log (Junior's crossing bank quotes no
  sustain line — gap named in the s32 harvest). Harvest `TELEMETRY
  sustain` from every NEW human launcher log; never mention
  economy/provisions to the owners.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: origin/main tip = the s33 spark commit — `git log
--oneline -3` reads: that commit · `2163cb3` s32 docs · `38ae352` s32
feat. Save `saves/world.json` md5 `98fe75edb6d72deab18cd48eaa88bdaf`
mtime 08-20 15:51 (banked=7 provisions=0 seals=2 sessions=13
boss_1_defeats=1 home=camp) · launcher logs **40×2** (`$TEMP` + `/tmp`,
pattern `game_two_session_*.log`; newest 08-21 01:39 — already counted)
· seat mail at `~/.pi/agent/mail/`: `game-two/` inbox EMPTY done/=22,
`game-two-audio/` holds the unanswered T3 cue-spec (FROZEN — never
reply-ask) · tmp/soak newest `20260820-232208` · untracked
`drafts/_refs/` only · `tmp/pilot_walk/world.json` = local scratch
world, **sessions=1 banked=20 home=zone_7** (s32-corrected numbers;
never delete, never play it) · coop S1 evidence dir
`drafts/_lag-t2-evidence/` = README only. Known ignored leftovers (NOT
deltas): `captures/map/world_41a9683d_*.png` · `tmp/s30_review_out.txt`
· `tmp/s31_diff.txt` · `tmp/s31_review_prompt.txt` ·
`tmp/s31_review_out.txt` · `tmp/s32_review_prompt.txt` ·
`tmp/s32_review_out.txt`.
EXPECTED deltas (classify, then proceed): **live save moved + sessions
up** = the owners walked THE SHARED SAVE (J2 harvest — the first
crossing on THIS world; verify digest chain log↔save; arrival [2,14] /
return [43,19] per the s30 pin; their verbatims = fun-lane gold, bank
QUOTES) · new human launcher logs (harvest silently) · **Junior
commits/bank** (coop S1 report, new findings, or ear-check relays —
read BEFORE rebasing; re-verify cited hashes after any rebase; s32 hit
this live: harvest banks landed mid-session, patch-id proved the
reviewed diff survived the rebase) · audio receipt / assets re-pin mail
(archive, never reply-ask) · coop S1 artifacts (override lane below).
`git pull --ff-only` FIRST. Single-instance guard before any launch
(separate call, judged by printed output). `--fresh` NEVER.

**Ordering law for this session:** deltas landed (owner play or Junior
bank) → J2/J3 harvest+adjudicate FIRST (docs-only, fast), THEN J1. If
a harvested log shows a crossing landed WRONG (bad tile, wrong zone,
crash at a gate), J1 pivots to that defect's reproduction lane
(classify + owner chat) before any hardening. No deltas → J1 → docs
close, stop.

## GATE 0 OVERRIDE — any live owner order preempts this whole queue

- **Ear-check verdicts** = LAW: bank verbatim, route per the checkpoint
  questions; stinger-overlap failure → depth-aware-duck grilled in
  game-two-audio, never a data tweak here.
- **T3/T4 renders landing** → sha-pinned fixture conversion (v1.1
  pattern) + data-only rows + suite + noDevice re-walk + one es-CR
  ear-check line. Water family needs a NEW cue-spec mail — the sent one
  is FROZEN.
- **A live coop session = lag segment S1**: support + harvest verbatim
  per `drafts/_lag-t2-evidence/README.md`; BOTH seats at the s33 tip
  first (mixed builds refuse NAMED; exit 2 = auto-rehost/rejoin).
- **The v19 brainstorm opening in chat** = it takes over as THE session:
  facilitate from the intake docket + the prep doc (pointers only; the
  dev proposes LIVE with touchstones, `hub kb query --domain
  game-research`), ONE candidate at a time, owners decide, one recorded
  line each; output `drafts/_v19-foundation-<date>.md` + AGENTS rewrite
  only after BOTH ratify. NO CODE in the brainstorm.

## J1 — seal `opens` law: ill-shaped or transition-less `opens` refuses NAMED at zone load (the session's one code deliverable)

**Provenance:** the scope refusal RECORDED twice — s31 ticket
(`drafts/_s31-edge-validation-20260821.md`) and s32 ticket
(`drafts/_s32-tile-shape-20260821.md` §Scope refusals) — now promoted
as the next named-refusal extension. Same doctrine class: lane-3 engine
hardening (strict-importer / named-refusal law), NOT v19-class, NOT
sim-class — any legal zone keeps byte-identical behavior; only illegal
DATA changes outcome. State that classification in the ticket.

**Problem it kills (grounded at s32 close — verified pointers):**
`opens` is consumed BLIND everywhere downstream of load:
- `world.rb:1321-1337` `interact_seal`: `opens = station[:opens]` →
  `breached?(@zone_name, opens)` → `spend_banked` → `restore_breach!
  (@zone_name, opens)` — an ill-shaped `opens` (nil / string pair /
  float / 3-element) or a legal-shaped `opens` naming NO transition
  burns the toll, opens NOTHING visible, and PERSISTS the corrupt/inert
  fact into the save at clean quit. The exact silent-corruption family
  s31/s32 killed — this one even costs the player banked earnings.
- `price_sheet.rb:29` reads `station[:opens]` pre-breach (price display
  path) — nil flows into the breached callback.
- The SAVE side is already guarded (`save_state.rb:173-176`:
  breached-vs-opens cross-check at restore) — the ZONE side (load time)
  is the missing half. My s32 tile-shape guard does NOT cover `opens`
  (it never routes through `check_passable!`).
- **Shipped data pre-scanned LEGAL at s32 close (3/3):** district
  [41,13]→[42,13] · district_two [41,13]→[42,13] · zone_7
  [31,14]→[33,14] — every seal's `opens` names a transition `at` in its
  own zone (probe output banked in the s33 spark commit message's
  session; re-run it in-session before writing code — data may have
  moved).

**Shape (verify each pointer live before building):**
- In `TileMap#validate!` (or a private helper beside `validate_v2!`),
  for each station with `type == "seal"`: `opens` must pass the s32
  tile-shape idiom (Array, length 2, both Integers — reuse/extract the
  predicate, don't duplicate drift) AND `transition_at(*opens)` must be
  non-nil, else `raise BadMap` with a message carrying the seal's `at`,
  the `opens` value, and what was expected. Also check `opens` is
  IN-BOUNDS before transition lookup (transition_at returns nil out of
  bounds — the named message should distinguish shape vs
  no-transition; dev's call on wording, message-asserted either way).
- **Class is BadMap** (tile_map.rb house style). Layer: Core only —
  TileMap owns zone-local invariants; world.rb NEVER touched
  (1800/1800).
- **Probe the importer** (read-only): does `tools/import_ldtk.rb`
  validate seal `opens` today? NAME the finding in the ticket either
  way (importer refusing ≠ engine guard unowed — hand-edited live
  zones bypass the importer; s31 precedent).
- **OUT of scope (RECORD if tempted, never build):** runtime
  `interact_seal` changes (this is a LOAD-time law) · breach/save
  semantics (restore side already guarded) · non-seal station semantics
  · importer changes (record follow-up if it lacks the check) ·
  world.rb.
- **Tests:** read `test/core/tile_map_test.rb` FIRST (the s32 block is
  the idiom). Message-asserted, one per mode at minimum: nil opens ·
  string pair · float pair · 3-element · legal-shape-but-NO-transition
  (the semantic kill — the seal that eats tolls) · out-of-bounds opens
  · a legal control (seal opening a real transition tile — the
  stations fixture family). **Blast-radius honesty: unlike s32,
  FIXTURES MAY TRIP** — seal-bearing fixtures live outside
  tile_map_test (world/save/price/breach tests were NOT pre-scanned at
  s32 close). Pre-scan them with a throwaway script BEFORE the guard
  lands (temp file, run, delete); a tripping fixture = fix the fixture
  in the same commit ONLY if the fixture was ill-authored (name each in
  the ticket); a fixture that legitimately needs a transition-less
  seal = STOP, reread the invariant, bring it to the ticket honestly.
  If REAL `data/zones/*.json` trips (contradicting the 3/3 pre-scan) →
  **DEFECT BRANCH** below.

**Laws:** no mocks (AGENTS #5) · minitest · read-before-edit every file
touched · ONE one-concern commit (`feat: refuse seals whose opens names
no transition at zone load` or similar) · hooks run the suite. **No
Rule-2 gate owed** — load-time refusal, zero visual surface moves;
state that in the commit body. **Fresh-eyes review owed (Rule 6):**
scrubbed read-only pi sub-session over the diff before push
(`env -u PI_CODING_AGENT -u PI_SESSION_FILE -u PI_SESSION_ID pi -p
--no-session`); its prompt forbids touching ANYTHING, including seat
mail ("touch NOTHING, including seat mail"); audit the mail dir after
it runs (s29 lesson; s31/s32 ran clean under the same wording). Then
push. If the remote moved: read incoming FIRST, rebase, verify
patch-id equality of the reviewed commit (s32 precedent), THEN push.

**DEFECT BRANCH (the named paid-call exception):** if the guard
refuses any REAL zone — STOP. The refusal line IS the reproduction;
bank it verbatim. Classify in writing (authoring defect / importer gap /
engine seam), write the blast radius, PUT IT IN THE OWNER'S CHAT before
fixing. A fix that moves live-zone bytes owes the affected wall
scripts' Rule-2 gates (possibly the full 21-script wall — DETACHED,
never under a bash-call timeout) + fresh-eyes. Never hotfix in the same
breath as discovery.

**Ticket note either way:** `drafts/_s33-seal-opens-<date>.md` —
provenance (the two recorded refusals + the s32-close 3/3 probe),
classification paragraph, importer finding, fixture pre-scan results,
what shipped, suite counts before/after, review receipt (verdict +
findings banked in the note; tmp/ is ephemeral), scope refusals
recorded.

## J2 — IF the owners walked the SHARED save: harvest (ZERO code by default)

Every NEW human launcher log → ONE note
`drafts/_s33-live-harvest-<date>.md`: log md5 + `TELEMETRY persist`
(digest chain + sessions; chain-anchor against the save) · **the
crossing check** (their earned `boss_1_defeats: 1` opens low_quay
[44,19] → zone_7 [2,14]; return [1,14] → [43,19] free — a mismatch is a
live defect, classify + owner chat) · `TELEMETRY varekka`
(dread-exposure row — now has Junior's baseline to compare against) ·
`TELEMETRY sustain` (R-A2, silent) · `AUDIO ambience` keys (amb_town on
zone_7 is the live signature) · `AUDIO drift` · any `frame_probe`
(over100-spike watch: Junior's caveat row). Their chat verbatims about
the gate moment = fun-lane evidence, QUOTES not summaries. Watch items
(route, never patch): wayfinding to the corner · ambush-feel at return
[43,19] (Junior's verdict was "tenso mas de boa" — a second seat's read
either corroborates or splits it; still owner-order-only to change) ·
wipe near the gate · toll/economy wishes (bank silently) · TOWN 1
emptiness reactions (feeds the docket's TOWN 1 v0 row).

## J3 — IF Junior banked again: harvest (his lane, never edit)

Likely classes: coop S1 report (route per the override lane +
`drafts/_lag-t2-evidence/README.md`) · new solo findings (docket rows,
one-line pointers) · ear-check relays from Gabriel's session (bank
verbatim, route per checkpoint questions) · over100-spike data (the s32
caveat row — if he banks a probe with period max spikes, attach to that
row; NO fix-stacking without a mechanical A/B first, s29 law).

## J4 — docs close

Checkpoint entry (s33) · docket rows where verdicts landed (pointers) ·
AGENTS one-liner ONLY if a verdict landed · commit docs + push. Honest
no-delta lines for whatever stayed pending. Author the s34 spark at
close (house pattern), commit it, clipboard it for the owner via the
verified CF_UNICODETEXT recipe (python ctypes, restype/argtypes pinned,
read-back vs SOURCE string).

## Laws that bite (short list)

- Deterministic gates decide; failed gate/critique BLOCKS ship — never
  downgraded. Presentation never mutates sim; audio pure sink; replays
  deterministic by tick count.
- Read-before-edit · one-concern commits, explicit paths (never `git
  add -A`) · hooks run the suite (~60 s/commit; pre-push re-runs) ·
  long jobs DETACHED (nohup + poll; NEVER under a bash-call timeout) ·
  JSON edits surgical · multi-line scripts in temp files, never inline
  heredocs.
- **world.rb = 1800/1800.** Any net growth fails line_caps_test —
  extract-on-touch into a plain object (Crossing precedent, s31) or
  don't touch it.
- Two instances fork a save — guard EVERY launch in a separate call
  judged by printed output; re-guard after Start-Process (~40 s ruby
  lag). Freeze ALL code/data edits while any sweep/replay runs.
- Junior's lane (`drafts/_junior-*`, docs/JUNIOR.md) is his — harvest,
  never edit. Owner overrides are law — one line, never re-litigated.
  No lore; placeholders only; es-CR everyday words in owner lines.
- Cross-repo: read siblings freely, write ONLY via seat mail at
  `~/.pi/agent/mail/<repo>/` (+ RECEIPT lines); never write into a held
  seat. Sub-agent prompts forbid seat-mail handling; audit the mail dir
  after every sub-agent run.
- Verbatim means verbatim; partial evidence = bank + name the gap.
  A rebase over a peer's push rewrites local hashes — re-verify after
  (patch-id, s32 precedent).
- **Sim-class anything** (tile behaviors, spawn logic, AI, balance,
  economy) → RECORDED for v19, refuse in writing. The return-spawn tile
  candidate did NOT fire (Junior's verdict); it re-arms only on a
  second seat's contrary read + owner order.

## Budget + stop conditions

One attended session ~2-3 h. Council 0. Paid calls 0 — the ONLY
exceptions are J1's defect branch and an override lane shipping a gated
change. Sub-agents: the fresh-eyes reviewer for J1's commit (required),
nothing else. Context guard 85% → compact-checkpoint skill.
**Stop when:** J1 shipped + reviewed + pushed (or its defect branch
banked + parked in the owner's chat), any landed deltas adjudicated/
harvested, checkpoint + push. **Stop early, honestly, if:** the owners
redirect (their word routes) · the brainstorm opens (it takes over) ·
any gate fails non-mechanically (STOP, classify in writing) · a
sim-class ask arrives without the brainstorm (refuse in writing, RECORD
for v19).
