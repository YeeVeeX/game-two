## Fresh-eyes review — E1c "the ten red pins" (a18079c, worktree detached)

**Method note.** The seat-lease gate blocked every `ruby <probe>.rb` invocation (its test-runner heuristic, cwd inside game-two, seat held by pid 3132). Per doctrine I did not route around it. Instead I verified the EVENT streams from the gate logs the session itself produced on MAIN (`tmp/e1c/regate_{a..d}.out`, the v22-e1 sweep logs in `tmp/wall/`), reproduced the pixel claims with Python on the sweep PNGs, and read every cited code/data path. Where a claim rests only on an unrunnable probe I say so.

### 1. "Never relax a row to green it"

- `impact_fx_reads` — byte-identical to `origin/junior/premium-build` (md5 `68a92009…` both sides). Numbers match MAIN's `src/app/fx.rb:16-19` (SPARK 12 / BURST 22 / DUST 14 / NUM 40). The clause keeps the failable sentence ("a frame showing a struck or dying body with no spark or burst anywhere fails") and adds an age gate. The age gate has an independent cue: hurt flash = 8 frames (`creature.rb:331`), inside the 12-frame star life, so a broken star at age ≤7 still produces flash + numeral + no star = FAIL. Weakness: "chips still flying" is circular (chips ARE the spark). Not a relaxation.
- `lobber_reach_reads` — correctly says brackets come only from `world.impacts` (`renderer.rb:248-263`: VOLLEY_EDGE (245,125,35) frames + VOLLEY_CORE core sized by `frames_left/delay` → shrinks) and that filled squares are `draw_attack` (`renderer.rb:1316-1341`: WINDUP/SLASH white, SPECIAL_* amber/pale-gold). **But the new phrase "laid in a straight line 2-4 tiles out" contradicts the row's own condition**: at LEVEL 5–7 the chain is offsets 2..5 (four brackets) and at LEVEL ≥8 it is 2..6 (FIVE brackets) — `data/balance/progression.json` `spell_growth {"5":[2,3,4,5],"8":[2,3,4,5,6]}`, applied by `progression.rb:81-87`. The pre-existing "FOUR contiguous … 5 or higher" was already wrong for ≥8 (tower4_run starts at LEVEL 14); this diff touched the row and hardened the wrong geometry. Fails safe (false red, not false green), but §2.8's "recalibrated (what the code draws)" is false in that phrase. → **B1**.
- Affirmative re-gates prove sensitivity to presence, not failure on absence. Text analysis says both rows remain failable; a one-off negative control (`fx_enabled:false` → `impact_fx_reads` must FAIL) would close it. Minor.

### 2. Verbatim takes
Both scripts are byte-identical to Junior's blobs (`1f941600…`, `5c033e2e…`). They were verified on MAIN by the gate itself: `regate_a.out` (head=f609c31, main clone) shows `actor_died frame=148 husk0 killer=blocker`, `drop_spawned frame=148 tile=[2,6]`, and for basement `drop_picked_up frame=922 actor=blocker`. The toll_pocket vision PASS and the basement `low_hp_pulse_reads` FAIL are MAIN's renderer's verdicts — no laundering. Legitimate move. Side-effect the record names correctly-but-softly: no wall script now declares `seal_breached`, and no v22-e1 sweep log contains a `seal_breached` event — the breach beat (TOLL PAID, slab→gold) has zero coverage wall-wide (already true since the LDtk import; the take makes it honest). The SEALED half IS staged (a-gate: "dark slab, thin gold seam, station block, legible -40 price").

### 3. Re-cuts
- taunt_anchor (regate_b): `special_started 470` → `taunted 482 victims=3` → `possession_changed 600 blocker→lobber` → `actor_died 608 rusher16 killer=blocker` → `human_retargeted 1004 hater24→lobber cause=hate` → `actor_died 1182 hater24 killer=blocker` → `fight_resolved 1362`. All five `_doc` facts check. Cast at 470 sits between right taps 458/478 ✓. Pulse 20 f (`combat.json:80`) → 496/500 = 70%/90% ✓. Old reel lost nothing: 978/982 (swap straddle) is replaced by 530→604 and the critic passed `possession_ring_moves` on exactly those; old reel's 1363/1367 staged nothing (its only `fight_resolved` was f830).
- aoe_specials (regate_b/c): `special_started 940`, `taunted 952 victims=1`, `human_retargeted 953 hater24→blocker cause=challenged`, dies 1030, `special_started 1275 attacker=striker` ✓; 958/964 = 30%/60% of the 20-f pulse ✓; old sweep: hater24 died f1062, cast 1157, victims=0 ✓.
- floor1_run (regate_c): hit `1249 striker→rusher15`, capture 1253 (age 4 = widest arms, `len=16`), death 1284, capture 1287 (age 3) ✓. Sweep frame 0700: three hits at 675 on pack, zero hits on rusher12 before 700, and NO sweep capture within 12 f of any landed hit (recomputed from the log) ✓. corpse_run 3062: hit 3036 on the LOBBER by rusher5, zero hits on rusher5 ✓.

### 4. Convictions
- zone8 blue lines — PROVEN independently: frame_0570 rows 411–412 / 475–476 and frame_1067 rows 222–223 / 286–287 are B−R ≈ +30 over x 320..767 only (neighbour rows ≈ 0); 64-px spacing = tile rows 24/26; cam y 357/546. `dungeon_1.json` decor `edge [10,24] w14` + `[10,26]`, `decor_rects` → `[x,y,w,2]` = x 320, y 768/832, w 448 ✓. **Routing disagreement:** the rows live in `authoring/dungeon_1.sidecar.json` (not `pilot.ldtk`); decor is render-only ("never blocking"), so this is a SAFE, verified data fix within the flywheel law — a sidecar edit + `tools/import_ldtk.rb` re-import, light and reversible. Parking it on WB-T7 keeps zone8 FAILED for no sim reason; the only cost is the data/ STALE flip T2c absorbs anyway.
- zone8 star — PROVEN: warm-arm columns 271–273 (16–19 px), rows 329–331 (20 px), centred (272,330) on the pale square x 264–279 / y 322–337. Correctly routed (fx.rb is his premium surface).
- floor2 coral — data-proven: `%`=wall_inner ×249 → `coral_wall.png`, palette wall (118,66,38) vs wall_inner (150,74,28), both tinted ×1.18 — same hue family. Design/palette decision → E4, correctly routed. (Note MAP_EDITING.md line 28 still lists district_two as hand-authored while `pilot_authoring_test.rb:14` byte-compares it — the doc is stale.)
- grass_fixture vessel label — recomputed 1.08:1 (record 1.04; methodology delta); even solid (158,52,30) at α200 over the band is 1.8:1, pure vs backing 2.81:1 — the colour cannot reach a small-text floor. Correctly routed (ratified colour, his file). Record nit: the vessel label draws at `text_a`, not `text_a − 40` (`controls_overlay.rb:112-116`).
- basement low_hp strip tint — code-proven: strip at z 0 (`controls_overlay.rb`), vignette bottom band at z 17 (`light.rb:146`), HUD plate z 19. His `65f52e5` spans controls_overlay+light+display with 4 gates owed → routing to his merge is defensible.

### 5. Pins ledger — the most consequential defect
Every E1c gate ran on a DIRTY tree and was committed afterwards; `regate.sh` stamps `HEAD7=$(git rev-parse HEAD)`. Result: all 8 pins cite a commit whose tree lacks the judged content — `toll_pocket/basement_pocket @ f609c31` (scripts landed 1aee2bc 15:41:43, pin 15:40:43), `aoe_specials/taunt_anchor/floor1_run @ 1aee2bc` (scripts landed 6289981/22c3308), `lobber_reach/tower4_run @ 22c3308` (row landed a18079c). `git show f609c31:harness/scripts/toll_pocket.json` still declares `seal_breached 1 / actor_died 8` — that tree FAILS the manifest the pin says passed; `rake pins` prints `PINNED toll_pocket f609c31 current`. This is the inverse of the class 5a39817 fixed ("a sweep pins the tree it JUDGED"). Mechanically harmless today (no src/data commits in between), documentarily false, and the process will repeat. → **B2**. The aoe b pin (hollow) is superseded by c 16 min later, `rake pins` reads latest — disclosed in the record, minor. basement_pocket's red pin (gate_rc=1/manifest_rc=0) is honest.

### 6–7. Sim untouched / scope
`git diff f609c31..HEAD --name-only` = 9 files, none under `src/` or `data/` (pilot.ldtk untouched). Junior's `lobber_reach_reads` row == old main, so HEAD's edit merges cleanly; `gate_checks.json` differs from his branch only by his other 8 lines. Nothing from T2a/T2c/WB/E4 landed. Claimed-but-not-in-diff: "CYCLE.md updated (L16 CLOSED)" — the edit sits uncommitted in the main clone (`M docs/CYCLE.md`), absent from the reviewed range.

### 8. What was missed
B1 (row geometry vs `progression.json`) and B2 (pin provenance). Smaller: toll_pocket `_doc` ¶1 narrates `down f200 / left f205 / interact f220` inputs that are not in the file; basement `_doc` says 923/945 show pickup sparks, but 945 is age 23 > BURST_FRAMES 22 (gleam pruned; only the +1 numeral survives) and the a-gate critic read `pickup_gleam_reads` as "not exercised" — the intended affirmative stager did not land on main.

**Bottom line:** the ten-row verdict table, the verbatim takes, the re-cuts and the convictions are all verified against MAIN's own evidence; nothing in the sim or renderer moved. The block is narrow and cheap: fix one gate-row phrase (+ re-gate the affirmative member, the session's own precedent) and make the pins' provenance honest before pushing.

{"verdict": "BLOCK",
 "blockers": [
  {"id": "B1", "claim": "The recalibrated lobber_reach_reads row describes geometry the code does not draw at the row's own condition: 'laid in a straight line 2-4 tiles out' while LEVEL 5-7 volleys occupy offsets 2..5 (four brackets) and LEVEL >=8 volleys occupy 2..6 (FIVE brackets); the pre-existing 'FOUR contiguous ... 5 or higher' is also wrong for >=8 (tower4_run starts at LEVEL 14). Introduced/hardened by a18079c; record §2.8 calls it 'what the code draws'. Fails safe (false red), but it is a factual error in a ship-gate row and violates the prose-number law.", "evidence": "harness/gate_checks.json lobber_reach_reads (HEAD); data/balance/progression.json spell_growth lobber {\"5\":[2,3,4,5],\"8\":[2,3,4,5,6]}; src/game/progression.rb:81-87 (highest threshold <= level wins); harness/scripts/tower4_run.json start.progression.level=14; regate_d.out line 563 critic passed by counting four brackets, not by the 2-4 phrase", "fix": "Reword: 'at LEVEL 5-7 four brackets at 2..5 tiles out; at LEVEL 8+ five at 2..6 (data/balance/progression.json spell_growth is the source)'; drop '2-4 tiles out'; re-gate lobber_reach detached (the session's own precedent after a row edit) and re-pin; tower4_run needs no re-gate (row still resolves to 'not exercised')."},
  {"id": "B2", "claim": "All 8 v22-e1c pins cite a commit whose tree does NOT contain the judged content: every gate ran on a dirty working tree and the edits were committed afterwards, so pins.json says toll_pocket/basement_pocket passed at f609c31 (whose toll_pocket.json declares seal_breached 1 / actor_died 8 and FAILS the manifest), aoe_specials/taunt_anchor/floor1_run at 1aee2bc (old scripts, cast f1157), lobber_reach/tower4_run at 22c3308 (old row). `rake pins` prints 'PINNED toll_pocket f609c31 current'. Inverse of the class 5a39817 fixed ('a sweep pins the tree it JUDGED'); the record does not disclose it.", "evidence": "harness/pins.json rows dated 21:40:43Z..22:26:06Z vs `git log --format='%h %cI' f609c31^..HEAD` (1aee2bc 15:41:43-06, 6289981 16:11:13, 22c3308 16:13:10, a18079c 16:28:29); tmp/e1c/regate.sh HEAD7=\"$(git rev-parse --short=7 HEAD)\"; `git show f609c31:harness/scripts/toll_pocket.json` manifest; `git log f609c31^..HEAD -- harness/scripts/aoe_specials.json` -> 6289981; tmp/e1c/pins_close.txt line 34", "fix": "Documentary: add a §3 line in the record mapping each pin to its content commit (toll_pocket/basement_pocket -> 1aee2bc; aoe/taunt -> 6289981; floor1_run -> 22c3308; lobber_reach/tower4_run -> a18079c) and mirror it in CYCLE's pins note. Process: regate.sh (and run_wall.sh) refuse to record a pin, or stamp commit '<head>+dirty', when `git status --porcelain -- harness src data` is non-empty; or adopt commit-then-gate order. No re-gate needed."}
 ],
 "majors": [],
 "minors": [
  {"id": "m1", "claim": "No negative control for either recalibrated row: the affirmative gates prove the critic sees a present star/chain, not that the rows still FAIL when the surface is absent. Text analysis says they remain failable (hurt flash 8 f < star 12 f; 'no spark or burst anywhere fails' retained; FOUR-tile demand retained), but 'chips still flying' is a circular age cue.", "evidence": "src/app/fx.rb:16 SPARK_FRAMES=12; src/game/creature.rb:331 @hurt_frames=8; gate_checks.json impact_fx_reads", "fix": "One detached negative-control gate with display fx_enabled:false on floor1_run expecting impact_fx_reads FAIL (do not pin it); add 'numeral still low (<9 px risen) and full-bright' as a second independent age cue."},
  {"id": "m2", "claim": "Record claims 'L16 debt CLOSED (CYCLE.md updated)' but the reviewed range f609c31..a18079c contains no docs/ change; the edit is uncommitted in the main clone.", "evidence": "`git diff f609c31..HEAD --name-only` (9 files, no docs/); main clone `git status` shows M docs/CYCLE.md, M docs/CHECKPOINT.md", "fix": "Land it in the close commit; the record's claim is ahead of the tree until then."},
  {"id": "m3", "claim": "toll_pocket.json _doc ¶1 narrates `down f200 + left f205 + interact f220` inputs that are not in the file (the correction ¶ implies removal but never says so); basement_pocket.json _doc says captures 923/945 show pickup sparks, but 945 is age 23 > BURST_FRAMES 22 (gleam pruned; only the +1 numeral survives) and MAIN's critic read pickup_gleam_reads as 'not exercised' — the intended affirmative stager did not land.", "evidence": "harness/scripts/toll_pocket.json hold keys: attack/down[[960,988],[1200,1204]]/swap/interact[545,1250,1290,1330]/right[20]/left[40]; src/app/fx.rb:117 bursts pruned at age>=22; regate_a.out line 270 '[PASS] pickup_gleam_reads: ... no pickup beat lands; not exercised'", "fix": "Both files are Junior's verbatim (byte-identity law): route the two _doc corrections to his branch by mail, then re-take; note in the record that basement's pickup read is still hollow on main."},
  {"id": "m4", "claim": "'seal_breach_reads has NO live stager' is overstated: the SEALED half is staged and passed affirmatively in toll_pocket; the BREACH beat (TOLL PAID, slab->gold) has zero coverage wall-wide — no wall manifest declares seal_breached and no v22-e1 sweep log contains a seal_breached event.", "evidence": "regate_a.out toll_pocket '[PASS] seal_breach_reads: dark slab with thin gold seam, station block, legible -40 price'; `grep -l '\"seal_breached\"' harness/scripts/*.json` -> none; `grep -l 'EVENT seal_breached' tmp/wall/*_v22-e1.log` -> none", "fix": "Record the breach-beat coverage hole as a named ticket (re-author or a new basement_2 stager), not only a §6 line."},
  {"id": "m5", "claim": "Record §2.7 says label alpha `text_a - 40`; the vessel label draws at text_a (200) — the -40 applies to action labels only. Contrast recomputed 1.08:1 vs the record's 1.04 (methodology delta, same verdict).", "evidence": "src/app/controls_overlay.rb:112-116; Python on captures/grass_fixture_walk_gate_a/frame_0150.png: band median (36,35,39), vessel px mean (67,31,26) -> 1.08:1; solid colour over band 1.80:1; pure (158,52,30) vs (10,8,12) 2.81:1", "fix": "One-word correction in the record."},
  {"id": "m6", "claim": "aoe_specials v22-e1c-b pin is a green row for a version the author himself calls a hollow pass (single capture 964); superseded by v22-e1c-c 16 min later and `rake pins` reads the latest by date.", "evidence": "harness/pins.json aoe_specials 21:48:37Z then 22:04:12Z; harness/pins.rb report uses max_by date; record §2.3 discloses the hollowness", "fix": "None required (append-only ledger + disclosure); the B2 provenance line covers it."},
  {"id": "m7", "claim": "docs/MAP_EDITING.md line 28 still lists district_two as hand-authored JSON while test/tools/pilot_authoring_test.rb:14 byte-compares it as an importer emission; the record's E4 routing (sidecar) follows the test, correctly.", "evidence": "docs/MAP_EDITING.md:26-28; test/tools/pilot_authoring_test.rb:14", "fix": "Doc line update (WB lane)."}
 ],
 "verified_clean": [
  "No src/ or data/ change in f609c31..HEAD (9 files: drafts x2, harness/gate_checks.json, harness/pins.json, 5 scripts); pilot.ldtk untouched",
  "origin/main (f609c31) is an ancestor of origin/junior/premium-build; 65 commits ahead, 0 behind; all 10 cited Junior commits exist on his branch",
  "basement_pocket.json and toll_pocket.json byte-identical to origin/junior/premium-build (md5 1f941600.../5c033e2e...); impact_fx_reads line md5-identical; Junior's lobber_reach_reads row == old main so HEAD's edit merges cleanly",
  "Verbatim takes verified on MAIN by the gate itself (regate_a.out head=f609c31): husk0 dies f148 drop [2,6]; basement drop_picked_up f922; toll_pocket pin came from MAIN's renderer, not Junior's",
  "impact_fx_reads numbers match MAIN fx.rb (SPARK 12 / BURST 22 / DUST 14 / NUM 40; spark at v.x+14,v.y+10 z 7; :taken (240,70,60); dust 3/2/2 px at <=110 alpha); wounded bar condition renderer.rb:1141 exact",
  "draw_impacts draws brackets only from world.impacts (renderer.rb:248-263, VOLLEY_EDGE orange frame + shrinking pale core); draw_attack draws filled white/amber squares; special_started is emitted for any creature (creature.rb:673) so tower4_run's zero special_started in the sweep log = zero volleys",
  "taunt_anchor re-cut events all reproduce in regate_b.out (470/482 victims=3/600 swap/608 rusher16 killer=blocker/1004 hate/1182/1362); cast between right taps 458/478; pulse 20 f so 496/500 = 70%/90%; old reel staged nothing the new one lacks",
  "aoe_specials re-cut events reproduce (940/952 victims=1/953 challenged/1030/1275 striker special); 958/964 = 30%/60% of pulse; old sweep: hater24 died f1062, cast 1157 victims=0",
  "floor1_run: hit 1249 on rusher15, capture 1253 (age 4 = len 16 widest), death 1284, capture 1287; sweep frame 0700 hits at 675 all on pack, no hit on rusher12 before 700; no sweep capture within 12 f of any landed hit (recomputed from the log)",
  "corpse_run frame_3062: hit f3036 by rusher5 on the LOBBER (pack), zero hits on rusher5 in the reel -> critic misread confirmed",
  "zone8 seams reproduced from the PNGs: 2-px rows 411-412/475-476 (f570) and 222-223/286-287 (f1067), B-R ~ +30 over x 320..767 only; dungeon_1 decor edge [10,24]/[10,26] w14 rgb(70,110,190) a70; decor_rects edge -> [x,y,w,2] = x320 y768/832 w448",
  "zone8 frame_0413 star reproduced: warm arms cols 271-273 (16-19 px) / rows 329-331 (20 px) centred (272,330) on pale square 264-279 x 322-337",
  "floor2: % = wall_inner x249 -> coral_wall.png, wall -> rock_wall.png, palette (118,66,38) vs (150,74,28), tint gain 1.18 — one hue family; district_two palette lives in authoring/district_two.sidecar.json",
  "grass_fixture: vessel label is the leftmost strip item (x_start 32), VESSEL_RGB blocker (158,52,30), GLYPH (225,218,205), BACKING (10,8,12); measured 1.08:1",
  "basement low_hp: strip at z 0 (controls_overlay.rb), vignette bottom band z 17 (light.rb:146, bh=0.28h covers the 28-px strip), HUD plate z 19; Junior's 65f52e5 spans 4 files with 4 gates owed",
  "Gate verdict quotes in the record match regate_a..d.out verbatim; determinism 8/9/11/12/7/12 byte-identical; MANIFEST lines match; zero 'critic transport retry' in all four regate logs; taunt_anchor b ran 15:48:37-15:53:16",
  "basement_pocket red pin (gate_rc=1, manifest_rc=0) recorded honestly 43 s after the regate summary; rake pins at close = 6 PINNED / 32 STALE / 5 FAILED as stated; world.rb 1782 lines; 43 wall scripts; 97 checklist rows parse",
  "pins.rb staleness watches only src/app, src/game, data — script/checklist edits never flip a pin (so 'current' is what the tool says, which is why B2 is documentary, not mechanical)"
 ],
 "disagreements_with_my_routing": [
  "zone8_crossing no_render_garbage: the fix is a SIDECAR edit (authoring/dungeon_1.sidecar.json carries the two blue edge rows and two blue stains), not a pilot.ldtk edit; decor is render-only ('never blocking'), so this is a verified, SAFE data fix inside the flywheel law and inside light/reversible autonomy — it should have been done here (edit sidecar -> tools/import_ldtk.rb -> copy -> suite byte-compare green) or as an immediate one-commit follow-up, accepting the data/ STALE flip that T2c re-pins anyway, instead of parking zone8 FAILED until WB-T7.",
  "The four other routings (fx star contrast, dust, vessel-label colour, low-hp strip z-order via 65f52e5, coral palette) are correctly placed on Junior's ratified surfaces / E4."
 ]}
