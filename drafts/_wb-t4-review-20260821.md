# WB T4 fresh-eyes review — receipt (2026-08-21, session 27)

Scrubbed read-only pi (`pi -p --no-session`, PI_* env scrubbed), prompt
`tmp/t4/review_prompt.md` (explicitly: touch NOTHING including seat
mail — the s26 lesson). Reviewing 3fdfae9..HEAD + ticket doc + spec §T4
+ banked evidence. Verbatim output below the rule.

**Adjudication (dev of record): VERDICT PASS, 9/9 laws CONFIRMED —
ship stands.** Defect 1 (cheap — my test comment claimed Ruby 0 falsy;
it is truthy and the loader refusal fires for real) fixed in place:
authored-zero refusal now pinned directly. Defects 2-4 are notes:
HUB_ZONES derivation-from-data queued for T5's touch; probe-coordinate
coupling accepted as pin behavior; quarantine sub-claim rests on
run_soak's own named-breach mechanism + banked before/after save md5.

---

All evidence collected. Final review:

---

**VERDICT: PASS**

**LAWS:**

- **L1 CONFIRMED** — `git diff --name-only 3fdfae9..HEAD -- data/zones/` lists only the 4 new pilot files; all 7 pre-existing zone files byte-untouched; `grep requires_defeats data/zones/` = zero hits (the boss gate ships in no zone file); pilot edges: zone_7→{dungeon_1, basement_1, basement_2}, basements→zone_7, dungeon_1→zone_7 (rope) — all in-cluster. Pinned by `test_pilot_and_fixture_zones_stay_inert` (test/core/tile_registry_test.rb:54), which asserts live→pilot empty, pilot→live empty, AND in-cluster (line 67).
- **L2 CONFIRMED** — check_transition early-returns only on `t[:type] == "rope_spot"` (src/game/world.rb:1120-1123); untyped live transitions flow to `Crossing#open?` (src/game/crossing.rb:24-28) which reproduces the v12 sealed check exactly (`requires_defeats` nil short-circuits); consent/arrival logic is move-for-move the old code (Chebyshev-1 vs world.rb:1555 `tile_distance`); interact's no-station path needs `type=="rope_spot"` — false in every untyped live zone. Banked proof: tmp/t4/evidence.log:2185-2205 — world_loop 10/10 OK + grass_fixture 8/8 OK against `wl_check.md5`/`gf_check.md5`, whose hashes match `tmp/t4/t4_world_loop_baseline.md5` (e.g. frame_0000 = `3d5de15f…` both sides), baselines mtime 21:57 vs first commit 22:06; spot gates ×3 rc=0 (lines 256, 2069, 2184).
- **L3 CONFIRMED** — save_state.rb absent from the diff (name-only grep = 0). `breached_refusal` (src/game/save_state.rb:162-180) validates tuples against seal stations' `opens` from zone DATA; zone_7's seal opens [33,14] (data/zones/zone_7.json:145-148), so `["zone_7",[33,14]]` is legal with zero schema change. Pinned by `test_the_pilot_save_facts_survive_the_strict_decoder` (test/game/pilot_loop_test.rb).
- **L4 CONFIRMED** — `way_locked?`/`water_drained?` are draw-time reads (src/app/renderer.rb:243-255); the swap is applied AFTER the memoized cache lookup (renderer.rb:275, map_artifact.rb:83-86); `TileVariants.rects` is a pure function of map+registry (src/app/tile_variants.rb:31-47), cache keyed on map only (renderer.rb:322); `passable?` unchanged — the `'#'` law (src/core/tile_map.rb:62-65); tile_map.rb comment even pins "passability never changes with state".
- **L5 CONFIRMED** — `test_committed_pilot_zones_are_the_importer_emission` (test/tools/pilot_authoring_test.rb) pins committed bytes == emission from authoring/pilot.ldtk (LF-normalized, `.gitattributes *.ldtk -text` present); the v2.1 diff (8082bb0) is purely additive whitelist (Transition +`requires_defeats`, SIDECAR_OPTIONAL +`water_drained_by`, level fields +`hub` with a NEW Bool refusal) — no refusal removed or weakened; emitted zones still boot through `Core::TileMap.new` inside the importer (tools/import_ldtk.rb:371-377), so loader refusals compose.
- **L6 CONFIRMED** — no cross-floor machinery anywhere in the diff (crossings remain instant `enter_zone` swaps); respawn/home code untouched (world.rb hunks are interact/check_transition/cross_through only). `hub:true` is data-only riding existing machinery: `hub` is read at world.rb:1195 (rehome-on-entry, camp/nest already `hub:true` in data) and save_state.rb:158 (data-driven hub law, no decoder change) — and since L1 proves the live graph can't reach zone_7, it cannot alter live-world behavior. Law-compliant camp precedent; the chain_check amendment is soak-checker-only.
- **L7 CONFIRMED** — new player-visible strings = 4 display-name keys ×3 locale files, identical/locale-invariant (ZONE 7, BASEMENT 1/2, DUNGEON 1); station line "TOLL PAID" is the existing placeholder verbatim; stamps reuse SEALED/OPEN; .ldtk identifiers all mechanical (zone_7, water, requires_defeats, town_1…); no fiction names in any grep of the diff.
- **L8 CONFIRMED** — drafts/_gate-verdicts.log: `20260820-223716 tmp\t4\map_critic` verdict FAIL with `map_pilot_well_reads: false` ("no distinct uniform ring or basin reads"), then `20260820-230703` PASS with the same check true. The fix in 7ab5612 is authored-data: `water_drained` [56,48,34]→[78,64,44] in BOTH sidecar and imported zone_7.json (provenance pin held); map_checks.json was EXTENDED (new check added, panels 6→11 to match reality) — the failing check's committed text stays demanding. One unavoidable caveat: the exact check text at FAIL-time was working-tree state, unrecoverable read-only, but the FAIL's cited wording matches the committed check.
- **L9 CONFIRMED** — diff-wide grep for lava/hazard/spawn_affinity/swim hits only the "swim stays reserved" comment; water passability "floor" in data/tiles.json, pinned live by tile_registry_test (`assert_equal "floor", reg.type("water")["passability"]`); no `hooks` key exists in tiles.json (grep rc=1); zone_7 `enemy_spawns: {}` (threat-free by data); dungeon combat uses existing kits as zone data, zero balance-file changes (no data/balance/ file in the diff stat).

**DEFECTS:**

1. **(cheap)** test/tools/import_ldtk_test.rb `test_requires_defeats_zero_refuses_via_loader` — the comment claims "0 is falsy at the pass-through, so it never lands in the emitted zone." False in Ruby (0 is truthy): an authored 0 DOES land via `t["requires_defeats"] = f["requires_defeats"] if f["requires_defeats"]` (tools/import_ldtk.rb:309) and refuses NAMED through the loader gate — stricter and better than documented, but the test pops the 0 case instead of asserting it. Fix: assert the 0 refusal directly and correct the comment.
2. **(note)** soak/chain_check.rb:25 `HUB_ZONES = %w[nest camp zone_7]` stays a hand-maintained list whose own comment says "re-derive from data/zones/*.json hub flags if the zone set ever changes" — a third hand-added entry is the trap firing in slow motion; derive it from data at T5 when this area is touched again.
3. **(note)** src/map_main.rb probe coordinates (32/33,14 · 5,5 · 15,9 · 3,16) duplicate authored zone_7/dungeon_1 geometry in code; an owner re-authoring in LDtk will fail probes NAMED at gate time — acceptable pin behavior, but the coupling is code-side, worth remembering.
4. **(note)** The soak's "38×2 launcher logs unmoved" sub-claim rests on run_soak's own named-failure mechanism + `soak_rc=0` (evidence.log:2228) rather than a directly banked count; the live-save half IS directly banked (tmp/t4/real_save_md5_before/after.txt identical `98fe75ed…`).

**ONE PARAGRAPH:** This is a clean T4 ship. The change-set does exactly what the spec's §Pilot content + D1–D12 demand and nothing it forbids: the live world is provably byte-stable (untouched zone files, banked pre-change baseline md5s re-verified in the gate captures, three spot gates green), the pilot cluster is mechanically INERT with a test that pins both directions of the graph, the well rides existing toll/breach machinery with zero save vocabulary, all render state is draw-time-only with one condition source shared by renderer and god-view, and the importer remained the only door — the committed zones are pinned byte-equal to the emission and the refusal set only grew. The map-gate FAIL→fix→PASS story checks out against the verdict log as an honest critic catch answered with authored data, not a relaxed check. The four defects are one cheap test-comment fix and three notes; none blocks. Ship stands; hand defect 1 to the dev of record for a follow-up commit. (Per my brief I ignored the seat-mail notice entirely — mail handling belongs to the seat-holding session.)
