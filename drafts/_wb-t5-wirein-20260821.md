# WB T5 — wire-in (ZONE 7 joins the LIVE world) + J1 frame-tail draw fix

Session 28 (hub), 2026-08-21. Spec:
`docs/superpowers/specs/2026-08-19-world-builder-pipeline.md` §T5 (D4/D9/D12
+ §THE GATE govern). Builds on T4 (`drafts/_wb-t4-pilot-20260821.md`, review
PASS 9/9). Trigger context: the SEVENTEENTH adjudicated 2026-08-20; the owner
ratified the pilot walk ("Aprobado", 2026-08-21); Junior walked it too
(`drafts/_junior-pilot-walk-20260821.md`). ONE full wall sweep gates both
jobs in this ticket.

## Grill block — the four T5 forks (settled BEFORE code, 2026-08-21)

### Fork 1 — gate placement: low_quay [44,19] (SE corner) → zone_7 [2,14]

The edge lives in **low_quay** — BOSS 1's own zone (owner vision verbatim:
the open zone sits PAST the boss; the earned defeat opens the way where it
was earned). Tile chosen by reading the grid + spawns
(`data/zones/low_quay.json`):

- **[44,19]** — SE corner, wall-adjacent on two sides (col 45 + row 20 are
  `#`), so the slab reads as a way OUT through the corner. It sits PAST the
  challenger spawn `[43,15]` (Chebyshev 4 — outside the boss's immediate
  fight ring, but the boss provably guards the approach). Nearest enemy
  spawn tiles: rusher [38,18] (6 away), rusher_hater [36,16] — none
  adjacent.
- Geographic coherence: low_quay EAST edge → zone_7 WEST meadow — walking
  east past the boss into the open zone. zone_7 arrival spawn **[2,14]**
  (the west dirt path, beside the return edge, one tile OFF it — no
  auto-fire ping-pong; gradient_anchor [4,14] two tiles east).
- Edge shape: `{ at: [44,19], to: zone_7, spawn: [2,14],
  requires_defeats: 1 }` — **UNTYPED** (ordinary two-way gate grammar,
  auto-fire, co-location consent); the fact-gate key does the locking
  (T4's `Crossing#open?` crossing.rb:24-28 + `way_locked?` renderer.rb:245
  — both already live, wired into zero zone files until now). Locked =
  sealed slab; gold only when `boss_1_defeats >= 1`.
- **Owned consequence (named, not hidden):** the return spawn [43,19] is 4
  tiles from the challenger spawn — returning from zone_7 can land you near
  a live BOSS 1 if he has respawned. Deliberate: the gate corner is visible
  on arrival with room to dodge, and "the boss guards the door" is the
  Souls-shortcut touchstone; a farther tile would break the past-the-boss
  legibility. If the owners hate it in play, the spawn tile is one data
  number.

### Fork 2 — the zone_7 side goes through the DOOR (D2)

The return edge is a new Transition entity in `authoring/pilot.ldtk`
(zone_7 level, __grid [1,14], fields to=low_quay / spawn=43,19 /
sealed=false / type=null / stairs_unlocked_by=null / requires_defeats=null
— emits as the plain untyped object per import_ldtk.rb:305-311) →
re-import → deliberate copy. The provenance pin
(`pilot_authoring_test`) re-emits and must stay green — the committed
zone_7.json equals the emission or the pin names it. The low_quay side is
a hand JSON edit (live zones are NOT LDtk-authored; one surgical
transition object). **INERT pin amended, not deleted**:
`test_pilot_and_fixture_zones_stay_inert` now permits EXACTLY the ratified
pair (low_quay→zone_7, zone_7→low_quay), names D12 completion + date, and
keeps basements/dungeon_1/grass_fixture fully inert in BOTH directions.
Re-anchor trap audit: the two zones gaining arrivals BOTH declare
`gradient_anchor` (low_quay [2,4], zone_7 [4,14]) — no live gate field
re-anchors (v12 trap pre-covered); basement/dungeon arrival lists keep
their order (the new entity appends LAST in the .ldtk → emitted transition
appends after the existing three).

### Fork 3 — `multi_floor_descent.json` (D10: the ONE new surface script)

Wall stays persistence-blind (banked=0 at replay start → the well toll is
UNPAYABLE in a wall script; drained/dungeon-via-well visuals are already
gated by the eleven-panel map gate + `pilot_loop_test`). Script shape:
`start.zone = dungeon_1` → walk to the rope [3,16] → interact (co-location
consent, solo = trivially consented) → arrive zone_7 [33,16] beside the
well → cross the town westward (footstep materials + amb region shifts in
the walked frames) → enter HOUSE 1 → stairs_down [26,3] → basement_1 →
stairs_up back → end near the plaza. Exercises rope-interact + typed
stairs BOTH ways + hole-landing zone + town render in one deterministic
script. Manifest block: `zone_entered >= 3` (v15 manifest law) + captures
at each crossing. Gated STANDALONE first, then joins `harness/scripts/`.

### Fork 4 — ritual-world caveat block

The SEVENTEENTH runsheet/verdict framing measured the six-zone world.
One block (not a rewrite) lands in the verdict doc + checkpoint: as of
2026-08-21 the measured world gains ONE boss-gated exit
(low_quay [44,19] → zone_7), authored content beyond it; the SIM numbers
the ritual measured were adjudicated 2026-08-20 — the verdict PRECEDES the
edge; nothing the ritual measured moved before its close.

### Riders

1. **zone_identity_data_test ZONES list**: zone_7 reachable = a real zone.
   The four pilot zones join the list (T4 authored them to the identity
   contracts: wall−floor luma >= 40, motif between, ambient <= 24) — if any
   pilot palette fails a contract, that is a FINDING to fix in authoring
   (sidecar + re-import), never a test relaxation.
2. **HUB_ZONES derives from data** (T4 review defect 2, queued verbatim
   "at T5's touch"): soak/chain_check.rb reads `hub: true` from
   data/zones/*.json instead of the hand list. Soak-checker-only commit.
3. **Junior sync line** goes in the owner queue: T5 moves live-zone bytes —
   protocol v3 refuses mixed builds; BOTH seats pull before any coop.
4. Live save discipline: `saves/world.json` md5 `98fe75ed…` must not move
   this session (bots on scratch `--save`; tmp/pilot_walk is the owners').

## J1 — frame-tail draw diagnosis (trigger FIRED by Junior's walk)

Junior's banked probe (his file §ACHADO, T4 zones, his machine):
`frames=11663 period{p50=16.8 p90=33.4 p99=50.0 max=185.5}
update{p50=0.4 p95=1.1 max=62.8} draw{p50=3.9 p95=15.3 max=121.3}
over20=1958(16.8%) over35=316 over100=3` — update innocent, the tail
lives in DRAW (p95 ≈ the whole 16.7 ms budget), 16.8% ≈ 2.4× the known
~7% live-world tail (lag-T4 verdict §6 closed that ticket
no-ship-by-default gated on the owners still FEELING it — Junior felt it).

### Static rect census (this machine, tmp/s28_rect_census.rb — mirrors
draw_map's per-frame calls exactly)

```
zone            size  typed  walls  grid  motif decor  trans   TOTAL
basement_1     12x 8     60     36    22      7     0      2     129
basement_2     12x 8     60     36    22      7     0      2     129
camp           20x11      0     64    33     17     8      4     128
district       44x26      0    453    72     81     0      4     612
district_two   44x26      0    447    72    158     0      4     683
dungeon_1      32x20      8    132    54     57     0      2     255
grass_fixture  26x13    209     74    41      0     0      2     327
low_quay       46x21      0    218    69    164     9      2     464
nest           30x17      0    121    49     43     5      6     222
slow_door      14x 9      0     46    25     10     0      4      87
zone_7         44x28    597    236    74    115     0      6    1030
```

zone_7 issues ~1030 `Gosu.draw_rect` calls per frame in draw_map alone —
1.7× district (612), and its typed-overlay pass (597, the T4 flora/water
variants) alone nearly equals district's whole map. Every one of these is
static per (map, drained-bool). Diagnosis hypothesis (spark): the per-frame
static tile pass is the draw tail. A/B probe on THIS machine
(GAME_FRAME_PROBE=1 bots, seed 77, 10800 ticks, zone_7 vs district,
sequential, scratch save; logs tmp/s28_probe/probe_*.log):

```
district: period{p50=16.3 p90=17.0 p99=17.2 max=23.3} update{p50=0.3 p95=0.6 max=3.7} draw{p50=1.0 p95=1.1 max=8.6}  over20=1
zone_7:   period{p50=16.5 p90=17.1 p99=17.2 max=30.3} update{p50=0.1 p95=0.2 max=7.0} draw{p50=2.2 p95=2.9 max=15.4} over20=6
```

**DIAGNOSIS CONFIRMED:** draw p50 2.2× / p95 2.6× district on the same
build+machine while update DROPPED (threat-free zone — the sim is
innocent, exactly Junior's shape). The multiplier tracks the census.

### Fix shape as PLANNED (macro) — superseded by the probes below

`Gosu.record(map.pixel_width, map.pixel_height)` over the immutable tile
geometry — floor bg + typed rects + wall tiles + grid lines + motif — once
per (map, drained_bool), drawn as ONE macro per frame at (0,0) inside the
existing camera translate. Custody: the recorded layer is a pure function
of zone config + registry + the drained BOOL (the same inputs the memoized
geometry already uses; the bool is the cache KEY, so each cached macro
stays pure — the T4 "state never contaminates a pure-config cache" law
holds per-key; breach facts only go false→true, so <= 2 macros/map,
invalidation-free). Decor/transitions/seal slabs/ambient/actors stay live
draws (state-dependent or above-grid by design). God-view keeps reading
TileVariants directly — untouched. **Ship bar: capture bytes must equal
the banked pre-change baselines EXACTLY** (world_loop + grass_fixture_walk
+ low_quay_run, `tmp/s28_baselines/baseline_md5s.txt`, 57 PNGs) — macros
perturb nothing or the fix doesn't ship (fallback: partial batching, or
ship T5 alone and bank the diagnosis).

### Fix SHIPPED: merged rect runs (after two probed dead ends)

**Dead end 1 — Gosu.record macro:** `Macro#draw` inside `Gosu.render`
raises `Gosu::Graphics can only be drawn to while rendering` on gosu
1.4.6 (probe tmp/s28_macro_probe.rb) — the capture harness can never
rasterize a macro, so the wall would gate a path the players don't run.
REJECTED.

**Dead end 2 — rasterized static texture (Gosu.render-once + blit):**
context-legal in both paths (probe tmp/s28_ctx_probe.rb: nested render
OK, render-in-draw-callback OK) and byte-identical in a translate-free
toy (tmp/s28_tex_probe.rb, md5-equal) — but the REAL capture verify
FAILED: 20/57 frames moved (first render per map corrupts under the
outer capture render — constant `e1bcf3bf…` frames — and the float
camera's FRACTIONAL translate resamples a texture differently than
coverage-rule solid rects fill; camera.rb is a float lerp, fractional
offsets are the norm while it settles). tmp/s28_macro_verify/md5_diff
(first run) banked the diff. REJECTED — any composed-layer approach
breaks byte-identity under this camera by construction.

**Shipped: horizontal+vertical run-merge of the static classes** — same
primitive (solid opaque `Gosu.draw_rect`), same camera math, spans
merged only when they tile EXACTLY (abut, same color/ref) — byte-safe by
construction, and the verify PROVED it: **57/57 capture PNGs md5-equal
to the pre-change baselines, replays rc=0×3**
(tmp/s28_macro_verify.out, second run; the first run's rc=1 false-pass
taught the script to judge rc before md5s). Static rect counts/frame:

```
district       walls 453->25 motif  81->81   static  534->106
district_two   walls 447->45 motif 158->158  static  605->203
low_quay       walls 218->21 motif 164->164  static  382->185
zone_7   typed 597->302 walls 236->28 m 115  static  948->445
```

**A/B probe AFTER (same protocol, seed 77, 10800 ticks):**

```
district: draw{p50=0.6 p95=0.8 max=9.2}  (was p50=1.0 p95=1.1) over20=5
zone_7:   draw{p50=1.2 p95=1.7 max=15.9} (was p50=2.2 p95=2.9) over20=2
```

draw p50 −40-45%, p95 −27-41% on BOTH the payoff zone and the live
world (district's wall pass collapsed 453→25). zone_7 now draws roughly
like pre-fix district. Junior's re-measure is the decisive seat
(owner-paced, pt-br two-liner below); expectation named: his draw p95
15.3 ms scales toward ~8-9 ms, over20 toward the ~7% baseline or below.
If his numbers DON'T drop, the trigger stays fired and the next lever is
named in §leftovers (motif rects don't merge — 164 singles in low_quay —
a glyph-atlas texture is OUT while the float camera law stands; per-
glyph batching into strips would need authored data changes).

### Junior re-measure (pt-br, staged for the owner queue — owner-paced)

> Junior: o fix da lentidão subiu — depois de `git pull`, roda de novo
> com `GAME_FRAME_PROBE=1` (mesmo passeio: town + porões + dungeon) e
> banca a linha `TELEMETRY frame_probe`. Esperado: `draw` p95 bem
> abaixo dos 15,3 ms e `over20` caindo na direção dos ~7% — se não
> cair, o ticket segue aberto com os teus números mandando.

## Sequencing (one bundled change-set)

baselines (banked pre-edit: `tmp/s28_baselines/baseline_md5s.txt`) → J1
A/B probes → J1 render fix commit (captures == baselines) → T5: authoring
re-import + zone_7 + low_quay edge + INERT amendment + identity list +
HUB_ZONES derivation (data + test commits, provenance pin green) →
multi_floor_descent + standalone gate → spot-gates (low_quay_run,
varekka_duel — the moved Rule-2 surfaces, vision critique) → ONE FULL WALL
DETACHED (nohup harness/run_wall.sh t5) → caveat blocks + docs →
fresh-eyes review BLOCKING over both jobs → checkpoint + AGENTS lane line +
push + owner queue es-CR + Junior pt-br re-measure two-liner.

## Evidence (append-only below this line)

## Ship evidence (append-only, s28)

**Commits:** `371aedd` T5 data (edge pair + INERT amendment + identity
list + edge pin; only low_quay/zone_7 moved — other five live zones +
basements/dungeon byte-identical, emissions cmp-equal) · `dd8ff40` J1
render (merged static runs; 57/57 capture PNGs == pre-change baselines,
tmp/s28_macro_verify.out run 2) · `1b0d3dd` HUB_ZONES derives from data
(+pin) · `<mfd>` multi_floor_descent.json joins the wall. Suite 996
green via hooks each commit.

- **A/B probes (this machine, seed 77, 10800 ticks):** before
  district draw{p50=1.0 p95=1.1} zone_7 {p50=2.2 p95=2.9} → after
  district {p50=0.6 p95=0.8} zone_7 {p50=1.2 p95=1.7}. Logs
  tmp/s28_probe/probe_*.log (before run banked in section above).
- **multi_floor_descent standalone gate PASS** (tmp/s28_mfd_gate.log):
  determinism 8/8 byte-identical ×2 · vision critique PASS
  (zone_identity_reads: "Dark grid zone, green field, and brown
  basement are tellable apart with banners covered; gold stays on
  ways").
- **Spot-gate low_quay_run PASS** (tmp/s28_spot_low_quay_run.log):
  determinism 11/11 ×2 · vision PASS · captures additionally
  byte-identical to the PRE-T5 baselines — the script's walk never
  frames the SE corner; the T5 slab's visual lands in varekka_duel's
  frames (gating next) and multi_floor_descent's zone_7 side.
- **Spot-gate varekka_duel PASS** (tmp/s28_spot_varekka_duel.log):
  determinism 5/5 ×2 · vision PASS. Pixel-audit of its frames showed the
  duel camera NEVER frames the SE corner — a Rule-2 gap for the LOCKED
  slab, named and closed by extending multi_floor_descent (below).
- **multi_floor_descent EXTENDED through the gate** (`3c0ff6c`,
  supersedes the first cut `a639c2b`): after the descent loop the script
  crosses the free return edge into low_quay at defeats=0 —
  frame_2890 puts the LOCKED slab (dark fill + gold seam) on camera
  beside the challenger (pixel-verified, crop tmp/s28_slab_2890.png);
  frame_2911 proves the mechanical refusal (possessed standing ON
  [44,19], zone still low_quay — the sealed-door law). Standalone gate
  PASS ×2 (11/11 byte-identical + vision, tmp/s28_mfd_gate2.log); the
  replay even caught the challenger engaging at the corner
  (varekka engaged=1 chants=1 interrupted=1 — the boss guards the door,
  live, in a deterministic script).
- **Docket rows updated** (J3): J-1 CLOSED (Junior's re-test) ·
  frame-tail row → TRIGGER FIRED + s28 fix pointer · basement-ambience
  question ANSWERED (design v0, T4 rider 8).
- **Full wall sweep**: tag `t5`, 21 scripts, launched detached — verdict
  appended below when it lands.

## FULL WALL VERDICT (tag t5, 2026-08-21 09:30)

**PASS — 21 scripts, fails: none** (tmp/s28_wall_launch.out: every
script `gate_rc=0 manifest_rc=0`, per-script logs tmp/wall/*_t5.log).
Determinism ×2 + vision critique on every surface, multi_floor_descent
included. Gate-0 re-check at close: save md5 `98fe75ed…` UNMOVED ·
launcher logs 40×2 · one mid-session mail = the game-two-assets seat's
mechanical re-pin record to `1b0d3dd` (approve-by-default class, no
action owed — their audit independently value-re-verified every pinned
render constant against the merged-runs renderer blob; archived,
done/=22).

## Leftovers, named

- Junior's frame-probe re-measure (owner-paced) — the decisive seat for
  the J1 trigger; staged pt-br two-liner above.
- Motif rects don't merge (scattered glyphs; low_quay 164 singles stay
  the largest remaining static class). Next lever ONLY if Junior's
  numbers stay high: authored motif strips (data) — texture/macro
  composition is DEAD by the float-camera law banked above.
- stairs_unlocked_by wiring — still schema-only (D4 optional, unchanged
  from T4).
- BOSS-1 dread iteration stays OPEN-FOR-EXPOSURE (PARKING_LOT) — the
  gate now guarantees organic ZONE 5 exposure for every zone_7-bound
  player; varekka telemetry harvest waits for human sessions.

## ADJUDICATION — Junior re-measure (s32, 2026-08-21): CLASS 1, CLOSES

Bank: `drafts/_junior-remeasure-20260821.md` (`61ccfa3`, his lane —
harvested, never edited). Protocol held on his seat: pull to `1850676`
FIRST · suite 1004 green on his clone · SAME T4 route (TOWN 1 →
BASEMENT 1/2 → well → DUNGEON 1 → rope back) on the SAME scratch save ·
suite finished before launch (idle CPU) · before/after logs in DISTINCT
files, md5s banked (s28 hygiene lesson applied).

Pre-declared classes (carried verbatim s29→s30→s31→s32) applied:

- over20 **16.8% → 5.15%** (744/14441) — ≤ ~8% ✓, below the ~7%
  old-world baseline
- draw p95 **15.3 → 8.5 ms** — inside the predicted ~8-9 ms window ✓
- period p90 33.4 → 17.5 ms · draw p50 3.9 → 2.6 ms · AUDIO drift
  +12.2% → +5.2%
- Human verdict: **"muito mais leve"** — the felt slowness that fired
  the trigger was not felt.

**Class 1 — CLOSES. The fix stands on the decisive seat; the docket
frame-tail row is CLOSED** (row updated s32; AGENTS lane-3 line
amended).

Route note (like-with-like law): his round added MORE transitions than
the original probe (incl. the T5 zone_7 → low_quay free edge) — the
deviation biases AGAINST the fix (more zone-load spikes) and it still
passed; NAMED, not averaged. Caveat carried from his bank: over100
3 → 9 (isolated spikes, period max 1469.6 ms / draw max 473 ms —
suspected zone loads / window focus), NOT the continuous tail; p90/p95
carry the fix story. Leftover stays named and UNBUILT: motif-strip
authoring (low_quay 164 singles, district_two 158) = DATA-class v19
candidate on owner word behind its own Rule-2 gate + wall;
texture/macro composition stays DEAD (float-camera law).

Bonus banked from the same round: **the T5 edge was crossed in HUMAN
play** (his scratch, free direction zone_7 → low_quay; `AUDIO ambience
key=none zone=low_quay` ×2 in his log) — the edge works in play, and
boot validation (`3c4b988`) loaded his world clean. The LIVE-save first
crossing (low_quay → zone_7 through the earned slab) remains ahead —
his seat volunteered for the next play session.
