# SPARK — v20 T1: FLOOR 1 (ZONE 2 → v2b descent retheme) — staged s114, 2026-08-28

You are the fresh implementation session for ticket T1 in game-two
(cwd `~/workspace/game-two`). AGENTS.md + MEMORY auto-inject beside
this brief; the live files beat this spark on any drift. Ruby per
shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; player surfaces stay PLACEHOLDER register (ZONE N — no
fiction names, standing owner order). Quality over cost (owner
order): thoroughness on design/judgment, never skipped verification.

MISSION: execute spec ticket T1 — retheme ZONE 2 (`district`) into
descent floor -1 per Junior's ratified v2b direction, through the
lawful pipeline, gated end-to-end. ONE ticket, ONE session (size
guard below). T1 is the cycle's FIRST FELT CHANGE — the owners play
this; presentation quality is the point, not a checkbox.

## STEP 0 — ENTRY + CLAIM (before any authoring)

1. `git fetch origin` + ff-merge. Read from DISK: checkpoint TOP
   entry (docs/CHECKPOINT.md, s114) · AGENTS.md §Current cycle (v20)
   · the spec ticket: `docs/superpowers/specs/2026-08-28-v20-descent-cycle.md`
   §T1 (its text governs scope) · foundation rows L2/L3/L13:
   `drafts/_v20-foundation-20260828.md`.
2. **Collision check (s56/s104 law):** read NEW Junior commits since
   `be4fdac` + grep checkpoint for `CLAIMED:` + check seat mail inbox
   (`~/.pi/agent/mail/game-two/inbox/`). He was iterating v2b
   geometry in his sandbox on 2026-08-28 and may have claimed T1,
   landed updated geometry, or ratified/vetoed the L3 pick
   (RATIFIED-J is pending — his async word may be in a new draft).
   - His seat CLAIMED T1 → do NOT duplicate: take T2 (independent,
     same spec) and record the swap in the checkpoint.
   - He landed NEWER v2b geometry/direction → that supersedes the
     08-28 doc as the transcription source (digest-verify it).
   - He VETOED v2b for floor 1 → STOP, surface to the owner (L3's
     pick was dev-recommended pending his veto).
3. **Push your claim FIRST:** one `CLAIMED: T1 (seat: hub, <date>)`
   line at the checkpoint top, commit, push, THEN start. A held-seat
   or push refusal = coordinate, never route around.

## SOURCE OF TRUTH (transcription, not invention)

- Direction doc: `drafts/_junior-zone2-map-directions-20260828.md`
  §v2b + reference PNG
  `drafts/_refs/junior-zone2-v2b-dois-espacos-4-pontes.png`
  (git-blob md5 `0ba972e094bf4ed065a1470e618dd03f` — verify before
  relying). Review with adversarial rows:
  `drafts/_s113-junior-zone2-directions-review-20260828.md`.
- v2b spec: 52×88 PORTRAIT · two themed halves (west dry/dark camps ·
  east mossy/alive with clearing) · central chasm IMPASSABLE via
  wall-ring (today's engine, zero new asks) · exactly 4 bridges,
  key bridges guarded · diagonal flow SW entry → NE exit · 27 minions
  arena-GROUPED · 3-wide corridor standard (v1 mining, adopted
  cycle-wide) · `floor: -1` metadata · palette per darkening-with-
  depth rule (sidecar owns palette/gradient scalars).
- Fidelity is approximate BY AGREEMENT (map-lane law); deviations the
  engine forces get NAMED in the ticket record, not silently absorbed.

## HARD LAWS (each has bitten before)

1. **Provenance:** district is a pilot-four importer emission. Author
   `authoring/pilot.ldtk` + district sidecar →
   `ruby tools/import_ldtk.rb … --sidecars … --out …` → DELIBERATE
   copy into `data/zones/`. NEVER hand-edit the zone JSON
   (`pilot_authoring_test` byte-compares; a hand edit = red suite).
   LDtk pin: jsonVersion 1.5.3, IntGrid `Terrain`, identifier style
   "Free". Import→emit→import must stay a byte-stable fixpoint.
2. **Transitions keep ENDPOINTS, gate semantics unchanged (L2):**
   camp→district edge re-typed into the stairs_down/hole family (the
   descent MOUTH stays HERE — the forest-first destination moves it
   later, one row, NOT this ticket) · district→district_two re-typed
   as the -1→-2 hole family. Typed-transition law: holes/stairs
   auto-fire, rope = interact. `requires_*` values UNTOUCHED. Coop
   ergonomics: transition tiles reachable with two living bodies
   co-located (v17 law).
3. **Zero balance constants:** 27 minions use district's EXISTING
   tier kit vocabulary (`data/balance/tiers.json` reading — kits
   named in the zone file, numbers live in balance/). No new kit, no
   new numbers this ticket. Arena-grouping = placement, not stats.
4. **Wall scripts are blind coordinate streams:** re-authored
   geometry INVALIDATES walks through district. Identify every
   `harness/scripts/` script traversing district (world_loop at
   minimum — trust the directory, not memory), re-author their
   district segments (or author `floor1_run.json` as the zone's own
   script per T5-precedent: new/changed zone = its own script + gate
   rows) and retire stale segments CONSCIOUSLY in the ticket record.
5. **Sim-identity canary:** an INTENDED map change follows the
   versioned-bank protocol in
   `test/harness/sim_identity_canary_test.rb`'s header — rebank with
   the version bump, never to silence a surprise (an UNINTENDED
   stream shift = a defect to fix).
6. **world.rb untouched** (extraction rides T4, not here). Line caps
   stay green.
7. **Two-instance law:** never launch a second solo instance on the
   live save; captures/replays run through the harness, and any
   interactive check uses `rake pilot`, never bin/play beside a
   playing owner.

## BUILD ORDER (recommended, not sacred)

1. LDtk authoring (52×88 IntGrid + entities: PackSpawn SW,
   27 EnemySpawn grouped, 2 Transition, floor/display fields) +
   sidecar palette (darkened band). BFS-verify connectivity BEFORE
   import (entrance→exit + every spawn reachable; Junior's concepts
   were BFS-proven — match that bar).
2. Import → fixpoint → deliberate copy → `bundle exec rake` green
   (provenance + caps + canary EXPECTED red → versioned rebank per
   protocol).
3. Script surgery (law 4): re-author/author scripts → `rake capture`
   spot-checks → read the PNGs YOURSELF (frames, not vibes): two
   halves legible, chasm reads impassable, bridges read as the
   4 choices, corridors ≥3 wide, palette darker than camp's.
4. Gates: `rake gate SCRIPT=harness/scripts/<each affected>.json` —
   BLOCKING; a red gate stops the ship, fix and re-run. If the full
   sweep threatens the session window: land zone+suite+one gate
   first, run the remaining re-pins DETACHED
   (`harness/run_wall.sh v20-t1` — ~5 min/script, NEVER under a bash
   timeout; poll by rc lines), bank verdicts in a same-day follow-up
   commit.
5. `rake soak N=1` (scratch save; bots cross floor -1; chain check
   clean). `rake perf` if tile count worries you (4,576 tiles — v2b
   is portrait 52×88; the review flagged perf for v1 not v2b, but
   verify, don't assume).
6. Ticket record `drafts/_v20-t1-floor1-<date>.md`: what shipped,
   deviations named, gate/soak/canary evidence paths, capture
   verdicts, script surgery ledger. Commit (explicit paths, one
   concern), push.

## RULE 2 VERDICT (blocking, not advisory)

Every gate's vision critique must PASS on the NEW surface reads
(stage the STRONG members: a bridge crossing, the chasm edge, both
halves' palettes — the 2026-08-26 lesson: after any tolerance clause,
gate on the class's strong member for the affirmative read). Gate
captures land in `<out_dir>_gate_a/_gate_b` — pixel-diff THOSE, never
the plain out_dir (2026-08-25 lesson).

## HAND-OFF (definition of done)

Suite green · affected gates PASS (or detached sweep running with
zone+suite landed and the follow-up owed SAME DAY) · soak clean ·
canary rebanked versioned · ticket record + checkpoint entry (top,
s115) pushed · CLAIMED line resolved to DONE. Then STOP — T2/T3 are
their own sessions. Queue line for the owner (es-CR, everyday words):
qué cambió, cómo probarlo (`bin/play`), y que Junior ratifique el
piso cuando lo juegue.

## FRESH-EYES REVIEW (owed after DONE, Rule 6)

Stage a scrubbed headless review (diff + ticket record + spec T1;
reviewer touches NOTHING, including seat mail — 2026-08-21 lesson) OR
hand it to Junior's seat by checkpoint note. Receipt lands beside the
ticket record. A failed review blocks T4 (same lane); T2/T3 (lane B)
may proceed.

## BUDGET/STOP (Rule 7)

One attended session. STOP and surface (never improvise) on: v2b
geometry unreachable through the importer's vocabulary · Junior veto
· gate red twice on the same surface after honest fixes · context
≥75% before the zone lands (compact-checkpoint skill, stage
continuation). Council: 0 (design is ratified; this is execution).
Bedrock spend: gates' vision critic only.
