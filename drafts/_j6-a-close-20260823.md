# J6-A close — menu chassis shipped (s53, 2026-08-23)

Ticket: `drafts/_j6-menu-brief-20260823.md` §J6-A (md5 `00802b28…`,
verified at open). Commits: A1 `feat(menu)` (inert chassis) + A2
`feat(presentation)` (visible wiring + 25th wall script). This draft
records deviations + evidence per the T5-close shape.

## Evidence (the ladder, in order)

- **Suite:** 1122 → 1137 (15-case menu unit lane; binding roster
  12→13 actions) — green at open, at A1, at A2 (hooks re-ran it at
  both commits).
- **Identity pairs** world_loop + low_quay_run, SKIP_CRITIC
  double-replay, md5 vs a PRE-CHANGE `e74bf32` baseline captured
  before any edit: **24/24 byte-identical at A1 AND at A2**
  (tmp/j6_baseline/*.md5; the spark's `e74bf32` supersedes the
  brief's `fbc15f1` — docs-only delta between them, code-identical).
- **menu_tour full critic-ON gate: PASS** — determinism 5 captures
  byte-identical across two runs; vision verdict **64/64 PASS**
  including the two new checks (`menu_reads`, `menu_world_ticks`).
  Sampling-artifact law: I read all five source PNGs myself before
  the critic ran; its claims (husk alive f0360 → corpse f0540,
  striker 80→65 + lobber 60→45 bars, XP sliver fills, MENU
  panel/cursor/hint legible, world readable behind the veil,
  CONTROLS sheet reads) match the exact frames.
- **Manifest: PASS** on the single-run log {zone_entered 2,
  attack_hit 5, actor_died 1} and on the wall-style double log
  (4/10/2).
- **Soak N=1 belt: PASS** — EP1 PASS, ticks 36120/36117, desyncs 0,
  exit 0, chain intact (fresh → dec77789, sessions 1→1); two real
  App::Window seats over the refactored seam, bots never emitted
  :menu (roster law).
- **Rule 6 review: PASS** (scrubbed read-only session, zero blocking;
  full verdict `tmp/`-logged and summarized below).
- **Language critique (rides the gate):** all 27 menu rows ×3
  locales landed VERBATIM from the brief's pinned table (diffed);
  presentation proven by `menu_reads` PASS; parity test pins the
  three key sets identical.

## Deviations (each named, none silent)

1. **window.rb honesty row (T5 precedent):** brief estimated ≤240
   ("±~20 net"); landed **246** (217 + 29; cap 300 test-enforced).
   The overrun is law-comment lines on the three new seams
   (menu_active? guard, handle_menu D3 note, button_down D2 note) —
   trimmed once already; stripping further would delete the WHY at
   the exact sites future sessions touch. B's estimate (≤265)
   re-prices from 246.
2. **Manifest floor corrected pre-commit:** first authored
   attack_hit=6 (miscount of the f398 double-hit lines); the gate's
   doubled log masked it (10 ≥ 6). Audited against the single-run
   dry log per the true-count clause and corrected to **5** before
   commit; re-verified PASS on both logs. Lesson: floors come from
   `grep -c` on the single-run log, never from eyeballing the event
   stream.
3. **"count row updates (7ab5612 law)": not applicable this ship** —
   the only count-declaring check in the harness is map_checks'
   "Twelve labeled zone panels"; menu_tour adds no zone panel
   (TEST 1 was already the twelfth at T5). No count row moved; law
   satisfied vacuously, recorded so the next cutter doesn't hunt for
   a phantom row.
4. **Beat-3 story richer than the pin:** brief D6 beat 3 = "husk has
   closed distance / HP has dropped"; the shipped f0540 shows the
   husk's CORPSE + two dropped pack bars + XP gain — the husk
   closed, landed damage (f398, while the menu was open), and was
   killed by pack AI (f418), ALL behind the open menu. The pin's
   precondition ("damage lands while the menu is up") holds; the
   still-ticking proof is stronger than specified. `menu_world_ticks`
   is worded on world-advance generally, so the check does not
   overfit this reel.
5. **Reel authoring note:** the pilot staged the world half
   (possessed = BLOCKER at seed 42, not striker — 160 HP made the
   menu-open beating safe by construction); menu/nav rows
   hand-spliced ≥10f apart (edge-merge law). Every spliced row is
   world-inert: rows landing while the menu is open route to the menu
   (world gets NullInput); the f340 opener and f632 closer land
   `:menu` on the world's input for one frame each, and nothing in
   src/game|net|core polls `:menu` (reviewer-grepped). The proof that
   matters: the replay event stream byte-matched the pilot session.

## Rule 6 review (PASS — findings banked, none blocking)

Reviewer verified pins independently (`git diff e74bf32..HEAD --
src/game/ src/net/ src/core/` EMPTY; Protocol.ACTIONS 12 verbs, no
:menu; splice gaps ≥ the 8f law; 64 check ids = 64 verdict rows;
strings byte-match ×3; 13 menu_* keys = code fallbacks).

- **BANKED → J6-B (named row): RESUME-confirm attack leak.**
  Tick-then-route means the frame that closes the menu routes the
  REAL input; an attack-edge RESUME confirm fires a swing as the
  world unfreezes (controllers.rb start_attack is level-triggered).
  Deterministic, netplay-safe, unexercised by the shipped reel (it
  closes via Esc). B owns the menu machine: swallow the close-frame
  route (one guard + unit case) or ratify as feel.
- **BANKED → J6-C (named row): menu frozen OPEN over a partner-end
  screen.** menu_active? stops ticking at session ended — a menu
  open when the PARTNER ends the session keeps drawing (z=50) over
  the end screen (z=10) and cannot close; Esc still closes the
  window (end-screen law's exit holds). C owns the netplay end
  surface (D14): force-close the menu at session end or tick it on
  ended sessions.
- **Named (brief-conformant, was silent): mid-drain force-close
  dropped.** Old button_down's `else close` let a second Esc
  force-close during the BYE drain; D2's two kept behaviors exclude
  it — drain resolves by timeout (2000 ms). Accepted cut, now on
  record.
- **NOTE:** overrun counter (z=20) dims under the menu veil (z=50)
  while open — consistent with D4 topmost; perf signal returns on
  close.
- **NOTE (standing practice):** manifest floors are structurally
  judged against the wall's DOUBLED gate log — always audit floors
  with `grep -c` on a single-run log (this ship's deviation 2 is the
  precedent).

## Banked-amendment carrier check (brief §carrier)

Cue-silence guard / spell_growth duplicate keys / EventLog curation:
J6-A touched none of their trigger files — all stay banked.
