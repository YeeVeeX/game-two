# v20 T3 — Potions identity + strip escalation (2026-08-29, Gabriel's hub seat)

Ticket: `docs/superpowers/specs/2026-08-28-v20-descent-cycle.md` §T3 ·
Law: foundation L14 (`drafts/_v20-foundation-20260828.md`) + R-A2 grill
spec escalation path (`drafts/_rA2-grill-spec-20260820.md`). CLAIMED
`2808a44` → phases landed `5dacb7d` (quirk) · `81cf358` (identity +
escalation) · `7521b07` (sustain_run re-cut) · `6b6ff74` (parking-lot
vote) — this record rides the close docs commit.

## Misread guard honored (the spark's #1 risk)

R-A2 Ticket A (bank BUY hint) and Ticket B (telemetry reasons{}) were
verified ALREADY SHIPPED in code before any edit (renderer.rb
`sustain_hint` ~l.214; telemetry.rb reasons ~l.331). What executed here
is the RECORDED ESCALATION PATH only: decision 7iii flipped to
always-on. Zero sim changes, zero balance numbers, layout untouched
(strings + the 7iii visibility condition only). economy.json untouched.

## 1. Word table (old → new, ×3 locales)

| key | en | es | pt-br |
|---|---|---|---|
| overlay.sustain (strip verb) | provision → **potion** | provisión → **poción** | suprimentos → **poção** |
| hud.provisions (counter/hint noun) | PROVISION → **POTION** | PROVISIÓN → **POCIÓN** | SUPRIMENTOS → **POÇÃO** |
| cue.provision_bought | PROVISION BOUGHT → **POTION BOUGHT** | PROVISIÓN COMPRADA → **POCIÓN COMPRADA** | SUPRIMENTO COMPRADO → **POÇÃO COMPRADA** (m→f agreement flip) |
| cue.provision_used | PROVISION USED → **POTION USED** | PROVISIÓN USADA → **POCIÓN USADA** | SUPRIMENTO USADO → **POÇÃO USADA** |
| cue.provision_refused | REFUSED (kept) | RECHAZADO (kept) | RECUSADO (kept) |
| map artifact header (EN, locale-invariant) | PROVISIONS → **POTIONS** | — | — |

Key NAMES stay mechanical (`hud.provisions`, `cue.provision_*`,
`overlay.sustain`, event symbols) — placeholder law; only display
strings moved. EN fallbacks in code track en.json (controls_overlay
LABEL_FALLBACK, renderer CUE_TEXT_FALLBACK + HUD_STOCK_FALLBACK,
menu SHEET). Telemetry untouched (machine/oracle vocabulary).
pt-br history preserved in strings_test comment: Junior ratified
SUPRIMENTO for v18; L14 (owner-ratified) supersedes.

## 2. Glyph proof (pt-br cedilla/tilde — proven BEFORE word commitment)

`tmp/t3_glyph_proof.rb` (real Gosu::Window + Gosu.render, game font
sizes 12/18px) → `tmp/t3_glyph_proof.png`, READ by eye: POÇÃO/poção
(ç+ã), POCIÓN/poción (ó) all render cleanly at both sizes, including
composites "U POÇÃO -5", "POÇÃO 0", "U R poção 2". No fallback needed.

## 3. Language critique (BLOCKING — PASS)

Own checklist pass (human-facing-output skill): GUI labels,
human-interactive, flat placeholder register held; feminine agreement
fixed in pt-br (COMPRADA/USADA vs old masculine COMPRADO/USADO).

Council (1 consult, Kimi, ~1400 max tokens — the session's single
council spend), accuracy and presentation scored separately, verbatim:

- Q1 es poción: CONFIRMED | ACCURACY PASS | PRESENTATION PASS —
  "'Poción' is Tibia's canonical term (mana/health potions), and the
  acute accent on 'ó' follows Spanish orthography; es-CR gamers will
  find this immediately familiar."
- Q2 pt poção: CONFIRMED | PASS | PASS — "'Poção' is Tibia-BR
  standard; feminine agreement (COMPRADA/USADA) correctly matches
  'poção' (feminine noun), fixing the prior masculine 'suprimento'
  mismatch."
- Q3 refusal words kept: CONFIRMED | PASS | PASS — "'Rechazado/
  Recusado' refer to the refused ACTION/attempt (implied: 'pedido
  rechazado'), not the potion; participles here are impersonal/
  event-describing."
- Q4 "POÇÃO 0" counter grammar: CONFIRMED | PASS | PASS — "Singular
  label + numeral is genre-standard across ARPGs (Tibia: 'Potions:
  0', Diablo: 'Health Potion x0')."
- Q5 noun-among-verbs strip label: CONFIRMED | PASS | PASS — "The U
  key's dual function (buy/use) defeats any single verb."
- Q6 uncovered risk: pt-br "POÇÃO 0" may momentarily scan as
  singular-count to Brazilian ears; "flag the PT-BR singular counter
  format for live player testing; consider 'POÇÃO: 0' or 'POÇÕES 0'
  if telemetry shows misread."
- OVERALL: **SHIP**.

Disposition of Q6: kept "POÇÃO 0" (symmetry with the LEVEL N grammar +
Kimi's own Q4 genre confirmation); surfaced to Junior as a
take-or-leave watch — his locale, his word under FULL SEAT SYMMETRY.

## 4. Boot-banner quirk fix (s116 named debt 2 — pre-sweep)

Root reproduced headlessly: world boots home (nest banner enqueued,
150-frame clock), `start_in` jump queues the destination banner BEHIND
it — stale ZONE 1 banner over start-zone ground (dash_strike_rip
QUIRK-RED ×2, floor1_run f0). Fix: `Harness.apply_start` clears the
banner queue before `start_in` (harness plumbing only; live crossings
keep full FIFO law; world.rb untouched). Tests: destination banner at
frame 0 + no-zone jump keeps the boot banner (scene_start_test).
Verify: `rake gate dash_strike_rip` rc=0 — the two previously-red rows
now read "Every banner names ZONE 2 only"; `rake gate floor1_run` rc=0.
SPARK DRIFT NOTE: the spark suggested low_quay_run as second gate, but
live low_quay_run has NO start.zone (its captures don't move with this
fix) — floor1_run substituted (named f0 quirk wearer in the s116
record). Commit `5dacb7d`.

## 5. Escalation diff summary (decision 7iii → always-on)

`src/app/controls_overlay.rb`: `vessel_line` appends `:sustain`
unconditionally (was provisions>0-gated); `provisions_line` always
returns "POTION N" (was nil at zero); draw's counter guard dropped;
one hardening line — a partial injected BindingMap (no :sustain row)
falls back to canonical glyphs so the always-on row never draws
glyph-less (new exposure created by always-on: pre-flip a partial map
only crashed after the first buy). Tests rewritten affirmative
(always-on at 0 AND >0, seven pairs at every count, POTION 0 / POÇÃO 0
/ POCIÓN 1 pins, fresh-real-world affirmative, partial-map fallback).
Gate rows rewritten affirmative: `provisions_counter_reads` (pair +
counter in EVERY strip frame, zero reads POTION 0 never absence),
`sustain_success_cue_reads` (POTION nouns); refusal row unchanged.
`harness/sustain_hint_checks.json` + `harness/map_checks.json` renamed
to the POTION nouns. Commit `81cf358`.

## 6. sustain_run re-cut ledger (byte-honest)

Manifest wants BEFORE = AFTER (byte-unchanged):
`provision_bought=6 provision_used=2 provision_refused=8 banked=4
drop_picked_up=16 pack_wiped=2 corpse_looted=2` (double-replay
minimums — `manifest_check.rb` judges the teed double-replay log).

Pre-re-cut diagnosis (headless, old choreography): refused=8, ALL
other families 0 — C2 re-sequencing (04b218b) killed every fight beat,
exactly the T1 wall record §2 class. Re-cut via NEW
`tools/tune_sustain_run.rb` (waypoint-tuner pattern, eighth working
precedent): probed the v2b west-half geometry live (rooms chained by
narrow slots; corridor waypoints), beats = 2 :none refusals at spawn →
2 :broke at the nest bank → 7-fight west-column sweep (9 kills, 9
pickups, 3 deposits, banked 20) → cue-free HINT frame at the bank
(banked≥15, stock 0) → 3 buys → 2 :at_cap → 1 use afield (hurt from
the sweep) → bridge-sentry kill for the carry drop → NE-arena
chain-aggro wipe → respawn → settle-gated corpse loot. Single-run
proof: 3/1/6/3/9/1/1 → doubled 6/2/12/6/18/2/2 ≥ wants ✓. Reasons
tally {none=2, broke=2, at_cap=2} (3 of the 5 classes on camera).
run_until 6799 → 20577; captures re-anchored on empirical beat frames
(none cue 44, broke 212, hint 13714, buy receipt 13746, at_cap 13808,
use 14266, wipe straddle 18697/18777, loot 20423, final 20576).
SPARK DRIFT NOTE: the R-A2 spec's "10 buys / 4 uses / 6 refusals"
description was the v18-era manifest; the LIVE manifest (6/2/8/...)
is what this re-cut honors.

## 7. Attended gate rc lines (all PASS)

| gate | rc | notes |
|---|---|---|
| dash_strike_rip | 0 | quirk rows affirmatively green ("Every banner names ZONE 2 only") |
| floor1_run | 0 | second quirk wearer |
| sustain_run | 0 | 10 captures byte-identical; the three rewritten rows read affirmatively on the strong member |
| sustain_run manifest | 0 | `MANIFEST PASS: provision_bought=6 provision_used=2 provision_refused=12 banked=6 drop_picked_up=18 pack_wiped=2 corpse_looted=2` |
| sustain_run hint checklist | 0 | standalone critic on frozen gate_a, 5/5 PASS ("Frame 13714 shows U POTION -5 above the bank with banked 20") |
| world_loop | 0 | everyday-loop canary green with the moved strip |
| world_loop manifest | 0 | banked=2 drop_picked_up=2 |
| ledger_loop | 0 | bank-hub surface green |
| ledger_loop manifest | 0 | banked=2 corpse_loaded=2 pack_wiped=2 |

## 8. Critic-retry finding (s116 debt 4 — resolved by inspection, no code)

`harness/vision_critic.py` ALREADY carries the bounded retry the debt
asked for: `_ATTEMPTS=3`, 30s sleeps, catching ReadTimeoutError /
ThrottlingException / BotoConnectionError / EventStreamError, client
read_timeout=300. The s116 "7 timeouts" were 3-attempt EXHAUSTIONS on
an unstable Bedrock evening, not missing-retry. Adding more attempts
risks 30+ min hangs per gate mid-sweep; the recorded fallback
(standalone re-run on frozen gate_a captures, same-artifact law) stays
the answer. No tooling change taken — the sweep runs proven behavior.

## 9. Full-wall re-pin (v20-t3) — HARVEST

**HARVESTED s119 (2026-08-29).** Sweep DONE 06:26:00→09:21:13
(2h55m), 36/36 rc lines, driver rc=1 (standing reds — expected).
Honest pass law (supersedes the spec's stale "rc=0 all scripts"):
red set must be a SUBSET of {aoe_specials, basement_pocket,
burn_duel, level_gate, loot_loop, nest_advance, toll_pocket,
varekka_duel, vat_economy, zone8_crossing} + threat_pull +
zone_catchup, each red for its SAME recorded manifest reason (s116
census); greens must include every v20-t1 green; sustain_run must
now be GREEN. **VERDICT: pass under the law** — 23 greens, 12
on-pattern reds + ONE new red triaged into the C2 queue with
evidence (taunt_anchor, below). No T3 regression anywhere. No
determinism red anywhere (all three gate-red scripts' double
replays byte-identical: 8/10/5 captures).

### 9.1 Full rc table (36 scripts)

| script | gate | manifest | | script | gate | manifest |
|---|---|---|---|---|---|---|
| aim_hold | 0 | 0 | | mercy_floor | 0 | 0 |
| aoe_specials | **1** | 0 | | moving_square | 0 | 0 |
| basement_pocket | 0 | **1** | | multi_floor_descent | 0 | 0 |
| burn_duel | 0 | **1** | | nest_advance | 0 | **1** |
| corpse_run | 0 | 0 | | respawn_telegraph | 0 | 0 |
| critic_reel | 0 | 0 | | safe_boundary | 0 | 0 |
| dash_strike_rip | 0 | 0 | | specials_chain | 0 | 0 |
| district_hunt | 0 | 0 | | sustain_run | 0 | 0 |
| dungeon_fork | 0 | 0 | | taunt_anchor | **1** | 0 |
| floor1_run | 0 | 0 | | threat_pull | 0 | **1** |
| grass_fixture_walk | 0 | 0 | | toll_pocket | 0 | **1** |
| ledger_loop | 0 | 0 | | town_gates | 0 | 0 |
| level_gate | 0 | **1** | | varekka_duel | 0 | **1** |
| level_up_beat | 0 | 0 | | vat_economy | 0 | **1** |
| lobber_reach | 0 | 0 | | world_loop | 0 | 0 |
| lobber_volley | 0 | 0 | | zone8_crossing | **1** | **1** |
| loot_loop | 0 | 0 | | zone_catchup | 0 | **1** |
| low_quay_run | 0 | 0 | | menu_tour | 0 | 0 |

Greens 23/36 — including **sustain_run 0/0** (the re-cut proven in
the full wall), **dash_strike_rip 0/0** (quirk fix affirmed in
sweep), all six t1 re-cuts, floor1_run + town_gates + district_hunt
(t1's reworded rows hold), and **level_up_beat 0/0** (the flap row
PASSED this sweep — watch stays armed, no action).

### 9.2 Red-vs-census comparison (reason identity, per script)

Every manifest FAIL line grepped from `tmp/wall/<s>_v20-t3.log` and
compared against the s116 census + the t1 logs (`<s>_v20-t1.log`) —
full FAIL sets byte-identical where noted.

| script | this sweep | census reason | SAME? / disposition |
|---|---|---|---|
| aoe_specials | vision `challenge_reads` ("pulse fires in frame_1173, room stays empty") | C2 empty-room cast, challenge_reads | SAME — C2-QUEUED |
| basement_pocket | drop_picked_up 0 | drop_picked_up 0 | SAME — C2-QUEUED |
| burn_duel | vessel_seized / inscription_burned / seizure_ended 0 (byte-identical to t1) | seizure family 0 | SAME — C2-QUEUED; **vision half now GREEN** (quirk fix cleared the stale-banner frames the t1 critic judged a wiped run on) |
| level_gate | actor_died / attack_hit 0 | fight never happens | SAME — C2-QUEUED |
| loot_loop | drop_picked_up / banked 0 (byte-identical to t1) | C2-drift | SAME — C2-QUEUED |
| nest_advance | banked / corpse_loaded 0 (byte-identical to t1) | C2-drift | SAME — C2-QUEUED |
| taunt_anchor | vision `challenge_reads` — NEW red vs t1r | (t1r PASS) | **TRIAGED §9.3 → C2-QUEUED by name** |
| threat_pull | human_leashed / corpse_loaded 0 | leash family | SAME — RETIRED-PENDING-C3 |
| toll_pocket | seal_breached / actor_died 0 (byte-identical to t1) | C2-drift | SAME — C2-QUEUED |
| varekka_duel | challenger_engaged / chant / vessel_seized / seizure_ended / drop_picked_up 0 (byte-identical to t1) | boss seizure never fires | SAME — C2-QUEUED; gate half green as in t1 |
| vat_economy | tribute_paid / body_regrown 0 (byte-identical to t1) | C2-drift | SAME — C2-QUEUED |
| zone8_crossing | body_regrown / tribute_paid 0 + NEW vision class | regrow/tribute beats | manifest SAME — C2-QUEUED; **vision FAIL dismissed §9.4** |
| zone_catchup | human_retargeted / human_leashed 0 | leash family | SAME — C2-QUEUED |

### 9.3 Triage 1 — taunt_anchor (NEW red): C2-class staging drift, t1r UNDER-READ. C2-QUEUED by name.

Verdict text: "The blocker pulse fires but no human anywhere answers
it; the room shows zero response." **The critic read truthfully —
hypothesis (b).** Evidence chain:

1. **Headless sim truth, BOTH sweeps byte-identical:** `EVENT taunted
   frame=880 actor=blocker victims=0` in `taunt_anchor_v20-t1.log`
   AND `_v20-t3.log` — the reel's taunt catches ZERO humans. The
   drift predates T3 (already present in the t1 sweep). Manifest
   stays green because it wants only `taunted: 1` (event fires), not
   victims.
2. **T3 touched no sim:** `git diff 2808a44..HEAD -- src/game/` =
   0 files (full diff: harness/* + src/app/* only — review-pinned C2
   re-verified from disk).
3. **PNG read (frame_0890, gate_a):** hollow rust pulse expanding in
   an empty pocket — no human body anywhere in the room. Matches the
   verdict.
4. **t1r's PASS was an under-read of the SAME vacancy** — its own
   wording admits it: "Pulse fired in an empty pocket with no humans
   inside to answer; response not exercised." Same facts, charitable
   reading. t3's critic read the same class strictly.

Disposition: **C2-QUEUED BY NAME beside aoe_specials** — same class
(challenge/taunt cast into a room C2 re-sequencing emptied), same C3
heir (flee/engage co-tune re-stages it). NOT re-cut (C3's debt), row
NOT reworded. The t1 record's taunt_anchor GREEN is reclassified: a
true determinism/manifest green whose challenge row passed on
charity.

### 9.4 Triage 2 — zone8_crossing NEW vision class: CRITIC MISREAD, dismissed 2-of-3. No row edit.

Verdict text: "Bottom-left way shows a raw red LEVEL 2 line with no
cream-on-chip styling while HUD already reads LEVEL 8"
(`level_gate_reads`). **Hypothesis (a) — pixel-convicted
hallucination:**

1. **The "raw red LEVEL 2 line" is the controls strip's own seat
   label "player 2"** — zoomed crop of frame_3331's bottom-left
   (`tmp/z8_f3331_bl_crop.png`): orange-red "player 2" over a tan
   fixture tile poking into the strip band, beside "J/Space attack".
   No LEVEL-N-REQUIRED line exists anywhere in the reel.
2. **Code:** the level-gate refusal site IS chip-compliant —
   `draw_refusal_text` (renderer.rb:754-762) draws cream
   [225,215,190] on a dark backing chip [12,10,14] α160 for EVERY
   refusal including `:level_required` (T5 P9/D3 law applied at this
   site — spark hypothesis (b) refuted). No surface draws raw red
   LEVEL text. The reel's pack runs level 8 vs requires_level 8, so
   the cue lawfully never fires.
3. **2-of-3 standalone protocol (frozen gate_a, same-artifact law,
   level_up_beat precedent):** reading 2 PASS ("bottom-left text is
   clipped and ambiguous" — the critic itself, on a calmer read) +
   reading 3 PASS ("No legible LEVEL N REQUIRED line appears") —
   logs `tmp/z8_critic2.log` / `tmp/z8_critic3.log`, both full-PASS
   rc=0. Sweep FAIL = the outlier.

Disposition: vision FAIL dismissed; **no gate_checks edit** (the row
is calibrated — the misread was pareidolia on a seat label, not row
miscalibration; no strong-member re-verify owed). zone8_crossing
stays red for its census manifest reason only. Watch: if any future
sweep re-reads the strip's "player N" label as a LEVEL line, reword
the row to name the seat label as non-evidence (edge_pip_reads
precedent).

### 9.5 Greens-superset check

Every v20-t1 TRUE green is green here. Set difference vs t1+t1r
greens: **lost** = taunt_anchor only — reclassified §9.3 as a t1r
under-read (the drift is provably present in t1's own log, so the t1
"green" was not a true pass of the challenge row); **gained** =
dash_strike_rip (quirk fix, s116 debt 2 paid) + sustain_run (T3
re-cut). 23 vs t1's 22. sustain_run GREEN as the law demands.

## 10. Fresh-eyes review (Rule 6) — PASS, zero blocking, five advisories

Headless scrubbed pi (read-only brief `tmp/t3_review_brief.txt`, PI_*
scrubbed, JSON-last contract). Verdict: **PASS**, `blocking: []`,
all C1-C10 checked with file:line evidence (C1 misread guard — hint +
reasons{} pre-exist at base, diff only flips 7iii; C2 src/game/ +
data/balance/ diff = 0 lines; C5 manifest blocks byte-identical,
reviewer recomputed 6/2/12/6/18/2/2 ≥ wants from the teed log
independently; C9 reviewer read the glyph PNG itself). Full verdict
JSON: `tmp/t3_review_out.txt` (banked verbatim, rides tmp/ — quoted
essentials here).

Advisory dispositions (all five executed or accepted):

1. **Manifest-line provenance** — EXECUTED: re-run banked verbatim to
   `tmp/t3_manifest_sustain_banked.log` (line quoted in §7).
2. **apply_start reach-in** (`instance_variable_get(:@banner_queue)`)
   — ACCEPTED: fails loudly on refactor (nil.clear raises), both
   directions test-pinned; a World banner refactor must remember it.
3. **Zero-slack wants** (bought/used/wiped/looted meet wants exactly
   when doubled) — ACCEPTED + PRICED FORWARD: the first sim knock
   re-kills sustain_run; `tools/tune_sustain_run.rb` is the recorded
   re-cut path; see T4 pricing note below.
4. **Wall pendency** — the §9 harvest closes it (honest-pass law).
5. **pt-br supersession of Junior's v18 SUPRIMENTO** — ACCEPTED:
   lawful under owner-ratified L14 + FULL SEAT SYMMETRY; surfaced
   take-or-leave in his queue line; his locale, his word.

**Reviewer's biggest uncovered risk (adopted as a T4 pricing note):**
T4's totem lands at floor -1's contested center — the exact geography
sustain_run's seed-7 choreography walks. Its sim delta will almost
certainly re-kill the zero-slack beats: **T4 must budget a sustain_run
re-cut (tuner re-run + gate + targeted hint critique) as a named cost**,
not discover it mid-sweep.

## Scope notes

- Map artifact header rename (POTIONS) is in-goal (L14 identity) but
  out of the spec's file list — named here; its surface's own gate
  (probes + map_checks row, updated) rides the next map session; the
  header is EN-only locale-invariant text via `header_text`, tested.
- PARKING_LOT: third owner chat vote recorded mid-ticket
  (owner-approved): real-time per-seat-locale translated chat (Roblox
  pattern) — quick-phrases named the promotable rung.
- gamesmith party-hunt sparkup read on owner ask: trigger NOT armed
  (fires at C3 spec time, second wave); the C3 spec author should NAME
  its evidence asks to get cited answers instead of the default set.
