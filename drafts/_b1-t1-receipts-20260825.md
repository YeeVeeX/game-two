# B1-T1 receipts — safe-zone law: data + refusal + guard (s71, 2026-08-25)

Spec: `docs/superpowers/specs/2026-08-24-b1-safe-zones-design.md` §B1-T1
(+ its s71 "T1 build reopen" section, written this session). Foundation
row 6, RATIFIED-G + RATIFIED-J 2026-08-22.

## What shipped

- **D1 data:** `"safe": true` in `data/zones/camp.json` (hand-custody,
  sibling of `hub`) and `data/zones/zone_7.json` (importer emission).
  Nest deliberately NOT covered (ratified list = both hubs exactly).
- **D2 load refusal:** `Core::TileMap` — `safe` reader
  (`cfg.fetch(:safe, false)`, hub plumbing mirrored) + NAMED BadMap when
  `safe: true` meets non-empty `enemy_spawns` (message carries zone +
  both keys). The importer's emit gate composes it automatically —
  proven live when the gate refused this session's own first importer
  test fixture (spawn-carrying zone marked safe).
- **D3 runtime guard:** acquisition refused wholesale while the active
  zone is safe — `select_target` never runs, live focus drops to nil,
  nil write emits nothing; displaced humans still leash-walk home.
  Lands in `Game::Aggro#assign_focus!` (see extraction below).
- **D3 reopen (spec's own clause):** chant-start verified to run
  WITHOUT focus (grill finding 4 was wrong on this) — one guard line at
  the chant-START branch of `World#tick_challengers` (`next if
  map.safe`); cooldown/in-flight machinery untouched; controllers.rb
  untouched as D3 expected. Recorded in the spec.
- **D6 custody answer:** `safe` = LEVEL-FIELD custody
  (`authoring/pilot.ldtk`, def uid 203, V_Bool-backed instances on all
  four levels, true on zone_7 only) — NEVER sidecar (sidecar refuses
  unknown keys; presentation/tuning only). Importer passthrough:
  `level_fields` + Bool refusal + emission directly after `hub`.
  Re-import emitted zone_7 with exactly one new line; basement_1/
  basement_2/dungeon_1 byte-identical (content), provenance pin green.
- **Line-cap extraction (the law fired at 1799/1800):** `Game::Aggro`
  (`src/game/aggro.rb`, new plain object) now owns the per-tick threat
  brain — assign_focus! (with the B1 guard), partition_pressure!,
  surround/pressure ring claims, density_pockets, beachhead_shields?.
  World keeps byte-compatible one-line view delegates (AiController
  duck-type, renderer, telemetry, tests unchanged — controllers.rb
  untouched). world.rb 1799 → 1729.

## Verification

- Suite: 1239 runs, 22617 assertions, 0 failures (was 1227 — +12: 3
  tile_map, 3 importer, 6 safe_zone integration).
- New integration surface `test/game/safe_zone_test.rb`: coverage pin
  (safe set == {camp, zone_7} across all zone files) · adjacent hostile
  in camp over 300 ticks = zero retarget/telegraph/attack_hit events,
  zero damage, focus nil · displaced hostile leash-walks home with hp
  kept · challenger in camp = zero chant/seize events over 150 ticks ·
  district controls prove both refusals zone-scoped.
- Identity pairs (s58 protocol): full wall (33 scripts), baseline from
  a git worktree pinned to `9130603` (mid-sweep contamination law) vs
  post-edit re-capture in the live tree — md5 per stream. Expectation
  pre-declared: ZERO movement (no renderer change; shipped zones carry
  no humans in safe zones, so the guard is behavior-invisible in every
  wall reel). Result: see the addendum line at the bottom.
- Concurrency note: the game-two-uiux seat's `rake uigate` run was live
  on this machine during the baseline sweep (its own repo, its own
  scripts). Judged safe: sims are tick-locked (wall-clock contention
  never changes PNG bytes) — and the pairwise compare is the proof.

## Deviations from the spark/spec (all investigated)

1. Mail inbox not empty: `from-game-two-assets-v21-repin-17b0ae8.md`
   (fire-and-forget FYI, "no reply expected") — archived to done/,
   banked here: assets seat re-pinned daaddd4→17b0ae8 after verifying
   the s68 tier seam (+17/-0 additive, 20/20 constants green).
2. world.rb at 1799, not the spec's assumed 1770 — extraction owed and
   executed (Aggro; the spark pre-ordered this).
3. Spec grill finding 4 wrong re: chant-start — reopened per D3's own
   clause (see above), one extra guard line + test.
4. Suite assertion-count baseline moved 22576→22584 pre-edit (s70
   number vs this session's first hook run, test count identical at
   1227) — minitest shuffle-order wobble in data-driven loops, not a
   test change; post-edit count 22617 judged against the same-session
   pre-edit run.

## Addendum — identity pairs verdict

PASS — 33/33 wall scripts, rc=0 both sweeps, **277/277 capture streams
byte-identical** (`tmp/s71_pairs_base/md5s.txt` == `tmp/s71_pairs_post/
md5s.txt`, diff empty). Zero movement, as pre-declared. The varekka
canary and the vat_economy reel are wall scripts — both pairs identical,
so the standing canary pins are re-proven by the same evidence.
