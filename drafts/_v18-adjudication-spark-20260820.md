# SPARK: v18 session 21 — ADJUDICATION of the SEVENTEENTH (mode FULL: evidence complete, 8/8 answers banked — read, decide, close)

You are the dev of record in game-two (cwd `~/workspace/game-two`).
Read `AGENTS.md` FIRST (rule 8) — the live file beats this spark on
any drift. Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`.
Working language English; owner surfaces es-CR ustedeo (everyday
gamer words — the foreclosure-register audit applies to your own
es/pt output).

**What this session is:** the ADJUDICATION the whole v18 cycle has
waited for. Both ritual coop sessions RAN 2026-08-19/20 (same-day
pair, owner amendment) and ALL EIGHT answers are banked verbatim.
Mode = FULL was established at session-20 close — this session
executes r9's Jobs 1–4 EXACTLY (the r9 spark's wording governs:
`drafts/_v18-seventeenth-harvest-spark-r9-20260819.md` §Jobs 1–4) and
closes v18 if the spec's terms are met. **This is a READING and
DECIDING session — docs-only. No code. No sim numbers move here; the
verdict is what UNLOCKS them.** Quality over cost (owner order): the
quality lever is THOROUGHNESS — every check quoted, every row walked,
every caveat attached mechanically — not spend (council 0, protocol
settled; Bedrock $0; no sub-agents).

## Read first, in order (all are up to date at `26b8f7c`+)

1. `AGENTS.md` — whole file (lanes, red lines, out-of-scope).
2. `docs/CHECKPOINT.md` — TOP entry only (session 20: the whole
   ritual night, T2 ship, quarantine spot, resume point = you).
3. Spec §Fun-verify (THE ARBITER, closed terms):
   `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md`
   — the four Half-A checks + the ROUTING TABLE. The decision is
   written on THESE terms, no others (v17 verdict format precedent).
4. `drafts/_v18-fun-verify-skeleton-20260818.md` — THE working file,
   whole file: ritual session 1+2 blocks (both seats verified) ·
   OWNER ANSWER SET 4/4 + administration amendment · ANSWERS 8/8
   block · both pre-registered caveats · HELD side-signals (owner 7
   observations + fragments + sustain lines) · chain links #1–#6 ·
   deviations named beside everything.
5. Junior's banked files (his seat's own banking, cross-verified
   session 20): `drafts/_junior-ritual-session1-20260820.md` ·
   `drafts/_junior-ritual-session2-20260820.md` ·
   `drafts/_junior-answer-set-20260820.md` (his 4/4, Q1 same-day
   variant noted) · `drafts/_junior-note-difficulty-items-20260820.md`
   (post-answer side-signal, enters AFTER the sets like every HELD
   item) · `drafts/_junior-specials-chain-retry-20260818.md`
   (pre-registered provisions signal).
6. Evidence bytes (verify quotes against THESE, never trust prose):
   `drafts/_v18-seventeenth-evidence/game_two_session_8503.log` (s1
   host) · `game_two_session_2874720530.log` (s1 joiner) ·
   `game_two_session_9048.log` (s2 host) ·
   `game_two_session_63472464.log` (s2 joiner) + consoles.
7. `drafts/_v18-seventeenth-runsheet-20260818.md` §Nota de ruteo —
   read AFTER the answers per its own law.
8. `drafts/_netplay-lag-forensics-20260819.md` — context for reading
   the lag-colored answers + the first post-verdict work item.
9. Project memory traps (auto-injected): verbatim means verbatim ·
   claims are not evidence · `$?` after pipes lies · judge by printed
   output.

## Job 0 — compressed standing gate (~10 min; anything moved = classify FIRST)

Baselines at session-20 close (2026-08-20 ~00:0x, HEAD `26b8f7c`):
launcher logs **34/34** both temp patterns, newest
`game_two_session_9048.log` (23:10, ritual s2, CONSUMED/banked) ·
save md5 `edfebf4accf0abf3aa86bb1170c62714` mtime 23:10, strict
decode (pinned shape) `digest=b5cae357290c01e464f49155bc7f9d13`
sessions=10 banked=7 provisions=0 breached=2 boss_1_defeats=1
notices=[] · Junior tip == origin/main (`3b9821b` consumed) · mail
done/=11, inbox EMPTY · tmp/soak newest `20260819-120805` ·
gate-verdicts tail = T2 spot-gate PASS entries (classified
`fec8b06`). New solo logs = chain links per the #1–#6 pattern (bank,
anchor moves, the s2-host `saved` expectation updates). New Junior
commits = read before rebase. **The owner may open with the audio
asks 5–8 (M5a verdict §Ear-check 2 + ask 9) — that lane is
owner-initiated, runs first if asked, context guard 40% applies (T2
spark precedent); otherwise never nag.**

## Job 1 — Half A: the four checks, formal table (spec wording, PASS/FAIL each, quoted lines)

Walk the spec's §Fun-verify Half-A checks against the BANKED BYTES
(open the log files; do not quote the skeleton's prose):

1. **Digest chain:** s2 host `persist loaded digest=` == the latest
   prior `persist saved digest=` (expected: `3a518bcc…` loaded ==
   s1's saved; NO solo play happened between — verify no log sits
   between 8503 and 9048). Joiner `loaded … source=handshake` == host
   digest BOTH sessions (s1: `66784a92…`; s2: `3a518bcc…`).
2. **Four netplay lines:** desyncs=0 + reason=quit + ticks ≥ 36000 on
   ALL FOUR (s1: 74469/74470; s2: 36079/36079) + AUTOPILOT-free logs.
3. **Separate launches, s2 loads s1's close:** two distinct hosting
   consoles banked; sessions counter 8→9→10.
4. **≥1 strictly-positive carried fact NAMED:** quote the s2 loaded
   line's banked=5 seals=2 sessions=9 (and boss_1_defeats=1 in the
   save decode) — name at least one > 0 in the result.

Each check: PASS/FAIL + the exact quoted line(s) + file. A FAILED
check is an adjudication RESULT (Half A no cumplida), not a bug hunt.

## Job 2 — Half B: the eight answers, read on the spec's terms

- Quote all eight VERBATIM (owner es 4 from the skeleton block;
  Junior pt-br 4 from his file). Deviations already named beside each
  set — carry them into the reading.
- **Attach BOTH pre-registered caveats BEFORE reading** (they are
  mechanical, not judgment): (a) same-day spacing — the "return
  later/next day" continuity reading weakens; (b) symmetric audio
  novelty — both seats had `AUDIO on` both sessions (Junior's own
  lines picked the branch).
- **Only AFTER both sets are read**, admit the HELD material: sustain
  lines (`bought=0 used=0 refused=0/1` all sessions) · Junior's
  pre-registered provisions signal + his post-answer difficulty-items
  note · the owner's 7 mid-session observations + post-session E/ctrl
  notes + fragments ("me encanta … reactivo", "no se puede jugar" lag
  verbatim, crash-night "but it was fun") · the lag forensics
  numbers as CONTEXT for the lag-colored answers (desyncs=0 — stalls,
  not divergence).
- The answers speak for themselves; your prose lives AROUND the
  verbatim, never inside it. No new questions to the humans.

## Job 3 — routing table, row by row

Quote EVERY row of the spec's routing table; mark TRIGGERED / NOT
TRIGGERED with the exact evidence line(s) that decide it. Each
triggered row becomes a RECORDED item with a next-spark shape (the
corpus brief may be CITED inside a recorded item's next-spark shape —
its only legal use). Do not build anything; do not soften any row;
do not invent rows.

## Job 4 — the verdict + the close

- Write the decision in the v17 verdict format (naming precedent:
  `drafts/_v18-fun-verify-verdict-20260820.md`): Half A cumplida
  o no + Half B reading + routing outcomes + the caveats' effect
  stated plainly.
- **CUMPLIDO both halves → v18 CLOSES:** one adjudication line in
  AGENTS.md (current-cycle block; keep it ONE line, the file is a
  thin index) · checkpoint new top entry carries the next-brainstorm
  inputs (v19 pool = 7 intake ideas · M5a deferred lanes + asks 5–9 ·
  flywheel findings · lag-FIRST work item · corpus brief · T3/T4
  world-builder order question). **Do NOT open v19** — that is the
  owners' brainstorm, at their word.
- **Any half NOT cumplida → honest verdict + named backlog,
  owner-paced re-run shape.** Never waive, never fudge, never
  average.
- Post-verdict queue to RECORD (not start): lag instrumentation
  (owner-named blocker; shape in the forensics doc) · audio-lib
  clock-anchor intake review (their `08676dc`, M5a verdict §Mails) ·
  E-skill telegraph flywheel candidate · T3/T4 lane order vs lag —
  all owner-priority calls at the brainstorm.
- Checkpoint + owner queue es-CR (~5 líneas: veredicto, qué abre, qué
  sigue de su lado — probablemente: el brainstorm v19 cuando quieran,
  los asks de audio de la mañana, y nada más owed). Commit docs
  (explicit paths, one concern each, hooks run the suite), pull
  --ff-only (rebase docs-only Junior commits after reading them),
  push promptly.

## Laws that bite

- **The spec's closed sections are LAW** — the four checks and the
  routing table adjudicate as written; disagreement with a check's
  design is a v19 note, never a waiver.
- **Verbatim means verbatim** — answers and telemetry byte-exact.
- **Caveats attach BEFORE reading; HELD signals enter AFTER both
  sets** — order is part of the protocol's validity.
- **No code, no data, no sim numbers, no gates** this session (the
  verdict UNLOCKS the frozen numbers; it never touches them itself).
  The suite runs only via commit hooks.
- **v19 does not open here.** The brainstorm is the humans', carrying
  the recorded inputs.
- Single-instance guard before any launch IF the owner asks for one
  (audio lane) — separate call, judged by printed output; never
  `--fresh`.
- Budget: one attended session, ~2h; council 0; Bedrock $0; no
  fan-outs. Stop conditions: verdict + AGENTS.md line (if earned) +
  checkpoint + queue pushed → STOP. Ritual-shape dispute or a check
  the evidence cannot decide → honest PARTIAL verdict + named gap →
  STOP. Owner redirects mid-session → his word is law, record in one
  line, follow.
