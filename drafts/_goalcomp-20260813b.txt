GOAL: FINISH v11 = DENSITY/RE-MASSING. Steps 0-2 DONE, wall is 6/9 - the work resumes MID-STEP-3. Read FIRST: project CLAUDE.md (Rule 8), docs/CHECKPOINT.md top entry, drafts/_v11-wall-log.md (wall map + re-pilot beat inventories), the committed spec docs/superpowers/specs/2026-08-13-v11-density-remassing-design.md (mechanism, ninth-verify questions + routing table live IN it). Do not re-derive closed decisions.

STATE (measured at goalcomp): branch v11-density at 55751dd (233 commits, tree clean), suite 302 runs/1238 assertions green, checks 40 (deep_drop_band_reads added; #20 template broadened). Step 0 revert wall 9/9+9/9 on main (commit 085b657). Spec f11a643 (adversarially reviewed, 9 findings folded, ledger drafts/_v11-spec-review.md). Implementation: anchoring 7470c2b, rider+telemetry 42c22e6, harness 55751dd. PHASE-1 WALL COMPLETE 6/6 official gates (loot_loop, world_loop, specials_chain, taunt_anchor, district_hunt, ledger_loop; one retry = taunt_anchor labeled critic INFRA, det 10/10). Density telemetry fired in all 9 triage replays (vat_economy pockets{mean=6.0 max=5} arrivals{pocket=13}; seed path live in district_hunt).

SEQUENCE (remaining):
(3a) RE-PILOT vat_economy, corpse_run, threat_pull (their story beats died with the respawn law). rake pilot NAME=<n> SEED=<s>; append commands with printf >> tmp/pilot/<n>/inbox.txt (NEVER Write - byte-offset reader); read log.txt; export emits the replay script; seeds: vat_economy 7, corpse_run 7, threat_pull 42. Re-stage ALL mandatory beats - per-script inventories in drafts/_v11-wall-log.md; exercised-check lists extractable from /tmp/q6_revert_wall_<s>.log verdicts. threat_pull MUST stage a deep kill so a band-2 ember/gold drop is on camera (no stream has b2>0; the new check must be SEEN passing).
(3b) Gate each re-piloted script (rake gate, 2-attempt INFRA retry; logs /tmp/v11_wall_<s>.log + /tmp/v11_wall_summary.log); real check-FAIL -> fix forward per drafts/_d1b-wall-log.md technique; append results to drafts/_v11-wall-log.md.
(4) rake perf ALONE (p95 < 16.6ms; prior 0.224ms) then full rake.
(5) merge v11-density -> main --no-ff, NO push; CHECKPOINT with measured numbers; commit.
(6) BLIND ninth fun-verify: no changelog, owner plays bin/play FIRST; UNIQUE log /tmp/game_two_session_$$.log, never relaunch while a window is open; harvest ALL telemetry (density + q6_cadence) BEFORE questions; questions + routing table FROM THE SPEC verbatim (headline = owner's own oracle: did the session stay alive past "a few rounds"; did the field feel worth pushing into); verdict -> drafts/_v11-fun-verify-<date>.md + CHECKPOINT + commit.
(7) Next scope debate via AskUserQuestion: Challenger (trigger triple-confirmed 6th/7th/8th, declined 2x, fairness ladder mandatory, dossier drafts/_scope-debate-v11.md) vs arc/purpose v12 (A3 + bible, owner wishlist on record) vs whatever the ninth verify routes; scope v12 rewrite + PARKING_LOT + CHECKPOINT committed.

STANDING RULES: export PATH="/c/Ruby34-x64/bin:$PATH" per shell. NO git push ever. Unit prices 8/12/2. Gradient [[0,1.0],[14,1.5],[28,2.0]] + strictly-increasing shape law. Zero balance constants in Ruby (presentational renderer constants OK). Red suite = fix, never --no-verify. Read-before-edit. Judge builds, not briefs. Blind verify integrity. Checks ADD-ONLY at 40. Density values (join 3/cap 5/scatter 2/guard 6) are hypotheses - retune only via verify routing, never unilaterally. Q7 cue redesign PARKED; Nest rename post-bible; video-critic leg PARKED.

OWNER QUEUE (unchanged): council MCP deepseek-r1 us. prefix; council-via-mmh-gateway + when-to-council update (own session); optional tracked bin/install-hooks.

DONE WHEN: 9/9 wall on the v11 build (incl. deep_drop_band_reads exercised) + perf + suite green, merged --no-ff NOT pushed; ninth verify handed BLIND with telemetry harvested first, verdict + routing recorded verbatim; next debate held via AskUserQuestion; scope v12 + PARKING_LOT + CHECKPOINT committed.
