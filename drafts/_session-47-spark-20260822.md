# Session 47 spark — re-author the five stale wall scripts (T2's wake, surfaced by s46's full sweep)

You are the dev of record in game-two (cwd `~/workspace/game-two`).
Read `AGENTS.md` FIRST, then the TOP entry of `docs/CHECKPOINT.md`
(s46 — T3 shipped + the wall-staleness defect), then
`drafts/_prog-t3-close-20260822.md` §Manifest-staleness (the
attribution table + the staging recommendation are the ticket's
foundation). Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`.

**Owner note:** T4's brief-cut (lobber-E growth, P10) is the alternate
job if the peers prefer lane momentum over wall health — say so at
session open and let the human pick. The dev-of-record recommendation
on record: wall first (it is the ritual's regression net, and every
coming pacing retune re-breaks unstaged long reels).

## Job 0 — integrity gate

- `git fetch` → origin tip expected = the s46 docs commit above
  `95a796f` (T3 ship: `fa57a41` + `efc65a0` + `350a185` + `95a796f` +
  docs). Docs-only/disjoint peer deltas = GOOD; anything touching
  harness/scripts/ or sim = classify before proceeding.
- Save `98fe75edb6d72deab18cd48eaa88bdaf` mtime 08-20 15:51 · launcher
  logs 40, newest 08-21 01:39 · mail inbox EMPTY (one fire-and-forget
  assets-seat receipt archived s46) · `drafts/_refs/` untracked.

## The job

Re-author, one script at a time, severity order: **vat_economy** (all
4 rows dead) → **low_quay_run** (economy loop diverges; canonical
zone-start script) → **corpse_run** (terminal bank beat) →
**nest_advance** (corpse sub-beat) → **sustain_run** (counts short).
For each:

1. `rake pilot` (or input surgery on the existing JSON where the
   divergence is late) until the staged story completes under live
   stats; **stage `start.progression` HIGH ENOUGH that mid-reel
   level-ups cannot flip outcomes** (level 5 ⇒ ΔE(6)=880, unreachable
   in a reel — the stat-stability law this ticket establishes).
2. Verify beats headlessly (`test/support/headless_script.rb` driver,
   T2/T3 pattern — temp file, delete after) → update the script's
   manifest to the TRUE staged counts (floors, per double replay).
3. Full `rake gate` (critic ON) + `rake manifest` on the teed log.
   The wall must end the session GREENER, never wider.

None of the five is canary-banked (bank = world_loop/varekka/burn
only) — input changes move no ACTIVE md5. varekka/burn/world_loop are
NOT yours to touch. Zero sim-number moves (scripts + manifests are
harness data; balance/ untouched). Stop when the session budget says
stop — leftovers carry to s48 with a one-line checkpoint note.

## Laws that bite

Ritual wording UNWRITTEN · live save untouched, md5 before/after ·
one-concern commits (one commit per re-authored script, or one batch
commit — your call, defend it) · code frozen during any detached
sweep · owner-pending carry (never nag): ear-checks · audio
footstep/bed renders · coop S1 · SHARED-save first crossing · J-5
spike call · WorldSmith proposal · R-A2 escalation call.

## After

Close note in `drafts/` (per-script: what diverged, new staging, gate
verdict) · ONE checkpoint entry (Job-0 baselines for s48) · s48 spark
(T4 brief-cut, or wall leftovers) clipboarded · commit docs + push
(rebase over peer work, never rewrite).

## Budget + stop

One session. Council 0. Sub-agents 0 (pilot work is interactive, not
delegable). Stop EARLY on: defect-class Job-0 delta, a script whose
story CANNOT complete under live stats without balance changes
(surface it — that is sim-design news, not authoring debt), the same
gate check failing twice for one script, owner redirect.
