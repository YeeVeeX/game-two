# SPARK: v18 TDD session 1 — canary baselines + the round-trip lane (increments 0-2)

You are the dev of record in game-two. Read AGENTS.md FIRST — the v18
scope contract is ground truth. The v18 spec is FINAL (owner ratified
twice 2026-08-17, veto window CLOSED "aprobado"): build it AS WRITTEN;
design forks do NOT re-open. Work in English; answer the owner in his
language.

## Read before acting (in order)

1. `AGENTS.md` — whole file: the v18 contract, standing orders,
   non-negotiables, commands (Ruby PATH per shell:
   `export PATH="/c/Ruby34-x64/bin:$PATH"`).
2. `docs/CHECKPOINT.md` — top entry: state at close, what this session
   owes.
3. `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md` —
   THE spec. Decisions 1-16 + test lanes 1-7 are the build order's law.
4. `drafts/_v18-spec-review.md` — the do-not-relitigate list: 20 Codex
   folds + 9 panel folds are already IN the spec; the 2 refutations
   (peer-quit lands reason=:quit on BOTH seats via BYE_REASONS; no
   stale-snapshot race — worlds construct at the READY→START barrier
   from Params frozen at launch) are settled with source evidence.
5. `git pull --ff-only` FIRST (Junior's seat is ACTIVE; if he landed a
   handoff file in drafts/, validate it: `swarmforge handoff validate`).

## The session's job, in order

0. **Increment 0 — bank canary baselines BEFORE any sim change.**
   One capture replay per wall script into
   `tmp/canary_baseline/<script>/` for ALL 17 scripts in
   `harness/scripts/` (the v17 increment-1 protocol — see the
   2026-08-16 "TDD session 1" checkpoint entry for the banked shape:
   17 dirs, per-script PNGs, manifest.md5). Machine-local, gitignored.
   ONE capture window at a time; this is a multi-hour unattended
   sweep (nest_advance alone is the monster) — start it immediately,
   verify the manifest counts 17 dirs before touching any code.
1. **Increment 1 — `Game::SaveState` + THE ROUND-TRIP LANE** (spec test
   lanes 1+7 — the safety net everything else stands on; v17
   digest-lane-first precedent). TDD: failing test first, then code.
   Authoritative content is the spec; the shape: pinned canonicalizer
   (recursive key sort; Integer/String/Boolean + ASCII-only leaves;
   anything else = raise) · `facts(world)` with the PURE projector
   (pending judgment resolves through the LIVE rules; enumerated
   transient zero-list; carried does NOT persist; ≥1-living assert;
   non-mutating — serialize twice = identical bytes AND digest_snapshot
   untouched) · strict decoder `refusal_for` (named refusals:
   schema/roster/zone/seal-tuple/type/range/duplicate; unparseable
   file included) · `World.new(..., save:)` applying in the PINNED
   order (home_zone → member facts → seat pointers over the LIVING
   set → `restore_breach!` side-effect-free → enter_zone) · THE LANE
   (world A runs T scripted ticks: banks, breach, a death → facts →
   fresh worlds B1/B2 same new seed + facts → digest_snapshot equal at
   construction AND StateDigest boundary windows byte-identical for K
   further ticks) · idempotence · persisted-leaf mutation sweep ·
   veil-tick quit sweep · classification-exhaustiveness table ·
   clamp lanes (hp, provisions) · apply-order lane (non-default home +
   only the third member alive). AFTER GREEN: full canary sweep (all
   17 scripts byte-identical vs increment-0 baselines) + `rake perf` +
   commit + push.
2. **Increment 2 — ONLY if increment 1 is green+committed AND there is
   room to finish it green** (never start an increment you cannot
   finish): persistence IO + solo wiring. `data/persistence.json` ·
   atomic writes (same-dir .tmp → flush+fsync → close → replace;
   replace-failure lane: bounded retry 3×50ms → NAMED error with .tmp
   intact; orphan-.tmp named at next launch) · save coordinator
   (writes IFF save-owner ∧ reason=:quit; joiner-initiated quit still
   saves on the host; desync/conn_lost/protocol/refusal/double-close
   write NOTHING; idempotent) · solo seed fix (spec decision 16:
   per-session seed in main.rb, seed printed, two-launch regression) ·
   `--fresh` + `.bak-<ts>` backup law · `TELEMETRY persist`
   saved/loaded/fresh lines (digest computed from the bytes actually
   written/applied — never an echo) · structural wall pin (harness/
   replay/pilot construct save:nil and never write). Canary sweep +
   perf + commit + push.

## Laws that bite

- Spec-vs-code contradictions → smallest faithful deviation, RECORDED
  in the checkpoint (v17 precedent). Never redesign mid-TDD.
- Hooks run `bundle exec rake` at every commit — a red suite blocks the
  commit; fix the test, never `--no-verify`.
- No mocks: real files (tmpdir), real Worlds. The netplay lanes
  (protocol v2, SESSION transfer, two-session test) are increment 3 =
  NEXT session; do not start them.
- All tunables in `data/**/*.json`; zero balance constants in code.
- Wall pins are sacred: `save: nil` structural; nothing in increments
  0-2 may alter any existing capture byte (the canary sweep is the
  proof, not a review opinion).
- Baselines are MACHINE-LOCAL: if tmp/ gets cleaned mid-session,
  re-bank from a sim-identical line before the next sim-touching step.
- Pull before push at every commit; commit with explicit paths (shared
  cwd law).
- Perf after each sim-touching increment: `rake perf` (p95 < 16.6ms;
  ~0.3ms historical — persistence adds ZERO per-tick work by design).

## Stop conditions

- Stop after increment 2 green+committed+pushed (or increment 1 if
  time runs short) + a new CHECKPOINT.md top entry naming exactly
  where the next session resumes (increment 3: protocol v2 + handshake
  save transfer + two-session netplay lane).
- If the round-trip lane exposes a determinism hole that survives 2
  honest fix attempts: STOP, bank the failing test + artifacts in
  drafts/, checkpoint, hand off. A hole in the safety-net lane is
  cycle-critical information — never route around it.
