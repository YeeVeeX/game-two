# SPARK -- v20 T5: SECOND WALL CLASS (engine, small) -- the lane-D unblock; content priority is OWNER LAW -- staged s120 (2026-08-29)

You are a fresh session executing v20 ticket T5 in game-two
(cwd ~/workspace/game-two). AGENTS.md + MEMORY auto-inject beside this
brief; live files beat this spark on any drift. Ruby per shell:
`export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language English.
Law of the ticket: spec section T5
(`docs/superpowers/specs/2026-08-28-v20-descent-cycle.md`) + foundation
L11 (`drafts/_v20-foundation-20260828.md`) -- foundation wins on
disagreement.

OWNER RULING (s120, Gabriel, recorded in checkpoint + here): CONTENT
PRIORITY -- lane D (floors -2/-3 rethemes, new creature kinds with own
kill_xp rows, cap 13/15) cuts IMMEDIATELY at T5 close. He watched the
soaks and named the gap himself: "I don't see any new zones, areas,
enemies, items, mechanics." T5 is the engine key that unblocks that
content -- keep it TIGHT (small ticket, zero gold-plating), then stage
the lane-D spark at close so the very next session ships content.

MISSION: give zones a SECOND wall class. Today data/tiles.json has
exactly ONE `passability: wall` type and the renderer draws every wall
through the hardcoded `:wall` palette key (renderer.rb wall pass, line
~434: `walls << [tx*ts, ty*ts, ts, ts, :wall] if map.wall?(tx, ty)`).
Junior's v3 MEDUSA drawing needs red inner walls + near-black bounds
COEXISTING -- confirmed-true limitation, s113 review row "one wall
color per zone". Ship: tiles.json second wall type + per-zone palette
key + importer IntGrid mapping + renderer wall pass drawing by each
wall tile's RENDER-REF (not the `:wall` literal) + Rule 2 re-gate.

## STATE YOU INHERIT (verified s120, re-verify from disk)

- T4 SHIPPED (record `drafts/_v20-t4-totem-20260829.md`): totem live
  on floor -1, NINETEENTH delta clock RUNNING (no freeze armed --
  arms at DECLARATION only). world.rb 1712/1800 via the Stations
  extraction; suite = 1328 runs green.
- The wall grew: harness/scripts/totem_pulse.json is a member;
  harness/gate_checks.json = 73 rows (totem_pulse_reads last).
- data/tiles.json: 6 types, int_grid 1-6 taken (next free: 7). The
  `render` field IS the palette-ref mechanism (D7) -- the registry
  accepts a new type as pure data; the renderer wall pass is the ONE
  hardcode (s113 review, "D7 already provided hooks" row: CONFIRMED
  with exactly this caveat).
- Renderer wall-adjacent facts: line ~597 station fill FALLS BACK to
  palette[:wall] -- that is a fallback chain, NOT the wall pass; do
  not touch it.
- Pilot provenance pin (test/tools/pilot_authoring_test.rb): zone_7,
  basement_1, basement_2, dungeon_1, district are BYTE-EXACT importer
  emissions. All OTHER zones are hand-maintained JSONs (lawful to
  hand-author).
- Junior's deep-floor drawings are banked and waiting on this ticket:
  `drafts/_junior-floor2-directions-20260829.md` (+ his zone2 v3 in
  `drafts/_junior-zone2-map-directions-20260828.md`, review
  `drafts/_s113-junior-zone2-directions-review-20260828.md`).

## STEP 0 -- ENTRY (before anything)

1. `git fetch origin` + ff-merge. Read: checkpoint TOP entry (s120) --
   spec section T5 -- foundation L11 -- s113 review verification table
   (the D7-hooks caveat row + adversarial rows) -- data/tiles.json --
   the renderer wall pass (draw_map region) -- src/core/tile_registry.rb
   + src/core/tile_map.rb wall?/passable? seam.
2. Collision check (s56/s104 law): new peer commits touching
   data/tiles.json, tools/import_ldtk.rb, src/app/renderer.rb, or a T5
   CLAIMED line in checkpoint / seat mail. If Junior claimed or landed
   T5: coordinate via checkpoint + mail, never duplicate.
3. Push a `CLAIMED: T5 -- <seat> <date>` line into docs/CHECKPOINT.md
   top BEFORE building (two-seat race law).

## MISREAD GUARDS (each sourced from a live trap)

1. **T5 is SIM-INERT by construction.** The new type is
   `passability: wall` -- identical blocking, identical flow fields,
   identical BFS. ZERO sim numbers move; ZERO world.rb touch owed. If
   you find yourself editing src/game/, stop and re-read the ticket.
   (One gated SIM piece at a time is cycle law; T4 was the piece.)
2. **Importer mapping is ADD-ONLY and the pilot pin proves it.** After
   adding the int_grid mapping, re-run `tools/import_ldtk.rb` against
   authoring/pilot.ldtk and md5-compare ALL FIVE emissions -- they must
   be BYTE-IDENTICAL (no pilot level uses the new value yet). The pin
   test enforces this; run it early, not last. (T4 precedent:
   zones_pre/zones_post md5 table.)
3. **Renderer fix = draw by render-ref, smallest possible touch.** The
   wall pass gains "which render key does THIS wall tile carry" (the
   registry knows; grass variants already prove per-tile render keys
   work for floors). Do NOT restructure draw_map; do NOT touch the
   station fallback chain (~597). Every EXISTING zone must render
   byte-identically -- their walls all carry render "wall" -> the same
   palette key resolves. Prove with canary gates, not eyeballs.
4. **Naming is mechanical** (placeholder law -- no fiction names).
   Pick for what it IS (e.g. `wall_inner` / `wall_2`); the palette key
   mirrors it. One line in the record defending the name.
5. **The Rule 2 gate needs a STRONG MEMBER staging BOTH classes**
   (2026-08-26 residual-tolerance lesson, cited by the spec). A gate
   row worded affirmatively + a reel that actually shows red inner
   walls against near-black bounds in the same frames. The fixture
   decision is yours to propose-and-defend; constraints:
   - The wall needs a PERMANENT regression surface (a script in
     harness/scripts/ = the wall directory is truth).
   - Hand-authoring a small fixture zone OUTSIDE the pilot five is
     lawful (grass_fixture precedent: real zone, wired inbound-inert
     -- INERT law lives in AGENTS.md worldbuilder section).
   - The import->emit->import fixpoint for the NEW int_grid value can
     be proven in importer tests with a synthetic LDtk doc (T4's
     test/tools patterns) -- the fixture zone itself may be
     hand-authored JSON; you do not need an LDtk level for v0.
6. **gate_checks.json is shared and utf-8** (python: encoding=utf-8).
   73 rows -- every row costs every future critique. ONE new row max,
   affirmative wording + honest "not exercised" exemption; gate the
   strong member THIS ticket (totem_pulse_reads precedent worked
   exactly this way, including the honest self-exemption reading on
   district_hunt).
7. **Mid-sweep source freeze**: while ANY gate/replay runs, edit no
   src/ or data/ file (2026-08-20 contamination + T4 observed law).
   Detach gates via nohup + poll the log; a Bedrock read-timeout in
   the vision critic is an INFRA flake -- determinism+manifest still
   judge, re-run the gate standalone (disrupted-gate law, hit live
   twice now).
8. **Authored-file byte laws** (if you touch authoring/*): fixpoint-
   prove BEFORE editing (pilot.ldtk = json indent=2 + CRLF + trailing
   newline; sidecars = indent=2 + LF). data/display.json is CRLF.

## PHASES

1. **Record-first**: open `drafts/_v20-t5-wallclass-20260829.md` --
   name, int_grid value, render-ref design (which registry/tile_map
   readers the renderer will use), fixture plan, gate row wording.
   Then build: tiles.json entry + registry/tile_map surface as needed.
2. **Renderer**: wall pass draws by per-tile render key; palette
   lookup falls back sanely for zones without the new key (name the
   fallback in the record -- a zone declaring wall_inner tiles without
   the palette key should refuse NAMED at load or fall back to :wall,
   pick one and defend it).
3. **Importer**: int_grid 7 -> new type mapping; add-only proof
   (guard 2 md5 table) + synthetic-doc importer test for the
   import->emit->import fixpoint of the new value.
4. **Fixture + wall script**: stage the two-wall surface; cut its
   script (tuner pattern only if input choreography is needed -- a
   static walk-through reel may be enough); captures that SHOW both
   classes adjacent.
5. **Rule 2 gate**: new row + gate the fixture script (affirmative
   verdict on the strong member) + canary gates (world_loop +
   floor1_run or district_hunt) proving existing zones byte-identical
   (determinism half) and visually unchanged (vision half). NO full
   wall sweep -- locality argument: no existing zone carries the new
   type.
6. **Suite green** (`bundle exec rake`, expect 1328+ runs; hooks run
   it per commit anyway). tiles/renderer tests per spec.
7. **Bank**: record (evidence sections open as UNCHECKED placeholders,
   filled only with real artifacts -- the fabricated-evidence trap is
   in global memory now) + checkpoint s-entry + push. Fresh-eyes
   review (Rule 6) on the diff -- fresh session/sub-agent, never
   self-review.
8. **AT CLOSE (owner-approved content priority -- do not skip): stage
   the lane-D first content ticket spark** at
   `drafts/_v20-t6b-floor2-spark-<date>.md` (or the next free ticket
   number; T6 is taken by the B4 mercy knob). It must carry, minimum:
   - Floor -2 = district_two retheme from Junior's banked drawing
     (floor2 directions doc; transcription-faithful build pipeline =
     T1 precedent: build script -> pilot.ldtk level -> importer ->
     deliberate copy), USING the new wall class where his drawing
     wants it.
   - New creature kind(s) with OWN kill_xp rows (deep pays more, L6:
     existing rows untouched), cap 12->13 riding the floor (L5:
     cap never outruns content).
   - The adversarial rows from s113 that bind execution: water must be
     wall-ringed or stay cosmetic (water is PASSABLE today, swim is
     reserved); `rake perf` BEFORE wire-in on any zone bigger than
     zone_8 (88x44 CALASSA = 1.5x current largest).
   - SIM-class discipline: new kinds + cap step = the gated piece of
     THAT ticket; it rides the delta clock (name it in the record).
   - Claim + serial law + the standard entry/collision block (copy
     this spark's STEP 0 shape).

## HARD LAWS (this ticket)

- Serial execution: ONE ticket, this one; CLAIMED at start.
- SIM-INERT: zero balance/sim changes; the delta clock accumulates
  NOTHING from T5 (presentation+data plumbing only) -- say so in the
  record's delta-clock line.
- Manifest wants: never weakened. Standing C2 reds: never touched.
- saves/ untouched; no bin/play; bot logs never fun-evidence.
- Commits: explicit paths, one concern each; suite rides every commit
  via hooks. drafts/_gate-verdicts.log accretes from gates -- commit
  it with the gate evidence commit.

## DoD (then STOP)

- Second wall class renders: fixture reel shows both classes adjacent,
  gate PASS with affirmative verdict on the new row.
- All five pilot emissions byte-identical post-importer-change (md5
  table in the record) + pin test green.
- Canary gates rc=0 (existing zones unchanged).
- `bundle exec rake` green; tiles/renderer/importer tests landed.
- Record banked + checkpoint s-entry + push + fresh-eyes review PASS.
- **Lane-D content spark STAGED and pushed** (owner ruling executed).
- Queue line for the peers (es-CR + pt-br per seat register): la
  segunda clase de pared ya existe -- el motor ya puede dibujar los
  pisos profundos de Junior; el proximo ticket ES contenido (piso -2,
  enemigos nuevos, cap 13).

## BUDGET/STOP (Rule 7)

One attended session. Gates: fixture gate + 2 canaries (+ re-runs only
per the disrupted-gate law). Council: 0 (engine plumbing; taste rides
the lane-D content ticket). STOP and surface on: pilot pin red after
the importer touch (twice = stop, do not force re-emission) -- any
determinism red -- renderer fallback ambiguity you cannot defend in
one line -- context >= 75% with attended work unlanded
(compact-checkpoint skill).
