# v22 TS — fresh-eyes review (s138) — VERBATIM

Reviewer: headless scrubbed `pi -p --thinking max -t read,bash`, detached worktree `tmp/ts/review/wt` at `60a73bd`, brief = `tmp/ts/review/ask.txt` + the record + the diff (52,996 bytes). Start 2026-09-06T19:07:44-06:00, end 19:30:26. Landings: record §8.

```
I have enough evidence. Summary of what I could and could not do, then findings per question, then the JSON.

**Execution limitation (stated up front):** the seat-lease gate blocked every test-runner/sim invocation from this session (`bundle exec ruby -Itest …`, `ruby -e` loading `test/support/headless_script`, even `ruby -rjson -e … File.read`). A LIVE session holds the game-two seat, so I did not route around it. Runtime claims (7/55 totem test, 3/3 canary, 1531/81965 suite) are verified only against the session's pasted artifacts (`tmp/ts/totem_test.txt`, `canary.txt`, `suite2.txt`, `commit4.txt`) plus independent arithmetic and code reading. Everything else below is my own read-only verification.

## Findings

**1. Integer law + Rule 3 — clean.** `totem_heal` is `[heal_min, (max_hp * pct) / 100].max` (stations.rb:152–154), Integer×Integer/Integer; `Creature#heal!` is `[@hp + amount, @max_hp].min` (creature.rb:628–631). `max_hp` is Integer by construction: kit JSON, `max_hp_at` Integer division (progression.rb:74, growth pct validated Integer at progression.rb:29–35), `scale_max_hp!` uses `.round`. `totem_config!` (stations.rb:179–196) refuses the legacy block naming `heal_amount` as unknown AND `heal_min/heal_pct_max_hp` as missing, refuses a missing block, Float, non-positive — each message names the key. `100` is a unit denominator, not a tunable. `pct 0` is a harmless class-level allowance; note the shipped suite actually refuses any pct ≤ 4 via the "scale-with-pool row is dead" guard (352·4/100 = 14), so the comment "pct may be 0 = flat heal_min" promises a config the game's own suite rejects — cosmetic inconsistency.

**2. Owner's words vs shape — the scaling is inert at live pools, and the record misstates where it bites.** Independently recomputed (pools 80/160/60 base, growth 6 %): L13 = 137/275/103, L21 = 176/352/132. At pct 5 every pool ≤ 319 heals 15; `(300·5)/100 = 15` TIES the floor, it does not beat it. The first strict beat is max_hp 320 → blocker **L18** (323 → 16), not "from L16" as the record §3 says (L16 = 304 → 15, L17 = 313 → 15). The design_table's "pct beats the floor from max_hp 300" uses `ceil(1500/p)`, i.e. the tie point. So at pct 5 the owner's "scale with hp pool" yields +1 hp at L18–20 and +2 at cap, for one kit. The record is honest that live pools all heal 15, but its stated onset is two levels early, and that paragraph is what the owner reads to decide. Recommendation below: **pct 8**.

**3. Tests — convict physics, staging legitimate.** `expected_heal` and positions derive from `CFG` (totem_test.rb:19–23, 87). `test_heal_scales_with_the_healed_bodys_own_pool` stages L21 via `load_progress!` + `sync_max_hp!`, big=blocker 352 → 17 (pct branch), small=lobber 132 → 15 (floor), both at Chebyshev 0/1 in ONE pulse, with `refute_equal` guarding that the branches differ. Center-then-LATE is legitimate: bridge-end rusher aggro is the DESIGN (contested bridge), the test isolates totem physics, and `hits_on_pack` fails loudly rather than hiding a hit; in district the only pack damage source is human melee `attack_hit`, so the guard covers it. Two nits: the scaling test has no explicit `hits_on_pack`/pulse-count guard (its equality assertions are implicitly sensitive, but the failure message would mislead), and `assert_equal CFG[:heal_min], heal` (line 112) hardwires that the L1 blocker is on the floor branch — a legitimate data move to pct ≥ 10 (160·10/100 = 16) turns this test red for a non-defect.

**4. Canary rebank — verified from the dumps.** CRLF-normalized `tmp/ts/old|new/*.events` hash to exactly e0b1…/6488…/3fd0… (old) and f023…/6488…/3fd0… (new); `diff` = three pure insertions at frames 482/670/850, `healed=0 range=4`; floor3_run/brasa2_run rc=0. `F67_HISTORY` equals 4d59f6a's `ACTIVE` verbatim. Header accurate (prefix through line 12 = frame 472, first divergent line = inserted frame 482). Could not re-run the canary myself (gate).

**5. Reel + row.** Inputs untouched: the `scenario`..`run_until` span hashes identically (b0f14e4b…) at 4d59f6a and 60a73bd; only `_doc`/captures/manifest moved. probe2.txt matches `_doc` frame/heal numbers and the guardian dies f2057 (new/totem_pulse.events line 88). Probe ring arithmetic reproduces `draw_pulse_ring` (renderer.rb:1588–1600: 35/86 px, alpha 184/131) and `Fx::NUM_FRAMES = 40` covers +12/+28. Frames 2174/2190/2894 show exactly that: crisp 3-px green square, larger+fainter across the pair, `+15` (and `+12`) numerals. **Row defect:** the clause "a WOUNDED pack body stands inside the ring at the beat … a body outside the ring shows no such numeral" is not literally true of the frames chosen — at the beat the ring is 0 px, at +12 it is 1.1 tiles out, at +28 2.7 tiles out, while the healed blocker stands at Chebyshev 3 (frame 2190 shows the `+15` body clearly OUTSIDE the drawn ring). The critic passed by reading "ring" as "reach" (its why never mentions the outside bodies), so the clause cannot discriminate and a stricter critic could false-FAIL. The cadence sentence is unverifiable-from-stills context; harmless, belongs in `_doc`. The row does not weaken the read otherwise — it adds two checkable demands (pair growth/fade, numerals).

**6. Manifest — honest.** Half rule is real (`tools/tune_totem_pulse.rb:121`: `[hits/2, 1].max * 2`); 20 is the same rule on the same count (21) as the v20 cut, so not a relaxation; totem_pulse/fight_resolved are exact ×2. Exactness is already enforced by the md5 pin + determinism half.

**7. Pins — provenance holds.** Gate log line 1 `head=8a60d76`; 8a60d76 = row + reel only; its parent 1aa730f = data+code+tests; 60a73bd touches no RENDER/JUDGED path except pins.json. 38 STALE via `RENDER_PATHS` (data/ moved in 1aa730f) is correct ledger behaviour; not re-gating here is right (ledger ≠ gate; T2c owns) — condition: district reels (floor1_run, district_hunt, world_loop, sustain_run…) now carry a sim-visible change on their own frames and must be re-pinned before the TWENTIETH.

**8. Collision/scope — clean.** `origin/junior/premium-build` (6b4fc36, 84 ahead) vs origin/main (4d59f6a here — the three TS commits are local-only, unpushed) differs in display.json / gate_checks.json / world.rb among watched files; `totem_pulse_reads` row md5-identical on both, `totem_pulse_frames 40` on both, his gate_checks hunks are other rows; his world.rb still constructs `Stations.new(sustain_cfg: data["balance/sustain"])` and calls `tick_totems!`, so the strict config merges cleanly. `git diff 4d59f6a..60a73bd -- src/game/world.rb` empty (1782 lines). Diff = 9 files, nothing from T2a/T2c.

**9. Dark-ship recommendation — directionally sound, proof list needs two corrections.** His save schema is already compatible (main's `Character::OPTIONAL_DEFAULTS` carries `bag`), and his loot RNG is salted (`loot.rb:26`), so streams stay RNG-neutral when OFF. But (a) his `bag` digest group is unconditional (world.rb on his branch: `+ [["bag", @bag.digest_fields]]`), so "digest lines constant" vs main is unattainable as phrased — the proof should be "EVENT streams byte-identical vs main + digest deterministic across seats"; (b) his `data/balance/status.json` carries `chill.step_frames_pct: 0.3` — a Float in a balance file, against the repo law regardless of switch state; his fresh-eyes review should demand the `totem_config!` pattern. Also, (c) gates T2a on his action; the record states the condition, but should carry the fallback ("if his dark-ship commit + canary receipt isn't on his branch by the time T6 closes, T2a runs on main's world.rb and he rebases") to honour the never-gate-on-peer order.

**10. Most consequential:** the pct-5 candidate plus the record's L16/"beats from 300" misstatement — the mechanical work is right; the one thing that could steer the owner wrong is that paragraph.

**pct recommendation: 8.** Smallest value where the scaling is FELT on the body the owner plays at his live level (blocker 275 → 22 vs 15), under his own "30 is too much" ceiling at cap (352 → 28); striker/lobber stay on the floor (their pools never reach 188 — an honest limit of the max() shape, not fixable under 30). Cheap to flip: the reel's L5 pools (198/99/74) all stay on the floor at pct 8 (198·8/100 = 15), so captures/stream are byte-identical and world_loop's canary (healed=0) doesn't move — one data line, re-gate, re-pin. If the owner wants ALL bodies to scale, only an additive shape (`heal_min + max_hp·pct/100`, pct 3 → L13 19/23/18) does it under 30 — that is a spec amendment for his word, not this ticket.

```json
{"verdict": "PASS WITH MINORS",
 "blockers": [],
 "majors": [
  {"id": "M1", "claim": "Record section 3 misstates where the pct branch bites: 'beats the 15 floor from max_hp 300 up (the blocker from L16)'. (300*5)/100 = 15 TIES the floor; the first strict beat is max_hp 320 = blocker L18 (323 -> 16), +2 only at cap. design_table.rb's floor_stops = ceil(1500/p) is the tie point, not the beat point. This is the paragraph the owner reads to choose the number.", "evidence": "ruby -e recompute: blocker L15=294 L16=304 L17=313 L18=323; pct5 -> 15/15/15/16; drafts/_v22-ts-record-20260906.md section 3 'Reading' bullet 2; tmp/ts/design_table.rb floor_stops line", "fix": "Correct the record to 'ties the floor at 300-319, first beats it at 320 (blocker L18, +1 hp; +2 at cap)'; fix the table wording to 'pct exceeds the floor from max_hp ceil((min+1)*100/p)' (320 at pct 5, 200 at pct 8); docs-only, no gate."},
  {"id": "M2", "claim": "totem_pulse_reads row clause 'a WOUNDED pack body stands inside the ring at the beat ... a body outside the ring shows no such numeral' is not satisfiable by the frames chosen: at the beat the ring is 0 px, at +12 it is 1.1 tiles out, at +28 2.7 tiles out, while the healed body stands at Chebyshev 3-4. The critic passed by reading 'ring' as 'reach' (its why never checks the outside bodies), so the clause cannot discriminate and a stricter critic could false-FAIL on frame_2190 itself.", "evidence": "captures/totem_pulse_gate_a/frame_2190.png (blocker with +15 visibly outside the drawn ring); probe_ring arithmetic reach_px 35/86 vs blocker d=3 (96 px); src/app/renderer.rb:1588-1600; gate log why: 'grows across paired frames and a wounded body shows rising +15'", "fix": "Reword to 'a wounded body within the pulse's reach (about four tiles of the fixture; an early still's ring may not have expanded to it yet) shows +N; a body farther out shows none', move the 3-second cadence sentence to _doc, optionally add a +36 capture (ring ~3.5 tiles) so the ring visibly encloses the healed body; re-gate once (bundle with the pct change re-gate)."}
 ],
 "minors": [
  {"id": "m1", "claim": "Spec L20 (4) ('the pulse reads Character#max_hp through the reader') is not met as written: the pulse reads Creature#max_hp (World internals) because TS ran before T2b; only a stations.rb comment records the deferral, the record does not.", "evidence": "src/game/stations.rb:143 m.max_hp / comment lines 124-126; spec section TS 'L20 touched'", "fix": "One line in the record + a T2b checklist item: re-point totem_heal at the Character reader or assert body.max_hp == character pool."},
  {"id": "m2", "claim": "test_pulse_heals_in_radius_clamped_and_never_the_dead hardwires that the L1 blocker pool is on the floor branch; pct >= 10 (160*10/100 = 16) turns it red for a legitimate data move.", "evidence": "test/game/totem_test.rb:112 assert_equal CFG[:heal_min], heal", "fix": "Assert expected_heal(body.max_hp) only, and prove the floor branch on the smallest pool (lobber 60) or via the L1 pool with a skip-if-pct-branch precondition."},
  {"id": "m3", "claim": "test_heal_scales_with_the_healed_bodys_own_pool has no hits_on_pack guard and no pulse-count assertion; a mid-window hit or hitstop fails the hp equality with a misleading message.", "evidence": "test/game/totem_test.rb:131-160", "fix": "Call hits_on_pack + pulses before the cadence loop; assert_empty hits and assert_equal 1 pulse."},
  {"id": "m4", "claim": "Config comment 'pct may be 0 = flat heal_min' promises a configuration the shipped suite refuses (any pct <= 4 fails the 'scale-with-pool row is dead' guard).", "evidence": "src/game/stations.rb:174-176 vs test/game/totem_test.rb:141-143 (352*4/100 = 14)", "fix": "Say so in the comment (class allows it; the game's suite convicts dead scaling) or tighten the config to pct >= 1."},
  {"id": "m5", "claim": "The design table that decides candidate rows lives in tmp/ts/design_table.rb (untracked, dies); the spec points at tools/pacing_table.rb, which is the XP-curve tool and cannot produce this table.", "evidence": "tools/pacing_table.rb header (XP pacing only); record section 3 pastes tmp output", "fix": "Promote to tools/totem_table.rb (pacing_table standing-script precedent) so the owner's-word re-tune recomputes; fix the spec pointer."},
  {"id": "m6", "claim": "Dark-ship proof list says 'digest lines constant with an empty bag' - unattainable: Junior's bag digest group is unconditional; and his status.json carries a Float (chill.step_frames_pct 0.3) that no switch makes lawful.", "evidence": "git show origin/junior/premium-build:src/game/world.rb (+ [[\"bag\", @bag.digest_fields]]); origin/junior/premium-build:data/balance/status.json:20", "fix": "Reword the proof to 'EVENT streams byte-identical vs main with switches OFF + digest deterministic across seats'; add 'no Float in balance data' to his review checklist; add the T2a fallback condition (run on main's world.rb if his dark-ship + receipt is not on his branch when T6 closes)."}
 ],
 "pct_recommendation": {"value": 8, "why": "Smallest pct where the owner's 'scale with hp pool' is FELT on the body he plays at his live level (blocker 275 -> 22 vs 15) while staying under his '30 is too much' at cap (352 -> 28); striker/lobber remain on the 15 floor (their pools never reach 188 - the max() shape cannot scale them under 30, an honest limit to name). Zero-cost to the evidence: the reel's L5 pools (198/99/74) all stay on the floor at pct 8, so totem_pulse captures/stream are byte-identical and world_loop's canary (healed=0) does not move - one data line, one re-gate, one re-pin. pct 5 makes the scaling clause inert until blocker L18 (+1 hp)."},
 "verified_clean": [
  "Integer law: totem_heal Integer*Integer/100, heal! min-clamp, max_hp Integer by construction (kit JSON, Integer growth division, .round in coop scale); no Float ingress in the new path; no balance constants in code (100 = percent denominator).",
  "Strict config: legacy heal_amount block refused naming both the unknown key and the missing heal_min/heal_pct_max_hp; missing block, Float, radius 0 refused by name (stations.rb:179-196, totem_test.rb:213-230).",
  "sustain.json: exactly four totem keys + top-level _doc (ambience.json precedent); Stations ignores _doc; no live doc/code still references heal_amount outside history.",
  "Tests derive expectations from CFG (expected_heal, R, cadence); both formula branches exercised in one pulse (blocker 352 -> 17 vs lobber 132 -> 15 at L21) with a refute_equal guard; center-then-LATE staging is legitimate and the hits_on_pack guard covers district's only pack damage source (human melee attack_hit).",
  "Canary: old/new dumps CRLF-normalized hash exactly to F67_HISTORY (e0b1/6488/3fd0) and ACTIVE (f023/6488/3fd0); world_loop diff = 3 pure insertions at frames 482/670/850 healed=0 range=4; floor3_run/brasa2_run byte-identical; F67_HISTORY equals 4d59f6a's ACTIVE verbatim.",
  "Reel: inputs byte-identical (scenario..run_until span md5 b0f14e4b at both 4d59f6a and 60a73bd); _doc frame/heal numbers match probe2.txt (2162/2342/2522/2702 blocker 150->165->180->195->198; 2882 striker +15 lobber +12; 3062 striker +9; guardian rusher_hater26 dies f2057); probe ring arithmetic reproduces draw_pulse_ring (35/86 px, alpha 184/131); Fx NUM_FRAMES 40 covers +12/+28; frames 2174/2190/2894 show ring growth+fade and +15/+12 numerals.",
  "Manifest: half rule real (tune_totem_pulse.rb:121); attack_hit 20 is the same rule on the same count (21) as the v20 cut, not a relaxation; totem_pulse 50 / fight_resolved 10 exact x2; MANIFEST PASS 50/10/42 in the gate log.",
  "Pin: gate log head=8a60d76; 8a60d76 = row + reel only; parent 1aa730f = data+code+tests+canary; 60a73bd touches only pins.json/drafts -> commit-then-gate holds; 1 PINNED / 38 STALE / 4 FAILED matches pins_after_gate.txt; the 7 s137 pins STALE by the RENDER_PATHS data rule (newest 1aa730f) is correct ledger behaviour.",
  "Collision: origin/junior/premium-build (6b4fc36, 84 ahead) vs origin/main differs in display.json / gate_checks.json / world.rb only among watched files; totem_pulse_reads row md5-identical on both; totem_pulse_frames 40 on both; his gate_checks hunks are other rows; his world.rb still constructs Stations with balance/sustain and calls tick_totems!.",
  "Scope: diff 4d59f6a..60a73bd = 9 files (sustain.json, stations.rb, totem_test.rb, canary test, totem_pulse.json, gate_checks.json row, pins.json, two drafts); src/game/world.rb untouched (1782/1800); nothing from T2a/T2c.",
  "Soak: SOAK PASS, desyncs=0 both seats, TELEMETRY totem heals=0 pulses=18 identical host/joiner, chain intact; live saves/world.json md5 2bd9c92fc65caf8da34c139a2ce53e36 unchanged, level 13, hp 137/275/103 as the record states.",
  "Motivating telemetry verified in drafts/_v20-nineteenth-evidence/session1/game_two_session_2336923504.log: sustain bought=21 used=21; totem heals=8 pulses=19.",
  "Design-table numbers reproduced independently (pools L1/5/13/21 = 80/99/137/176, 160/198/275/352, 60/74/103/132; pct sweep 4..10; 300 hp/min at 15/180 vs 40 at 10/900; ring duty 40/180 = 22 %, 0.33 Hz).",
  "Session test artifacts consistent: totem_test 7 runs/55 assertions, canary 3/3, suite 1531 runs/81965-81966 assertions 0 failures (tmp/ts/*.txt) - NOT re-run by the reviewer (seat-lease block)."
 ],
 "disagreements_with_my_routing": [
  "Reviewer could not execute any test or headless sim: the seat-lease gate blocks test-runner and ruby-loading-test-paths from this session while the LIVE seat is held, and I did not route around it. The brief's 'you MAY run single test files' is unattainable under the current gate; either run the fresh-eyes reviewer in a seat-free window, or carve an explicit read-only exemption for worktrees under tmp/. Until then, runtime claims in this review rest on the session's pasted artifacts.",
  "Heartbeat-vs-flicker is named by the spec as the row's purpose; the 22 %-duty / 0.33 Hz arithmetic is convincing, but I would make the make_clip.sh + self_eval.py critique OWED before the TWENTIETH (one clip, ~$2-5) rather than optional - Rule 2's artifact for a motion claim is a clip.",
  "Agree with not re-gating the 38 STALE pins here, with one named condition: the district reels (floor1_run, district_hunt, world_loop, sustain_run, threat_pull...) now carry a sim-visible change on their own frames (5x pulses, radius 4, possible heals) that nobody has judged; T2c's re-pin must land before the TWENTIETH, not after.",
  "The dark-ship recommendation (c) is the right direction but gates T2a on Junior's action; state the fallback condition explicitly (T2a on main's world.rb if his dark-ship + canary receipt is not on his branch when T6 closes) so the never-gate-on-peer order is honoured mechanically, and add the two proof corrections in m6."
 ]}
```
```
