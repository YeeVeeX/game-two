# v20 T1 — FLOOR 1: ZONE 2 rethemed to Junior's v2b (2026-08-28)

**Ticket:** spec `docs/superpowers/specs/2026-08-28-v20-descent-cycle.md` §T1
(lane A, the cycle's first felt change). **Seat:** hub (CLAIMED `37be092`,
DONE this session). **Landing commit:** `005eab3`. **Status: SHIPPED** —
suite green (1318 runs / 0 F), Rule 2 gate PASS on the zone's own script,
perf PASS, soak PASS ×2 (incl. explicit floor -1 crossing), canary
versioned-rebank complete. Wall re-pin sweep running detached
(`tmp/wall/v20-t1-*`), verdicts owed in a same-day follow-up commit.

## What shipped

ZONE 2 (`district`) is now **descent floor -1** per Junior's ratified v2b
("dois espaços + 4 pontes", 52×88 portrait), transcribed from the
reference PNG (git-blob md5 `0ba972e094bf4ed065a1470e618dd03f`, verified)
at 8px/tile through the lawful pipeline:
`tools/build_district_v2b.py` (extraction + BFS proofs) →
`authoring/pilot.ldtk` new level + `authoring/district.sidecar.json` →
`tools/import_ldtk.rb` → deliberate copy into `data/zones/district.json`.

- **Geometry:** two themed halves — west dry/dark camps, east mossy/alive
  (Junior's own palette lifted from the PNG: floor 38,30,20 · rock/wall
  126,96,58 · trail dirt 66,52,33 · moss 52,76,40 · bridge wood
  168,126,70 · chasm water 13,17,30) — split by a central chasm rendered
  as WATER tiles fully wall-ringed (today's engine, zero new asks; BFS
  asserts no water tile reachable). Exactly **4 bridges** (rows 14-15 /
  34-35 / 54-55 / 74-75); removing the wood provably disconnects the
  halves (asserted). 3-wide corridor standard verified — zero pinch
  carves needed (Junior's drawing already honored his own v1 finding).
- **Spawns:** 27 minions arena-grouped, transcription-faithful: 16 red +
  8 grey markers → `rusher` (24), 3 pink markers → `rusher_hater`
  (bridge-1 + bridge-3 guardians + the east clearing) — district's
  existing kit vocabulary, zero balance edits, hater count preserved.
- **Metadata:** `floor: -1`, display ZONE 2 (placeholder law), sidecar
  owns palette + `drop_gradient [[0,1.0],[35,1.5],[70,2.0]]` +
  `gradient_anchor [11,87]` (the mouth landing; multipliers unchanged,
  breakpoints geometry-scaled).
- **Provenance:** district joins the pilot provenance pin
  (`pilot_authoring_test` ZONES), import→emit→import fixpoint green, the
  other four pilot emissions byte-identical (verified pre-copy).

## Transitions (endpoint law, save-law)

| Way | Row | Notes |
|---|---|---|
| nest ↔ district | district `[0,13] → nest [28,8]` | **byte-identical row**; west door pocket carved |
| camp → district (MOUTH) | camp `[0,5] → district [11,87]`, `type: stairs_down` | spawn moved SW (was [40,13]); requires_* none, as before |
| district → camp (return) | `[42,13] → camp [1,5]`, `sealed`, `type: stairs_up` | **seal station + door byte-identical at [41,13]/[42,13]** |
| district → district_two (NEW) | `[40,0] → district_two [1,13]`, `type: hole` | the −1→−2 continuation (foundation L2: inter-floor ways are destination-independent) |

**The save-law coupling (the ticket's sharpest edge):** the strict decoder
refuses any save whose breached tuple no longer matches a seal's `opens`
(`save_state.rb breached_refusal`) — the owners' live chain carries
`["district",[42,13]]`. The seal block therefore kept byte-identical
coordinates, and (42,13)/(41,13) happen to be PASSABLE in Junior's own
v2b grid — zero geometry contortion. `map_artifact_test`/`save_store_test`
pins confirm. L9 honored: no save schema touch, no migration.

## Deviations from the reference (named, engine-forced)

1. `(40,0)` NE exit tile carved — the hole to −2 (his north marker read
   as the descent continuation).
2. South marker `(11,87)` carved into a 3×2 landing pocket (rows 86-87 ×
   10-12): the PNG walls the marker off with border rock; the engine
   needs the landing walkable. The mouth LANDS here; the camp RETURN
   stays at (42,13) per the save law — his "entra no SW" flow preserved,
   the exit-up door relocated by law, not taste.
3. West door pocket (1..3 × 12..14) + `(0,13)` carved — keeps the nest
   row byte-identical and nest/grass_fixture inbound spawns `[1,13]`
   walkable (their files untouched).
4. **Calm-entry law:** two spawns sat inside rusher aggro (10) of the
   west door: `(8,12)→(12,12)` and `(11,20)→(12,22)` — nudged within
   their own arena/trail so every door keeps the old map's
   nearest-hostile-at-11 property. All other 25 markers land verbatim.
5. Small navy pockets in the rock (his black rectangles) transcribed as
   wall-ringed water voids — same impassable-void language as the chasm.

## Verification evidence

- **Suite:** 1318 runs, 0 failures (hook-enforced at commit `005eab3`).
- **Rule 2 gate (blocking):** `rake gate SCRIPT=harness/scripts/floor1_run.json`
  → **GATE PASS** (`tmp/t1_gate_floor1.log`, gate_rc=0): determinism 10
  captures byte-identical ×2 runs; vision critic PASS on all rows
  including the NEW strong members — `floor1_two_halves_read` ("west dry
  dark / east dense green moss"), `floor1_chasm_reads` ("flat impassable
  void, no body ever inside"), `floor1_bridge_reads` (frame_0990: plank
  band with navy both sides of bodies), `zones_distinct` (tan canyon +
  navy chasm vs nest).
- **Captures eyeballed** (Rule 2 wall, my own read before the critic):
  `captures/floor1_run/` — ZONE 2 banner legible (f250), trail
  single-file chase, mid-bridge crossing with drops (f990), east-half
  moss + LEVEL 2 stamp (f1350).
- **Perf:** `PERF ticks=6990 p50=0.526ms p95=0.764ms max=8.857ms
  zone=district` → PASS (4,576 tiles + 27 humans cost nothing against
  the 16.6ms budget).
- **Soak:** standard `rake soak N=1` PASS (36k ticks ×2 seats, 0
  desyncs, chain intact `fresh -> dec77789`) + explicit floor-1 run
  `ZONES=district TICKS=6000` PASS (`EP1 zone=district`, chain intact,
  combat-outside-hubs asserted by chain_check).
- **God-view/map artifact + seal pins:** suite-covered (map_artifact_test
  green with the byte-identical seal tiles).

## Canary — versioned rebank (protocol satisfied)

- Ratification: the map change is RATIFIED (foundation L3 + spec T1,
  owner live s114); world_loop's input re-cut rides the S73 precedent.
- **varekka_duel + burn_duel: BYTE-UNCHANGED** (`ecc750ec…`/`36d6281c…`)
  — the district roster change provably shifted no other zone's RNG
  stream (creature construction draws none; empirically confirmed).
- world_loop: old `982bfd66…` (37 lines) → new `e0b1f38f…` (42 lines).
  Streams banked: `tmp/canary/old_stream.txt` / `new_stream.txt`.
  Prefix byte-identical through line 5 (both fights open at frame 390
  with `cause=acquired`); every divergent line classes as (a) roster
  renumbering (15→27 spawns), (b) combat re-sequencing on the new
  geometry, (c) drop tiles, (d) respawn anchor tiles. Both streams close
  the loop: fight resolved → drop picked → nest → banked.
- Outgoing bank preserved as `S80_HISTORY` (immutable) in
  `test/harness/sim_identity_canary_test.rb`.

## Script surgery ledger (law 4)

Census ground truth: 20 of 33 world scripts traversed district
(headless zone census). Baseline manifest census (OLD map, same lens):
`tmp/manifest_baseline.txt` — the headless lens itself fails many
manifests pre-change (manifests count DOUBLE-replay events), so
old-vs-new DELTA is the honest regression signal.

- **Re-cut this ticket:** `world_loop` (headless-verified: fight → loot
  738 → nest 869 → banked 1172; manifest banked+drop_picked_up intact) ·
  `dash_strike_rip` (two-victim rip staged: frame 398, one special hits
  rusher14+rusher12) · **NEW `floor1_run`** (zone-own script, T5
  precedent: mouth → trail fight → bridge-4 crossing under its sentry →
  mossy east; manifest fight_resolved≥2 + actor_died≥4; THE Rule 2 reel
  for this floor).
- **Retired-pending-reauthor (consciously, delta-verified):**
  `lobber_reach`, `lobber_volley` (dense old-map lobber choreography;
  their verb surfaces stay covered by aoe_specials/specials_chain which
  still satisfy manifests on the new map) · `corpse_run`, `ledger_loop`,
  `district_hunt`, `low_quay_run`, `threat_pull` (district-leg
  choreography lost its staging; wipe/ledger/leash surfaces remain
  covered by mercy_floor/nest_advance-family + unit suites). Re-author
  rides the wall follow-up or lane-D sessions — each keeps gating on the
  determinism half meanwhile.
- **Unaffected by delta:** all remaining scripts (baseline == new
  failures or OK==OK); their re-pins ride the detached sweep.
- **gate_checks.json:** district rows rewritten for v2b (zones_distinct,
  gradient_depth_reads → mouth-anchored arena-group wording,
  drops near-the-mouth wording, low_quay contrast) + three NEW
  affirmative strong-member rows (two_halves / chasm / bridge — the
  2026-08-26 lesson: gate the strong member, never only tolerances).

## Known seams (named, not hidden)

1. **Nest-door band seam:** with the gradient anchored at the mouth
   (11,87), the nest door pocket sits in band 2 (d≈76-78) — a fresh
   chain entering from ZONE 1 farms 2.0× drops immediately. Priced as
   acceptable this ticket (the descent flow is the cycle's identity; the
   nest edge is L2's late-row problem). Watch at the NINETEENTH.
2. **Beachhead pockets:** both door pockets sit inside beachhead_tiles
   (4) — hostiles refuse acquisition around them (arrival grace). On
   this map that makes the landing pocket a readable safe nook; combat
   tests that staged inside it were moved out (threat_pressure lesson:
   the shield silently no-ops aggro staging).
3. **Trail single-file chase:** the trail spine strings chargers into a
   line (good bridge/dash grammar, visible in floor1_run f250); arena
   packs still group-fight inside blobs.

## Test evolutions (all cited v20 T1 in-code where non-obvious)

`tile_map_test` (district graduates to typed/floor -1; camp carries
exactly ONE typed row = the mouth) · `tile_variants_test` (district now
draws typed surfaces + new affirmative assert) · `import_ldtk_test`
(fixture compares its own committed expected emission; live-district
coupling moved to the pilot pin) · `pilot_authoring_test` (+district) ·
canary (above) · combat stagings re-coordinated to v2b geometry:
homecoming, threat_leash, threat_pressure (incl. wall-pinned target +
heal-top-up staging maintenance), guard_scope, provocation, coop_feel,
density_respawn, world_test (volley wall / mark / cornered-lobber /
gate-distance / arrival pins), seal_breach, drop_band, threat_respawn,
challenge, challenger.

## Owed / next

- **Same-day follow-up commit:** wall sweep verdicts
  (`tmp/wall/v20-t1-*`, DONE marker + per-script rc lines) banked here.
- Fresh-eyes review (Rule 6) — staged after this record lands; a failed
  review blocks T4 (lane A/C), never T2/T3.
- Junior's ratification of the played floor (his v2b, his call) — rides
  his next play session; RATIFIED-J on the cycle remains async-pending.
- T2 (cap 12 + pacing table) and T3 (potions identity) are their own
  sessions; the NINETEENTH's delta clock starts at T4's totem, with this
  retheme named beside it as presentation+graph change.
