# SPARK: v17 execution opens — multiplayer etapa 1 (brainstorm → forks → spec)

You are the dev of record in game-two. Read AGENTS.md FIRST — the v17
scope contract is ground truth (owner-ratified 2026-08-16: multiplayer
etapa 1, live lockstep co-op with Junior over Tailscale, one shared
deterministic sim). This session opens the cycle's execution. The owner
is present at the start; work in English, answer him in his language.

## Read before acting (in order)

1. `AGENTS.md` — whole file: the v17 contract, the five named forks, and
   the STANDING ORDERS (no lore in this repo; placeholders only; text =
   functional/placeholder, locale-invariant names).
2. `docs/CHECKPOINT.md` — top entry: state at close + the mandated
   execution order for this session.
3. `PARKING_LOT.md` — the v17-debate-close section + the long-banked
   multiplayer research notes (lockstep-over-Tailscale staged path;
   GameLift rejected — Junior has no AWS).
4. `drafts/_junior-etapa0-20260815.md` + `drafts/_junior-etapa0-20260816.md`
   — the cross-machine determinism evidence and the digest machinery the
   cycle builds on (3/3 sim-identity digests identical on the current line).
5. `docs/JUNIOR.md` — his seat's setup (pt-br, his machine, timezone).

## The session's job, in order

1. **Brainstorm** the etapa-1 shape against the banked facts: the sim is
   tick-locked (Window#update = exactly one tick; slows, never skips);
   sim-identity is proven cross-machine twice; Tailscale is a trusted
   overlay (host+join only, no open internet).
2. **Close the five forks** — dev recommendation + rationale for each,
   owner veto (v13 precedent). Present them one at a time if the owner is
   responsive; if he goes absent, record recommendations and proceed:
   - second-player embodiment: shared pack / own pack / seat-swap
   - tick-sync + input-delay scheme (lockstep cadence, input buffering)
   - session bootstrap: host/join + seed exchange
   - desync policy — etapa-1 LAW: detect LOUDLY (per-N-tick digest
     compare, counted + visible) and end the session honestly; NO
     rollback/resync (that is etapa 2+ material, parked)
   - latency budget + the stop condition that aborts a session
3. **Spec** at
   `docs/superpowers/specs/2026-08-16-v17-multiplayer-etapa1-design.md`
   + dual review (Codex leg FIRST, fold findings; then the panel with the
   Rule-7 token envelope declared up front).
4. Stop after the spec commit — TDD starts next session, **digest lane
   FIRST** (it is the safety net everything else stands on). If the owner
   is absent mid-phase, checkpoint to drafts and stop.

## Laws that bite in this cycle

- **No mocks in the netplay test lane**: the integration test is two REAL
  sim instances in one process exchanging inputs tick-by-tick.
- **The wall stays single-player and untouched.** New UI (connection
  state, waiting screens, second-player identity ring) = Rule-2 visual
  surfaces: scripted capture + critique before ship; placeholder/
  functional text only.
- **Junior's seat is ACTIVE and pushes**: pull before push, every time;
  never push main; coordinate parallel work via drafts/ +
  `swarmforge handoff validate`.
- Suite must stay green at every commit (hooks enforce); perf budget
  16.6ms p95 stands — lockstep must not regress the tick.
- Oracle at cycle end (the SIXTEENTH ask): did a real two-seat session
  HOLD (digest counters, both seats) + did it feel like playing TOGETHER
  (both players asked separately). Everything in the spec serves those
  two halves.
