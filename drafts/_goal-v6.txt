PROJECT: game-two - Ruby+Gosu grid ARPG (monster flip). Repo: C:/Users/gabri/workspace/game-two. Claude = dev of record; owner = tester. main at 85accc8 (54 commits), 65 tests/180 assertions green.

ACTIVE WORK: A0.5 KIT SPECIALS + PACK MARK. M2.1 fun-verified ("feels better now"); owner directive: "add some spells and methods of teamwork". Direction locked by owner Q&A: one special per kit (big-moment ~10s cadence) + focus-target mark. Gambits stay A1.

SPEC (REVISED, dual review folded - the binding work order): docs/superpowers/specs/2026-08-09-a0.5-kit-specials-pack-mark-design.md. Reconciliation: drafts/_a05-review-reconciliation.md (Codex REJECT 8 findings + Fable lanes driven by dev-of-record after agent stalled 2x; 14 findings folded, all code-verified). Keystones: (1) ACTION SPINE - generalized current_action (:attack/:special), per-action frames/damage/arc, per-victim hit registry; resolve_attacks reads the action, never kit[:attack] directly. (2) Slam=ring control + windup-interrupt override (stagger! alone does NOT stop in-flight telegraphs; rushers are interrupt_on_hit:false). (3) Volley=owner ref + frames_left countdown in tick_world (NEVER absolute frame - hitstop advances @frame while sim frozen), tick_impacts before resolve_attacks, cleared in enter_zone (covers transition+wipe). (4) Lunge=GridWalker plan_dash (ONE scan returning landing+crossed+duration, consumed by dodge AND Lunge; two scans = replay divergence). (5) STAGGERED special exhausts 480/600/720f (equal clocks = rotation homework), second swap-inert clock, revive! resets to ready. (6) PROVENANCE LAW: voluntary Tab REFUSED mid-special windup/active (mirrors stagger refusal); recovery does not block. (7) Mark: pack-owned, leash 14t from possessed bounds the aggro-gate override, cleared on death/re-mark/enter_zone/leash; wired via controller view param (guard nil - tests pass nil). Controls L/E special, ;/Q mark, both EDGE_TRIGGERED. HUD: special pip per bar. core/input.rb needs ZERO changes; window.rb +2 lines only.

BUILD ORDER (kill-cheap, corrected): 1. action spine + Slam (THE probe - stop if spine fights state machine) 2. provenance law 3. Lunge 4. Volley 5. mark 6. harness lanes + pips + specials_chain.json + new vision checks (never weaken existing 13).

NEXT SEQUENCE: writing-plans skill over the spec -> implementation plan -> branch a0.5-specials-mark -> execute (test-per-fix, commit-per-task) -> rake + perf + 3 gates -> impl-diff adversarial review (NEVER merge unreviewed) -> fold -> merge --no-ff -> owner fun-verify: "cast->swap->cast: situational or rote? allies a weapon you aim?"

NEW TOOL: grok-voice-consult skill (workspace/.claude/skills/) - Grok 4.3 via mmh LiteLLM gateway, reasoning=high temp=1.0, for ALL player-facing text/names/lore/UX copy consults. Grok proposes, bible adjudicates. Trap: small max_tokens -> content=None (keep 16k default).

PARALLEL TRACKS (owner call at promotion, do not start): D0 loot loop (death-economy design c293420, docs/design-corpus/death-economy-design.md, 3-critic gated) OR A1 gambits OR fiction-binding session (A0 + A0.5 + death-economy order forms; bible b027453 on main, docs/lore/) OR audio/visual identity pass.

STANDING RULES: rake gate = BLOCKING (double replay + md5 + vision verdict; SKIP_CRITIC=1 = iteration only). rake perf blocking p95<16.6ms. Read-before-edit. One-bundled-change-set. No mocks. Measure never recall. window.rb<=300 lines. Data-driven JSON (zero balance constants in code). Events registered on first use. De-slop: names from INSIDE fiction, spec-speak ships. PATH: export PATH="/c/Ruby34-x64/bin:$PATH" per shell. Commit per task; never push (no remote). Workflow tool historically flaky -> Agent fan-out fallback; bank verdicts to drafts/_*.md immediately.

DONE WHEN: A0.5 merged post-review, all gates green, owner replays and verdicts the chain (fun-verified or produces next concrete directive).
