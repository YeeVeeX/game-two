# E1 — harness truth (T0 d1–d9, b1/b2, a5, d18) — s135, Gabriel seat

Ticket: spec §5 E1 (`docs/superpowers/specs/2026-09-05-v22-one-body-cycle.md`);
findings `drafts/_t0-review-20260905.md` §2 (raw verbatim in
`drafts/_t0-review-20260905/area_b.md` + `area_d.md`). Law:
`drafts/_v22-foundation-20260905.md`. Claimed at `d47bee4`.
No fun-verify pending (the eighteenth closed, the twentieth undeclared) — no
measurement freeze armed; the wall, manifests and gate rows are free to move.
SIM untouched: harness + scenes + rows + one renderer-construction factory.

## Pieces (fixed order, one commit each)

### (1) Critic PERSONA rewrite (d8) — commit `03e38b1`

`harness/vision_critic.py` PERSONA still framed every verdict on flat-rect
placeholder art ("do not ask for sprites… Player is ember orange, enemies
pale bone") after sprites + material tiles shipped — Rule-2-critical: every
PREMIUM gate was judged against frames that no longer exist. Rewritten to the
v22 presentation truth, derived from source (not memory):

- 32×48 sprites (manifest.json `frame_w/frame_h`, anchor [2,14], 4 facings)
  on 32px material tiles.
- Gold possession halo + bobbing chevron = the possessed YOU
  (`renderer.rb draw_possession_halo`, `possess_halo_rgb [255,214,120]`);
  white ring RETIRED (art-less fallback only). Partner halo cyan
  (`partner_ring_rgb [80,200,220]`), no chevron.
- Families by silhouette + colour ramp (gen_premium_art.py KITS ramps):
  humans bone-pale; warden pink / stinger cyan; lurker pale green; serpents
  violet (petrifier pale stone); ember red-orange; spore green; pack =
  striker ember orange / blocker deep rust / lobber pale amber.
- HUD PANEL **top-left** (`hud.rb` header + `hud_plate_rect [20,8,352,108]`
  — the spark-up said "bottom-left", code wins), controls strip bottom edge,
  minimap top-right (`minimap.rb rect`), boss nameplate + phase pips + hp bar
  top-center (`boss_bar_y 112`), zone banners.

Two-person voice + verdict-law text + JSON contract UNCHANGED. Also fixed
`KEY_MOMENTS_PROMPT` "Threketh" → generic (E5/d17 overlap, no-lore order).

**Baseline gate (the persona's Rule 2 evidence):**
- [x] `rake gate SCRIPT=harness/scripts/world_loop.json` → **PASS first-pass,
  86/86 rows, determinism 10/10 byte-identical** (tmp/e1_gate_world_loop.log,
  verdict banked in `drafts/_gate-verdicts.log` 20260905-224100 approx —
  committed with piece 2). Historical first-pass context: ~5/22 fails (FASE 1
  sweep) and 2/37 (T7) under the OLD persona; the flip-rate read for the new
  persona is the full sweep below.

### (2) Pins + verdict log → MAIN ledger (d4) — commit `5f8762b`

`Pins::DEFAULT_PATH` and the critic's verdict-log/critique paths now resolve
through `git rev-parse --path-format=absolute --git-common-dir` (main clone =
no-op; worktree = home; no git = old local fallback), with
GIT_DIR/GIT_INDEX_FILE/GIT_WORK_TREE/GIT_PREFIX scrubbed — the pre-commit
hook exports GIT_* and the first commit attempt broke on it live (a child
`git worktree add` died on the parent's index.lock; same class would misroute
the resolver under any hook/rebase-exec).

- [x] Plain-clone probe: `Harness::Pins::DEFAULT_PATH` =
  `C:/Users/gabri/workspace/game-two/harness/pins.json` (test-pinned).
- [x] Worktree probe: `git worktree add` throwaway → DEFAULT_PATH points
  HOME, `vision_critic._main_repo_root()` points HOME (test-pinned; current
  source copied into the worktree — it checks out HEAD).
- Note: `drafts/_gate-verdicts.log` is TRACKED (Junior banked it s134); both
  seats append; conflicts resolve as pure union (s134 precedent).

### (3) SKIP_CRITIC / CHECKS-override pin refusal (d5) — commit `9ca02be`

`pins.rb record` reads the env the gate rode: `SKIP_CRITIC=1` → named refusal
rc 2, no ledger write; `CHECKS` present and ≠ `harness/gate_checks.json` →
named refusal; CHECKS explicitly = default records fine. `run_wall.sh` drops
an exported CHECKS up front. Netplay gates (harness/net/, own checklist) never
record pins — refusal keys on pin RECORDING, not gate execution.
- [x] Suite tests: both refusals + explicit-default allow + run_wall content
  pin (real subprocess with env).

### (4) The census manifests re-cut or retired (d6) — commit `890f627`

Fresh observation: `Headless.run_script` single-run EVENT counts (manifest
law: declared min per DOUBLE replay; tune_sentinels margin = declared as the
single-run count). Observed table (single / declared):

| script | declared | observed single | judgment |
|---|---|---|---|
| level_gate | actor_died 1 · attack_hit 5 · zone_entered 3 | 0 · 0 · 2 | **RETIRED** — the level-up beat is dead; the refused-then-pass door story cannot happen. `level_gate_reads` = debt row (no live stager). |
| loot_loop | drop_picked_up 1 · banked 1 | 0 · 0 | **RETIRED** — drops spawn (3) but pickup misses, bank never fires; beats stay staged by world_loop/corpse_run/sustain_run/ledger_loop. |
| nest_advance | banked 4 · corpse_loaded 2 · corpse_looted 2 · pack_wiped 2 · drop_picked_up 8 · vessel_kept 2 | 0 · 0 · 0 · 0 · 1 · 0 | **RETIRED** — all advance beats dead; covered by corpse_run/ledger_loop/sustain_run. |
| threat_pull | retargeted 20 · leashed 20 · corpse_loaded 1 | 16 · 0 · 0 | **RE-CUT** → `{human_retargeted: 16}`. Leash observes 0 in EVERY live script — chase/leash staging owed (debt). |
| vat_economy | tribute 2 · regrown 2 · banked 2 · inscribed 2 · pickup 6 | all 0 | **RE-CUT** → `{attack_hit: 27, actor_died: 5, zone_entered: 2}`. ALL economy beats dead; the L16 re-author ticket owns restoring them; its 2 debt vision rows untouched (spark-up fence). |
| zone_catchup | entered 5 · retargeted 1 · leashed 2 | 6 · 0 · 0 | **RE-CUT** → `{zone_entered: 6}`. Crossings healthy; the chase-displacement beat observes 0 — `zone_catchup_reads` weakened (debt). |

Retired files carry `_retired` headers naming the owed re-author (F6/T7
precedent). `gate_scope.json` gate_fixture reason updated (menu_tour stages
it now). Wall 42 → 39 at this piece.
- [x] Re-cut manifests re-verified green against fresh observation.
- [x] `gate_checks_audit_test` green.

### (5) zone8_crossing re-authored on the seal-less DUNGEON 1 (d7)

Old script staged a DUNGEON 1 seal breach RETIRED by the swap
(`dungeon_1.json "stations": []`), inputs choreographed on the pre-swap map;
`gate_scope.json` still claimed dungeon_1 for `seal_breach_reads`.

New route (tuner on the tune_sentinels reach() pattern, exact-landing on the
rope, INTERACT crossing — rope_spot is an interact transition): pack_spawn
[10,8] → west chamber south → row-25 causeway east → middle column north →
NE strip → rope [29,7] → H → zone_8 [62,18] → 4 steps in. Staged level 12
(level 8 and 10 collapse against today's 24-stinger fauna — sentinel law
probes legibility, not difficulty; rope requires_level 8 still exercised).
Manifest from observed singles: `{zone_entered: 3, actor_died: 9,
attack_hit: 25, projectile_fired: 23}` (zone_entered ×3 = birth + start-zone
apply + the crossing). `seal_breach_reads` scope → `["basement_2"]` ONLY —
toll_pocket actually stages a live seal there (declared + census-green).
- [x] Headless double-run md5 identical (f63ad045db == f63ad045db).
- [x] `rake gate` PASS (vision first-pass, determinism 10/10 byte-identical) + `rake manifest` PASS (doubled 6/18/50/46 >= declared 3/9/25/23) - tmp/e1_gate_zone8.log.

### (6) Four white-ring rows → halo grammar (d9) — done

`taunt_convergence_reads`, `god_mark_reads`, `writ_frame_reads`,
`burst_legibility_budget` anchored on the retired WHITE possession ring (with
art loaded the ring branch is dead fallback, never drawn). Re-anchored to the
gold ground halo + chevron / partner-halo grammar (words from
`renderer.rb draw_possession_halo` + `display.json`); judgment intent
unchanged. Also fixed 12 double-encoded em-dashes (`â€”` → `—`) found while
in the file — the bytes were valid UTF-8 mojibake the critic read literally.

Affirmative gates after the wording change (s121 lesson — strong member):
- [x] `specials_chain` GATE PASS — `taunt_convergence_reads` **affirmative**
  ("Humans stack adjacent to the possessed blocker after the pulse;
  self-anchor working as designed") + `burst_legibility_budget` affirmative
  ("Volley tiles and the rust pulse never flood the space").
- [x] `taunt_anchor` GATE PASS — `burst_legibility_budget` affirmative
  ("ring shock and rust pulse frames … halo … readable"); taunt row
  self-gated (single taunted human on the sampled frames).
- **Debt rows named:** `god_mark_reads` — NO live script presses `mark`
  (verified: zero `hold.mark` across harness/scripts) — self-gates
  everywhere until a mark-staging script exists. `writ_frame_reads` —
  affirmative capture arrives with piece 9's `boss1_writ`.

### (7) Renderer.build factory + netplay art (d1/b2)

- [ ] pending

### (8) Gate rows for PREMIUM passes 3–11 + petrify/blink/aura (d3/b1/a5)

- [ ] pending

### (9) Boss sentinels + boss-bar row (d2/d18)

- [ ] pending

## Sweep

- [ ] `harness/run_wall.sh v22-e1` detached; `rake pins` pasted.

## Review (Rule 6)

- [ ] headless scrubbed reviewer, verdict verbatim.

## Rule 7 declared spend

Critic Bedrock calls (pre-cleared AWS-internal): baseline world_loop gate ×1,
zone8 gate ×1, net gates ×3, piece 6/8/9 affirmative gates, full sweep ×39+3.
Reviewer ~USD 5 one pass. Council: USD 0 (mechanical harness truth).
