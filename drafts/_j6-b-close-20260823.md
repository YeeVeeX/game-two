# J6-B close — prefs + settings shipped (s54, Junior's seat, 2026-08-23)

Ticket: `drafts/_j6-menu-brief-20260823.md` §J6-B (md5 `00802b28…`).
Commits: B1 `feat(prefs)` (inert module) + B2 `feat(presentation)`
(settings screen + wiring + reel beats). Dev of record: Junior's seat
(pi via the shared gateway), per s53's Next row. This draft records
evidence + deviations per the T5/J6-A close shape.

## Evidence (the ladder, in order)

- **Suite:** 1137 → 1145 green (prefs round-trip/degradation lane,
  switch! lane, settings unit lanes, swallow-guard case; hooks re-ran
  the suite at every commit).
- **Identity pairs** world_loop + low_quay_run, md5 vs a PRE-CHANGE
  `61dbf06` baseline captured from a clean worktree
  (`tmp/j6b_baseline_md5.txt`): **24/24 byte-identical** at the full
  B working tree. B1 alone constructs nothing in live code
  (inert-by-construction; pairs were run once against the complete
  package, named honestly here).
- **menu_tour determinism half: PASS** — 7 captures byte-identical
  across two runs (extended reel: settings beats appended, run_until
  700 → 840).
- **Manifest: PASS** {zone_entered 2, attack_hit 5, actor_died 1} —
  floors verified by grep on the SINGLE-run log
  (`tmp/j6b_menu_single.log`, the J6-A deviation-2 lesson); the
  extended tail added zero events (menu-open presses route NullInput
  by construction).
- **Dev-eye frame read (sampling-artifact law):** all 7 PNGs read by
  the dev before any verdict — f0360 four root rows legible, cursor
  on RESUME, world readable behind the veil; f0540 same panel ~180f
  later with the striker bar visibly damaged (still-ticking); f0600
  CONTROLS sheet full glyph table incl. MOVE + MENU/Escape; f0770
  SETTINGS panel `> LANGUAGE: EN / WINDOW SCALE: AUTO / FULLSCREEN:
  OFF` + hint, world readable behind; f0660/f0820 closed, damage
  persisting.
- **Strings:** all B rows ×3 locales landed VERBATIM from the brief's
  pinned table (settings/language/scale/fullscreen + invariant
  AUTO/ON/OFF); parity test green.

## Deviations + owed rows (each named, none silent)

1. **VISION VERDICT OWED (critic-ON re-gate → hub seat).** This
   seat's AWS credentials are dead (`InvalidClientTokenId` on both
   junior profiles; `CRITIC_AWS_PROFILE` has nothing valid to name),
   so `vision_critic.py` cannot run here. Shipped with: determinism
   half PASS + manifest PASS + the dev-eye read above. The critic-ON
   `menu_tour` run is ONE command on the hub seat and is owed BEFORE
   anything ships on top of this surface. Gate law not waived —
   handed, named, to the seat that holds the eye.
2. **Volume rows DEFERRED (brief D13, the named path):** the audio
   seat's RECEIPT had not landed at execution (inbox empty at
   session open and at ship). No volume/mute rows, no feature
   detection shipped; `menu.volume`/`menu.mute` strings wait with
   the rows. Defer travels to J6-C or a rider.
3. **window.rb honesty row:** landed **265** — exactly the brief's
   estimate (cap 300, test-enforced). C re-prices from 265.
4. **Rule 6 banked row RESOLVED (RESUME-confirm attack leak):**
   chose SWALLOW over ratify-as-feel — `route` returns the held
   NullInput for exactly the close frame after a RESUME confirm
   (`@swallow_route`, cleared on open so a stale swallow never
   crosses a reopen); unit cases pin both the swallow and the
   one-frame scope. Esc-close still routes real input on its close
   frame — `:menu` is world-inert (A-close grep), unchanged.
5. **menu_reads check text moved with the surface** (same commit,
   netplay-checks precedent): root rows now read RESUME / CONTROLS /
   SETTINGS / QUIT; new self-scoping `menu_settings_reads` (66th
   check). The reel NEVER commits a setting change (nav-only visit;
   LANGUAGE stays EN — check-comparability law D11).
6. **D12 observation note — DONE (same day):** Junior's live pt-br
   session (log `game_two_session_118674722`, clean quit via the
   SAIR row — `persist saved`, sessions 6→7): scale presets and
   fullscreen obeyed live, locale switch flipped every surface, and
   `data/prefs.local.json` appeared machine-written
   (`pt-br`/`auto`/`false` = his final picks). The one observation
   captures cannot make (v16 (a) precedent), now on record.

## Banked-amendment carrier check (brief §carrier)

Cue-silence guard / spell_growth duplicate keys / EventLog curation:
J6-B touched none of their trigger files — all stay banked. J6-C's
banked row (menu frozen OPEN over a partner-end screen) rides
untouched to C.
