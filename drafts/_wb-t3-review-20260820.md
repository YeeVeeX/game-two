# WB T3 fresh-eyes review — receipt (2026-08-20, session 26)

Scrubbed read-only pi (`pi -p --no-session`, PI_* env scrubbed), prompt
`tmp/t3/review_prompt.md`, reviewing ba912cf..5cc4115 + ticket doc +
spec §T3. Verbatim output below the rule.

**Adjudication (dev of record):** VERDICT PASS, 8/8 laws CONFIRMED —
ship stands. Both cheap defects fixed in place same session (the T4
precedent):

1. Poller comment overclaimed respawn safety → fixed with the STRONGER
   mechanism, not a comment edit: adjacency guard (Chebyshev 1 — a
   multi-tile jump is never a step), unit-pinned; side effect: multi-tile
   dodges no longer fire footsteps (the cue-spec mail's own named risk —
   P.S. appended to the sent mail, new md5
   `d556358aa68d4d10c39f27191801a8cc`).
2. Unmapped-grid-char refusal now has its own named message + test pin.

**Mandate note (named, not absorbed silently):** the reviewer session
ALSO handled an unrelated incoming seat mail (knowledge-hub round7B
closure note → archived + delegated to gamesmith's live seat by mail).
Out of its read-only brief; the ACT was audited after the fact and
stands (the mail's own text said "yours to bank or delegate"; game-two
owes nothing — s25 banked our side; the delegation mail content was
verified correct). Lesson: review prompts now say "touch NOTHING
including seat mail".

---

VERDICT: PASS

LAWS:
- **L1 CONFIRMED** — `git diff --stat` touches zero `src/game/` files; `src/core/tile_map.rb:56` `passable?` is still the bare `'#'` check (diff adds only the pure readers `char_at`/`used_chars`); registry changes are load-time validation, not tick behavior; the v12 re-anchor trap is defused because `district` declares `gradient_anchor [1,13]` (grass_fixture's inbound arrival can't move the band map) and the fixture has `enemy_spawns: {}` (no RNG-draw shift); verified live: `md5sum -c tmp/t3_world_loop_baseline.md5` → 29/29 OK against the post-change captures on disk.
- **L2 CONFIRMED** — `poll_world` (src/app/audio_bridge.rb:337-358) writes only `@audio.handle_event` + `@out.puts`; poller/`@last_*` state is bridge-local; `test_fixture_walk_with_bridge_is_sim_invisible` (test/app/audio_bridge_test.rb:200) digests 5 windows with/without audio over a 300-tick walk that crosses grass→dirt→stone AND the plaza region boundary — both new lanes exercised; `../game-two-audio/vendor/miniaudio.dll` present here, so the test runs rather than skips.
- **L3 CONFIRMED** — mechanism is structural: `tile_variants.rb:44` `next if map.palette[ref] == floor_rgb`, and nest's `dirt [28,24,22]` equals its `floor [28,24,22]` (data/zones/nest.json:7,17), so nest yields ZERO rects (pinned by `test_nest_dirt_remap_is_footstep_only` + `test_live_remapped_zones_draw_nothing`, same law on the god-view path in `map_artifact_test`); the ticket's baseline claim independently re-verified live (29/29 md5 OK above).
- **L4 CONFIRMED** — selection is FNV-1a over `"zone:x:y"` (src/app/tile_variants.rb:21-27); grep of all three new draw paths finds no `rand`/`Random`/`sample`/`shuffle`/`Object#hash`; caches key on map identity but hold values that are pure functions of zone config + registry.
- **L5 CONFIRMED** — `test_grass_fixture_stays_inert` (test/core/tile_registry_test.rb:46) scans every other zone's transitions and refutes `grass_fixture` as a target; live transitions grep confirms the only edge is outbound (fixture→district), dev entry via `--start-zone`.
- **L6 CONFIRMED** — `hooks` still a reserved key that raises (src/core/tile_registry.rb:24,99 + `test_hooks_stays_reserved_after_variants_unlock`); `passability` still refuses `swim`; all new types are `floor`; region `intent` is consumed only by `ambience_key` (the D9 layer explicitly shipped now) — no damage/pursuit/spawn code reads it; fixture is threat-free by data.
- **L7 CONFIRMED** — each seat's bridge polls its OWN possessed body (`window.rb:130` passes `seat: @session.seat`, solo path defaults 1); `body.object_id` and poller state never leave the bridge; nothing is written into World; netplay digests hash sim state only, and the L2 digest test pins the polling lanes byte-blind.
- **L8 CONFIRMED** — the old code iterated the FULL effective char map, and camp's palette has no `dirt`/`grass`/`wood` keys (verified), so registering the T3 types would have refused every live zone; the new scope (`used_chars` + `tile_types` keys) fixes that with no hole: a grid char with no registered type hits `effective[ch] → nil → @types[nil] → nil → raise BadMap "unknown tile type"` (src/core/tile_registry.rb:64-70) — actually STRICTER than the old code, which silently skipped unmapped grid chars.

DEFECTS:
1. src/app/audio_bridge.rb:166 — comment overclaims: "respawn teleports reset the anchor without firing" — `FootstepPoller#step` resets only on zone/body-id change, so a same-zone respawn (wipe while already in the home zone rebinds the SAME body object to a distant tile) fires one spurious footstep cue. Pure-sink, deterministic, unmapped no-op today — **cheap** (fix the comment or add the guard before owner renders land).
2. src/core/tile_registry.rb:69 — the unmapped-GRID-char refusal (the exact L8 hole case) is verified by reading but pinned by no test; the only unknown-type test routes through a `tile_types` override (test/core/tile_map_test.rb:185-189). Also the message reads `tile char "z" maps to nil: unknown tile type` for that case — awkward wording. **Cheap** (one test + message polish).
3. Review note, not a diff defect: the suite could not be re-run by this reviewer (seat-lease blocks test runners while session 01a021d0 holds the seat — policy honored, not routed around). Green-claim evidence accepted instead: hook-enforced `bundle exec rake` on all 7 commits, plus banked gate logs verified on disk (`tmp/t3/spot_gates.log` world_loop + low_quay `GATE PASS` rc=0; `tmp/t3/gate_fixture2.log` determinism 8/8 + `GATE PASS`) and the live 29/29 baseline re-check.

ONE PARAGRAPH: This is a clean T3 — the ship's central risk (moving sim or look while wiring presentation) is defended structurally rather than by promise: passability stays the `'#'` law, footsteps/ambience are read-only polls whose emissions terminate in the sink (digest-pinned with the library actually present), and nest's remap is invisible by palette-equality construction (a mechanism I could and did re-verify live: current world_loop captures still match the pre-change baseline 29/29). The `validate_map!` rescope is the one genuine behavior change outside presentation, and it is both necessary (camp et al. lack the new palette refs — the old scope would have refused every live zone) and strictly sound (unmapped grid chars now refuse where they previously passed silently). Tests assert at or above the ticket doc's claims (palette equality, zero rects, INERT scan, digest invariance, god-view variant resolution); the evidence trail in tmp/ matches the doc's citations, including the honestly-reported first gate FAIL. The two defects are comment drift and a missing pin on an already-correct refusal path — neither blocks; ship stands.
rc=0
