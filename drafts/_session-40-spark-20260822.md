# SPARK session 40 — v19 opens for real: harvest, receipts, and the first lane grill

You are the dev of record in game-two (cwd `~/workspace/game-two`).
Read `AGENTS.md` FIRST (rule 8 — live file beats this spark), then the
top of `docs/CHECKPOINT.md` (s39 = THE BRAINSTORM, agenda A–F complete)
and `drafts/_v19-foundation-20260822.md` END TO END — it is the cycle's
law: 4 lanes RATIFIED-G (28 ledger rows), ritual shape frozen at design
level, everything PENDING-J. Ruby per shell:
`export PATH="/c/Ruby34-x64/bin:$PATH"`.

## Job 0 — harvest gate (baselines from s39 close)

`git pull --ff-only` FIRST (Junior may have pushed his ratification
pass, the WorldClaw write-up, or play banks — read before rebasing).
Baselines: save `98fe75ed…` mtime 08-20 15:51 · launcher logs 40,
newest 08-21 01:39 · mail inbox EMPTY. Classify every delta before
building; defect-class deltas preempt (classify + ask).

## The menu (owner-priority; solo progress is default per the
## 2026-08-22 order — lanes are RATIFIED-G and buildable now)

1. **Junior's marks landed?** (hub chat or his session output) → flip
   PENDING-J rows in the foundation ledger; if ALL ratified → the
   AGENTS cycle-section rewrite is UNLOCKED and owed THIS session
   (v18-closed block → v19 lanes; template: current AGENTS structure;
   one-concern commit). Objections → record verbatim, route to the
   humans, touch nothing contested.
2. **Assets receipt mail** (owed from s39, format in their v12 mail):
   `RECEIPT: v12-design md5 0a87985625a069f574b391219cb5e8e3 read=yes` ·
   `RECEIPT: gating-decision = deferred (game feel untouched while
   integration is parked; re-decide after the J-5 projection pick)` ·
   `RECEIPT: capture-contract = queued-for-v19-intake (fence:
   session-end only, zero per-tick cost)`. Write via the write tool to
   `~/.pi/agent/mail/game-two-assets/inbox/`.
3. **Lane 1 grill opens** (grill-and-ticket skill): PROGRESSION v1 spec
   — XP curve pick (shelf note re-verify before any number lands in
   data/), damage-formula stack, save schema v2 + round-trip lane
   FIRST, pack-level carrier, lobber-E growth, `requires_level` gate.
   world.rb extraction (Progression object) is OWED at the cap —
   plan it into ticket 1, not as an afterthought. Spec →
   `docs/superpowers/specs/`; tickets → drafts/.
4. Owner-paced (never nag): ear-checks · T3 renders · coop S1 ·
   SHARED-save crossing · J-5 spike on owner word · WorldSmith details
   (incoming — zero speculation until they land).

## Laws that bite today

- Measurement hygiene: ritual question wording stays UNWRITTEN until
  spec freeze; sim numbers (progression/difficulty/respawn/sustain)
  are UNFROZEN until the ritual stages — Lanes 2/3 data moves are
  sequenced EARLY for exactly this reason. R-A2 stays silent.
- One knob per re-session (verdict law, carried into Lane 2/3 staging).
- Rule 2 blocking gate + wall debt on every new visible surface; suite
  via hooks; no lane code dispatched to Junior's seat before his marks.
- Docket is pointers-only; foundation ledger is the decision record.

## Budget + stop

One session. Council 0 (the s39 exception is spent). Sub-agents: none
by default; the C3 research spoke stays undivided until owner word.
Stop when: harvest classified → receipt sent → (marks? AGENTS rewrite)
→ Lane 1 grill produces its spec skeleton + first ticket → commit,
push, checkpoint, s41 spark clipboarded. Stop early on owner redirect
or defect-class delta.
