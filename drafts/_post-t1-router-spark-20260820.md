# SPARK: post-T1 ROUTER — T3 lag harvest if T2 evidence exists, else P1 (R-A2 sustain discoverability)

You are the dev of record in game-two (cwd `~/workspace/game-two`). Read
`AGENTS.md` FIRST (rule 8) — the live file beats this spark on any drift.
Ruby per shell: `export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language
English; owner surfaces es-CR ustedeo (everyday gamer words — never the
foreclosure register; that drift bit our own es/pt once, memory 2026-08-18).
Quality over cost: council 0 default; Bedrock = only what Rule-2 gate
critics cost. Grill-and-ticket discipline on whichever lane runs (read
that skill first; declare the grill budget up front, grill ≥ implementation).

## Program state (2026-08-20, origin/main `638fa68` at staging — verify live)

- v18 CLOSED (SEVENTEENTH CUMPLIDO); **v19 NOT open** (owners' brainstorm,
  at their word). Priority list P0–P6: session-22 spark (`git show
  1fd00c8:drafts/` or the checkpoint). P1 = R-A2 sustain discoverability.
- **P0 lag: T1 SHIPPED + fresh-eyes PASS** (commits `7629052` T1a always-on
  handshake/close-line telemetry · `dacf946` T1b `GAME_FRAME_PROBE` frame
  probe · `f2420d0` review closure). Full battery green (wall + 3 netplay
  gates + perf + flag-ON soak). Receipt:
  `drafts/_lag-t1-review-20260820.md`. Spec/tickets:
  `drafts/_lag-spec-20260820.md` + `drafts/_lag-tickets-20260820.md`.
- **The banked verdict so far:** the JOINER seat paced both ritual
  sessions (~53.5 Hz vs host 60.5 — counter arithmetic + audio-drift
  oracles, forensics §Correction). Junior's machine facts (S0-J2,
  EXECUTED by his seat): display **59 Hz**, Intel HD 3000 (2011), i3-2310M
  — 53.5 ≈ 91% of a 59 Hz vsync ceiling. Hardware/draw-path leads.
  Spike class (0.8–3.3 s) is a SEPARATE phenomenon, undiagnosed.
- **T2 probe matrix STAGED, owner-paced** (never nag):
  `drafts/_lag-probe-runsheet-20260820.md`. S0-J (Junior solo ~4 min,
  flag ON) = the decisive segment; S1/S2/S3 coop; S0-J2 already done —
  do NOT re-run it.
- **RATIFIED 2026-08-20 (owner, law):** Junior's work-split offer answered
  — Recorte A **SÍ** (his seat = P0 execution/measurement, zero source
  code his side, banks into `drafts/_junior-*`) · R-A3 **CONGELADA** until
  the brainstorm · world.rb touches = DIFF if ever assigned · LDtk GUI
  install SÍ. Answers doc: `drafts/_hub-answers-work-split-20260820.md`.
  His seat may therefore have pushed measurement docs — pull FIRST, read
  anything new under `drafts/_junior-*` before routing.

## Job 0 — standing gate (~10 min; anything moved = classify in writing FIRST)

Baselines at staging: `origin/main 638fa68` · save
`saves/world.json` md5 `edfebf4accf0abf3aa86bb1170c62714` mtime 08-19
23:10 (strict decode digest `b5cae357…` sessions=10 banked=7
provisions=0) · launcher logs 34/34 both temp patterns (newest
`game_two_session_9048.log`) · mail inbox EMPTY, done/=13 · tmp/soak
newest `20260820-020422` · untracked `drafts/_refs/` (by design).
EXPECTED deltas, classify-then-accept: Junior-seat docs-only commits in
his lane (`drafts/_junior-*`, CLAUDE.md-side files) · S0-J/S1-segment
launcher logs IF the owners played (that is T2 evidence — route to T3) ·
his seat's soak scratch runs. Single-instance guard before any launch,
SEPARATE call, judged by printed output. `--fresh` NEVER.

## THE ROUTER (walk in order; first gate that fires wins; state your route in one line before working)

1. **Owner-initiated lane open?** (audio asks 5–9, the v19 brainstorm, or
   any explicit owner order in chat) → that lane preempts everything.
   For the brainstorm you bring the recorded inputs (checkpoint session
   21/22): lag status, R-A1–R-A4, flywheel R1–R7, world-builder T3/T4
   order, the 7 intake ideas, R-A3 diagnosis + GAP, the work-split
   precedent. Propose ONE order, defend with touchstones, execute their
   call. Do NOT open v19 yourself.
2. **T2 evidence exists?** — any of: `TELEMETRY frame_probe` /
   `NETPLAY handshake` lines pasted in chat from real play ·
   new `drafts/_junior-*lag*` segment docs · new launcher logs on this
   machine from S1–S3 · the owner says they played. → **Route T3.**
3. **Else → Route P1.**

## Route T3 — lag harvest + verdict (docs + at most ONE data-only fix ticket)

1. Bank every segment's bytes md5-identical into
   `drafts/_lag-t2-evidence/` (host logs from this machine's temp dir;
   Junior's lines arrive pasted or via his seat's drafts — verbatim means
   verbatim).
2. Per segment, extract: `NETPLAY handshake` (d, link_slow, rtt_ms) ·
   `TELEMETRY netplay` (per-seat rate = (ticks+stalls)/run_ms; stall
   asymmetry; worst-run pair ratio = stall_ms_max/(stall_worst_run−1),
   guard the 0/0 degenerate) · `TELEMETRY frame_probe` (period modes,
   update vs draw, over-censuses) · samplers (tailscale path flips,
   netstat retransmit deltas).
3. **Check S0-J against the recorded prediction** (runsheet §S0-J2):
   period p50 ≈ 16.9 ms with a ~33.9 ms vsync-miss mode, draw{} names
   the eater. Say MATCHED or DIVERGED explicitly. DIVERGED = re-plan the
   hypothesis in writing, do not force the 59 Hz story.
4. Answer in writing, in the forensics doc: **which seat limited the
   lockstep, and what ate its frame** — plus the spike-class verdict
   (path RTO vs machine hitch vs focus/drag), each claim with a log line
   or file:line.
5. Emit exactly **ONE fix ticket (T4)** with its own verify + its own
   session. NEVER implement the fix here (before/after comparability —
   the tickets file law). If the verdict is "his hardware ceiling +
   accept", say so honestly — a null fix is a legal verdict; the ticket
   then becomes the spike-class mitigation OR a recorded acceptance.
6. Checkpoint + owner queue es-CR (~5 líneas, everyday words) + push.

## Route P1 — R-A2 sustain discoverability (presentation, Rule 2 + wall + locale trio)

**Problem (verdict routing row 4, TRIGGERED):** provisions/sustain exist
and were never touched — `TELEMETRY sustain bought=0` on 4/4 ritual
lines; the owner asked "cuales provisiones?". His question is **BURNED**
as a measure (the explanation was delivered post-verdict): the next
measure is BEHAVIOR — bought>0 in future session lines + the refusal
REASON sub-item. The PRICE debate is a recorded brainstorm debate — no
value in `data/**` moves this session.

**Grill first (budget declared, ≥ implementation):** answer in writing —
where does a player LEARN provisions exist (strip? vat prompt? nothing?);
what is the smallest visible exposure that changes behavior (a strip
label + count? a vat-adjacent hint line?); which wall scripts' captures
change (the controls strip renders in EVERY frame — a strip change can
re-pin the whole wall: cost it BEFORE choosing the surface); what does
the refusal-reason log line need to discriminate (no_banked vs at_cap vs
wrong_zone vs …, from the actual refusal code paths). Read before
deciding: `src/app/controls_overlay.rb`, the sustain/vat code (grep
`TELEMETRY sustain` and `refused` in src/), locale string files (grep
how existing strip labels resolve through Core::Strings), verdict §row 4
/ R-A2 only (do not re-read the whole verdict). Kill any exposure that
does not plausibly move `bought` — cite the touchstone (Tibia
vendor-legibility research shelf: `hub kb query --domain game-research`).

**Tickets (cut to fit; likely two):** (a) code+data: the visible exposure
— strings through the locale trio (en / es-CR / pt-br, placeholder
register, functional verbs only, human-facing-output checklist; language
critique = separate accuracy/presentation axes, BLOCKING at ship) +
Rule 2 gate on every affected wall script (run the affected subset
detached; full `harness/run_wall.sh` ONLY if the strip surface moved —
detached, ~5 min/script, judge by per-script rc lines, never a bash
timeout); (b) code, log-only: `TELEMETRY sustain` gains `refused_reason=`
counts (no gate risk, chain_check untouched — verify its regex first).
TDD both; fresh-eyes review (scrubbed pi, diff + tickets) BLOCKING
before push, receipt in drafts/.

**Out of scope, named:** provision PRICE/effect values · vat mechanics ·
R-A1 respawn scalars (still blocked on P0's T3/T4 read) · any sim-class
change · anything R-A3/R-A4 (frozen debates).

## Junior seat coordination (Recorte A ACTIVE — the ratified split)

`git pull --ff-only` before ANYTHING and before every push; his docs-only
commits rebase clean (read them first — the law). His seat banks
measurement into `drafts/_junior-*` — harvest, never edit his files, never
push into his lane. The repo is the channel; seat mail on this machine is
for THIS machine's sessions. If his seat's numbers contradict yours,
verify from bytes before arguing — then argue with citations.

## Laws that bite (unchanged, the short list)

- Deterministic gates decide; bots/critics advise. A failed gate or
  critique BLOCKS ship — never advisory.
- Measure before tuning: no balance/pacing/netplay values move outside an
  authorized T4.
- Claims are not evidence — file:line, log line, or UNVERIFIED tag.
- Read-before-edit · one-concern commits, explicit paths (never `git add
  -A`) · hooks run the suite · long jobs (gates, walls, headless pi)
  DETACHED, never under a bash-call timeout — poll and judge by printed
  output; a disrupted gate is judged by a standalone re-run.
- Two concurrent instances fork the save — single-instance guard, always.
- No new visible surface without its Rule 2 gate + wall debt paid in the
  same session. Log-only additions carry no wall debt.
- Owner overrides are law the moment they land — record in ONE line.
- Never write into `~/workspace/game-two-lore` or Junior's lane.

## Budget + stop conditions

One attended session ~2.5–3 h, context guard at 85% → compact-checkpoint
skill. Council 0; no fan-outs; the only sub-agent is the fresh-eyes
reviewer. **Stop when (T3):** evidence banked + verdict written + ONE T4
ticket + checkpoint/queue + push. **Stop when (P1):** grill + tickets +
both tickets shipped behind gates + language critique PASS + review PASS
+ checkpoint/queue + push. **Stop early, honestly, if:** the grill kills
the premise · a gate fails non-mechanically · T2 evidence is partial
(bank what exists, name what is missing, do not synthesize) · the owner
redirects.
