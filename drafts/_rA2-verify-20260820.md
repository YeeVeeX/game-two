# R-A2 verification receipts (2026-08-20)

Spec: `drafts/_rA2-grill-spec-20260820.md`. Ticket B committed `0f62c7f`
(suite green via pre-commit hook: 925 runs, 0 failures). Ticket A verified
below.

## Suite

`bundle exec rake` green at Ticket A working tree: 925 runs, 17709
assertions, 0 failures (line-cap test included — world.rb 1795/1800 after the
PriceSheet extraction the cap forced; extraction is delegating code motion,
every `station_price` call site + test untouched).

## Wall cost — measured, not guessed (grill Q3 discharged)

Method: one replay per wall script → md5 manifest per script, BEFORE
(`tmp/rA2_base/`, clean-HEAD code) vs AFTER (`tmp/rA2_after/`, hint code).

**Contamination incident (named):** the first baseline sweep ran while the
renderer edit landed (09:03:34) — every replay process started after it
loaded post-change code. 8/18 baselines were invalid (respawn_telegraph →
world_loop by start time). Recovery: `git worktree add tmp/rA2_clean HEAD`
(pre-change `df38cb7`; `grep -c sustain_hint` = 0 verified) and re-baselined
the 8 from there. Lesson banked to project memory. Ticket B (telemetry) is
capture-inert — log text only, PNG bytes untouched.

**Result (tmp/rA2_recover.log Phase 3):** 15/18 UNMOVED byte-identical;
MOVED = `low_quay_run` (frames 1, 4198, 5217, 8610), `nest_advance` (1777,
3266, 10310, 12973), `vat_economy` (2787).

- Spawn-frame claim held where it applies: every default-start script's
  frames are byte-identical (banked=0 at spawn ⇒ no hint). low_quay_run's
  frame 1 moved because its zone-start staging pre-banks 2000 — hint
  correctly visible from frame 1 there.
- `sustain_run` UNMOVED at its original 8 captures: they sit ON press frames
  (receipt cue live at the bank tile ⇒ hint suppressed) — the suppression
  design working as specified.

**Frame-level attribution (sampling-artifact law):** read the exact PNG
pairs. `low_quay_run/frame_0001` before vs after: identical except
`U PROVISION -5` above the banked numeral `2000` at the bank tile (altar
`-8` untouched beside it). `nest_advance/frame_1777`: same pattern over
numeral `6`. Diff is solely the hint.

## sustain_run capture extension

The sustain surface's own script now captures the hint's three states —
frames added (sim-inert; only the dump list changed): **2429** (post-bank,
pre-buy: hint visible), **2560** (refusal receipt live: hint suppressed,
REFUSED owns the slot), **2585** (buy would refuse, cue expired, body still
in ledger radius: hint absent — never advertises a refusal).
Frame-2585 correction (own-eyes read of the gate capture): the absence-state
there is BROKE (banked numeral 0, PROVISION 1 counter live), not at-cap as
first narrated — the buys drained banked faster than the armchair timeline
assumed. The check's meaning is unchanged (absent whenever a buy would
refuse); the gate's own teed telemetry adjudicates the session's refusals:
`reasons{at_cap=1 broke=1 none=0 no_effect=1 seat_race=0}` — Ticket B's line
discriminating live, byte-identical across both gate replays.

## Rule 2 gates (BLOCKING) — verdicts

`tmp/rA2_gates_run.log` + per-script logs in `tmp/rA2_gates/`:

- `low_quay_run` rc=0 — GATE PASS (determinism + vision)
- `nest_advance` rc=0 — GATE PASS
- `vat_economy` rc=0 — GATE PASS
- `sustain_run` rc=0 — GATE PASS (11 captures byte-identical ×2 runs)
- **Targeted hint critique** (`harness/sustain_hint_checks.json` on
  `captures/sustain_run_gate_a`) rc=0 — 5/5 PASS:
  `hint_speaks_when_buy_would_succeed` (frame_2429, banked 10, pack
  adjacent) · `hint_absent_when_it_would_lie` (0061/11510) ·
  `receipt_owns_the_slot` (2430/2522) · `hint_legible` ·
  `hint_composition_exact` ("reads exactly U PROVISION -5").
- 15/18 wall scripts byte-identical ⇒ their standing verdicts carry (a
  byte-identical capture cannot change a vision verdict).

## Netplay gates — not owed, argued

`harness/scenes/netplay_scene.rb` banks at most 1 (`bank! 1` desync poke) —
banked=1 < cost=5 ⇒ the hint cannot render in any netplay gate frame. The
suite's netplay/digest tests are green (PriceSheet construction is
pre-lockstep, per-world, deterministic).

## Language critique — composed line, separate axes

Surface: `U PROVISION -5` / `U PROVISIÓN -5` / `U SUPRIMENTOS -5`
(composed: binding glyph + ratified `hud.provisions` + altar/vat `-price`
grammar; ZERO new string keys).

**Accuracy:** U is the live primary binding (`data/bindings.json` sustain
`["U","R"]`, resolved through the same `Core::BindingMap` that feeds input —
a rebind updates the hint). PROVISION/PROVISIÓN/SUPRIMENTOS are the exact
stock words the counter and cues already use (owner-ratified strings). -5 is
`provision_cost` quoted from the same `PriceSheet`/economy source the verb
charges — the hint cannot drift from the sim's price. The line appears ONLY
when the advertised action would succeed — it never lies about availability.
Alternate key R unshown — deliberate whisper (primary-glyph convention;
the strip teaches both keys once stock exists).

**Presentation:** world-space CAPS house style (numeral/cue family), noun
translates + glyph/price locale-invariant (placeholder register law),
es-CR accent correct (PROVISIÓN), pt-br noun is the ratified SUPRIMENTOS.
Three tokens, no verbs, no filler — matches the quiet-HUD register.

Vision-critique language dimension rides the targeted hint critique
(`harness/sustain_hint_checks.json`, check `hint_composition_exact`).

## Fresh-eyes review

<!-- receipt: drafts/_rA2-review-20260820.md -->
