# SPARK — v20 T4: TOTEM PILOT on floor -1 — first SIM delta; NINETEENTH delta clock starts here — staged s119 (2026-08-29)

You are a fresh session executing v20 ticket T4 in game-two
(cwd ~/workspace/game-two). AGENTS.md + MEMORY auto-inject beside
this brief; live files beat this spark on any drift. Ruby per shell:
`export PATH="/c/Ruby34-x64/bin:$PATH"`. Working language English.
Law of the ticket: spec §T4
(`docs/superpowers/specs/2026-08-28-v20-descent-cycle.md`) +
foundation rows L4/L10 (`drafts/_v20-foundation-20260828.md`) — on
disagreement the foundation wins.

MISSION: ship Junior's contested/cadenced heal totem as a new station
type `totem` at floor -1's (district's) contested center — a
fixed-cadence AoE heal pulse, free but SLOW, territory you fight for.
This is v20's FIRST SIM DELTA: the NINETEENTH fun-verify's delta
clock STARTS at this ticket (cadence reform 2026-08-28 —
delta-triggered, never calendar; RECORD the clock start in the ticket
record). L10 executes here: station/sustain behavior extracted out of
world.rb into a plain object, net lines DOWN.

## STATE YOU INHERIT (verified s119, re-verify from disk)

- T3 CLOSED: wall harvested clean (23 greens incl. sustain_run 0/0
  re-cut + dash_strike_rip quirk fix; 12 standing C2-era reds + ONE
  new: taunt_anchor C2-QUEUED by name — record
  `drafts/_v20-t3-potions-20260829.md` §9). Potion identity + always-on
  U/R strip pair + POTION N counter are LIVE surfaces.
- world.rb = 1769/1800 (31 lines of headroom — the extraction is not
  optional, it is the cap's survival).
- The LDtk importer ALREADY decodes Station entities
  (`tools/import_ldtk.rb` ENTITY_FIELDS: "Station" requires `type`,
  optional price/opens/line). district.json carries stations:["seal"].
  world.rb consumes station types inline (e.g. type=="bank" ~line
  522) — that inline consumption is the L10 extraction seam.
- `data/balance/sustain.json` does NOT exist yet — T4 creates it
  (spec names this file for the totem block).
- No fun-verify is pending: no measurement freeze armed. It arms only
  at DECLARATION (cadence reform); this ticket only STARTS the delta
  clock.

## STEP 0 — ENTRY (before anything)

1. `git fetch origin` + ff-merge. Read: checkpoint TOP entry (s119) —
   spec §T4 — foundation L4 + L10 — T3 record §9 (the wall state you
   must not regress) — `harness/scripts/sustain_run.json` header +
   `tools/tune_sustain_run.rb` header (your priced re-cut path).
2. Collision check (s56/s104 law): new peer commits touching
   src/game/, data/zones/district*, or a T4 CLAIMED line in the
   checkpoint / seat mail. If Junior claimed or landed T4: coordinate
   via checkpoint + mail, never duplicate.
3. Push a `CLAIMED: T4 — <seat> <date>` line into docs/CHECKPOINT.md
   top BEFORE building (two-seat race law, project memory 2026-08-24).

## MISREAD GUARDS (each has bitten before — all live)

1. **Placement is AUTHORED**: the totem entity goes in
   `authoring/pilot.ldtk` (Station entity, type=`totem`, at the
   contested center of district's floor plan), re-imported via
   `tools/import_ldtk.rb`. Hand-editing `data/zones/district.json`
   trips pilot_authoring_test's provenance pin (project memory
   2026-08-24; python json.load→dumps(indent=2)+CRLF reproduces LDtk
   bytes exactly if you must touch the .ldtk programmatically).
2. **L10 extraction is OWED INTO THIS TICKET, not optional**:
   station/sustain behavior out of world.rb into a plain
   `src/game/` object (Crossing/Progression precedent). world.rb
   NET LINES DOWN vs 1769; `test/app/line_caps_test.rb` green. A T4
   that ships the totem but grows world.rb is a FAILED ticket.
3. **ALL numbers data-driven** (Rule 3): `data/balance/sustain.json`
   new file/block — `cadence_ticks`, `radius`, `heal_amount` (+
   anything else tunable). ZERO balance constants in code.
4. **EventBus `:totem_pulse` registered at first use**
   (EventBus::EVENTS whitelist — unknown symbols raise).
5. **The pre-registered PRICED-FLIP condition is WRITTEN IN THE
   TICKET RECORD BEFORE CODE** (foundation L4, council amendment):
   totem-vs-potions telemetry rows (totem heals vs potions
   bought/used), and the EXACT measurement that flips the totem to
   its PRICED-activation variant — the recorded condition: potions
   stay B=0 while the totem carries sustain, measured at the
   NINETEENTH ritual, not guessed. Write it, then build.
6. **Telemetry wording is oracle/machine surface**: `totem heals=N`
   beside the sustain line — lifted post-verdict wording, NEVER a
   player surface. No new player-visible strings without the
   human-facing-output gate.

## PHASES

1. **Record-first**: open `drafts/_v20-t4-totem-20260829.md` with the
   L4 priced-flip pre-registration + delta-clock start line. Then
   design the seam: which world.rb station/sustain lines move to the
   new plain object (name it for what it does, e.g.
   `src/game/stations.rb`), what World keeps (the only mutator law —
   World owns cue/event writes).
2. **Sim**: totem station type — fixed-cadence pulse on world tick
   (cadence from sustain.json), AoE heal (radius, heal_amount) to
   damaged HUMAN bodies in range; emits `:totem_pulse`; telemetry
   `totem heals=N` accumulates beside the sustain line. Integration
   test with REAL files (no mocks): a real damaged body on district
   ground heals on cadence; a body out of range doesn't; cadence
   honors the data value.
3. **Authoring**: pilot.ldtk totem entity at the contested center →
   re-import → provenance-pinned district.json emission carries the
   totem station. Suite green.
4. **Rule 2 gate**: the pulse must be VISIBLE (renderer draws a
   readable pulse/heal cue — new gate_checks row if needed, worded
   affirmatively). Gate `floor1_run` (district ground) or cut a
   totem-own script if floor1_run never dwells at center; the capture
   verdict is the artifact.
5. **PRICED COST (adopted s118 review risk — budget it, don't
   discover it)**: the totem re-sequences the exact geography
   sustain_run's zero-slack seed-7 choreography walks. After the sim
   lands, run sustain_run's headless manifest proof; if it drifts,
   re-cut via `tools/tune_sustain_run.rb` (manifest wants
   byte-unchanged — the min law), then `rake gate
   SCRIPT=harness/scripts/sustain_run.json` + the standalone hint
   checklist (`harness/sustain_hint_checks.json`) on its frozen
   gate_a. district_hunt/floor1_run may also move (center-adjacent) —
   verify by headless manifest diff BEFORE assuming either way.
6. **Soak + telemetry evidence**: `rake soak N=1` + one explicit
   `ZONES=district` episode — chain check clean, bots cross totem
   ground. Confirm the `totem heals=N` line in a scripted replay log
   (bot logs are never fun-evidence; they ARE valid mechanical
   telemetry evidence).
7. **Wall cost is LOCAL, not a re-pin**: floor-1 scripts + canaries
   (world_loop, ledger_loop) only. Verify one NON-district script's
   captures are untouched via md5 spot-check before claiming
   zone-locality. NO full wall sweep this ticket.
8. **Bank**: ticket record (seam ledger, data block, priced-flip
   pre-registration, gate + soak + telemetry evidence, delta-clock
   line) + checkpoint s-entry + push. Fresh-eyes review (Rule 6) on
   the diff — sim change = never self-reviewed.

## HARD LAWS (this ticket)

- Serial execution: ONE ticket, this one; CLAIMED at start.
- One gated SIM piece at a time — the totem IS the piece; nothing
  else sim-class rides along (no cap changes, no creature tweaks).
- Measurement freeze: NOT armed (no fun-verify pending); do not arm
  one — T4 only starts the delta clock.
- Bot logs never fun-evidence. Two-instance law (never two solo
  instances on one save). saves/ untouched. No bin/play.
- Manifest wants: never weakened. Standing C2 reds: never touched.
- Commits: explicit paths, one concern each; suite rides every commit
  via hooks.

## DoD (then STOP)

- Totem live on district ground from AUTHORED data; pulse heals on
  cadence in an integration test with real files.
- world.rb NET DOWN from 1769; line-caps test green; extraction
  object has its own tests.
- `bundle exec rake` green (all ~1324+ runs) · Rule 2 gate PASS with
  pulse visible · sustain_run headless proof checked (re-cut executed
  if needed, gate + hint checklist PASS) · soak N=1 clean ·
  `totem heals=N` observed in a scripted replay log.
- Ticket record banked (priced-flip pre-registration BEFORE code +
  delta-clock start recorded) · checkpoint entry · push.
- Queue line for the peers (es-CR + pt-br per seat register): el
  totem de curación ya vive en el piso -1 — se pelea por el centro,
  cura por pulsos lentos, y arranca el reloj del próximo fun-verify.

## BUDGET/STOP (Rule 7)

One attended session. Gates: floor-1 gate + sustain_run re-cut
contingency (gate + hint standalone) + canaries — no full wall.
Council: 0-1 (pulse-feel taste only if genuinely contested).
STOP and surface on: cap-test red after extraction · a SECOND
sustain_run re-cut failure · any determinism red · pilot provenance
pin tripping twice · context ≥ 75% with attended work unlanded
(compact-checkpoint skill).
