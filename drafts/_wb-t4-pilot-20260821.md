# WB T4 — pilot content authoring (ZONE 7 + TOWN 1 + THE WELL + DUNGEON 1)

Session 27 (hub), 2026-08-21. Spec:
`docs/superpowers/specs/2026-08-19-world-builder-pipeline.md` §T4
(§Pilot content + D1–D12 govern). Builds on T3 seams
(`drafts/_wb-t3-safe-behaviors-20260820.md`, review PASS 8/8).

## Grill block (the three shape forks + riders — settled BEFORE code)

### Fork 1 — authored-source home + authoring mode → `authoring/`, committed

- `authoring/pilot.ldtk` + `authoring/<zone>.sidecar.json` ×4, committed
  (git is the world store, D1). `.gitattributes -text` for `.ldtk`
  (T2 precedent, already present for the fixture pattern — verify).
- Import is the DELIBERATE copy (D2/D12): `ruby tools/import_ldtk.rb
  authoring/pilot.ldtk --sidecars authoring --out tmp/t4/zones` then
  `cp` into `data/zones/` — output never defaults into the live dir.
- The .ldtk is hand-built against the T2 fixture's REAL 1.5.3 vendor
  shape (`test/fixtures/spike_district.ldtk`) via a THROWAWAY generator
  (temp file, run, deleted — multi-line-script law). The committed
  artifact is the .ldtk; **the importer's refusal set is the arbiter of
  "authored in LDtk"** (pin + tamper tells + realEditorValues backing =
  `field_value`, import_ldtk.rb). Every field instance carries
  consistent `__value` + `realEditorValues` pairs, optionals as
  consistent-null (`__value: null`) — satisfying the refusals, never
  relaxing them. Defs carry the full palette of layers/entities/fields
  so the owner's pinned 1.5.3 opens and edits the same project later.
- **Round-trip pin in the suite:** a test imports `authoring/pilot.ldtk`
  + sidecars and asserts the emitted bytes == the committed
  `data/zones/<zone>.json` ×4 — the committed world provably came
  through the only door.

### Fork 2 — THE WELL = existing machinery, zero new sim vocabulary

- **Water** joins `data/tiles.json`: char `~`, int_grid 6, render
  `water`, footstep `water`, passability `floor` ("swim" stays
  RESERVED/refused). `footstep_water` is an honest new sink key —
  unmapped = silent no-op by T3 law (the frozen cue-spec mail is NOT
  edited; a future mail specs the family when the owner wants it).
- The well = 3×3 water; CENTER tile is a `hole` transition,
  `sealed: true`, to dungeon_1; a `seal` station on the adjacent plaza
  (`price: "breach_cost"` — toll machinery VERBATIM per spec; no new
  economy key; the future questline re-skins this unlock). Station
  `line: "TOLL PAID"` — verified renderer.rb:920 resolves
  `tr("breach.line", …)` so the locale line wins anyway; zero new
  strings.
- **Persistence = the breached tuple** `["zone_7", [hole]]` through the
  EXISTING family (D11). Verified against `save_state.rb`
  `breached_refusal`: it validates any tuple whose tile is a seal
  station's `opens` in that zone's DATA — zone_7's seal satisfies it;
  **the strict decoder needs ZERO changes** (schema untouched).
- **Drained look = render-only swap.** New optional zone key
  `water_drained_by: [x, y]` (sidecar-owned in authoring — D2's own
  wording names T4 the deliberate-extension slot). At DRAW time (never
  in the memoized TileVariants cache): a typed rect resolving to
  `:water` draws `:water_drained` when
  `world.breached?(zone, water_drained_by)` — renderer + god-view both
  (the T3 no-second-color-source law). Passability NEVER changes with
  state (`passable?` stays the `'#'` law). TileMap validates the key's
  shape + requires `water_drained` in the palette.

### Fork 3 — impassable borders stay `#`

Trees/fence/house walls are `#` with per-zone palette identity (wall =
bark/foliage tones) + motif — v16 identity channels, wall law untouched
(tile_registry.rb wall-law validation stands; rewiring `passable?` stays
a RECORDED post-verdict increment). NO decor sidecar extension this
ticket — named fallback: if the map critique fails zone_7 legibility on
flatness, decor lands as its own deliberate extension commit.

### Riders (settled here, not re-litigated)

1. **zone_7 is `hub: true`** — the camp precedent verbatim (bank station
   + hub), the owner's own framing ("the starting town"). Consequences
   owned: entering rehomes (existing v12 machinery, data-only);
   `home_zone: "zone_7"` becomes save-legal (decoder hub law); the
   chain_check combat exemption below becomes DATA-TRUE. **hub authors
   as an LDtk LEVEL FIELD** (Bool, beside floor — zone metadata is
   spatial-truth custody, D2).
2. **stairs_unlocked_by stays schema-only** (T2 state). D4: "DUNGEON 1
   MAY use one if authoring time allows (optional, not owed)"; the spark:
   "wire if authoring uses it". Authoring uses the ROPE for the return
   loop (the owed beat); the D4 amendment's wiring lands with the first
   ticket that authors one. Named deferral, zero code.
3. **Boss gate = `requires_defeats` transition key** (Int ≥ 1): sim
   guard in check_transition (`boss_1_defeats < n` blocks, silent like a
   sealed door), renderer + god-view render the LOCKED way as the
   sealed slab (gold only when open — "gold means walkable" law),
   seal_stamps cover it (SEALED/OPEN). Implemented + tested via
   fixtures; **shipped in ZERO zone files** (T5 wires the live edge —
   D12). Importer supports the field for schema parity.
4. **Rope = interact, gates = auto-fire.** check_transition SKIPS
   `type: "rope_spot"` (absent type = today's behavior BYTE-EXACT —
   every live zone is untyped; that is the regression bar).
   World#interact gains the rope branch after station dispatch (the
   importer's one-tile-one-entity law keeps them disjoint): free, whole
   pack, SAME co-location consent law + gate_wait cue as gates (shared
   helper extracted). `hole`/`stairs_up`/`stairs_down` auto-fire like
   today's gates; hole one-way-ness = no return transition authored at
   the landing (D4), stairs two-way = two authored transitions.
5. **Soak trap → hub-exemption is now data-true.** chain_check.rb
   HUB_ZONES gains `zone_7` (deliberate amendment + test: a threat-free
   zone_7 episode passes, a combat-free dungeon_1 episode still FAILS).
   Rotation evidence: `ZONES=zone_7,dungeon_1` run, scratch save.
6. **DUNGEON 1 combat = existing kits, conservative counts** (rusher ×4
   + rusher_hater ×1, spread wide). ZERO balance-file changes — spawn
   lists are zone data; nothing the live world measures moves.
7. **Owner walkthrough needs NO cli change** — `--start-zone` stays
   bot-gated (teleport-cheat law). The dev launch rides the hub law:
   `ruby -Isrc soak/seed_save.rb tmp/pilot_walk/world.json zone_7 60 3`
   (banked covers the 40 toll) then `bin/play --save
   tmp/pilot_walk/world.json` — home_zone=zone_7 starts the session in
   town. Recipe goes in the owner queue (es-CR).
8. **Ambience rows (data-only):** zones `zone_7 → amb_meadow`,
   `dungeon_1 → amb_dungeon`; TOWN 1 region intent=town → amb_town free
   from T3. Basements stay silent (nil key by design).
9. **Zones authored:** `zone_7` "ZONE 7" (44×28, floor 0, hub, TOWN 1
   region, well, depot bank, 2 houses w/ interiors + empty store/NPC/
   board SLOTS) · `basement_1`/`basement_2` "BASEMENT 1/2" (floor −1,
   stairs) · `dungeon_1` "DUNGEON 1" (floor −1, hole landing, rope
   back, conservative combat). Locale lines ×3 files, locale-invariant.
   Depth darkens palettes (authored, D3); palettes honor the identity
   value-structure contracts (wall−floor luma ≥ 40, motif between).

### Risks named (spark law)

1. check_transition/interact are LIVE sim code — regression bar =
   byte-stability: world_loop + grass_fixture_walk baselines banked
   BEFORE the first sim commit; spot-gates + double-replay md5 after.
2. Renderer typed-rect + transition-slab changes touch the live draw
   path — live zones carry no water/typed/locked keys, so every new
   branch short-circuits; the gates prove it, not the promise.
3. Hand-authored .ldtk vs a future owner resave — appBuildId churn is
   tolerated BY DESIGN (D1 pins jsonVersion only); the round-trip pin
   test catches emitter drift the moment it lands.

## Sequencing (one bundled change-set, one concern per commit)

baselines → C1 core (tiles.json water + TileMap keys) → C2 sim
(rope/hole/boss-gate) → C3 render (renderer + map_artifact + stamps) →
C4 importer (hub/requires_defeats/water_drained_by) → C5 authored data
(authoring/ + imported zones + locales + ambience + round-trip pin +
INERT pin + labels test) → C6 soak (chain_check amendment) → C7 probes
(map_main + map_checks) → gates → walkthrough evidence → ticket §below
→ fresh-eyes review (BLOCKING; prompt: touch NOTHING incl. seat mail) →
checkpoint + push + owner queue.

## Ship evidence (Rule 2/5 — all PASS before ship)

**Commits (one concern each):** `506747f` core (water type · requires_defeats
· water_drained_by validation) · `a6a27af` sim (rope interact · sealed-hole
toll · fact-gate; **crossing policy extracted to `src/game/crossing.rb`** —
the line-cap gate fired at 1828/1800 during commit, answered with the
extract-on-touch law, world.rb closed at 1800) · `0628d0a` render (locked
slab + drained swap, renderer + god-view, ONE condition source) · `8082bb0`
importer v2.1 (hub level field · requires_defeats · water_drained_by
sidecar) · `f966823`+`8471c04` authored data + pins · `c6fea00` soak
amendment · `7ab5612` map gate probes/checks. Suite 971→994 green via
hooks on every commit.

- **The door held:** the hand-authored `authoring/pilot.ldtk` imported
  through `tools/import_ldtk.rb` FIRST TRY (4 zones, loader-validated);
  committed `data/zones/{zone_7,basement_1,basement_2,dungeon_1}.json`
  are pinned byte-equal to the emission (`pilot_authoring_test.rb`,
  eol-normalized for Junior's autocrlf clone).
- **Loop mechanized** (`test/game/pilot_loop_test.rb`, REAL data): town →
  toll drain (breached tuple `["zone_7",[33,14]]`, D11 — decoder accepts
  with ZERO schema changes, pinned) → fall (one-way, no return at the
  landing) → rope interact back beside the well → the drained way stays
  open. Basement stairs two-way. Fixture-level laws in
  `typed_transitions_test.rb` (rope never auto-fires · co-location consent
  on the climb + gate_wait cue · boss gate shut at 0 / open at 1).
- **Live look byte-stable:** world_loop 10/10 + grass_fixture_walk 8/8
  captures md5-identical to pre-change baselines
  (`tmp/t4/t4_*_baseline.md5`, banked before the first sim commit).
- **Spot gates PASS ×3** (`tmp/t4/evidence.log`): world_loop ·
  low_quay_run · grass_fixture_walk — determinism byte-identical ×2 +
  vision PASS each.
- **Map gate PASS:** probes 11/11 (drained ring == water_drained ≠ water ·
  hole gold · meadow grass family · dungeon floor + rope gold) + critique
  7/7 vs the updated eleven-panel checks. **First critique run FAILED
  honestly** (`map_pilot_well_reads`: basin Δluma ~7 didn't read at 6px) —
  fixed by re-authoring `water_drained` to parched clay [78,64,44] in the
  SIDECAR + re-import (the authoring loop, check unrelaxed), re-run PASS.
- **Soak PASS** (`tmp/soak/20260820-232208`): 2 episodes over the pilot
  rotation `ZONES=zone_7,dungeon_1`, seeded scratch chain intact
  (`586f9e4d → eadd9ce6 → 62ff9bab`, sessions 1→2), quarantine held (live
  save md5 `98fe75ed…` + 38×2 launcher logs unmoved). chain_check
  amendment: zone_7 hub-exempt, dungeon_1 still owes combat (fights=2
  landed live in ep2).
- **noDevice audio evidence** (SOAK_AUDIO=1, from the soak's own logs):
  `START_ZONE zone=zone_7` + `AUDIO ambience key=amb_meadow zone=zone_7` +
  footsteps grass/dirt (ep1) · `START_ZONE zone=dungeon_1` + `key=amb_dungeon`
  + `material=stone zone=dungeon_1` (ep2). **Named gap:** no bot wandered
  into TOWN 1, so `amb_town` wasn't exercised live — the region→key lane is
  unit-pinned (audio_bridge_test region-boundary digest test) and the town
  region ships in data; the owner's walk will hit it.
- **Owner dev-walk recipe VERIFIED** (scratch): `ruby -Isrc
  soak/seed_save.rb tmp/pilot_walk/world.json zone_7 60 3` (strict-decode
  verified, home=zone_7 banked=60) → `bin/play --save
  tmp/pilot_walk/world.json` — session starts in town, toll affordable,
  no cli changes, `--start-zone` stays bot-gated.

## Deferred, named (never silently)

- `stairs_unlocked_by` wiring — authoring didn't use it (rope is the
  return); wires with the first ticket that authors one (D4: optional).
- `amb_town` live exercise + owner renders (footstep families + beds) —
  owner-paced (cue-spec mail unanswered, nothing owed).
- Decor for pilot zones — not needed to pass legibility; a future
  deliberate sidecar extension if wanted.
- T5 wire-in (boss-gate edge into a live zone + `multi_floor_descent.json`
  wall script + full wall sweep) — its own ticket, own gate.
