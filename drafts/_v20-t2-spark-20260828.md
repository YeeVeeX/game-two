# SPARK -- v20 T2: PROGRESSION BAND STEP 1 -- cap 12 + k re-price + standing pacing script -- staged s116, fresh session (2026-08-28/29)

You are the fresh session for ticket T2 in game-two (cwd
~/workspace/game-two). AGENTS.md + MEMORY auto-inject beside this
brief; live files beat this spark on any drift. Ruby per shell:
export PATH="/c/Ruby34-x64/bin:$PATH". Working language English;
player surfaces stay PLACEHOLDER register. Quality over cost (owner
order): thoroughness on judgment, never skipped verification,
economize context instead.

MISSION: execute spec ticket T2 (docs/superpowers/specs/
2026-08-28-v20-descent-cycle.md section T2) end-to-end in ONE
session: data/balance/progression.json cap 10->12 + k re-priced from
a real hours-per-level table, the pacing math promoted to
tools/pacing_table.rb as a STANDING script, at-cap xp-pin arithmetic
re-verified for cap 12, live-save copy-decode proven. NOT this
session: T3 (own spark, cut after T2 -- its full-wall DoD needs the
standing-reds story reconciled first), kill_xp rows, requires_level
rungs, new creature kinds, the boot-banner quirk, any C2-QUEUED wall
red (all named debts, not yours).

## STEP 0 -- ENTRY + CLAIM (before anything else)

1. git fetch origin + ff-merge. Read from disk, in this order:
   docs/CHECKPOINT.md TOP entry (s116) -- spec section T2 --
   foundation row L5 (drafts/_v20-foundation-20260828.md, the
   content-coupled cap law + dwell target evidence) --
   drafts/_v20-pacing-analysis-20260826.md (the measured arithmetic:
   formula, band, dwell tables; it is the tool's blueprint) --
   drafts/_v20-t1-wall-20260828.md sections 2 and 9 (standing wall
   reds BY NAME so no sweep result surprises you) --
   src/game/progression.rb + test/game/progression_test.rb +
   progression_data_test.rb + progression_integration_test.rb.
2. Collision check (s56/s104 law): new Junior commits since 60699ad +
   checkpoint CLAIMED grep + seat mail inbox
   (~/.pi/agent/mail/game-two/inbox/). His floor -1 ratification may
   have arrived -- bank the verbatim in the checkpoint, one line.
   T2 already claimed by his seat -> take T6 instead (B4 mercy-floor,
   data-only, lane E) and record the swap; NEVER improvise T3.
3. Push a CLAIMED: T2 (seat, date) line at checkpoint top, commit,
   push, THEN start. Push refusal = coordinate, never route around.

## THE MATH YOU INHERIT (verified live s116 -- trust but re-read)

- Formula (progression.rb delta_e): dE(L) = k*(L^2 - 3L + 4) = cost
  of L-1 -> L. Live: k=40, cap=10. At cap, award pins xp at
  dE(cap+1)-1 (projector-invariant law) = 3679 today.
- Measured income band: 8-20k XP/sim-hour (pacing analysis section 2,
  three chain-verified human sessions).
- Dwell target (L5, owner-ratified): L10->11 and L11->12 each
  ~15-30 min inside that band. At k=40: dE(11)=92k=3680 (11-27.6 min),
  dE(12)=112k=4480 (13.4-33.6 min) -- k=40 is BORDERLINE-in-band; a
  modest raise is plausible, unchanged is a legitimate table-backed
  outcome. The TABLE decides, the record explains; do not agonize an
  edge call -- record it (dev-of-record judgment, foundation L5).
- **HARD FLOOR discovered s116, supersedes the spec's stale snapshot:
  the LIVE save sits AT THE PIN -- progression {level:10, xp:3679}
  (not xp=644 as the spec row says).** Saved xp must stay valid
  progress into level 10 under the NEW k: 3679 < dE(11) = 92k
  requires **k >= 40**. Anything lower strands the owners' chain at
  an over-range xp -- refused or force-normalized, both unacceptable.
  If the table ever argues k < 40, STOP and surface; do not migrate.
- New pin under cap 12: dE(13)-1 = 134k - 1. Verify the pin rows
  compute it (the unit lane is parameterized on purpose -- "a k/cap
  retune must never break this file"; progression_data_test smokes
  the SHIPPED file against the formula, never a pinned literal).

## PHASES

1. **tools/pacing_table.rb (the standing script, write it FIRST).**
   Reads data/balance/progression.json + its kill_xp block LIVE (zero
   constants in the tool); emits the hours-per-level table: per level
   L=2..cap+? -> dE(L), cumulative XP, minutes at 8k and at 20k
   XP/h; plus a candidate-k sweep mode (arg or env: a list of k
   values -> dwell minutes for L10->11 and L11->12 each). Header
   comment carries the standing law: RUN ON EVERY CURVE/CAP TOUCH,
   output pasted into every touching ticket's record. Mirror the
   pacing-analysis doc's arithmetic exactly -- it is the verified
   blueprint; cite it in the header.
2. **Pick k against the dwell target.** Run the sweep, read the
   table, pick per L5's target with the k>=40 floor. Paste the table
   + a 3-5 line rationale into the ticket record. k=40-unchanged is
   fine if the table says in-band (the cap step is the felt change
   this ticket ships).
3. **Land data/balance/progression.json: level_cap 12 + chosen k.**
   Data-only -- zero code changes outside tools/. Suite via hook at
   commit (the pin rows + wiring smoke must pass on the shipped
   values).
4. **Copy-decode proof (save-law, L9).** Copy saves/world.json to
   tmp/ (md5 the live file FIRST, judged from captured output). Load
   the COPY through the play-path strict decoder with the new config
   (a small tmp/ ruby probe via the same save_state path the game
   uses, or a targeted test on the copy path). Assert: decode
   succeeds, level=10, xp=3679 intact, no normalization side-effect.
   At session end re-md5 the live file -- byte-identical or the
   session fails NAMED. Never touch the live file or its .bak
   siblings; --fresh/migration is forbidden territory.
5. **Sim-drift audit (the s116 lesson -- C2 drifted the wall
   silently; k must not repeat it).**
   - k UNCHANGED: cap+pin are replay-inert (no wall script reaches
     L10 -- highest is zone8_crossing at L8). Zero re-gates owed;
     say so in the record with the one-line reasoning.
   - k CHANGED: any reel that LEVELS UP mid-script re-sequences
     (stat growth timing moves kill frames). Find the owed set
     MECHANICALLY: for each harness/scripts/*.json compare its
     start progression level against the final "TELEMETRY
     progression level=" line in the fresh tmp/wall/*_v20-t1*.log
     bank -- any script whose level moved mid-reel owes a blocking
     rake gate re-run (known members: floor1_run -- the LEVEL 2
     stamp at f1350 -- and level_up_beat). The canary trio
     (world_loop/varekka/burn) never levels mid-reel and should
     stay byte-stable: if the suite's canary reds anyway, STOP --
     classify the stream diff BEFORE any versioned-rebank; an
     unexplained coupling is a finding, not a rebank.
   - Re-gate rc lines + verdicts go in the record; drafts/
     _gate-verdicts.log is tracked evidence -- commit it with docs.
6. **Fresh-eyes review (Rule 6, review law).** Headless scrubbed pi
   over the ticket diff (scrub PI_* env; reviewer reads spec T2 +
   record + diff; touches NOTHING including seat mail; last message
   = one JSON verdict object). Bank the verdict verbatim in the
   record. BLOCKING items: fix this session. A failed review blocks
   T3 (same lane).

## HARD LAWS

- kill_xp rows UNTOUCHED (L5: deep-pays-more lands via NEW kinds in
  lane D, existing rows frozen at first ship). requires_level rungs
  UNTOUCHED. growth pcts UNTOUCHED. No save schema change.
- All tunables in data JSON; the tool reads data, never embeds it.
- src/game/world.rb untouched (extraction rides T4). Line caps green.
- Two-instance law: never launch bin/play beside a live owner seat;
  interactive checks via rake pilot only if truly needed (unlikely).
- Standing wall reds are NAMED, not yours: 11 C2-QUEUED manifest reds
  + threat_pull + zone_catchup (C3 session) + dash_strike_rip
  QUIRK-RED (boot-banner fix, own micro-session). Do not chase, do
  not re-run the full wall -- T2 owes at most the level-crossing
  re-gates from phase 5.
- Any detached child script must unset MSYS_NO_PATHCONV before
  exec-ing ruby/bundle (banked 2026-08-29 lesson); judge detached
  work by DONE markers + rc lines, never log size.
- Commits: explicit paths, one concern each (tool+data+tests = the
  feat commit; record+checkpoint+verdicts-log = docs). Never
  git add -A.

## DoD (then STOP)

- drafts/_v20-t2-progression-<date>.md: pasted pacing table + k
  rationale + pin math (134k-1 verified) + copy-decode transcript +
  live-save md5 before/after + phase-5 audit outcome (owed set empty
  OR re-gate PASS lines) + review verdict JSON verbatim.
- Suite green via hooks on every commit; push.
- Checkpoint s117 entry (top): what shipped (cap 12, k=<value>,
  table banked), phase-5 outcome one-liner, review one-liner, next =
  T3 (needs its own spark: full-wall DoD vs standing reds + banner
  quirk reconciliation), CLAIMED resolved DONE. Commit, push.
- Queue line for the owner (es-CR, everyday words): que subio el tope
  a 12, si el ritmo de niveles cambio o quedo igual y por que, que el
  save vivo carga intacto (probado en copia), y que sigue T3 (la
  identidad de pociones).

## BUDGET/STOP (Rule 7)

One attended session. Council: 0 (arithmetic + data ticket; the
table IS the argument). Bedrock spend: phase-5 re-gates only if k
changes (expected 0-2 gates), plus one timeout retry each (7
Bedrock read-timeouts hit s116 -- a crashed critic re-runs
STANDALONE on the frozen gate_a captures, same-artifact law). STOP
and surface (never improvise) on: the table arguing k < 40 (live-save
floor) -- copy-decode refusal or any save normalization side-effect
-- canary red not fully explained by level-timing -- the same gate
red twice after honest fixes -- Junior collision -- context >= 75%
with the ticket unlanded (compact-checkpoint skill, stage the
continuation).
