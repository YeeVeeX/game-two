# Fresh-eyes review — wall #4 presentation fixes, `junior/premium-build` 3631ccc^..223c5c8

**Verdict: MERGEABLE WITH MINORS** — no sim/digest/net touch; suite green; canaries YES x3. Minors are (a) a false-positive path in `tools/gate_batch.sh --ref`, (b) `wall_triage.rb` cannot see `gate_batch.sh` re-gates, (c) two overlays still under the z-17 vignette, (d) halo contour is a staircase not a closed outline.

Suite: `bundle exec rake` → `1538 runs, 89553 assertions, 0 failures, 0 errors, 0 skips` (108.6 s).
Canaries (`ruby tools/a3_stream_diff.rb world_loop brasa2_run floor3_run`, "= ACTIVE bank?" cell): world_loop `e0b1f38f` **YES** · brasa2_run `3fd04895` **YES** · floor3_run `648810ff` **YES**.
`git diff --stat 3631ccc^..223c5c8 -- src/game src/net src/core` → **empty** (16 files touched: data/display.json, harness/gate_checks.json, src/app/{controls_overlay,light,minimap,renderer,signage}.rb, tests, tools/gate_batch.sh, drafts).

## Findings

| sev | file:line | finding | law / failure mode | one-line fix |
|---|---|---|---|---|
| MAJOR | tools/gate_batch.sh:47-54 | `--ref` compare iterates `captures/${s}_gate_a/*.png`; 5 wall scripts (corpse_run, specials_chain, taunt_anchor, threat_pull, vat_economy) have `out_dir: captures/pilot/<x>_replay` (Rakefile:127-128 derives `<out_dir>_gate_a`), so the glob is empty → `tot=0`, `diff_n=0` → prints **IDENTICAL(0)** = false byte-inert proof | prose-number / evidence law: a "proof" that can pass on zero frames | read `out_dir` from the script JSON; `[ "$tot" -eq 0 ] && cmpres="NO-CAPTURES"`; also count ref-only files (asymmetric today) |
| MINOR | tools/gate_batch.sh:41,59 vs tools/wall_triage.rb:28,31 | batch prints `=== GATE <s> ...` / `=== <s> gate_rc=N manifest_rc=M captures_vs_ref=… ===`; triage only opens blocks on `=== (WALL\|REGATE) ` — every gate_batch re-gate is invisible to history, so rows that passed there classify REPEAT/NEW instead of FLIP-PRONE/DEBT | tooling drift: two grammars for one ledger | print `=== REGATE $s …` in gate_batch (or teach triage `GATE`) |
| MINOR | tools/wall_triage.rb:57-66 | a REGATE block with `gate_rc` nil (still running / aborted) or INFRA-only rows counts every previously-failed row as `regate_pass` → spurious FLIP-PRONE | misclassification of real debt as critic noise | `next if b[:gate_rc].nil? \|\| b[:rows].key?("INFRA")` |
| MINOR | tools/gate_batch.sh:26-31 | `gate_batch.sh --ref X a b` (tag omitted) silently takes `--ref` as the tag and `X` as a script (skipped); `--ref` as last arg → `shift 2` fails, `--ref` stays in `$@` and is "skipped (no script)" | arg-parse footgun, exit 0 | `[[ "$tag" == --* ]] && usage`; `[ -n "$ref" ] \|\| usage` |
| MINOR | src/app/renderer.rb:1108-1115 | contour is per-row `w+2` on the same row: where the ellipse steps in (rows ±3..±6, hw 17.5→15.5→12.6→8.2) the gold rim pixels of the wider row have floor, not contour, directly above/below (1-3 px per step); cap row at 1114 is `w-2` wide so the two rim pixels of the top/bottom slice are uncapped | Rule 2 claim "reads on light floors" is a staircase, not a closed 1px outline | draw the contour as the SAME ellipse with rx+1, ry+1 (one extra loop), then gold on top |
| MINOR | src/app/renderer.rb:1696-1706, 1734,1740 | zone banner / stamp lines draw at z 10, y 48 = inside the base vignette band (bh 0.28·540=151) and inside the low-hp band (0.16·540=86) → banner text is tinted by both vignettes at z 17 | same failure class the commit fixed for ControlsOverlay (65f52e5) | z 18 like the interact prompt |
| MINOR | src/app/renderer.rb:1666 (safe chip backing, no z) · :1492-1494 (writ veil, no z) | screen-space, z 0, under the z-17 vignette; the safe chip TEXT is z 20 (1668) so backing and text are split across the vignette | tint mismatch backing vs text | pass z 19 to the backing rect |
| MINOR | src/app/fx.rb:259-261 | comment says "a PACK victim's spark is CRIMSON" — display.json now sets `fx_spark_pack_rgb [255,180,80]` / core `[255,235,200]` (3631ccc); only the chips (fx.rb:284, hardcoded `235,60,60`) are still crimson, and those are a balance-free literal outside display.json | stale comment + one hardcoded colour beside 4 display rows | update comment; move chip rgb to a display row |
| MINOR | harness/gate_checks.json:350 (impact_fx_reads) · 223c5c8 msg | row says "star ~14"; `Fx::SPARK_FRAMES = 12` (fx.rb:16); DUST is 14 | prose-number law | write "~12" or point at `Fx::SPARK_FRAMES` |
| NOTE | test/app/signage_test.rb:71-73 | new low_hp_* / possess_halo_contour_* rows are only guarded generically by display_knobs_test (key present); no test asserts `low_hp_band_* < 0.22/0.28` or `alpha_floor ≤ 1` | a future edit can silently re-widen the pulse to a wash | add a one-line range assert like the `aura_fill_alpha_max < 64` one |

## Answers

**1. Sim / digest / EventLog / src/game / src/net.** None. `git diff --stat 3631ccc^..223c5c8 -- src/game src/net src/core` is empty; every source touch is `src/app/**` (controls_overlay.rb, light.rb, minimap.rb, renderer.rb, signage.rb) + `data/display.json` (all new keys are presentation rows: display.json:17-22, 244-250, 284-287) + `harness/gate_checks.json` + tests + tools. `Signage.aura_alpha_pct`/`chebyshev` are pure and read only `c.tile`/`me.tile` (signage.rb:68-80, 185-188). Canaries byte-identical (YES x3 above). The `world.rb:212 possessed(seat = 1)` call is read-only.

**2. Z-order audit (vignette at z 17, light.rb:158; Gosu orders by z then call order, so a z-0 call made AFTER `draw_screen` still sits UNDER it).** Above 17: HUD plate 19 (hud.rb:54-61) + bars/pips 20-21 (hud.rb:74-149); ControlsOverlay 19 (controls_overlay.rb:106,114,…, changed in 65f52e5 — verified all six draw_text calls); Minimap 19/20/21 (minimap.rb:134-149); BagScreen 30 (bag_screen.rb:33); boss bar 20 (renderer.rb:1054-1064); interact prompt 18 (signage.rb:317); exit arrows 18 (signage.rb:241); edge pips 21 (renderer.rb:1689); level flash 18 (light.rb:139); ledger beat 29-31 (renderer.rb:1811). **Still below 17 / z 0:** zone banner z 10 (renderer.rb:1706), stamp/breach lines z 10 (1734,1740,1742 — at y 48 they sit inside both vignette bands); safe-chip backing z 0 (1666, its text is z 20 → split); writ veil + border z 0 (1492-1494); wipe veil z 0 (1772), stagger veil z 0 (1778), hurt vignette bars z 0 (1795-1798) — the last three are veils whose tint under the vignette is arguably intended, but `draw_hurt_vignette`'s 8-px crimson edge bars (display.json:23-24) lie exactly under the darkest vignette pixels. Fx numerals/callouts are world-space z 8/6 (fx.rb:143,153) — tinted only when they scroll into the band, correct.

**3. Low-hp pulse arithmetic (light.rb:116-129, display.json:281-287).** `a = 150 · (0.75 + 0.25·depth) · (0.85 + 0.15·breath)`: onset (depth 0) = 150·0.75·[0.85..1.0] = **96..113**; full depth (hp→0) = 150·1.0·[0.85..1.0] = **128..150**. Bands at 960×540: bw = 0.12·960 = 115 px, bh = 0.16·540 = 86 px → the clear centre is 730×368 = **76% × 68%**, untouched by the pulse (the quads/triangles at 159-171 are dark only at the outer vertex and `clear` at the inner one, so alpha ramps 0 at the inner edge). Stacking with the base vignette (bw 211, bh 151, alpha 130 in zones / 70 in safe+hub): both are edge gradients drawn every frame at z 17; at the very edge the composite is 1-(1-130/255)(1-150/255) ≈ **80%** dark-wine, but inside x∈[115,845], y∈[86,454] only the base ramp exists and inside [211,749]×[151,389] nothing. It cannot read as a whole-frame wash by geometry; the ledger_loop critique came from the old 0.22/0.28 bands + ControlsOverlay at z 0 (both fixed in 65f52e5). Wipe guard: `wiping = states.current == :nest_respawn` (light.rb:115) and `draw_wipe_overlay` runs `if world.states.current == :nest_respawn` (renderer.rb:268) — same predicate on the same frame, so pulse and wipe veil are mutually exclusive. The stagger veil (renderer.rb:270) and hurt bars (271) are NOT excluded and do co-draw with the pulse (same frame, staggered/hurt possessed at low hp) — all in the red family, z 0 under the pulse.

**4. Aura re-cut (signage.rb:180-208).** Fill alpha = `26 · (1 - phase·0.5) · pct` → max 26 at phase 0, near (pct 1.0), min 26·0.5·0.5 ≈ 7 far. Alpha-over stacking of N fills: 1-(1-26/255)^N → N=3 ≈ 28%, N=5 ≈ 42%, **N=7 ≈ 53% (≈135/255)** — plus the 3-px line at 150 and contour at 170 where they coincide. radius_tiles 2 (combat.json:754) → each square 5×5 tiles, so 7 overlaps needs 7 aura bearers within a 5×5 — possible in brasa spawns, not typical. The fill draws after each body in the depth-sorted pass (renderer.rb:1241) at z 0 → it tints bodies drawn earlier in the same pass (sprites are z 0 too), not Fx (z 6/8). `world.possessed(@local_seat)`: `def possessed(seat = 1)` (world.rb:212) — arity OK, same call already used at renderer.rb:248. `aura_alpha_pct` degenerate-safe: `return far_pct if far <= near` (signage.rb:76), tested (signage_test.rb:69); dist ≤ near → 1.0, ≥ far → far_pct, linear between; no division by zero.

**5. Halo contour + chevron (renderer.rb:1095-1142).** rx 19, ry 6, t = dy/6.5: hw per |dy| = 19.0, 18.8, 18.1, 16.9, 15.2, 12.6, 8.2 (rows 0..6). Line 1113 draws contour `w+2` on the SAME row as the gold slice; line 1114 fires only at |dy| == 6 and draws a `w-2`-wide row one pixel OUTSIDE (dy<0 → -1 above, dy>0 → +1 below), so the caps are outside the ellipse, not over the gold — correct sign. Gaps: between rows the width steps in by 2·Δhw (≈1 px rows 2→3, 3 px 3→4, 5 px 4→5, 9 px 5→6) and nothing draws contour on the row above/below those exposed gold rim pixels, so the "1px contour" is a same-row staircase; the top/bottom cap (`w-2`, offset +1) leaves the 2 rim pixels of the |dy|=6 slice with no dark neighbour above/below (rim is 2 px wide, 1123-1124). Not a bug of order (contour loop precedes gold loop, so gold wins where they overlap) but the claim "ground-independent" holds only along horizontal tangents. Chevron: `top = y - 7 - art_lift - bob`, art_lift = anchor_y(14) - 2 = 12 (art.rb:43, manifest.json:4-7), so top = y - 19 - bob; the sprite frame is 48 px anchored at y+ (anchor 14) → sprite top = y - 14 (with the 2-px transparent margin, visible head ≈ y - 12). New rows land at top-1..top+6 = y-20-bob .. y-13-bob: the lowest edge row (top+6, bob 0) is **1 px above the frame top and 3 px above the visible head** — no overlap, zero margin at bob 0; a taller frame (`frame_h`/anchor change in the manifest) would collide. Previously the chevron ended at top+5.

**6. gate_checks.json rewords.** Only 4 of the 7 named rows are in the range (223c5c8 impact_fx_reads, 3041aa1 telegraph_reads, 9d7522f + f6f51a9 specials_distinct/whirlwind_reads); `hurt_flash_not_white` (94f7d4e), `minimap_reads` and `interact_prompt_reads` (d557f67) predate 3631ccc. Checked anyway: hurt_flash_not_white (line 34) says the star "may be bright or pale, judge the BODY only" — matches fx.rb:254-286 with the new pack spark rows (display.json:187-188); the stale "CRIMSON" comment at fx.rb:259 contradicts it (finding). minimap_reads (346): gold = open / cold grey = locked matches minimap.rb (`minimap_way_open_rgb`/`_locked_rgb`, display.json:291-292) via `Renderer.way_locked?`; station magenta matches `minimap_station_rgb`. interact_prompt_reads (370): "on a rope spot", "beside a station / on a totem no prompt", and (3631ccc) breached seal → nil, all at signage.rb:24,132-137. whirlwind_reads (198): "arc dash, max_tiles 3, windup 6, active 1" = combat.json:32-39 exactly; the draw site is renderer.rb:1367-1373 (dash telegraph line) — correct, and the row keeps a real failure mode (no line in windup / body not displaced). specials_distinct (70): "warm amber windup, pale-gold active tiles" matches draw_attack; the added "(if that is all a reel shows, this row is not exercised)" is the permissive clause — it is bounded by "no ring, no pip spend", but a critic can now pass any reel by declaring every effect "an ordinary windup"; acceptable only because `special_started` in the event stream is the manifest's job (a3_stream_diff shows special_started 0 OFF / 9 ON for these three scripts). telegraph_reads (30): "hostile body visible inside the red-edge swell" matches the human telegraph draw; not-exercised clause requires an absence of red-edged swells — still falsifiable. impact_fx_reads (350): "star ~14" is wrong (SPARK_FRAMES 12, fx.rb:16); "judge within a dozen frames of a hit" is consistent with 12. No row contradicts another (telegraph_reads and specials_distinct both classify pale ground squares as non-hostile, consistently).

**7. Tools.** gate_batch.sh: `set -u` + `"${1:-}"`/`shift || true` is correct for zero args (line 26); `[ "${1:-}" = "--ref" ]` OK; `--ref` without a value → `shift 2` fails silently and `--ref` becomes a "script" (skipped, exit 0) — finding; tag omitted → `--ref` is the tag — finding. **cmp loop false positive confirmed**: 5 scripts' `out_dir` ≠ `captures/<s>` (see MAJOR), and any run where `rake gate` aborted before capturing (e.g. `GATE FAIL: no captures produced`, Rakefile:135) also yields IDENTICAL(0). Ref-only extra frames are never counted. Pins CLI: `record --script --tag --gate-rc --manifest-rc` matches `pins.rb:10` and `parse_args` (169-176) slice-of-2 flags; recorded only when rc=0 and mrc=0 (56-58) — same as run_wall.sh. `tasklist //FI` is the right Git Bash escape; note it refuses on ANY ruby.exe (a running `rake` suite too). wall_triage.rb: `=== REGATE <s> HH:MM:SS ===` parses (regex line 28, `\S+` script, trailing space); close line regex (31) tolerates the batch's extra `captures_vs_ref=` suffix but never sees the batch OPEN line (`=== GATE`) — finding; `[FAIL] (\w+): (.*)` (35) takes the first `word:` so a phrase with colons stays whole; rows are snake_case so `\w+` is safe. Classification: DEBT before FLIP-PRONE (70-75) is the intended precedence; nil-gate_rc / INFRA-only REGATE blocks over-count passes — finding.

**8. What a Rule 2 gate cannot catch.** (a) Partner halo contour (renderer.rb:1178, `chevron: false`) and the aura falloff keyed on `@local_seat` (signage.rb:185) only render with two seats — the wall is single-player; the netplay gates (`harness/net/*`) use their own checklist and would not judge them. (b) ControlsOverlay at z 19 now draws ABOVE the netplay end-screen veil and its text (netplay_overlay.rb:89-97, z 0/10) — the strip is guarded by `world.possessed` (controls_overlay.rb:102) so it likely still shows on a frozen end-screen world; visible only in a netplay session, never on the wall. (c) The false-positive `IDENTICAL(0)` path means the "extraction byte-inert" proof planned in `drafts/_wall4-close-20260906.sh` is sound for ledger_loop/town_gates (out_dir = captures/<s>) but would be silent for the five pilot-out_dir scripts. (d) Minimap fallback colours (minimap.rb:84-87) only matter for a zone with no palette — no wall zone lacks one, so the row move is only test-guarded (display_knobs_test), not gate-guarded. (e) The critic samples ~4% of frames: the halo staircase gaps and the chevron's 1-px clearance are sub-sampling artefacts a vision verdict will not resolve.

## Verified
Suite line and 3 canary cells quoted above (headless only; no window opened). `git diff --stat` on src/game|net|core empty. Every z quoted read from the cited line. Arithmetic in §3-5 from display.json rows 17-24, 195-199, 242-250, 281-289 and manifest.json anchor [2,14]. `out_dir` mismatch scan: 5/44 scripts. Working-tree changes to `harness/pins.json` / `drafts/_gate-verdicts.log` ignored as instructed.

## Not verified
Any rendered pixel (no capture/gate run — wall owns the window); the actual brasa3 overlap count; two-seat behaviour of §8(a)(b); whether `tmp/_regate.sh` output is appended to the sweep log that wall_triage reads (it prints REGATE headers, tmp/_regate.sh:5-7, but the log destination was not traced); gate_batch.sh was not executed (would need ruby dead).
