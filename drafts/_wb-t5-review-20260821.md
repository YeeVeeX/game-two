# Fresh-eyes review — s28 (J1 render fix + WB T5 wire-in)

## VERDICT: PASS (all ten laws hold; three note-level defects, none blocking)

**L1 BLAST RADIUS — PASS.** `git diff --name-only c0dc412..HEAD -- data/zones/` = exactly `low_quay.json` + `zone_7.json`; explicit diff of the other nine zone files = **0 bytes**. Basements/dungeon_1 emission-equality is pinned by `test/tools/pilot_authoring_test.rb:26-33` (byte-compares all four pilot zones against the importer emission) — green via hooks at every commit (see L2 caveat).

**L2 THE DOOR — PASS (static + hook evidence; live run seat-blocked).** Seat-lease refused the test run (LIVE session 01a023f9 holds the seat — policy, not routed around). Static proof: `pilot_authoring_test.rb` re-emits via `Tools::LdtkImporter` and asserts committed == emission for zone_7; `git show 371aedd -- authoring/pilot.ldtk` contains the new `Transition` entity with `__value: "low_quay"`; pre-commit/pre-push hooks run `bundle exec rake`, and 6 commits landed. The low_quay diff is **one** transition object, +12 lines, exactly `{at:[44,19], to:zone_7, spawn:[2,14], requires_defeats:1}` — nothing else.

**L3 RENDER PURITY — PASS.** `dd8ff40` stat: `src/app/renderer.rb` only (+90/−22). `static_runs` memoizes on `@static_runs_cache[map]` (map-only key; registry is global config); the drained bool is computed in `draw_map` each frame and applied at draw time (`ref = :water_drained if ref == :water && drained`) — never cached. `tmp/s28_macro_verify.sh` judges rc FIRST (`REPLAYS FAILED … md5 comparison VOID; exit 1`, header names the v1 false-pass); `.out` shows `rc=0` ×3 then `MACRO BYTE-IDENTICAL: 57/57 PNG md5s equal`. Timeline: baseline banked 05:06:16 → renderer.rb edited 05:38:13 → verify 05:41–05:44 → commit 06:17:26. Baseline file = 57 lines, and its third dir (`captures/pilot/quay8_r10_replay`) IS low_quay_run's real `out_dir` — not stale.

**L4 SAVE DISCIPLINE — PASS.** `md5sum saves/world.json` = `98fe75edb6d72deab18cd48eaa88bdaf`; mtime 2026-08-20 15:51:53 predates the first commit (371aedd, 2026-08-21 06:16:21). No `src/game/` file appears in the range diff at all.

**L5 INERT LAW AMENDED — PASS.** `tile_registry_test.rb`: `RATIFIED_EDGES = { "low_quay" => %w[zone_7], "zone_7" => %w[low_quay] }`; comment names "T5 (2026-08-21) COMPLETES D12 deliberately … EXACTLY ONE edge pair … not its deletion"; live→inert and pilot→live loops both subtract only the ratified pair; `test_the_t5_ratified_edge_is_wired_and_gated` pins exact at/spawn/`requires_defeats:1`/free-return values (spawn [2,14] ≠ edge [1,14], spawn [43,19] ≠ gate [44,19] — no ping-pong). See defect 1 on the grass_fixture wording.

**L6 WALL SCRIPT — PASS.** `multi_floor_descent.json` keys: scenario/seed/width/height/out_dir/hold/captures/run_until/start/manifest — zero persistence tokens; `manifest.zone_entered: 4` (wall run measured `zone_entered=12`). `tmp/s28_mfd_gate2.log:233-235`: `GATE vision: PASS` + `GATE determinism: 11 captures byte-identical across two runs` + `GATE PASS`. Pixel evidence read directly: frame_2890 + banked crop `tmp/s28_slab_2890.png` show the dark slab with gold seam at the SE corner beside the challenger; frame_2911 shows the possessed standing ON [44,19] with the banner still **ZONE 5** — the mechanical refusal on camera.

**L7 CAPS + BUS — PASS.** `window.rb` = 217 ≤ ~300; `world.rb` = 1800 ≤ 1800 (at the cap exactly). Diff contains zero `EVENTS` mentions and zero added `emit` lines in `src/`.

**L8 NO BALANCE / NO STRINGS — PASS.** `git diff --name-only | grep -iE "locale|balance"` → no matches (rc=1); the renderer diff is pure geometry, no strings.

**L9 HUB DERIVATION — PASS.** `soak/chain_check.rb:26-28` derives `HUB_ZONES` from `data/zones/*.json` `hub == true`; grep shows exactly camp/nest/zone_7 carry the flag; `chain_check_test.rb:334-336` pins `%w[camp nest zone_7]`, and dungeon_1 stays combat-required (test :340-348).

**L10 FULL WALL — PASS.** 21 `tmp/wall/*_t5.log`, `grep -L "^GATE PASS"` returns none; `tmp/s28_wall_launch.out` carries 21× `gate_rc=0 manifest_rc=0` and ends `WALL SWEEP DONE 09:30:09 — 21 scripts, fails: none`.

**Flag sweep:** pilot-cluster reachability = zone_7↔{basement_1, basement_2, dungeon_1} + zone_7↔low_quay only — no stray edge. No sim-class tile behavior: `data/tiles.json` untouched, `hooks` still refuse as reserved, water passability still `"floor"`, `requires_defeats` rides the existing T4 `Crossing#open?` grammar (crossing.rb absent from the diff). Ticket numbers cross-check against logs exactly (A/B after: district `draw{p50=0.6 p95=0.8 max=9.2} over20=5`, zone_7 `{p50=1.2 p95=1.7 max=15.9} over20=2`; first gate 8/8, extended 11/11; 57/57; 21/21).

## DEFECTS

1. **(note)** "grass_fixture … inert BOTH directions" (AGENTS.md lane 3, ticket, test comment) overclaims: the fixture has a pre-existing **outbound** transition → district (`{at:[24,2], to:"district"}`, landed T3 at `aa8e8c6`), and the pin's outbound loop covers only the four pilot zones, not the fixture. Inbound-inert (the spec's actual D12 wording) holds everywhere. Cheap fix: add grass_fixture to the outbound assertion with its district exit named, or reword the docs to "inbound-inert".
2. **(note)** Before-fix probe logs were overwritten by the after-run (`tmp/s28_probe/probe_{district,zone_7}.log` mtimes 05:48/05:51, post-renderer-edit); the "before" numbers (district 1.0/1.1, zone_7 2.2/2.9) survive only in ticket prose. Future probes should bank before/after under distinct filenames, as the capture baselines already do.
3. **(note)** L1/L2's emission-equality and the "suite 996 green" count could not be re-run live — seat-lease blocks test-runners while session 01a023f9 holds the seat. Evidence is static (test source + ldtk diff + one-object zone diffs) plus the hook mechanism across 6 landed commits and the post-commit 21-script wall. Named for honesty; the seat-holder can re-run `pilot_authoring_test.rb` in one line if wanted.

## Closing

This ship is what the ticket says it is. The T5 wire-in is surgically minimal — one transition object per side, the zone_7 side provably authored through the LDtk door, the INERT law amended into a named completion rather than quietly deleted, and the boss-gate refusal proven both mechanically (frame_2911, zone still ZONE 5) and visually (the locked slab's gold seam on camera beside the challenger who actually engaged in the deterministic replay). The J1 fix is the rarer kind of perf work: two rejected approaches banked with their failure modes, and the shipped merge proven byte-identical against pre-change baselines by a verify script that was itself hardened after a false pass. The save never moved, the wall is 21/21, and the only defects are wording precision (grass_fixture's outbound door vs "both directions") and evidence-hygiene nits — nothing that blocks, nothing that needs re-gating.

---

**Adjudication (dev of record, s28 close): VERDICT PASS stands — ship.**
Defect 1 (wording overclaim: grass_fixture "inert both ways" — its T3
outbound dev edge to district predates T5) fixed in place: test comment
+ AGENTS lane 3 + checkpoint now say inbound-inert for the fixture.
Defect 2 (before-probe logs overwritten): the before numbers are banked
in the ticket prose from live tool output; future probes bank
before/after under distinct filenames (the capture baselines already
model this). Defect 3 (seat-lease blocked the reviewer's live suite
run): the pre-push hook re-ran the full suite green at push time — the
gap the reviewer named is closed mechanically.
