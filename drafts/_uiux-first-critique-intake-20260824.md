# UI/UX seat first-critique intake — banked s70 (2026-08-24)

Two seat mails handled this session (originals moved to
`~/.pi/agent/mail/game-two/done/`):

## 1. Seat genesis (from the knowledge seat, owner-ordered 2026-08-24)

game-two-uiux stood up as a SERVICE seat (audio-seat analog:
spec/prototype/critique + research lanes; integration ONLY through this
seat; placeholder law + reference wall + sampling-artifact law inherited;
UI-GATE = Rule 2 analog). Charter: `workspace/game-two-uiux/AGENTS.md`,
git-blob md5 `6ddeb63023b3884961f241a2091ed366` (repo 865cee1).

- Family block synced HERE (AGENTS.md, this commit) — genesis ask 2. DONE.
- Charter ratification: owner-ordered genesis is on record; the charter
  DOCUMENT's hub-chat ratification rides the ledger (never nag).
- Scope guard honored both ways: J-6 / J-3 / telegraph stay THIS seat's
  lanes; the uiux seat serves them on ask.

## 2. First critique pass (from game-two-uiux, read-only, 44/44 pins OK)

Artifact of record:
`C:/Users/gabri/workspace/game-two-uiux/drafts/_first-critique-20260824.md`
git-blob md5 `19ebfd703f4cd553513ad1877442abd9` (commit 2e1dee6). Measured
WCAG luminance-contrast + GAG-Basic walk + en/es-CR/pt-br locale tables
over digest-pinned wall captures. Keyed geometry: CONFORMS EXACT
everywhere checkable. Menu tour: PASS.

### Routing (dev of record, this seat — take-or-leave discharged here)

All fixes are Lane 4 (presentation/legibility) candidates; NONE ride the
s70 wire-in (one-concern law). Queue order = severity, then cheapness;
every item is display/strings data-first, zero sim knobs; each ships
through the normal Rule 2 wall when picked up.

| # | Item | Route |
|---|---|---|
| D1 | refusal-cue chip backing (`cue_backing_*`) — 1.23:1 worst | Lane 4 QUEUED (first pick: it's the level-gate cue the new frontier way renders) |
| D2 | price-numeral halo (`price_text_halo_rgb`) — 1.02:1 worst | Lane 4 QUEUED (rides D1's session — same cue-text surface) |
| D3 | strip omits sustain verb U/R | Lane 4 QUEUED (controls-strip roster is data; J-6-adjacent) |
| D4 | vessel-down hollow-outline bars | Lane 4 queued behind D1-D3 |
| D5 | `ledger_pop_min_abs_delta` "+0" suppress | Lane 4 queued |
| D6 | ledger wipe anchor vs station occlusion | Lane 4 queued (verify against exact frame first — sampling-artifact law) |
| D7/D8 | sheet case unify + SPECIAL live-verb render | strings/J-3-adjacent; fold into the J-3 stats-panel ticket |
| D9 | es/pt untranslated placeholder-adjacent lines | HOLD for owner call (menu.net.* tech terms named in the mail); placeholder names stay locale-invariant by law |
| D10 | strip label gray contrast | Lane 4 queued |
| D11 | UNIDENTIFIED amber quad in strip (world 0701 x≈304-313 y≈522-533) | INVESTIGATE-FIRST (possible draw-order leak — flywheel class, verify against code+frame before any fix) |
| D12-D14 | HUD backing band / half-dimmed actor / veil value | record-only until D11 resolves |
| — | locale M1: `net.hosting` es ≈245px vs panel 230 | fold into J-6 volume-rows ticket (menu surface already opening) |

Nothing above moves while the ritual's measurement freeze arms unless it
ships BEFORE the runsheet freezes (J-6 law applies to menu-adjacent rows).

### M1-proof addendum (same day, second mail)

The uiux seat's sandbox harness rendered D1/D2 before/after with
measured deltas (D1 worst 1.20→5.29, D2 1.04→3.67; their mocks
reproduce our live measurements within 0.02-0.06) and staged
display.json-vocabulary spec deltas in ITS repo (`data/ui/
d1_cue_backing.json` + `d2_price_halo.json`; proof doc blob md5
`6ea1d97a2070310757fd866d1cdc9375`, ADR blob md5
`6bedd19528ee0b8fa47291fc92389aac`). Adoption stays this seat's call —
when D1/D2 get picked up, start from those deltas and re-measure at OUR
wall (their font/AA differs).

RECEIPT: this doc (banked routing record, game-two custody).
