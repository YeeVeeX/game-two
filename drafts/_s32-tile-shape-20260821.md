# s32 — TileMap tile-shape law: ill-shaped tiles refuse NAMED at zone load (2026-08-21)

**Commit:** `38d14a3` `feat: refuse ill-shaped tiles NAMED at zone load`
(src/core/tile_map.rb + test/core/tile_map_test.rb, one concern).
Suite **1004 → 1011** (7 new tests, all message-asserted), 0 failures,
green via hooks at commit.

## Provenance

s31 fresh-eyes review **nit 1, RECORDED**
(`drafts/_s31-edge-validation-20260821.md` §review — "next spot if the
named-refusal doctrine extends": the nil-`at` UNNAMED crash in TileMap).
Probed live at s31 close on this machine — all six modes verified before
the s32 spark was authored:

| Ill-shaped tile | Pre-fix behavior | Family |
|---|---|---|
| `at: nil` | NoMethodError `negative?` in `passable?` | UNNAMED crash |
| `at: ["3", "1"]` | NoMethodError `negative?` | UNNAMED crash |
| nil `pack_spawn` entry | NoMethodError `negative?` | UNNAMED crash |
| `gradient_anchor: "middle"` | NoMethodError `negative?` | UNNAMED crash |
| `at: [1.5, 1]` | **constructs CLEAN** — `Array#[]` truncates the Float, validates the WRONG tile; `transition_at`'s `==` never matches → DEAD transition | silent corruption |
| `at: [1, 1, 9]` | **constructs CLEAN** — destructure drops the 3rd element; same DEAD-transition death | silent corruption |

The silent family is the exact corruption class s31 killed for spawn
tiles — this closes it for every tile that reaches `check_passable!`.

## Classification

Lane-3 engine hardening (strict-importer / named-refusal doctrine),
**NOT v19-class, NOT sim-class**: any legal zone keeps byte-identical
behavior — only illegal DATA changes outcome (a refusal at load instead
of a crash or a dead transition at runtime). No visual surface moves →
**no Rule-2 gate owed** (load-time refusal only). `world.rb` untouched
(sits at 1800/1800 — deliberately Core-only).

## What shipped

ONE shape guard at the single choke point `check_passable!`
(src/core/tile_map.rb): signature `(label, (tx, ty))` → `(label, tile)`;
guard FIRST — `tile.is_a?(Array) && tile.length == 2 && tile.all? { |v|
v.is_a?(Integer) }` else `raise BadMap, "<label> must be an [x, y] tile
(got <tile.inspect>)"` — then destructure and run today's passability
check unchanged. Every caller untouched: the one guard covers all five
(pack_spawn, enemy spawns, transitions' `at`, stations' `at`,
gradient_anchor). Class is **BadMap** (tile_map.rb house style — World's
ArgumentError precedent does not carry down).

**Blast radius, proven:** the suite constructs every real zone + every
fixture through `TileMap.new` → `validate!`; 1011 green with zero trips
— no fixture fix, **no defect branch** (all shipped data already
well-shaped, matching the s31 19-edge pre-scan).

## Tests (test/core/tile_map_test.rb, extended — the BadMap idiom lives there)

One message-asserted case per probed mode: nil at · string pair · float
pair (the silent-truncation kill) · 3-element · nil pack_spawn entry ·
string gradient_anchor · one explicit legal control
(`test_legal_integer_tiles_stay_valid_through_the_guard` — every other
construction in the file rides the guard implicitly).

## Review receipt (Rule 6 — scrubbed read-only sub-session, pre-push)

**PASS-WITH-NITS** (tmp/s32_review_out.txt — banked here, tmp is
ephemeral). All six contract claims VERIFIED against code; reviewer
independently constructed all 11 real zones through the NEW guard (0
trips) and scanned every guarded tile field in shipped data with the
guard's exact predicate (0 ill-shaped, 0 floats — floats live only in
`drop_gradient`, which is not guarded). Regex escaping in all seven
tests verified against Ruby `inspect` output. No caller of
`check_passable!` outside tile_map.rb (method is private).

Findings banked:

1. **Integral-float edge (nit, deliberate tightening):** `at: [2.0, 1]`
   previously worked END-TO-END (Array#[] truncates 2.0→2 — the right
   tile — and `Array#==` is numeric, so `2.0 == 2` matched at runtime).
   The new guard refuses it. Zero such floats in shipped data (live
   scan) and the LDtk importer refuses non-Integer `cx`/`cy` NAMED at
   import (`tools/import_ldtk.rb:288`) — the live authoring pipeline
   cannot produce them. Recorded so a future hand-author isn't
   surprised: integral floats are ILLEGAL tiles now, by design.
2. Reviewer could not re-run the suite (seat-lease holds the
   test-runner for this live session) — gap closed mechanically by the
   pre-push hook re-running `bundle exec rake` at push (it did: green).
3. Old-code failure modes verified statically (git show + MRI
   semantics), not live — seat-lease blocked even read-only probe
   invocations; the reviewer respected policy and did not route around.
   The six modes were ALSO probed live at s31 close on this machine
   (the provenance table above), so the static reading is corroborated.

Reviewer also confirmed `transition[:spawn]` is NOT guarded here —
pre-existing scope (s31's `Crossing.validated_arrivals` owns spawn
validation at WORLD load; this guard owns zone-local tiles), not a
regression. Mail dir audited after the run: inbox 0 / done 22 / audio
T3 cue-spec untouched — clean.

## Scope refusals (RECORDED, never built this session)

- **Semantic cross-checks** (e.g., a seal's `opens` naming a real
  transition tile): `opens` isn't routed through `check_passable!`
  today — a different validation pass, out of this ticket's law.
- **world.rb**: untouched by design (1800/1800 — next material touch
  owes its own extraction).
- **Importer mirror**: unchanged from the s31 decision (name-only
  universe; RECORDED follow-up stands).
