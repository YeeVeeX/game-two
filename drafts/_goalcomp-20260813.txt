GOAL: EXECUTE v11 = DENSITY/RE-MASSING per the APPROVED PLAN at C:\Users\gabri\.claude\plans\agile-greeting-bengio.md - READ IT FIRST (self-contained execution SSoT). Do not re-derive closed decisions. Also read project CLAUDE.md (Rule 8) + docs/CHECKPOINT.md top entry.

FORKS CLOSED 2026-08-13 (owner via AskUserQuestion, pre-spec as the debate ordered): (1) core shape = RE-MASS TOWARD CLUSTERS: respawn tile chosen at RELEASE time in respawn_due_humans; joins nearest surviving pocket below pocket_cap; all capped/field empty -> SEED new pocket at kit spawn tile farthest from pack; home fallback. (2) depth bias = NEUTRAL (one hypothesis: dense field fixes stale; bias stays a later data knob). (3) Q6 rider = band tint + size/glow by band, NO pickup fanfare: spawn_drop stamps band on the drop record, renderer keys size/palette per band.

STATE (measured at goalcomp): main 227 commits at d6615f5, tree clean, suite 285 runs/1178 assertions green, checks 39 ADD-ONLY. Revert wall chain CONFIRMED DEAD = INFRA (summary log one START line; vat log cut mid-replay; no ruby proc).

SEQUENCE (= plan steps): (0) RE-RUN full 9-gate revert wall on d6615f5 BEFORE v11 code (rake gate x9; logs /tmp/q6_revert_wall_<s>.log + summary; INFRA -> retry max 3 rounds/script; real FAIL -> fix forward per drafts/_d1b-wall-log.md); 9/9+9/9 required; append map to drafts/_q6-wall-log.md noting dead chain; commit. (1) Write + commit spec docs/superpowers/specs/2026-08-13-v11-density-remassing-design.md (forks verbatim, mechanism, data schema, corpse-guard fairness rule, density telemetry format, NINTH-verify questions + routing table IN THE SPEC). (2) TDD on branch v11-density per plan: release-time anchoring; defer rules incl. corpse_guard during wipe grace; data threat.json density{join_radius_tiles 3, pocket_cap 5, scatter_radius_tiles 2, corpse_guard_tiles 6}; event :human_respawned{actor,tile,anchor}; band stamping + renderer rider; density telemetry like q6_cadence; update home-tile respawn tests (meaning change = the increment). (3) Wall: replays WILL desync (expected) -> re-pilot via rake pilot (re-stage ALL mandatory beats), export, full 9-gate wall; ADD one band-2-drop-distinct check (39->40, ADD-ONLY); map -> drafts/_v11-wall-log.md. (4) rake perf ALONE (p95 < 16.6ms) then full rake. (5) merge --no-ff to main, NO push; CHECKPOINT with measured numbers. (6) BLIND ninth fun-verify: no changelog; owner plays first; UNIQUE log /tmp/game_two_session_$$.log, never relaunch while a window is open; harvest ALL telemetry (density + q6_cadence) BEFORE questions; questions/routing from spec verbatim; verdict -> drafts/_v11-fun-verify-<date>.md + CHECKPOINT + commit. (7) Next debate via AskUserQuestion: Challenger (trigger triple-confirmed 6th/7th/8th, declined 2x, fairness ladder mandatory) vs arc/purpose v12 (A3 + bible; owner wishlist on record) vs ninth-verify routing; scope v12 + PARKING_LOT + CHECKPOINT committed.

STANDING RULES: export PATH="/c/Ruby34-x64/bin:$PATH" per shell. NO git push ever. Unit prices 8/12/2. Gradient [[0,1.0],[14,1.5],[28,2.0]] + strictly-increasing shape law. Zero balance constants in Ruby (presentational renderer constants OK per precedent). Red suite = fix, never --no-verify. Read-before-edit. Judge builds, not briefs. Blind verify integrity. Q7 cue redesign PARKED; Nest rename post-bible; video-critic harness leg PARKED with dossier.

OWNER QUEUE (unchanged): council MCP deepseek-r1 us. prefix; council-via-mmh-gateway + when-to-council update (own session); optional tracked bin/install-hooks.

DONE WHEN: revert wall 9/9 harvested + logged; spec committed; implementation merged --no-ff with 9/9 wall + new check + perf + suite green (NOT pushed); ninth fun-verify handed BLIND with verdict recorded + routing applied verbatim; next scope debate held via AskUserQuestion and scope v12 + PARKING_LOT + CHECKPOINT committed.
