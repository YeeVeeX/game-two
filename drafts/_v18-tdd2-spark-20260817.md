# SPARK: v18 TDD session 2 — protocol v2 + the two-session persistence lane (increment 3, then 4 if room)

(Supersedes an untracked reconcile draft of this file written 16:01
against pre-push state — its premise died at `c94fffb`; verification is
kept as Job 0 because a fresh session trusts git and the suite, never
prose.)

You are the dev of record in game-two. Read AGENTS.md FIRST — the v18
scope contract is ground truth. The v18 spec is FINAL (owner ratified
twice 2026-08-17, veto window CLOSED "aprobado"): build it AS WRITTEN;
design forks do NOT re-open. Work in English; answer the owner in his
language (es-CR, ustedeo, flat mechanics register — no pitch talk).
Increments 0-2 shipped last session (`5994eaf` + `c94fffb`): SaveState
+ the round-trip lane green, solo persistence LIVE (two-launch digest
chain verified on real windows).

## Read before acting (in order)

1. `AGENTS.md` — whole file (Ruby PATH per shell:
   `export PATH="/c/Ruby34-x64/bin:$PATH"`).
2. `docs/CHECKPOINT.md` — top entry ("v18 TDD session 1"): what landed,
   five recorded micro-decisions, the increment-3 resume map.
3. The spec `docs/superpowers/specs/2026-08-17-v18-persistent-world-design.md`
   — this session executes decisions 2/5/6/8 + test lane 3 (and
   decisions 11/12 + lane 4 if increment 4 opens). Re-read them whole.
4. `drafts/_v18-spec-review.md` — do-not-relitigate. TWO refutations
   are settled with source evidence and are THE DESIGN this session
   builds on: (a) either seat's clean Esc lands reason=:quit on BOTH
   seats (session.rb BYE_REASONS) — the host saves on joiner-initiated
   quit; (b) NO stale-snapshot race — BOTH Worlds construct at the
   READY→START barrier from Params frozen at launch, so the save rides
   Params and both seats build from byte-identical facts.
5. Source you will extend — read BEFORE designing: `src/net/protocol.rb`
   (pinned 10-bit action order; vocabulary tables), `src/net/session.rb`
   (SESSION/Params; `handle_bye` ~:357 — refusal detail is
   fingerprint-only TODAY; decision 6b widens exactly that),
   `test/net/netplay_integration_test.rb` (real-loopback two-seat
   pattern), `harness/net/*` + `harness/scenes/netplay_scene.rb`
   (determinism law in the header), `src/main.rb` + `src/app/window.rb`
   + `src/app/save_store.rb` (increment-2 wiring you will extend).

## Job 0 — verify state (trust git + the suite, not prose)

- `git pull --ff-only` FIRST (Junior's seat is ACTIVE; validate any
  drafts/ handoff: `swarmforge handoff validate`). Confirm history
  holds `5994eaf` + `c94fffb`, tree clean.
- `bundle exec rake` green (was 706/13451).
- `tmp/canary_baseline/` holds 17 dirs + manifest.md5 (MACHINE-LOCAL).
  If tmp/ was cleaned: re-bank from the CURRENT sim-identical line
  BEFORE any sim change (`tmp/bank_canary_v18_resume.sh` protocol,
  ~21 min unattended).
- Any check fails → reconcile it first (the checkpoint + tdd1-spark
  laws govern); never build on an unverified base.

## Job 1 — increment 3: protocol v2 + SESSION save transfer + refusal taxonomy + wire preflight + THE TWO-SESSION LANE

(spec decisions 2/5/6b/6c/6d/8; test lane 3). TDD: failing test first.

- **Protocol v2, ONE bump for the cycle** (decision 8): 11-bit mask —
  append `:sustain` LAST (existing bit positions unchanged). The BIT
  rides now; the VERB is increment 5 — do not implement it. SESSION
  gains `save_schema`/`save_digest`/`save` (the canonical facts STRING,
  or null for a fresh world — never a nested object). Suite pins the
  FINAL v2 vocabulary (a second silent bump must fail a test). INPUT
  stays ≤40B. v1 peer → the existing named version refusal + pull hint.
- **Host launch order** (decision 6c): load file → strict-decode
  (console refusal, exit 1, no window) → encode the ACTUAL
  `Protocol.encode(:session, …)` line with the real canonical string →
  refuse NAMED at the console if it exceeds `wire_budget_bytes` (3072)
  BEFORE the socket opens → print `TELEMETRY persist loaded …
  source=file` → listen. Extend `--fresh` to compose with `--host`
  (checkpoint micro-decision 4: solo-only until host custody — which
  is now). W4 budget test: the encoded worst-case line (all seals, max
  counters) fits.
- **Save rides Params** (the frozen-snapshot refutation): both Worlds
  construct `World.new(…, seats: 2, save: params.save_facts)` at the
  barrier in `window.rb#update_session`. The joiner digests the
  RECEIVED canonical string BEFORE parsing (decision 5 — a
  parse→re-serialize can never enter the verdict); digest mismatch =
  BYE `save_digest`; schema skew = BYE `save_schema`; strict-decode
  failure during the PRE-WINDOW pump = console refusal, exit 1, NO
  window (bindings-error precedent). Joiner prints `loaded …
  source=handshake` and NEVER writes.
- **Refusal taxonomy** (decision 6b): BYE vocabulary grows
  `save_schema`/`save_digest`/`save_invalid`; `handle_bye` records
  refusal text for ALL refusal reasons — both seats print the SAME
  named refusal and exit 1. Status 2 stays link-fault-only; RC-matrix
  tests extended (launchers must NOT rehost on a refusal).
- **Host save-at-quit**: main.rb host branch builds SaveStore +
  SaveCoordinator (owner = host). ⚠ TRAP left scoped in increment 2:
  `Window#close` hardcodes `reason: :quit` — correct solo-only; session
  mode MUST route `@session.reason` into the coordinator or the
  desync/conn_lost negative lanes silently break.
- **THE LANE** (lane 3, extends netplay_integration_test): PER-SEAT tmp
  save roots. Session pair 1 (fresh world, save=null) over real
  loopback banks value + breaches via scripted inputs; the host
  coordinator writes a REAL save at quit → session pair 2 constructs
  from that file, SESSION transfers the canonical string, BOTH seats'
  loaded digests == the saved digest, K further ticks zero desyncs,
  carried fact asserted (banked₂start == banked₁end), joiner save root
  asserted EMPTY after both sessions. Refusal lanes: schema skew /
  tampered facts (digest) / malformed facts pre-pump (no window) /
  oversize (host-start preflight) / v1 peer.
- AFTER GREEN: netplay gates re-run WITH critic —
  `rake gate SCRIPT=harness/net/<netplay_session|netplay_desync|
  netplay_conn_lost>.json CHECKS=harness/net/gate_checks.json` (scenes
  construct fresh worlds; SESSION carries save=null — the wall stays
  single-player and save-blind). `rake perf`. Canary sweep
  (`harness/run_canary.sh`) if anything under src/game or a
  harness-loaded file was touched — when in doubt run it; it is
  unattended. Commit + push (explicit paths).

## Job 2 — increment 4, ONLY if increment 3 is green+committed AND there is room to finish green

(never start what you cannot finish): coop feel (decisions 11/12; lane
4). `data/balance/coop.json` seat scalars (`respawn_delay_scale` 2.0 →
schedule-time `(respawn_frames × scale).round`; `human_hp_scale` 1.25 →
spawn-time `(max_hp × scale).round`; `ally_flee_hp_pct` 0.35). seats=1
= the block is NOT READ, zero arithmetic evaluates (decision 7ii — no
300→300.0 Float may ever enter the digest; scaled values are explicit
`.round` Integers). Third-body caution at the PINNED AiController
precedence: forced-seizure handling → flee check → mark/aggro (a
fleeing body ignores the mark) → committed in-flight actions FINISH;
flee = no new attacks + move toward the follow anchor; seats≥2 only.
Lane 4: scalar-unit tests, nothing-evaluates-at-seats=1 pin, flee
precedence lanes (seized ally doesn't flee mid-seizure; committed swing
finishes), two-sim digest identity with the coop block active. THIS ONE
IS SIM-TOUCHING: full canary sweep (17/17 byte-identical — seats=1
untouched by construction) + netplay gates + perf + commit + push.

## Laws that bite

- Spec-vs-code contradictions → smallest faithful deviation, RECORDED
  in the checkpoint (five precedents already listed there).
- Hooks run `bundle exec rake` at commit and push — fix the test, never
  `--no-verify`. Pull before push; explicit-path commits (shared cwd).
- No mocks: real loopback sockets, real Worlds, real files (per-seat
  tmpdirs). The two-session lane must FAIL loudly if sockets can't
  bind — never stub the transport.
- Netplay staging law (project memory, hit live): sim-mutating pokes
  key on WORLD TICK per world, never scene frame — the two sims run
  ~D ticks apart and frame-keyed pokes manufacture fake desyncs.
  `now_ms` stays a pure function of the frame in scenes.
- Fingerprint law: any new content-hash exchanged at the handshake is
  EOL-normalized (`10b6138` precedent — autocrlf clones differ).
- The wall stays single-player: netplay lanes live OUTSIDE
  harness/scripts/; harness Worlds construct `save: nil` (structural
  pins in test/harness/wall_pin_test.rb — do not weaken them).
- All tunables in `data/**/*.json` (coop.json — no constants in code);
  placeholder text only (no lore — standing owner order).
- Perf after any sim-touching increment (p95 < 16.6ms; ~0.2ms current).
- Bounded soaks: the two-session lane runs bounded tick counts (K in
  the hundreds), never wall-clock waits; judge by TELEMETRY/digest
  asserts, not process liveness (the zombie-soak lesson).

## Stop conditions

- Stop after increment 4 green+committed+pushed (or increment 3 if time
  runs short) + a new CHECKPOINT.md top entry naming exactly where the
  next session resumes (increment 5: sustain sim — provisions economy +
  edge-triggered verb + first-success-per-tick seat race; then 6:
  sustain presentation + Rule-2 gate; 7: rake map; 8: docs + close).
- If the two-session lane exposes a cross-session desync (a loaded
  world diverging at a digest window) that survives 2 honest fix
  attempts: STOP, bank the failing test + both seats' artifacts in
  drafts/, checkpoint, hand off. That is W2 firing in the safety net —
  cycle-critical information, never route around it.
- If the netplay gates fail their CRITIC half after protocol v2: that
  is a blocking Rule-2/6 gate — fix or stop+checkpoint; never ship on
  SKIP_CRITIC.
- Owner queue at close: one line, only if something needs his hands
  (playtest or verdict). No design homework for him.
