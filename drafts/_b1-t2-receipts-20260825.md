# B1-T2 receipts — the visible boundary: renderer + strings + wall (s72, 2026-08-25)

Spec: `docs/superpowers/specs/2026-08-24-b1-safe-zones-design.md` §D5 +
§B1-T2. Commit: `a30837d`. world.rb UNTOUCHED (the T1-provided
`zone_maps` read was sufficient — no reopen needed).

## What shipped

- **Threshold frames (D5 piece 1):** every transition tile whose two
  sides DIFFER in safety carries a thin static 3px frame at the tile
  border, derived at draw time from the live transition tables
  (`Renderer.threshold_kind(map, world.zone_maps[t[:to]])`) — never the
  spec's prose inventory (its own nit-2). Mint green
  ("reach that door = safety", read from the unsafe side) · dark ember
  ("beyond this = danger", read from inside — deliberately dimmer than
  TELEGRAPH_EDGE: signpost, not attack). The frame sits OUTSIDE the
  gold fill's inset and composes with the seal slab: lock state stays
  the slab/gold read, the frame carries the destination fact. Static
  by design v0 — a boundary is state, not an event; pulse is T3-feel
  territory. Same-safety edges and unknown destinations carry nothing.
- **SAFE chip (D5 piece 2):** persistent while the active zone is safe
  — small type on a quiet near-black backing under the LEVEL strip in
  the pack status block (overlay vocabulary; the banner slot stays
  transient). Tibia PZ-icon touchstone. Renders through the v13
  strings resolver; EN fallback keeps a bare Renderer.new drawable.
- **Strings ×3:** `safe.chip` = en SAFE · es SEGURO · pt-br SEGURO
  (the ratified wording). Human-facing-output check at build,
  axes separate: ACCURACY — the chip asserts an enforced invariant
  (B1-T1 load refusal + acquisition guard), correct everyday words in
  all three locales; PRESENTATION — one all-caps word matching the
  placeholder register family (HUB 1 / LEVEL / PROVISION), functional
  state word translates, placeholder names stay invariant. PASS both.
- **display.json:** 9 new keys (chip x/y/rgb/alpha/backing_alpha,
  safe/danger threshold rgb+alpha) — display vocabulary, zero
  balance numbers.
- **NEW wall script `harness/scripts/safe_boundary.json`** (wall
  33→34): camp start → chip under banner (170) → chip persists past
  banner expiry (340) → east approach, ember frame beside the body
  (580) → district_two, mint frame on the camp-bound door read from
  the unsafe side with the hostile field converging (660) → free
  return, HUB 1 banner + chip + honest XP/HP marks from the d2 fight
  (770). The east loop per D5's seal note — the district-side
  threshold stays gate-refused on fresh saves.
- **`harness/gate_checks.json` +`safe_boundary_reads`:** chip
  present in EVERY safe-zone frame / absent in unsafe frames; frames
  only on safety-DIFFERING doors; distinctness named against the
  telegraph, respawn tell, vat green, and gold fill; honest
  "not exercised" clause per house style.
- **Tests (+6):** `test/app/safe_boundary_render_test.rb` —
  threshold_kind four-quadrant pins over real TileMaps + nil-dest
  guard, three-locale chip wording pin, display-key declaration pin.

## Verification

- Suite: 1245 runs / 22636 assertions / 0 failures (was 1239; +6).
- `rake gate SCRIPT=harness/scripts/safe_boundary.json`: PASS —
  determinism 5/5 captures byte-identical, vision PASS (the new row
  judged on substance: "SAFE chip persists in every hub frame, absent
  in ZONE 3, and the unsafe-side gate carries a mint frame"),
  MANIFEST PASS zone_entered=8 (double replay, min 4).
- zone_7 co-verification (spark requirement): `town_gates` re-gate
  PASS — determinism 6/6, vision PASS ("SAFE chip persists below the
  LEVEL strip in every frame and gate tiles carry quiet dark-red
  frames"), MANIFEST PASS. Dev read the frames before the critic:
  frame_0900 shows both basement slabs + ember frames composing,
  frame_0610 the chip beside the LEVEL 6 REQUIRED refusal.
- Pixel-level self-reads before any critic: door-tile crops verified
  ember frame over gold (camp east/west) and mint frame over gold
  against d2's slate walls (`tmp/door_east_170.png`,
  `tmp/d2_threshold_660.png`).
- God-view untouched by construction: map_artifact resolves its own
  tile colors (no draw_map reuse) — map probes stay 13/13 unmoved.
- FULL wall sweep (34 scripts, detached, code frozen): see addendum.

## Deviations (all investigated)

1. Origin tip `0a7492b`, not the spark's `f65fe05` — own-seat
   docs-only banking commit (uiux M2 locale audit into drafts/),
   post-checkpoint s71. Lawful, no action.
2. Script manifest corrected 2→4 during build: the teed log counts the
   frame-0 construction+staging pair (`nest` + `start_in` camp) per
   replay — house convention verified against zone8_crossing (4) and
   dungeon_fork (3). 4 is the honest per-replay total.
3. Final capture moved 740→770: at 740 the ZONE 3 banner was still
   dwelling over camp visuals (banner FIFO — the active entry finishes
   its 150 frames before HUB 1's queued banner starts). Not a defect;
   the capture just waited out the queue so the reel's last frame
   pairs camp visuals with the camp banner.
4. The d2 visit runs hot on schedule: a rusher acquires and the allies
   answer (fights=1, kills=1, striker HP dent) — kept: the danger side
   SHOULD read dangerous opposite the sanctuary frames.

## Addendum — full wall verdict (pre-shake sweep, tag s72_b1t2)

**33/34 gates GREEN + vision PASS across the board; ONE red:
`varekka_duel` MANIFEST FAIL** (gate itself PASS — determinism +
vision; wants chants≥6/interrupts≥4/pickups≥2 per double replay, got
2/0/0). This is the RECORDED stale-manifest debt surfacing
mechanically, not a T2 regression:

- Sim identity is machine-proven: the suite's stream canary
  (test/harness/sim_identity_canary_test.rb) still hashes varekka at
  `31c699cb…` — the J7-B-ratified stream — green in every hook run
  this session; the renderer is a pure sink.
- The s60 re-pilot record (`drafts/_varekka-repilot-rebrief-20260824.md`)
  already established the shipped manifest ENCODES the pre-J7-B
  door-bounce exploit (six honest generations could not re-earn it)
  and its re-cut is RATIFIED as option (a) (s66 line 2, Junior's
  pick) — queued as the NEXT dev ticket (s73), which also owes the
  volley wall script.
- Why it never surfaced before: no FULL wall with manifests ran
  between J7-B's push (s66) and this sweep — s71 used deliberately
  manifest-blind capture pairs; today's stream shows the honest
  post-J7-B duel (1 chant, full 450f seizure `frames=450`, no
  interrupts, no pickups — exactly the s60 profile).

Spark law honored: STOP-EARLY on gate red — red recorded here + in
the checkpoint, surfaced to the owner in-session; the fix is the
already-ratified next ladder rung, never an ad-hoc manifest edit
inside the visual ticket.

## Addendum 2 — OWNER ORDER mid-session (2026-08-25): screen shake −50%

Order landed live in chat ("reduce it like 50%"); executed same
session as its own one-concern commit `f67a1eb`:

- `data/balance/combat.json` feel block: shake_hit 3.0→1.5 ·
  shake_player_hit 6.0→3.0 · shake_kill 8.0→4.0. `shake_decay` 0.85
  DELIBERATELY untouched — that is the length/falloff axis; halving
  amplitudes halves strength at every envelope point (shake_x =
  sin(phase)·amp, linear). Data-only — the knobs already lived in
  data (non-negotiable 3 satisfied); shake amp/phase are
  digest-EXCLUDED presentation (feel.rb decision-6 note), so no
  netplay/save surface moves; cross-seat data divergence stays
  covered by the v17 build-identity handshake.
- Deterministic magnitude proof: headless probe through the REAL
  Game::Feel + the LIVE data file — displacement ratios 0.500
  (±0.001 float rounding) across all ten ticks of all three verbs
  (on_kill peak 5.6→2.8px, on_player_hit 4.2→2.1px, on_hit
  2.1→1.05px).
- Rule 2: re-gates world_loop + specials_chain + critic_reel all
  PASS (double replay + critic + manifest). All 38 pre/post capture
  frames byte-identical — investigated to root cause, not waved
  through: only CONTROLLED kills/hits shake (world.rb:1551,
  1518-1523), and killer+possession forensics show every
  kill-adjacent wall capture rides an ALLY kill (critic_reel@695
  killer=lobber while striker possessed · aoe@1171 killer=striker
  pre-swap · level_up@643 killer=blocker post-swap · world_loop@692
  killer=lobber after 301→304 swaps to striker) — the wall never
  sampled a possessed-shake window, so identical bytes are expected,
  and the probe carries the magnitude evidence.
- Full wall re-sweep owed on any visual-surface move: running
  detached (tag s72_shake), verdict below.
- Feel judgment stays the owner's at next launch; a flywheel clip is
  one `harness/make_clip.sh` away on his word.

## Addendum 3 — full wall verdict (post-shake sweep, tag s72_shake)

**33/34 green; the SAME single red (varekka_duel stale manifest).**
Sweep raw: 32 green in-sweep + 2 flagged (varekka_duel + level_gate).

- `varekka_duel`: identical red profile to the pre-shake sweep
  (manifest 2/0/0 vs 6/4/2) — the recorded debt, unchanged.
- `level_gate`: **judge-variance incident, resolved.** Vision FAIL
  twice ("two zones read identical") on bytes machine-proven
  unchanged: the a30837d-pinned worktree re-capture md5-matches the
  post-shake gate_a on all 5 frames, determinism green in all three
  gate runs, and the reel's only crossing is gate_fixture's
  level-gated SELF-LOOP (`"to": "gate_fixture"`) — one zone by
  construction, which the pre-shake judge correctly called
  not-exercised. Fix `8fc440b`: zone_identity_reads now instructs
  zone COUNT from banner NAMES (evidence the critic can actually
  see) — tightening, not loosening; no other wall reel crosses a
  self-loop. Re-gate PASS with the honest verdict ("Every banner
  names TEST 1, the fixture loops into itself").

Net session verdict: every gate green except the varekka manifest,
whose re-cut is the ratified next ticket (s73). Shake −50% ships
fully walled.


