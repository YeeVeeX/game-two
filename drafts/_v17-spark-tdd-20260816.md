# SPARK: v17 TDD opens — digest lane FIRST, then seat plumbing (increments 1-2+)

You are the dev of record in game-two. Read AGENTS.md FIRST — the v17
scope contract is ground truth (multiplayer etapa 1, lockstep co-op with
Junior over Tailscale). The spec is COMMITTED and dual-review-folded;
this session executes it. Design is CLOSED — build, don't re-debate.
Work in English; if the owner appears, answer him in his language.

## Read before acting (in order)

1. `AGENTS.md` — whole file (contract + non-negotiables + standing orders).
2. `docs/CHECKPOINT.md` — top entry (state at close + this session's order).
3. `docs/superpowers/specs/2026-08-16-v17-multiplayer-etapa1-design.md` —
   READ FULLY. Every law you need is pinned there (sampling law, seat-order
   law, digest composition, canary protocol, seat-semantics table).
4. `drafts/_v17-spec-review.md` — the review ledger. Three REJECT-grade
   claims are REFUTED WITH REASONS there (lockstep deadlock, GC sampling,
   Gosu cadence) — do NOT re-litigate them; 14 folds are already in the spec.
5. `drafts/_junior-etapa0-20260815.md` §SIM-IDENTITY — the three banked
   digest md5s your canaries must reproduce (a4150c… / 22dbad… / d148b8…).

## The session's job, in spec order (each increment green + committed)

1. **Increment 1 — DIGEST LANE (the safety net everything stands on):**
   extract the shared event-serialization helper (world_scene consumes it;
   its EVENT lines stay byte-identical — that IS the canary), build
   `Net::StateDigest` + per-object `digest_fields` (flat named scalars,
   stable ids, spec decision 6 coverage list) + the mutation-sensitivity
   sweep + CountingRng wrappers (value-transparent — wall untouched by
   construction) + the headless script-driver test helper + the THREE
   banked-digest canaries in the default suite.
2. **Bank full-wall canary baselines BEFORE touching World:** one replay
   per wall script into `tmp/canary_baseline/<script>/` (the `rake canary`
   BASELINE dirs). No seat plumbing until baselines exist.
3. **Increment 2 — seat plumbing:** Pack seat map + World per-seat tick
   (bare input back-compat) + decision-11 seat semantics + swap/death
   arbitration + waiting-for-body + judgment assignment + per-seat cameras
   + renderer/overlay `local_seat:` seam. BLOCKING: full-wall `rake canary`
   + the three headless digests — byte-identity or it's a defect (fix,
   NEVER recalibrate).
4. Increments 3-6 (protocol/lockstep/wire/two-sim lane) only if 1-2 are
   clean and context allows — never start one you can't finish green.

## Laws that bite in this session

- **No mocks:** the integration lane is two REAL Worlds + REAL loopback
  TCP (127.0.0.1 explicit) in one process, synchronous pumps, NO THREADS.
- **The wall stays single-player and untouched**; harness/net/ is the
  netplay home; nothing new enters harness/scripts/.
- **Rule-2 surfaces arrive at increment 7, not before** — this session
  ships no player-visible UI; placeholder/functional text only when it
  does. window.rb cap ≤300 stands (~30 lines of netplay glue max).
- **Suite green at every commit** (hooks); `bundle exec rake`; pull before
  push EVERY time — Junior's seat is ACTIVE (he pushed session drafts
  mid-day). Coordinate via drafts/ + `swarmforge handoff validate`.
- Codex note if a review leg is needed: `--sandbox read-only` is BROKEN on
  this machine (os error 206) — use `--sandbox danger-full-access` plus an
  explicit no-write order in the prompt.
- Junior's seal-1 wall report (`drafts/_junior-sessions-3-4-20260816.md`)
  is RECORDED-ONLY — no balance levers this cycle; it is next-debate
  material.

## Stop condition

Stop when increments 1-2 are green + pushed (deeper only if clean);
checkpoint at close (CHECKPOINT.md top entry: measured state + exact next
step). A broken canary is the session's headline — fix or checkpoint
honestly; never push a red or recalibrated line.
