# D1+D2 adoption — refusal-cue chip + price-numeral halo (s75, 2026-08-25)

Lane-4 queue rung (owner-ratified ladder, s75 spark): adopt the two
blocking-severity uiux M1 deltas. Take-or-leave law: adoption is THIS
seat's intake decision, re-measured at OUR wall (their font/AA differs
— the DELTA and the mechanism are the evidence, never their absolute
numbers).

## Evidence triage (digests verified live this session)

Routing note of record: `drafts/_uiux-first-critique-intake-20260824.md`
(D1/D2 rows + M1-proof addendum). All digests below re-computed from
`game-two-uiux @ 9907021` git blobs this session — no hash retyped from
memory:

- `drafts/_m1-proof-20260824.md` = `6ea1d97a2070310757fd866d1cdc9375`
  ✓ matches the banked routing note (UI-GATE PASS on before/after
  mocks; D1 worst 1.20→5.29 adjacent, D2 1.04→3.67 adjacent; their
  current-state mocks reproduce our live measurements within 0.02–0.06).
- Staged deltas (no digest in the routing note — pinned HERE at
  adoption): `data/ui/d1_cue_backing.json` =
  `97ac3f709d9f2891f205ef8f32f209ad` · `data/ui/d2_price_halo.json` =
  `dbf21b0fd0d840ec5e47959fa574e9ad`.
- M2 correction carried: the live cue is hud_font 14px (not 12); at
  14px their chip render showed 0px accent escape, 4px spare.

## What lands (scope of record)

**D1 — refusal-cue text chip** (`d1_cue_backing.json` verbatim):
refusal TEXT lines (`provision_refused` + `level_required`, the two
kinds that carry text) switch from raw `CUE_REFUSED` red caps
(measured 1.23:1 where letters cross light-green tiles) to banner-cream
`[225,215,190]` on a dark backing chip `[12,10,14]` alpha 160 pad 4
(ledger-panel style family). The X-bar stays `CUE_REFUSED` red — the
refusal IDENTITY stays red; only the explanation line changes surface.
Success cues (`CUE_OK` ring + text) are OUT of scope (not flagged).
New display.json keys: `cue_text_rgb`, `cue_backing_rgb`,
`cue_backing_alpha`, `cue_backing_pad`.

**D2 — economy-numeral halo** (`d2_price_halo.json` adopted with one
deviation): 1px dark halo `[20,14,12]` under every world-anchored
economy text (bank banked total, R-A2 BUY hint, altar/vat `-price`)
plus the HUD carried counter — the four `DROP_CORE` text sites in
renderer.rb. Halo mechanism = their harness verbatim (union of
Chebyshev-1..d offset draws in halo color, glyph on top, same z, call
order stacks). Ledger beat numerals are OUT of scope — panel-backed
(BEAT_PANEL alpha 160), ground already controlled. New display.json
keys: `price_text_halo_px`, `price_text_halo_rgb`.
**Deviation from the staged spec:** their `price_text_rgb` key is NOT
adopted — the value is unchanged `[205,70,225]` and the purple identity
linkage with drops/pips (DROP_CORE, "owned by no other element") is
intentional; keying only what changes behavior. Their notes already
mark the color key as our call (ADR-0001).

## Order + gates (ONE delta per gate pass; critic budget ≤5)

1. D1 ships alone → `rake gate` critic-ON on **town_gates** (stages
   `level_required` on BOTH grounds — uiux worst case was its light-
   green frames 0575/1060) + **sustain_run** (stages the REFUSED line).
   Gate rows `sustain_refusal_cue_reads` + `level_gate_reads` gain a
   chip clause (corpses_persist s74 precedent: clause, not new row —
   the chip is HOW the existing judgment now reads).
2. D2 ships alone → gate critic-ON on **world_loop** (bank numeral +
   carried counter over pink walls — their 0983 class) + **mercy_floor**
   (`-5` over the orange body quad — their 0020 class). Rows
   `bank_station_reads` + `carried_count_reads` gain a halo clause.
3. Suite green via hooks per commit; canary law checked: D1/D2 are
   renderer+display only — the sim-identity EVENT stream is untouched
   by construction (`sim_identity_canary_test` asserts it every suite
   run; no rebank).

Wall count stays 35 — no new scripts; both deltas judged on existing
walled surfaces.

## Receipts (filled at close — empty until then)

- D1: (pending)
- D2: (pending)
- Suite / canary: (pending)

