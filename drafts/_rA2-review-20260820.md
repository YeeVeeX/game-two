# R-A2 fresh-eyes review receipt (2026-08-20)

Reviewer: scrubbed headless pi (no session, env PI_* removed), inputs = spec +
both commits + verification receipts; prompt digest: md5 8e4f57c582a2b496c4f9e54b86168174.
Dev-seat action on findings: 1 (LOW, unknown-reason guard unpinned) CLOSED
same session — test_sustain_unknown_refusal_reason_counts_without_a_new_bucket
(telemetry_test.rb), suite green. Findings 2-6 INFO/process — recorded, no action.

--- verbatim reviewer output ---

# R-A2 fresh-eyes review — VERDICT: **PASS**

Both commits align two-way with the spec; every blocking rubric item verified against live code and gate artifacts read with my own eyes. Findings below; none blocking.

## Ticket B (e36a227 (pre-rebase 0f62c7f)) — telemetry refusal reasons

| Rubric item | Verdict | Evidence |
|---|---|---|
| Append-only line shape | ✓ | `sustain_summary` keeps the exact `bought= used= refused=` prefix; `reasons{...}` appended (telemetry.rb:321-325). The pinned prefix assert at sustain_test.rb:221 survives unchanged. |
| Fixed reason order, deterministic | ✓ | `SUSTAIN_REASONS` frozen constant drives both init and print; zero-buckets always print (ends{} precedent). Confirmed live in teed gate logs: `reasons{at_cap=0 broke=0 none=0 no_effect=0 seat_race=0}` byte-identical across gate_a/gate_b replays. |
| Unknown reason: no invented bucket | ✓ code / ⚠ test | `@sustain_reasons[e[:reason]] += 1 if key?` (refused= still counts). See finding 1. |
| Discriminating cases via real verb paths | ✓ | `test_telemetry_sustain_reasons_discriminate_broke_from_none` drives `:broke` (banked=0 at BANK) and `:none` (stockless off-bank) through real `hold(:sustain)` ticks — no mocks. The 5 symbols match the actual refusal sites (pack.rb:73-85, world.rb:595) exhaustively. |

## Ticket A (d31f579 (pre-rebase 4e53e0c)) — bank BUY hint

- **Condition exact**: `sustain_hint` = bank-type + `banked >= provision_cost` + `provisions < provision_cap` + nil-if-cue-on-this-tile (renderer.rb:210-219); proximity stays at the draw site — the hint rides `draw_station_ledger`'s existing `LEDGER_RADIUS_TILES=3` loop, never inside the content method. Matches spec Q2 clause-for-clause. Cue channel is single-slot (world.rb:1344) and cue text draws at y-32 — the hint (y-32) genuinely shares/yields that slot.
- **Zero new strings**: true for locale keys — `data/strings/*` untouched; composes glyph (`Core::BindingMap.glyphs(:sustain).first`, window.rb:75 passes the live map, so a rebind updates the hint) + ratified `hud.provisions` + `-price`. See finding 4.
- **Decision 7iii**: `src/app/controls_overlay.rb` not in the diff; strip gate untouched.
- **Spawn byte-identity argument sound**: banked=0 ⇒ first guard returns nil ⇒ no draw call. Empirically confirmed: 15/18 unmoved; the 3 moved (low_quay_run pre-banks 2000 via staging, nest_advance, vat_economy) each gated rc=0 (tmp/rA2_gates_run.log), targeted critique 5/5 PASS read directly from the critique log.
- **PriceSheet extraction pure motion**: old `station_price` body vs new is token-identical (case arms, `@economy.fetch`, breached comment); `breached?` bound as method object on the live World, `@zone_name` passed at call time; `@pack` is assigned once in `spawn_pack` before construction and `respawn_pack` revives in-place (world.rb:1372-1385) — no stale-reference hazard. All 4 call sites (renderer.rb:443, district_two/seal_breach tests) untouched. Line-cap honest: world.rb 1799→1795 against the enforced 1800 ceiling (line_caps_test.rb:23) — real extraction, not comment-trimming.

## Language axes (separate scores)

- **Accuracy: PASS** — U is the live primary binding (`data/bindings.json` sustain `["U","R"]`) resolved through the same BindingMap that feeds input; −5 quotes `provision_cost=5` from the same PriceSheet/economy source `sustain()` charges (world.rb:598-599) — cannot drift; shown only when the buy would succeed.
- **Presentation: PASS** — CAPS house style, three tokens matching the altar/vat `-price` grammar, PROVISIÓN (es-CR accent correct), SUPRIMENTOS (ratified pt-br), noun-only translation, no new words.

## Scope

No drift: economy.json values untouched, strip untouched, refusal cue text untouched, sim behavior untouched (sustain_run.json diff is capture-list-only; renderer draw is read-only over world state).

## Findings

1. **LOW** — `src/game/telemetry.rb:293-296`: the unknown-reason guard (commit message claims "unknown reasons count in refused= but never invent a bucket") has no pinning test. Requirement without a check. Only reachable if a future refusal site adds a sixth symbol; suggest a one-assert test next time the file is touched.
2. **INFO** — spec ticketed reader asserts in `test/game/`; they landed in `test/app/sustain_hint_test.rb` (`test_world_exposes_the_data_driven_price_readers`). Coverage present; placement drift only.
3. **INFO** — PriceSheet extraction rode Ticket A's commit (one-concern stretched), but it was forced mid-ticket by the script-enforced cap and is named in the commit message. Acceptable.
4. **INFO** — `HUD_STOCK_FALLBACK = "PROVISION"` duplicates the en locale value in code; follows the existing `CUE_TEXT_FALLBACK` precedent (renderer.rb:185), so the "zero new string keys" claim stays truthful.
5. **INFO** — spec Q3 predicted sustain_run would move "by construction"; measured result was UNMOVED at its original 8 captures (press-frame receipt suppression). The receipts name this honestly and remediate with frames 2429/2560/2585 + the targeted critique — the divergence is a point in the design's favor, not a gap.
6. **Process note** — I could not re-run the suite myself: seat-lease blocks test execution from this reviewer session (dev seat 01a01f9e holds it). Judged instead by the enforced pre-commit hook (`exec bundle exec rake` — both commits exist ⇒ suite was green at commit time) plus direct reads of the teed gate/critique logs, which corroborate every receipt claim I spot-checked (including the receipts' own frame-2585 correction and the live `reasons{at_cap=1 broke=1 none=0 no_effect=1 seat_race=0}` line in sustain_run's gate log).

**PASS — push unblocked.** Nothing written to the repo per the request; the dev seat should copy this verdict into `drafts/_rA2-review-20260820.md`.
