# Fresh-eyes review — junior/premium-build be2bd31..3892c1f (S1–S3 + polish)
## Verdict: BLOCKED
The bag is classified as persisted but is never serialized/restored, and burn adds desync-blind sim state; merging this range as-is can lose earned loot and defeat lockstep diagnosis.

## Suite: 1478 runs, 85377 assertions, 0 failures, 0 errors, 0 skips  ·  Canaries: YES / YES / YES

## Findings (BLOCKER first, then MAJOR, then MINOR; max 20; "none" if none)
| # | sev | file:line | finding | law / failure mode | one-line fix |
|---|---|---|---|---|---|
| 1 | BLOCKER | `test/game/save_state_test.rb:676-678`; `src/game/loot.rb:16-20` | Bag `used` and `contents` are classified `:persisted`, but every World constructs a new empty bag and `SaveState.facts` has no bag projection/apply path. Any loot earned before clean quit disappears on relaunch. The comment says T1 will add persistence, but T1 is not in this reviewed range; `docs/CYCLE.md` explicitly sequences S1 after T1. | Determinism/persistence law: every persisted sim fact must round-trip through the strict save schema; the classification currently claims behavior the build does not provide. | Do not merge S2/S3 until T1’s schema/player record actually serializes, validates, restores, and round-trip-tests bag stacks (or classify the bag honestly as session-only and do not ship earned loot yet). |
| 2 | MAJOR | `src/game/creature.rb:382-398`, `src/game/creature.rb:175-178`; `test/net/state_digest_test.rb:37-41` | `@burn_interval` controls every subsequent DOT tick reset, but it is absent from `Creature#digest_fields`, `CREATURE_FIELDS`, and save classification. Two creatures can have identical digests yet apply future burn damage on different frames. | Lockstep law: “every sim-affecting field is in the digest … and classified … and pinned.” | Add `burn_interval` to the digest, classification, field pin, and mutation-sensitivity coverage. |
| 3 | MAJOR | `src/game/world.rb:1048-1050`; `data/balance/status.json:2,20-22` | Aura still applies its old immediate `burn!` damage and then adds the new 6×3 DOT. The registry says the aura ignites “instead of dealing instant field damage,” and proposal §1.7 says burn becomes a DOT; the implementation silently stacks both damage models. `status_test` only checks `burning?`, so it misses the damage regression. | Concrete balance/spec failure; also violates “one gated SIM piece” evidence because the test does not assert the shipped damage contract. | Replace the instant aura damage with status application (or explicitly ratify/data-model combined impact+DOT), then assert exact HP/cadence in `status_test`. |
| 4 | MAJOR | `src/game/loot.rb:33-38`; `src/game/world.rb:489-510` | An item on a station tile with no bag room emits `bag_full`, returns `false`, and prevents the interaction path from ever reaching bank/altar/vat/seal. Because the item remains, repeated presses remain blocked: a full bag can soft-lock that station until room is made elsewhere. | Concrete interaction-order failure: item pickup swallows the station interaction even though pickup failed. | On `bag_full`, fall through to the station interaction (or prioritize the station when pickup cannot succeed), while preserving the named refusal cue. |
| 5 | MAJOR | `data/items.json:9-10`; `src/game/loot.rb:46-59` | `ember_salve` advertises `resist: { burn: 600 }`, and the bag UI renders that effect, but `use_cure_item` only cures/removes/emits; no resistance state is applied or digested. The player-visible item description promises an effect that does not exist. | Data-driven/correctness failure: catalog behavior and sim behavior disagree; any future resistance timer would also be lockstep state. | Either implement and digest the data-driven resistance timer or remove the unimplemented `resist` effect until its ticket. |
| 6 | MAJOR | `src/app/bag_screen.rb:12-15,35-40,115-132`; `src/app/renderer.rb:685-701,1290-1292`; `src/app/hud.rb:244-251`; `src/game/loot.rb:19,27` | New visual/balance values are hardcoded throughout code (5×4 layout, dimensions/colors/fonts, drop bob/fade geometry, burn tint/icon), while `status.json`’s tint/icon are not read for rendering. Loot also supplies code defaults `20`/`1800`, masking missing balance rows. | AGENTS.md: “Data-driven: all tunable values live in `data/**/*.json`. Zero balance constants in code”; task law also includes visual constants. | Move S1–S3 presentation metrics/colors and status visuals into display/status data, and use strict `fetch` without balance defaults for required economy rows. |
| 7 | MINOR | `src/game/bag.rb:75-95`; `src/game/loot.rb:48-54` | Pin order is omitted from the bag digest even though `use_cure_item` searches `bag.sorted`, whose first eligible item can be changed by pinning. There is currently no caller for `pin!`, so this is dormant, but exposing the promised pin feature would make an undigested UI choice affect the sim. | Lockstep law: UI-only state must never touch sim; every sim-affecting field must be digested. | Make cure choice independent of presentation/pin order (canonical item id/order), or make pin order authoritative and digest/persist it. |
| 8 | MINOR | `src/app/bag_screen.rb:39,44-50` | Provisions are prepended as a virtual flask without contributing to `bag.used`; when a 20-slot bag is full and provisions are positive, the 5×4 grid has 21 entries and silently hides the last real stack. A real `flask_sap` drop can also appear as a second flask stack. | Concrete display correctness failure: the read-only bag screen can omit owned items and its title count does not describe its grid. | Reserve a separate provisions cell/chip, or merge the virtual quantity deliberately without consuming one of the bag’s configured slots. |

## Verified claims
- Read `AGENTS.md`, `docs/CYCLE.md`, proposal §1–§2, the BUILD record, commit stats, and all changed Ruby/data/test diffs in the range.
- Full headless suite is green: 1478 runs / 85377 assertions / 0 failures / 0 errors / 0 skips.
- Canary stream diff reports `= ACTIVE bank? YES` for `world_loop`, `brasa2_run`, and `floor3_run`; the unrelated ally-brain ON column diverges as expected.
- Line caps pass by direct count: `src/app/window.rb` 278 lines; `src/game/world.rb` 1798 lines.
- Catalog contains 16 items; locale names/kinds exist in en/es/pt-br; icon manifest md5/coverage is suite-tested.
- `item_dropped`, `item_picked_up`, `bag_full`, and `item_used` are registered in `World::EVENTS` and have emitters.
- `bag` is absent from `Net::Protocol::ACTIONS`; I/B are handled by `Window#button_down`, so the toggle itself is presentation-local.
- Missing icon-sheet loading is rescued and all three icon callers draw a rectangle/glyph fallback rather than requiring the PNG.
- Item drops, bag contents, loot RNG draw count, and the four currently declared burn digest fields are represented in the digest tests/classification.
- No new player-visible fiction names were found; item labels are functional placeholders.

## Not verified
- No Gosu window, replay, capture, wall, map, gate, or visual critic was run because the task explicitly reserves the only GL window for another process.
- Therefore the regenerated atlas appearance, item icon readability, bag-screen clipping, HUD overlap, burn tint readability, and fallback rendering were reviewed from code/data only, not from frames.
