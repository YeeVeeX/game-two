# Process artifacts (verdicts, reviews, calibration history) are TRACKED —
# adversarial review 2026-08-15. Only the media reference corpus stays out.
drafts/_tibia-videos/
captures/
tmp/
*.gem
.bundle/
vendor/bundle/
.playwright-mcp/
data/bindings.local.json
data/prefs.local.json
# v18 persistence: the machine-local, host-authoritative world save (F2 —
# git-tracked saves rejected: conflicts, save-scumming, pull friction).
saves/
# pi session memory (machine-local lessons; each seat keeps its own)
/MEMORY.md
# E3a replay bundles: machine-written runtime evidence, top-level and
# OUTSIDE data/ (fingerprint law, s55) — delivery is by manifest sha256s
# + verification receipt, never git blobs.
bundles/
# Python bytecode caches (tools/*.py import each other; never tracked)
__pycache__/
*.pyc
