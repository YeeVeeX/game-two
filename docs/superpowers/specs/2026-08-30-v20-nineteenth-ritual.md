# v20 fun-verify — THE NINETEENTH (declared 2026-08-30; reform-shaped)

First ritual under the cadence reform (owner order 2026-08-28):
delta-triggered, freeze armed at DECLARATION, declaration→verdict
window TARGETS ≤48h. This spec is deliberately COMPACT — the
eighteenth's protocol laws are inherited by reference
(`docs/superpowers/specs/2026-08-26-v19-eighteenth-ritual.md`), not
re-transcribed; only what changed for v20 is spelled here.

> **DO NOT READ §7 (frozen questions) if you are Gabriel or Junior.**

## 1. Declaration (the trigger + the owner word)

- Delta accumulated since the eighteenth's verdict (all
  player-facing SIM, per ticket records): **T3** potions identity +
  discoverability (R-A2 strip escalation executed) · **T4** totem
  pilot (contested/cadenced heal, floor -1 center) · **T6b** floor -2
  fauna (lurker/warden rows) + cap 13 · **T7** floor -3 fauna
  (stinger, first ranged hostile) + cap 15. Map rethemes (floors
  -1/-2/-3) ride beside as presentation + graph, NAMED per reform.
- Declared 2026-08-30 in the hub chat: the dev surfaced "NINETEENTH
  fun-verify declaration — it's ripe" beside the ambience concur;
  owner (Gabriel) verbatim: **"approved, proceed"**. Freeze arms at
  the declaration commit (hash + frozen blob md5s recorded in the
  s124 checkpoint entry).
- Window: 10/10 answers + adjudication TARGET ≤48h from declaration
  (~2026-09-01 EOD). Target, not kill condition — slippage is NAMED,
  never silently absorbed (the eighteenth's six-day freeze is the
  recorded failure this shape exists to prevent).

## 2. Session shape (reform-minimal)

- **ONE declared ritual session per peer**, and one coop session
  covers BOTH (recommended vehicle: Gabriel hosts, Junior joins —
  also pays the standing coop-invite owner-pending item; the v17
  handshake proves build identity for both seats at once). Solo
  sessions stay legal and symmetric (never gate on peer
  availability); if solo, each peer declares + launches his own.
- Declaration-before-launch in chat, per session (eighteenth law;
  its s2 retroactive-count precedent stands for owner word only,
  compression named at adjudication).
- Play is UNSCRIPTED — no routing, no feature tour. The world as it
  is; the descent if it pulls.
- Across-days is NOT this ritual's object (that was the
  eighteenth's); one sitting suffices.

## 3. Exposure ledger (REQUIRED before a peer's ritual session)

| Seat | Ordinary session on a v20-delta build? | Evidence |
|---|---|---|
| Junior | **PAID** — 4 ordinary sessions, T6b/T7 builds, 2026-08-29/30 | his solo chain: ratification docs `drafts/_junior-floor2-ratification-20260830.md` + `_junior-floor3-ratification-20260830.md` (latest link: saved digest `fc76ec9d…` sessions=4, level 7; log md5s banked there) |
| Gabriel (host) | **OPEN** — zero sessions since the eighteenth's s2 (save mtime 2026-08-28 04:55) | one ordinary session on build ≥ the declaration commit pays it, BEFORE his ritual session; owner-word compression (ritual = first contact) legal, NAMED at adjudication |

## 4. Half A — MECHANICAL (inherited, v20-adjusted)

Chain-walk law, md5 banking of ORIGINAL launcher logs, launch dates
from original-file ctime, AUTOPILOT grep = 0, joiner evidence = his
REAL launcher log file (console tees are corroboration only), link-
quality thresholds + pre-question re-run window (≥14% stalls /
≥2500ms stall_ms_max) — ALL byte-inherited from the eighteenth's
spec §§4–5, 11. Evidence dir: `drafts/_v20-nineteenth-evidence/`.

Gabriel-chain anchors at declaration (re-verify at his first
launch): save md5 `1d71e34f9abf60a7d7ffe180fe0d55e0`, schema 2,
sessions=18, level=10 xp=3679, banked=422, boss_1_defeats=5,
provisions=0. Launcher logs at declaration: 41
(`game_two_session_*.log` in %TEMP%). His save predates every v20
retheme — first v20 load is also the L9 save-compat proof on the
MAIN chain; a strict-decode refusal at his first launch is a
stop-the-line event, not a ritual datum.

Named beside the reading (never a check): level boundaries crossed
in-window (gap arithmetic printed — the band 10→15 is the growth
instrument's object) · floors reached per session (zones-entered
telemetry) · deaths per floor.

## 5. Telemetry-only rows (pre-declared readings; NO questions)

Read from the ritual sessions' HUMAN launcher logs only, HELD until
10/10 answers are banked:

1. **Totem row (T4's pre-registered priced-flip, quoted terms):**
   potions `bought=0` while `totem heals=N > 0` in the same
   descent-touching sessions → the flip to PRICED-ACTIVATION becomes
   eligible (its own lane-C re-session + gate, never an inline
   tweak). Anti-condition verbatim (T4 record §1): any B>0 →
   "coexistence"; totem heals=0 → "undiscovered" — neither fires the
   flip; both route to verdict discussion. Junior's ordinary s3/s4
   `heals=0 pulses=0` is pre-ritual context, named but non-firing.
2. **Sustain row (post-T3-escalation reading):** the eighteenth left
   B=0/U=0 on every line WITH the escalation already recorded
   eligible; T3 then SHIPPED the identity + strip escalation. Any
   B>0 or U>0 in a ritual session → the discoverability ship LANDED
   (record texture from `reasons{…}`). Still 0/0 across all ritual
   sessions → the escalation FAILED at its own game — the recorded
   field-drop rung (L14) enters the owners' decision queue; owner
   word decides. This row never blocks the close.

## 6. Half B — FELT (administration; inherited laws)

Capture-before-debrief · one-by-one · byte-virgin from §7 · player's
language · counter-questions recorded AS the answer · nothing
explained until 10/10 · verbatim banking · HELD material only after
10/10 · each seat administers its own player — all inherited
(eighteenth §6). Contamination reading rule inherited. TARGET: all
ten answers banked the same day the last ritual session closes.

**Named prior (recorded now so adjudication doesn't discover it):**
Junior's floor-3 ratification already contains an unprompted stinger
positive ("gostei muito da adiçao de inimigos que batem de longe")
and floor approvals. Ratification feedback is a DIFFERENT instrument
on the same axes — at adjudication it discounts convergence warmth
on his difficulty/free-verdict answers, never corrections (caveat
law, inherited).

## 7. THE FROZEN QUESTIONS — administered at capture, one-by-one

> **Peers: stop reading. Devs: paste byte-virgin, one at a time.**

**Owner (es-CR), in this order:**

1. Comparado con cuando empezaron en este mundo, ¿la fuerza del
   grupo se siente igual o diferente? ¿Por qué?
2. Moviéndose por el mundo, ¿qué tanto se nota dónde es seguro y
   dónde es peligroso?
3. ¿Qué los lleva a bajar (o a no bajar) al siguiente piso?
4. Entre más hondo bajan, ¿el juego se siente igual, más fácil, o
   más peligroso?
5. Veredicto libre.

**Junior (pt-br), in this order:**

1. Comparado com quando vocês começaram nesse mundo, a força do
   grupo parece a mesma ou diferente? Por quê?
2. Em dupla, como sentiu a dificuldade dessa vez? *(coop wording —
   the byte-identical v19 re-ask; if his ritual session is SOLO,
   administer "Dessa vez, como sentiu a dificuldade?" and NAME the
   wording deviation beside the A/B)*
3. O terceiro corpo (a IA) — como se comportou?
4. O que te leva a descer (ou a não descer) pro próximo andar?
5. Veredicto livre.

Lineage: owner 1–2 and Junior 1–3 are byte-identical v18/v19 re-asks
(the A/B chain); owner 3–4 and Junior 4 are the NEW depth
instruments (motivation probe + forced-alternative gradient probe —
one probe per question, council law); free verdicts by tradition.
Potions and totem deliberately have NO question (telemetry-only, §5
— the eighteenth burned the provisions question; behavior measures
both).

## 8. Hygiene — ARMED from the declaration commit until the verdict

1. **Ritual questions virgin:** §7 unread by peers, unrehearsed.
2. **Frozen instruments:** this spec §7 · the runsheet
   (`drafts/_v20-fun-verify-runsheet-20260830.md`) · `docs/JUNIOR.md`
   — blob md5s in the s124 checkpoint entry.
3. **Frozen oracle wording:** whole `src/game/telemetry.rb`
   (totem + sustain + progression lines included) ·
   `src/app/save_store.rb` persist_line · `src/game/save_state.rb` ·
   `src/net/session.rb` close line · `src/app/autopilot.rb` marker.
   No edits, including add-only, until the verdict.
4. **Frozen sim numbers (the measured set):** `data/balance/*.json`
   (all nine, `sustain.json` included) · zone
   `requires_level`/`requires_defeats` values · code seams
   `src/game/creature.rb` (stinger engage/retreat included) +
   `src/game/aggro.rb`. Owner override stays law — one recorded
   line, measurement consequence named at adjudication.
5. **Bot logs are never fun-evidence** (permanent law).
6. **Legal during the window:** docs · soak/flywheel (bot law) ·
   harness-only work — provided rules 3–4 files stay untouched.
   Lane G (ambience) is RECORDED but executes POST-verdict.
7. **Verbatim means verbatim.**

## 9. Adjudication + routes

FRESH session (never the administering one), Half-A table quoted
from banked bytes → caveats attach BEFORE readings → ten answers
verbatim → HELD material (telemetry rows §5, mid-session
observations, forensics) → routes walked → free verdicts always.
Verdict doc: `drafts/_v20-fun-verify-verdict-<date>.md`. Skeleton
(evidence bank): `drafts/_v20-fun-verify-skeleton-20260830.md`.

Pre-registered routes (walked, not invented):

- **Free-verdict regression** naming a delta piece (totem / potions
  / fauna / caps / a floor) → that piece gets its OWN re-session
  before v20 closes; nothing else blocks.
- **Totem row** → §5.1's three-way route (flip-eligible /
  coexistence / undiscovered).
- **Sustain row** → §5.2 (landed / field-drop rung to owner queue).
- **Depth probes negative** (descent doesn't read as
  risk-on-purpose) → lane-D iteration item for the v21 grill, named
  with the verbatim.
- On CUMPLIDO: frozen sim numbers unlock · lane G (ambience)
  executes · triggered rows enter the owners' queue · the v20 close
  becomes reachable on the foundation's terms.
