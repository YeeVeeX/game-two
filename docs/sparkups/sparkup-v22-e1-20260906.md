# Spark-up -- game-two session s135: E1 HARNESS TRUTH (one ticket, this session)

E0 is DONE and pushed (s134, `57c80fd` + `83007cb`): phased bosses no longer crash --
`begin_action` snapshots the begun skill (`@action_cfg`), `action_config` returns the
snapshot while a cast lives. This UNLOCKS piece 9 below: a sentinel can now drive a boss
into a multi-skill phase without killing the session. E1 re-frames the vision critic and
the pins ledger that EVERY later gate depends on -- it runs BEFORE T1's first gate by
spec order. Spec = `docs/superpowers/specs/2026-09-05-v22-one-body-cycle.md` section 5
(E1 row); findings = `drafts/_t0-review-20260905.md` (d1-d9, b1/b2, a5, d18; raw verbatim
in `drafts/_t0-review-20260905/area_b.md` + `area_d.md`). Law =
`drafts/_v22-foundation-20260905.md`. E0 record (context on what changed in creature.rb):
`drafts/_v22-e0-record-20260906.md`.

No fun-verify is pending (the eighteenth closed, the TWENTIETH is not yet declared), so
no measurement freeze is armed. The wall, manifests, and gate rows are FREE to move --
that is this ticket's whole point. SIM stays untouched: E1 is harness + scenes + rows.

## 0. Orient (10 min, before any edit)

1. `fleet` -- confirm no other LIVE session holds this seat.
2. `git fetch && git pull` -- **Junior is ACTIVE right now** (his wall #2 landed 00:52
   -03:00 mid-s134; he pushed a new branch `junior/premium-build` -- his lane, do not
   touch it, but READ any new main commits of his before claiming: on any overlap his
   text comes first).
3. CHECKPOINT top: CLAIMED must be none. Read the s134 entry (top) -- it carries the E0
   context and the gate-verdicts-log merge note.
4. Glance `C:/Users/gabri/AppData/Local/Temp/g2a_push_20260905.done` -- the assets seat's
   detached push marker (exit=0 = pushed; nonzero = the NEXT ASSETS session routes it,
   not you; just record what it says in your close entry).
5. Read spec section 1 (standing constraints) + the E1 row WHOLE; read AGENTS.md
   "Enforcement" + "Commands" (hooks run the full suite on every commit, ~95 s).
6. Push the CLAIMED line: `CLAIMED: E1 harness truth (critic persona, pins ledger,
   census manifests, ring->halo rows, netplay art, PREMIUM+boss rows) -- <seat>, s135.`
7. Rule 4: read every file BEFORE editing it (the gate re-arms per file after each edit).

## 1. The nine pieces -- fixed order, ONE commit each

Rationale for the order: piece 1 changes how every verdict is judged (bank a baseline
immediately); 2-3 make pins recordable and honest; 4-5 make the runner's exit code mean
something; 6-9 make the new surfaces judgeable. The full re-pin sweep comes LAST, after
all rows/manifests are final -- anything else pins stale rows.

### (1) Critic PERSONA rewrite -- `harness/vision_critic.py` lines ~66-81

The PERSONA block still says "Flat-rect placeholder art is a DELIBERATE choice ... do
not ask for sprites ... Player is ember orange, enemies pale bone" (T0 d8, graded
Rule-2-critical: every PREMIUM gate was framed against frames that no longer exist).
Rewrite to the v22 presentation truth: 32x48 sprites on material tiles (32px grid), HUD
plate bottom-left + controls strip, hostile FAMILIES read by silhouette + colour ramp,
gold halo = the possessed YOU (the white ring is retired), partner halo cyan, boss
nameplate + phase pips, zone banners, minimap top-right. KEEP the two-person voice
(Tibia veteran + game-feel designer), KEEP the verdict-law text and the JSON contract
below it UNCHANGED -- you are re-framing perception, not judgment. Derive the colour
words from `data/art/manifest.json` + `tools/gen_premium_art.py` ramps, not memory.
Also fix `vision_critic.py:225` "Threketh" while inside the file (E5 overlap, one word,
no-lore order).

THEN bank the baseline: `rake gate SCRIPT=harness/scripts/world_loop.json` -- paste the
verdict + note first-pass flip vs the historical ~5/22 and 2/37 rates in the record.
This one gate is the persona's Rule-2 evidence; the full sweep at the end is the bulk.

### (2) Pins + verdict log write to the MAIN ledger from any worktree

T0 d4: the 064bd80 sweep ran in worktree `game-two-wall3` (pruned) -- pins and per-row
verdict JSON died with it; `harness/pins.json` is `[]` and `rake pins` says "no pins
recorded yet" while a 42-script wall actually ran. Fix: `harness/pins.rb`
(`DEFAULT_PATH = File.expand_path("pins.json", __dir__)`) and the verdict-log path must
resolve through the MAIN clone: `git rev-parse --git-common-dir` -> the main repo root
-> its `harness/pins.json`. In the main clone the resolution is a no-op (test both
shapes: a plain-clone path test + a worktree test if cheap -- `git worktree add` a temp,
assert the resolved path points home, prune it). Note: `drafts/_gate-verdicts.log` is
now TRACKED (Junior banked it s134; both seats append; merge conflicts resolve as pure
union -- s134 precedent in the checkpoint). Sweeps will dirty it; commit it with the
sweep's close commit.

### (3) `SKIP_CRITIC=1` / non-default `CHECKS` refuse to record a pin

T0 d5: a SKIP_CRITIC sweep records `--gate-rc 0` and `rake pins` would report PINNED
for scripts no critic ever judged; an exported `CHECKS=harness/net/gate_checks.json`
leaks into every `rake gate`. Fix in BOTH ends: `harness/run_wall.sh` (and/or
`Rakefile` gate task) must pass the truth to `harness/pins.rb record`, and `pins.rb
record` REFUSES (named, nonzero or a loud no-op) when the run rode SKIP_CRITIC=1 or a
CHECKS override. NUANCE: the three netplay gates legitimately use
`CHECKS=harness/net/gate_checks.json` -- they live OUTSIDE `harness/scripts/` and never
record pins, so the refusal must key on PIN RECORDING, not on gate execution. Suite
test for the refusal (minitest, real subprocess or direct pins.rb call).

### (4) The 7 census manifests re-cut to OBSERVED counts, or retired

T0 d6: `level_gate loot_loop nest_advance threat_pull vat_economy zone8_crossing
zone_catchup` fail their manifests deterministically every sweep since 2026-08-29; the
runner exits 1 and "PASS" has been a human eyeballing the red set -- a gate downgraded
to advisory for three cycles (Rule 6 violation, now repaid). Per script: double-replay
headless (the tune_sentinels.rb pattern reads counts from `Headless.run_script`; or
`rake capture` + count EVENT lines), JUDGE whether the script still stages the surface
it exists for, then EITHER re-cut the manifest to observed counts (paste old->new per
script in the record) OR retire to `harness/retired/` with a header naming the owed
re-author (F6/T7 precedent). zone8_crossing is piece 5 -- do not manifest-patch it
here. vat_economy: its 2 failing VISION rows (HUD level strip, volley reach) are an
L16 debt ticket, NOT E1 -- fix only its manifest; leave the rows.

### (5) `zone8_crossing` re-authored on the seal-less DUNGEON 1 + `gate_scope.json`

T0 d7: the script stages a DUNGEON 1 seal breach the T7 range retired
(`dungeon_1.json "stations": []`); inputs choreographed on the pre-swap map;
`gate_scope.json:28` still lists dungeon_1 for `seal_breach_reads`. Re-cut the route on
today's geometry (zone_8 -> DUNGEON 1 rope at [29,7] -- read `data/zones/dungeon_1.json`
+ MAP_EDITING section 3 for arrivals; the tune_sentinels waypoint pattern generalizes),
re-cut its manifest from observed counts, fix `gate_scope.json` (the suite's
gate_checks_audit_test pins scope claims -- it will tell you). If a seal beat is still
wanted for `seal_breach_reads`, the live seals are elsewhere (grep `"seal"` in
data/zones) -- point the scope row at a script that ACTUALLY stages one, or mark the
row not-exercised-anywhere and note it as a debt row in the record.

### (6) The four white-ring rows -> halo grammar -- `harness/gate_checks.json`

T0 d9: rows at (today's) lines ~106/158/238/254 -- `taunt_convergence_reads`,
`god_mark_reads`, `writ_frame_reads`, `burst_legibility_budget` -- anchor on "the
possessed's white ring", retired by Junior's PREMIUM halo (`58e6153`): with art loaded
the ring branch is dead fallback code, never drawn. Rewrite the anchor language to the
gold possession halo (read `src/app/renderer.rb` halo drawing + `display.json` knobs
for the true words). Keep each row's judgment intent identical; only the anchor
changes. GATE each touched row on a script that stages its surface AFFIRMATIVELY
(s121 lesson: after wording changes, gate on the class's STRONG member -- world_loop
stages possession + god-mark; taunt_anchor stages convergence; writ needs a boss chant
-> that affirmative capture arrives with piece 9's BOSS 1 sentinel; say so in the row
record if it waits).

### (7) `Renderer.build` factory -- one construction, four call sites

T0 d1/b2: `harness/scenes/netplay_scene.rb:67` builds `App::Renderer.new` WITHOUT
art:/ambience:/tileset: -- the three net gates judge the quad fallback and the partner
cyan halo has NEVER been captured. The wiring is copy-pasted 4x (window.rb:104,
world_scene.rb:29, menu_scene.rb:35, netplay_scene.rb:67). Add a `Renderer.build`
factory (in renderer.rb or a small app-level helper) that takes data/display/strings/
bindings/local_seat and loads Art+Ambience+Tileset ONCE, canonical bindings for
capture scenes (`local: false` -- the check-comparability law in world_scene's header);
window.rb passes its live bindings. All four sites call it. window.rb is at 270/300 --
the factory should NET-REDUCE its lines. Then re-run the three net gates:
  rake gate SCRIPT=harness/net/netplay_session.json CHECKS=harness/net/gate_checks.json
  (same for netplay_desync.json, netplay_conn_lost.json)
-- the partner halo's first capture ever. Read `harness/net/gate_checks.json` first: if
its rows still describe flat rects, re-frame them in the SAME commit (they are outside
the wall; no pins). Net-scene determinism law: sim pokes keyed by WORLD TICK, never
frame (netplay_scene.rb header + project memory).

### (8) Gate rows for PREMIUM passes 3-11 + petrify/blink/aura

T0 d3/b1/a5: six PREMIUM passes shipped with NO gate row -- Fx spark/burst/dust; Light
glows/vignette/kill punch/level flash; damage numbers + wounded hp bars; ally callouts
+ row pulse; pickup gleam + low-hp pulse; interact prompt (signage and minimap already
have rows 85/86). Plus three sim tells with no row: petrify stone telegraph (serpent_b,
tower floors), blink flash (serpent_c), aura ring (ember_b, brasa floors). ONE row per
surface, each naming a script that STAGES it -- verify against the exact PNGs in
`captures/` (or a fresh `rake capture`) before writing the row; the sampling-artifact
law applies (critics see ~4% of frames -- a row for a surface the script shows for 6
frames must say which beat). Candidate stagings to VERIFY, not trust: world_loop
(fights = Fx/Light/damage numbers/callouts), loot_loop (gleam), mercy_floor (low-hp
pulse), ledger_loop (interact prompt at stations), tower2/3_run (petrify), tower3_run
or floor3_run (blink -- serpent_c placement is in data/zones), brasa1/2_run (aura).
Junior's surfaces: these rows JUDGE his work -- rows are harness truth (E1 scope, either
seat), but write the pt-br close line so he can re-word any row that misreads his
intent; his re-wording is law (E3 stays his).

### (9) One boss sentinel per boss + a boss-bar row

T0 d2/d18: NO active wall script stages any boss beat (`challenger_engaged` appears
only in `harness/retired/*`); `boss_phase_pips_read` / `challenger_tell_reads` /
`writ_frame_reads` have never passed affirmatively; the boss bar (`d3a00c5`) has no
row; make_tour.sh cards promise boss reels that never show one. E0 makes phased-boss
sentinels SAFE to author. Three new scripts (the tune_sentinels.rb waypoint pattern +
`start.zone` + level param; dev inspection via `bin/warp <zone>` first to scout):
  - `boss1_writ` (or similar placeholder name): BOSS 1 challenger in the MUSGO vault
    (low_quay [41,18]) -- stage chant/writ/seizure beats (challenger_engaged,
    challenger_chant_started in the manifest).
  - `boss2_phases`: serpent_boss, dungeon_4 far end -- drive hp through 60% so the
    reel SHOWS phase pips moving + a petrify/beam cast.
  - `boss4_phases`: ember_boss, ember_3 dais -- through 50% (dash+beam rotation; the
    exact surface E0 fixed).
  Sentinel law: short waypoint routes, walk-by swings, reel ends on a fight beat,
  manifest = observed counts (tune_sentinels header). Add the boss-bar gate row + make
  `boss_phase_pips_read` / `writ_frame_reads` affirmative on these scripts (finish
  piece 6's writ row here). New scripts = new pins; they do NOT join the sim-identity
  canary ACTIVE bank this session (FASE 6.7 precedent: sentinels join the bank in a
  later ticket once routes prove stable -- note it as the owed follow-up).
  Wall count moves 42 -> 45: recompute with `ls harness/scripts | wc -l` wherever the
  record cites it (prose-number law).

## 2. The full sweep -- DETACHED, after all nine pieces are committed

`harness/run_wall.sh v22-e1` -- ~5 min/script x 45 = ~3.5-4 h. Launch via
`powershell Start-Process` (hidden bash, no bash-call timeout -- a timeout once killed
the critic mid-gate and forged a false negative), logs tee to `tmp/wall/`. HARD LAWS
while it runs: NO source edits (a mid-sweep edit contaminated 8/18 baselines once), no
live seat, no soak. Poll by process count + per-script rc lines, not log size. When it
lands: `rake pins` output PASTED into the record (expect PINNED for the wall, STALE
none, FAILED explained or fixed, UNPINNED none). First-pass vision flips under the new
persona: re-run flipped gates standalone (re-run, never relax -- the recorded
protocol); count them in the record against the old flip rate. If the sweep cannot fit
the session: commit + push the nine pieces FIRST (suite-green core), launch the sweep
detached, and hand harvest to s136 via the checkpoint (named follow-up, not half-done).

## 3. Fresh-eyes review (Rule 6) -- BEFORE the close push

Headless scrubbed `pi -p` (unset all PI_* in the runner script; brief as an attached
.txt; demand the JSON verdict as the LAST message; touch NOTHING including seat mail).
Brief must inline: the FULL diff (all nine commits -- `git diff <claim>..HEAD` on the
harness/scenes/rows files), the T0 findings d1-d9 verbatim, and the pasted evidence
(baseline gate verdict, manifest old->new table, net-gate results, `rake pins`).
Starved reviewers fabricate -- inline, do not point. A vision context on the NEW rows:
attach ONE representative gate frame (<500KB JPEG -- convert if needed) showing a new
row's surface so the reviewer judges row wording against a real frame, not imagination.
Budget ~USD 5, one pass; a BLOCK verdict blocks the push (fix or reconcile, never
downgrade). Record verdict verbatim in the ticket record; landed findings noted.

## 4. Fences and traps (s134-verified where marked)

- Docs + harness/** + the four scene/window Renderer call sites ONLY. NO sim files
  (src/game/**), no data/balance, no data/zones, no pilot.ldtk (piece 5 re-cuts a
  SCRIPT, not a zone).
- Never the live save (`--save <scratch>` on any launch; `bin/warp` for scouting).
- `rake gate` writes to `captures/<out>_gate_a/_gate_b`; pixel-diff gate_a, never the
  plain out_dir (stale-baseline trap, s96).
- minitest `-n /regex/` gets MSYS-mangled into a path (s134 live): use full test names
  with `-n test_...`, or set nothing and run the file.
- Detached scripts that exec ruby: `unset MSYS_NO_PATHCONV` inside the script (s119).
- `drafts/_gate-verdicts.log` is tracked + shared append-only: on rebase conflict,
  resolve as union (both sides are pure appends; s134 precedent).
- Junior may land mid-session: fetch before every doc commit; his files (tools/
  premium_art, his verdict docs) are read-only to you beyond what E1's rows require.
- An owner word that reverses an L-row = scope break: record verbatim, stop, surface.
- Rule 7 totals: reviewer ~USD 5 + critic Bedrock spend (pre-cleared AWS-internal,
  declare per-gate count in the record) + USD 0 council (E1 is mechanical harness
  truth; council only if a judgment call turns taste-heavy). Stop conditions: nine
  commits + sweep launched or harvested + review PASS + push; anything beyond = s136.

## 5. Close (every session)

Ticket record `drafts/_v22-e1-record-<date>.md` (evidence UNCHECKED until pasted --
never pre-fill hashes or verdicts; s134 caught a fabricated md5 live). CYCLE.md E-row
updated. CHECKPOINT entry (es-CR line for Gabriel, pt-br line for Junior -- his
surfaces got rows; invite his re-wording). CLAIMED -> none. `git fetch` + rebase +
push. Tree clean except tmp/. Harvest any new mail in `~/.pi/agent/mail/game-two/
inbox/` into the triage doc section 2. Next ticket after E1: T1 (schema 3) via the
ORIGINAL spark `docs/sparkups/sparkup-v22-e0-e1-t1-20260905.md` section 3 -- still
valid, reviewer brief already staged at `docs/sparkups/t1-review-brief-20260905.md`.
Checkpoint to disk before any /compact.
