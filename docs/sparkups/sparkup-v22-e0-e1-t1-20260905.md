# Spark-up -- game-two session s134: E0 (BLOCKER fix) -> E1 (harness truth) -> T1 (schema 3) -- one ticket per fresh session

Order is fixed by dependency, not preference: **E0** is a BLOCKER found by T0 (a phased boss
can crash the session), **E1** re-frames the vision critic and the pins ledger that every
later gate depends on, **T1** is the first lane-A ticket. Each is ONE session; claim ONE at
a time (`CLAIMED:` line pushed before the first edit; `git fetch` first -- Junior pushes
often). Spec = `docs/superpowers/specs/2026-09-05-v22-one-body-cycle.md` (E0/E1 in §5,
T1 in §3); T0 evidence = `drafts/_t0-review-20260905.md` (+ `drafts/_t0-review-20260905/
area_*.md` verbatim). Law = `drafts/_v22-foundation-20260905.md` (L20 restated per ticket).

## 0. Orient (10 min)
`fleet` -> `git pull` -> `git status` -> CHECKPOINT top (CLAIMED must be none) -> read the
spec §1 (standing constraints) + the claimed ticket's block WHOLE -> push the CLAIMED line.
Read `AGENTS.md` "Enforcement" (hooks run the suite on every commit, ~90 s) and the ticket's
files BEFORE editing (Rule 4; the read gate re-arms after each edit).

## 1. E0 -- boss rotation snapshot fix (½ session; BLOCKER a1)
- Read `src/game/creature.rb` 60-125 (`kit`, `boss_phase`, `advance_boss_skill!`,
  `action_config`) and 649-713 (`begin_action`, `activate_action`); `data/balance/combat.json`
  phases (serpent_boss p1 `[spread, arc3]`, p2 `[beam, spread, arc3]`; ember_boss p1 `[dash,
  beam]`, p2 `[beam, dash, beam]`); `test/game/boss_phases_test.rb`.
- REPRODUCE FIRST (the reviewer's trace was static): a test that drives `ember_boss` to <=
  50 % hp in `grass_fixture` and runs ~300 ticks -- expect `NoMethodError` at
  `@dash_plan.duration` or a started-arc != resolved-arc mismatch. Paste the failure.
- Fix shape (spec E0): `begin_action` snapshots `@action_cfg = kit.fetch(kind)`;
  `action_config` returns the snapshot while an action is live (nil when idle); the boss
  index still advances at start. Tests: started == resolved per cast across a multi-skill
  phase; the ember_boss phase-2 drive passes; `boss_phases_test` reads the payload's cfg.
- Canary: the fix moves streams only where a phased boss fires a multi-skill rotation ->
  run `test/harness/sim_identity_canary_test.rb`; if a bank moves, follow its header's
  INTENDED rebank protocol (versioned, stream diff pasted). `rake gate SCRIPT=harness/
  scripts/tower4_run.json` + `brasa3_run.json` (the boss reels; expect no vision change).
- Fresh-eyes review (headless scrubbed pi on the diff + this block) BEFORE push. Record in
  `drafts/_v22-e0-record-<date>.md`. Junior authored the boss block -- the pt-br
  checkpoint line tells him what changed and why.

## 2. E1 -- harness truth (1-2 sessions + one detached sweep)
Spec §5 E1 lists the nine pieces; do them in this order, one commit each: (1) critic
PERSONA rewrite (`harness/vision_critic.py:72-77` -> the v22 presentation: sprites 32x48 on
material tiles, HUD plate, hostile families by silhouette/colour, gold halo = YOU; keep the
verdict law text unchanged) -- then ONE baseline gate (`world_loop`) to bank the flip rate;
(2) pins to the MAIN ledger from any worktree (`run_wall.sh --pins`, git-common-dir) + the
verdict log path; (3) `SKIP_CRITIC=1` / exported `CHECKS` refuse to record a pin; (4) the 7
census manifests re-cut to observed counts (`tools/tune_sentinels.rb`) or retired to
`harness/retired/` with a header -- the runner's rc must mean something again; (5)
`zone8_crossing` re-authored on the seal-less DUNGEON 1 + `gate_scope.json`; (6) the four
white-ring rows -> halo grammar; (7) `Renderer.build` factory used by window.rb, world_scene,
menu_scene, netplay_scene (the partner halo's first capture; re-run the 3 net gates); (8)
rows for PREMIUM passes 3-11 + petrify/blink/aura, each on a script that stages it; (9) one
boss sentinel per boss + boss-bar row. Then a full sweep DETACHED (`harness/run_wall.sh
v22-e1`, ~3.5 h, powershell Start-Process, no source edits while it runs) -> `rake pins`
pasted -> record. Fresh-eyes review before push (a vision context on the new rows).

## 3. T1 -- save schema 3: per-PLAYER character records + the player identity file (1 session)
Follow spec §3 T1 EXACTLY (it carries the `[council s133]` amendments: `migration` block
with `legacy_seed_claimed_by`, deterministic guest-record creation on BOTH seats at session
start, read-only mirror rule until T2b, sorted player-id digest order, `bank_items` inside
the character record, `--fresh` = new host character with no migration block). Before
coding: read `src/game/save_state.rb` whole, `src/core/data_store.rb`, `src/net/
fingerprint.rb`, `src/net/protocol.rb`, `src/net/session.rb` (`handle_hello`, `Params`),
`src/game/progression.rb`, `src/game/world.rb` `digest_snapshot` + `apply!` callers,
`test/game/save_state_test.rb` (CLASSIFICATION rows), `test/net/state_digest_test.rb`.
- D-T1 (legacy seed) is a both-peers line: if neither line has landed, implement the REC
  (seed once, keyed by player id) and record it as the default the peers can override.
- Proofs on COPIES only: `cp saves/world.json tmp/t1/gabriel_v2.json`; Junior's chain by
  mail (`~/.pi/agent/mail/game-two/inbox/`) -- absent = prove on the owner's copy + a
  synthetic v2 + a synthetic v1 (must refuse NAMED) and RECORD the gap. `--fresh` backup law
  on a copy. `rake soak SEED_SAVE=1 N=1`. Netplay gate `harness/net/netplay_session.json`
  with `CHECKS=harness/net/gate_checks.json` (HELLO shape changed). Canary unchanged.
- Reviewer: the staged brief `docs/sparkups/t1-review-brief-20260905.md` (headless scrubbed
  pi on the diff) + `council` (DeepSeek or Kimi) on the migration/refusal table, <= $1.
  A failed review blocks the push.

## 4. Fences and stop conditions
Docs + the claimed ticket's files only. Never the live save (`--save <scratch>` on every
launch; `bin/warp` for inspection). Never hand-edit `data/zones/**` / `pilot.ldtk`. Junior's
files: read his text first on any overlap; his RECEIPT lines are law. An owner word that
reverses an L-row = scope break: record verbatim, stop, surface. Gate refusals are policy.
Rule 7 per ticket: reviewer ~$5, council <= $1, sweeps detached. Checkpoint to disk before
any /compact.

## 5. Close (every session)
Ticket record in `drafts/`, spec row status, CHECKPOINT entry (es-CR line for Gabriel,
pt-br line for Junior), CLAIMED -> none, `git fetch` + rebase + push, tree clean except tmp/.
Harvest any spoke receipts in `~/.pi/agent/mail/game-two/inbox/` into the triage doc
(`drafts/_v22-seat-spokes-triage-20260905.md` §2) -- the assets bible-bridge spoke was
RUNNING at s133 close (`tmp/spokes/assets_s133.log`, done-marker `assets_s133.done`).
