# Spark-up -- game-two session s136: T1 SAVE SCHEMA 3 (one ticket, this session)

E1 HARNESS TRUTH is DONE and pushed (s135, `03e38b1`..`093579c`, 16 commits): the vision
critic is framed on the art that actually ships, pins + verdict log resolve to the MAIN
clone from any worktree, SKIP_CRITIC/CHECKS runs refuse to pin, the red census manifests
are re-cut or retired, `zone8_crossing` runs on today's map, four ring rows speak halo,
`Renderer.build` gave the netplay gates art (partner halo captured for the first time),
rows 86 -> 97, and three BOSS sentinels exist -- `writ_frame_reads`,
`challenger_tell_reads`, `seizure_reads` passed AFFIRMATIVELY for the first time in the
project. Record `drafts/_v22-e1-record-20260906.md`; reviewer verbatim (BLOCK, resolved)
`drafts/_v22-e1-review-20260906.md`.

**Why T1 now, and not more wall work.** Junior's branch `junior/premium-build` carries
15 commits (S1 items catalog, S2 bag + item drops + pickup + bag screen, S3 consumables
+ statuses, a multi-agent lanes fence, his own wall #3 + four fresh-eyes reviews;
~60 files, +3041 lines, suite 1486/0, `world.rb` 1798/1800). His own commit messages and
his classification table say it plainly: **"the bag is NOT serialized yet ... Persistence
= one line on T1's schema-3 player record"** and "S1 ... lands on main after T1". T1 is
the merge gate for his whole branch. Wall polish is not.

Spec = `docs/superpowers/specs/2026-09-05-v22-one-body-cycle.md` **section 3, ticket T1**
(read the WHOLE row -- it is the design, already council-reviewed; this spark does not
repeat it). Law = `drafts/_v22-foundation-20260905.md`. Reviewer brief ALREADY STAGED:
`docs/sparkups/t1-review-brief-20260905.md`.

No fun-verify is pending (the eighteenth closed; the TWENTIETH is undeclared), so no
measurement freeze is armed. T1 touches persistence + digest only: NO surfaces.

## 0. Orient (15 min, before any edit)

1. `fleet` -- confirm no other LIVE session holds this seat.
2. `git fetch --all && git pull` -- **Junior is active.** Then READ his branch before you
   design anything: `git log --oneline origin/main..origin/junior/premium-build` and
   `git diff origin/main...origin/junior/premium-build -- test/game/save_state_test.rb`.
   That diff is the contract your character record must satisfy. On any overlap his text
   comes first.
3. CHECKPOINT top: CLAIMED must be none; read the s135 entry (top) whole.
4. **Harvest the E1 sweep (Step 1 below) BEFORE claiming T1** -- it is a 15-minute chore
   that closes s135, and its state decides whether you may run GL replays this session.
5. Read spec section 1 (standing constraints, L20 laws) + the T1 row WHOLE + the staged
   reviewer brief. Read AGENTS.md "Commands" + "Enforcement" (hooks run the full suite,
   ~95-190 s per commit).
6. Recompute the live numbers T1 was cut against (prose-number law -- compute, never
   trust; the spec's numbers are ALREADY STALE, two were caught while writing this):
   `ls harness/scripts | wc -l` (43 after E1) - `rake pins` - `wc -l src/game/world.rb`
   (cap 1800; main is **1782** today, not the spec's 1776, and HIS BRANCH is **1798** --
   if you touch world.rb keep net lines <= 0 as the spec demands, and expect the T2a
   extraction to be forced soon after his merge)
   - `grep -n "SCHEMA\|FACT_KEYS\|MEMBER_KEYS" src/game/save_state.rb`
   - `grep -n "MACHINE_WRITTEN" src/core/data_store.rb` - `grep -n "EXCLUDED" src/net/fingerprint.rb`
   - `grep -n "hello" src/net/protocol.rb`.
   **Live discrepancy to settle at the ticket:** the spec bounds `insurance` by
   `death.json max_stacks`, but `data/balance/death.json` has NO such key today (it holds
   corpse/loot/wipe timings only) -- T5 is the ticket that introduces insurance. Decide
   and RECORD: either add `insurance: { max_stacks: <n> }` to death.json as part of T1
   (data-only, zero behavior since nothing reads it yet) or bound the validator by a
   documented constant and name T5 as the owner of the data key. Do not silently invent a
   number in code (zero balance constants in code is a repo non-negotiable).
7. Push the CLAIMED line FIRST (s56 law): `CLAIMED: T1 save schema 3 (per-player
   character records + data/player.local.json + HELLO player_id + 2->3 migration) --
   <seat>, s136.`
8. Rule 4: read every file BEFORE editing it.

## 1. Close s135 first: harvest the sweep (15 min, bounded)

The 43-script re-pin sweep launched 02:25 at `332649b`, tag `v22-e1`, ~13 min/script
(so ~9 h; it may still be running when you start).

- `cat tmp/e1_sweep.log | grep -E "gate_rc=|WALL"` and `ls tmp/e1_sweep.done`.
- **If DONE:** `rake pins` -> paste into the s135 record's Sweep section, then commit
  `harness/pins.json` + `drafts/_gate-verdicts.log` as the sweep's close commit.
- **If STILL RUNNING:** leave it alone. You may do every non-GL part of T1 (persistence,
  digest, tests are all headless) but you must NOT run `rake gate` / `rake capture` /
  `bin/play` / soak until it finishes -- one GL replay at a time is the law. Harvest when
  the marker appears; if it is still running at your close, hand it forward again in the
  checkpoint (do not kill it).
- **Expect FAILED pins; they are honest, not breakage.** Already seen: `aoe_specials
  challenge_reads` (the named L16 debt -- the challenge pulse fires into an empty room)
  and `basement_pocket` manifest (`drop_picked_up` 0). T0 d6 headlined SEVEN census
  scripts and E1 fixed those seven, but the `WALL FAIL:` line d6 itself quoted listed
  THIRTEEN. The six never touched: **basement_pocket menu_tour mercy_floor
  multi_floor_descent toll_pocket tower2_run**. Do NOT fix them this session -- record
  them as ticket **E1c** (same shape as E1 piece 4: observe headlessly, then re-cut to
  observed counts or retire with an owed re-author header). Note also that Junior already
  fixed one of this class on his branch (`60e2817`: `wipe_reads` demanded wipe TEXT the
  v16 owner order removed, failing `ledger_loop` + `sustain_run` on DESIGN, not a bug) --
  that fix arrives with his merge; do not duplicate it.
- **One small pins wart to fix while you are here (5 min, its own commit):** `pins.rb
  record` stamps HEAD at record time, so because s135 committed docs mid-sweep the pins
  carry four different commits for one sweep of one unchanged tree. Make `run_wall.sh`
  capture `git rev-parse --short=7 HEAD` ONCE at start and pass `--commit "$SWEEP_HEAD"`
  to every `pins.rb record` call (the flag already exists), and add the law to the
  runner's header: a sweep pins the tree it judged, not the tree docs drifted to.

## 2. T1 -- ordered pieces, ONE commit each

The order is chosen so every commit is independently green and the risky hop lands last
with the most tests already around it. Do not reorder without saying why.

**(1) `data/player.local.json` + `src/app/player_file.rb`** -- machine-written on first
boot (`{ "player_id": "<uuid v4>", "created_at_ms": <int> }`), lenient-NAMED reader
(corrupt -> regenerate + printed line, never a brick; App::Prefs is the precedent).
Register in BOTH `DataStore::MACHINE_WRITTEN` and `Net::Fingerprint::EXCLUDED` (the TWIN
LAW -- `prefs.local` is the working precedent, not `bindings.local`) and `.gitignore`.
Bots (`--bot`) and harness scenes derive `bot-<seed>` deterministically so replays stay
byte-identical. Tests: fresh-create, corrupt-file regeneration, twin-law membership,
determinism of the bot id.

**(2) `src/game/character.rb`** -- plain object + validator, no World coupling:
`{ level, xp, xp_debt, insurance, home_zone, form, forms: { <kit>: { hp, inscribed } },
bag, equipment, attributes }`. Integer-only; `Progression` invariant per character;
`xp_debt >= 0`; `insurance` bounded by DATA not a code constant (see the Orient step 6
discrepancy: the key the spec names does not exist yet); `home_zone` must be a hub
(move `home_refusal` into the character validator). **Junior's three keys land as
OPTIONAL keys with documented defaults (`bag []`, `equipment {}`, `attributes {}`) and
`bank_items []` INSIDE the record** (council s133). Do NOT import or reference his
branch code -- his `Game::Bag` does not exist on main; his merge wires Bag -> record in
one line, which is exactly the debt his classification table names.

**(3) `SCHEMA = 3` + the 2->3 migration hop** (the irreversible piece -- its own commit,
its own proofs). Host character from `progression` + `members` + `home_zone`; `xp_debt 0`,
`insurance 0`; retire `members`/top-level `home_zone`/`progression` from `FACT_KEYS`;
write the `migration` block `{ from_schema: 2, legacy_level: L, legacy_seed_claimed_by:
null }` (optional; absent in fresh worlds). `--fresh` creates the host character NEW at
`progression.json new_character.level` with NO migration block. Schema 1 REFUSES NAMED
("save schema: 1 unsupported (expected 3)"); delete `upgrade_v1` + `V1_FACT_KEYS`.
Anything else refuses NAMED with the offending key/path in the text.
**D-T1 is owner-pending with a recommendation and marked "never nag"** -- implement the
bolded rec (legacy seed ONCE, keyed by player id, recorded in
`migration.legacy_seed_claimed_by`) as dev-of-record, record it in ONE line, and surface
it for both peers without gating on it. It is data-only and reversible.

**(4) HELLO + digest** -- `Protocol::MESSAGES[:hello]` gains `player_id`;
`Fingerprint.mismatch` still compares the FIVE build fields only (identity is not a build
fact); a guest id equal to the host's refuses NAMED ("player id collision: both seats
share one player file"). `Character#digest_fields` (level, xp, xp_debt, insurance, form,
home_zone) per character in **sorted player-id string order** enter `digest_snapshot`;
pin it in `test/net/state_digest_test.rb`.

**(5) Bridge to today's World** (T1 ships BEFORE T2b, so this is a NAMED interim, not
undefined behavior): `apply!` rebuilds today's three-body pack from the host character's
`forms`; `progression` stays fed from `characters[host]`; seat pointers unchanged. The
guest's record EXISTS on both seats (created at session start from HELLO's id + the
host's save string, which already travels in v18's `save` field) but its `level/xp` are
READ-ONLY MIRRORS of the host character until T2b drives them -- write that rule in
`Character` and test-pin it so both digests agree. `world.rb` net lines <= 0 (the
extraction is T2a's).

**(6) CLASSIFICATION rows** in `test/game/save_state_test.rb` for every new key
(required / optional-default / retired-refused) -- this table is the save's contract and
Junior's branch extends the same table, so keep the shape identical to his.

## 3. Gates (all of them; the spec's list, do not trim)

- `bundle exec rake` green via hooks on every commit.
- **Migration proved on COPIES of BOTH chains**: the owner's `saves/world.json` (COPY --
  never the live file) and Junior's chain by seat mail; if his copy is absent, prove on
  the owner's copy + a synthetic v2 file and RECORD the gap by name.
- `--fresh` backup law on a copy (`.bak-<ts>` appears FIRST), a synthetic v1 file refuses
  NAMED, and every refusal text pasted verbatim in the record.
- `ruby tools/dev_save.rb` regenerates a scratch save under schema 3 (the warp path must
  keep working).
- `rake soak SEED_SAVE=1 N=1` green (two real processes, scratch save, chain_check).
- `rake gate SCRIPT=harness/net/netplay_session.json CHECKS=harness/net/gate_checks.json`
  (HELLO shape changed) -- GL, so only after the sweep is done.
- **Canary UNCHANGED, no rebank**: T1 changes no sim decision. If a canary moves, STOP
  and find out why before touching the bank.

## 4. Review (Rule 6) -- BEFORE the close push

Two reviewers, because save files are irreversible:
1. **Headless scrubbed `pi -p`** using the STAGED brief
   `docs/sparkups/t1-review-brief-20260905.md` (unset all PI_*; detached worktree at the
   reviewed commit; `--thinking max --tools read,bash`). Inline the full diff + the
   migration proofs + the refusal table in the brief file -- starved reviewers fabricate.
   Demand the JSON verdict as the LAST message. ~$5.
2. **`council`** (DeepSeek or Kimi) on the migration/refusal table + the D-T1 seed rule.
   <= $1. Brief as a FILE (the CLI takes argv; a big brief exceeds Windows' 32K limit)
   and export `AWS_PROFILE=voice-dev` in the runner.

A BLOCK verdict blocks the push -- fix or reconcile with a stated disagreement, never
downgrade. s135's reviewer returned BLOCK with four real blockers (two were defects I had
introduced, one was my own test going red in a worktree, one was a script I wrongly
retired); budget real time for landing findings.

## 5. Fences and traps (s135-verified where marked)

- **Never the live save.** Copies for every proof, `--save <scratch>` on every launch,
  `bin/warp` for scouting. `main.rb` already refuses the persistence path by name -- keep
  that true.
- No surfaces, no HUD, no field behavior, no `data/zones/**`, no `pilot.ldtk`.
- **Do not import Junior's branch code.** His keys enter as optional defaults only.
- **Gates run DETACHED, never under a bash-call timeout** (s135: I re-learned this live --
  `timeout 900` SIGTERMed a gate at status 143 and produced a false negative). Launch via
  `powershell Start-Process bash.exe ... -WindowStyle Hidden`, poll by marker/CPU.
- **The critic is NON-streaming now** (s135 fix): Bedrock's `converse_stream` stalled
  7-24 min at ~1 s CPU while non-streaming answered a 97-row verdict in 206 s.
  `CRITIC_STREAM=1` restores streaming; `CRITIC_READ_TIMEOUT` overrides the window.
  Judge a slow critic by process CPU time, not elapsed wall time.
- **Do not commit while a sweep runs** (pin provenance -- see Step 1's fix).
- The suite must stay green **from a linked worktree too** (s135 blocker: a test that
  hardcodes the repo root goes red exactly where the worktree fix matters). If you add a
  path-resolving test, compute the main root the way `harness/pins.rb` does.
- minitest `-n /regex/` gets MSYS-mangled into a path -- use full `-n test_...` names or
  run the file.
- Detached scripts that exec ruby: `unset MSYS_NO_PATHCONV` inside the script.
- `drafts/_gate-verdicts.log` and `harness/pins.json` are tracked and shared: resolve
  conflicts as a pure union (both sides are appends).
- A verdict's `why` proves the SURFACE was present, never WHICH frame -- settle any
  staging claim with a headless probe of the renderer's own draw condition (s135, twice).
- Junior may land mid-session: `git fetch` before every doc commit; his files are
  read-only to you beyond what the spec's T1 file list requires.
- An owner or peer word that reverses a decision = scope break: record verbatim, stop,
  surface.

## 6. Rule 7 -- budget and stop conditions

Declared up front: headless reviewer ~USD 5 (one pass) + council <= USD 1 + critic
Bedrock spend for the netplay gate and any re-gate (pre-cleared AWS-internal; declare the
per-gate count in the record). **Stop conditions:** the six pieces committed + all gates
green + both reviews landed + push. Anything beyond that -- E1c's six census scripts, the
`floor3_run` 733-1646 and `tower4_run` 1630-1641 capture additions, sentinels joining the
canary ACTIVE bank, T2a's `world.rb` extraction -- is a NAMED follow-up, not this
session. If T1 threatens mid-unit compaction, land the suite-green core (pieces 1-2) and
move the hop to a named follow-up in the same commit message; never half-done on main.

## 7. Close (every session)

Ticket record `drafts/_v22-t1-record-<date>.md` -- evidence boxes UNCHECKED until pasted;
**never pre-fill a hash, a verdict or a proof** (s134 caught a fabricated md5 live; s135
caught two of my own misreads). CYCLE.md T-row + owner-pending list updated (D-T1 line).
CHECKPOINT entry with an es-CR line for Gabriel and a pt-br line for Junior -- his line
must name the EXACT key names and shapes his S2 merge will fill (`bag`, `equipment`,
`attributes`, `bank_items` inside the character record, optional with defaults) so his
merge is one line, and it must tell him T1 is on main so he can rebase. CLAIMED -> none.
`git fetch` + rebase + push. Tree clean except tmp/ (plus the sweep's two files if it is
still running -- say so). Harvest any new mail in `~/.pi/agent/mail/game-two/inbox/` into
the triage doc. Checkpoint to disk before any /compact.

**Next ticket after T1:** T2a (Party/Character/ZoneState extraction, byte-inert) -- and
Junior's branch merge becomes unblocked the moment T1 lands, so coordinate that in his
pt-br line rather than deciding it alone.
