# Temporary bring-up canary — proves the Actions runner itself works on
# this repo (free-plan private repos can refuse jobs on billing/settings
# grounds with no useful check output). Deleted once ci.yml is green.
name: canary
on:
  pull_request:
  workflow_dispatch:

jobs:
  canary:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "runner alive"
