source "https://rubygems.org"

ruby ">= 3.4"

gem "gosu", "= 1.4.6"
gem "rake", "~> 13.0"
# M5a audio bridge: FFI binding to the sibling library's vendored
# miniaudio.dll (game-two-audio). Required only when the library is present.
gem "ffi", "~> 1.17"

group :test do
  gem "minitest", "~> 5.25"
end
